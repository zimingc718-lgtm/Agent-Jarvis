# 需求输入

责任人：产品 owner

## 原始输入记录

### INPUT-2026-09-08-001

用户要求讨论第一个功能：UI 设计，具备大模型对话功能（悬浮），支持 DeepSeek、OpenAI 以及本地大模型接入。

### INPUT-2026-09-08-002

经讨论确认：采用底部悬浮对话；首版支持文本流式对话；使用 Google 登录 Agent-Jarvis 自身账号体系；第三方模型账号授权也需要；授权方式采用官方 OAuth 优先、API Key 兜底；首批授权范围仅模型 Provider；本地大模型通过 OpenAI 兼容地址接入；技术栈采用 Next.js 全栈；服务端使用 SQLite 加密字段保存 Provider 凭据；模型设置使用独立设置页；保存最近会话；视觉风格为科幻控制台。

### INPUT-2026-09-08-003

用户要求：测试计划必须根据产品需求说明书、架构设计说明书、模块任务开发说明书制定；各说明书必须经过多角色评审、反馈、迭代后，方可进入下一阶段。

### INPUT-2026-09-08-004

用户要求按计划先更新项目说明书，然后严格按项目流程开始执行。

### INPUT-2026-09-10-005

用户要求重新审视当前前端 UI 设计，确认本次 L3 UI 基础设施变更覆盖全部现有前端界面；视觉采用浅色、默认紫色主题。用户明确约束：使用 shadcn/ui、Tailwind 与 HSL 三元组 Design Token；现有左下角菜单中的子菜单名称为「模型」，Provider 的启用与优先级选择已实现，不能另行发散为聊天内 Provider 选择器；当前 P3 技能上传相关变更不纳入本次 L3 验收。用户要求严格执行项目 R1-R4 与证据流程，并要求按 AIPM 定义的产品 owner、架构、模块开发、测试四角色逐层审查，L3 不得在产品分析后直接拍板进入实现。

### INPUT-2026-09-10-006

用户在 P2 路线选择中明确决定：一次性重写所有已批准前端界面，而非分阶段迁移；仍保留深色主题，浅色紫色为默认主题。

### INPUT-2026-09-10-007

用户要求提升流程效率，并明确：规则必须做成可验证的脚本、由机器核验；同时保证「实现的真实、测试的准确、每个需求都有清晰的回应与实现」。经讨论确认的目标清单：

1. **流程重量随撤回代价缩放**——按「双向门 / 单向门」（可逆性）分档，而不是按改动落在哪个目录分级。L1 目前跑完整 R1–R4，实测为改 3 句话付出 225 行流程文字，不可持续。
2. **每个变化点必须声明「门」与「发现方式」**——发现方式只有三种合法答案：某条机器检查、某个真实入口操作、或「发现不了」；填「发现不了」即为风险登记项，必须按单向门处理。
3. **能机器化的规则不得只写进文档**——新增规则必须同时交付可执行的检查命令。
4. **闭合五个已暴露的检测盲区**：① ID 撞号（TASK-044..048 实际发生过，g1–g4 全部放行）② `known_warnings` 与门禁实际状态不符（LV-AXE、g3 两条错了一整天）③ `check-specs` 与 `migrate_specs --dry-run` 判定不一致 ④ 长跑 dev server 与新增构建配置失去同步（导致「对话框菜单挤在左上角」）⑤ `review r1..r4` 对无 CP 表的 CR **静默跳过**。
5. 用户同时确认：一个 CR 一条分支、合并到 main；不采用四条常驻角色分支。

## 状态

- 当前状态：ACCEPTED
- 日期：2026-09-08
- 来源：用户对话
