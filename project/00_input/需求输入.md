# 需求输入

责任人：产品 owner

## 原始输入记录

### INPUT-2026-09-08-001

用户要求讨论第一个功能：UI 设计，具备大模型对话功能（悬浮），支持 DeepSeek、OpenAI 以及本地大模型接入。

### INPUT-2026-09-08-002

经讨论确认：采用底部悬浮对话；首版支持文本流式对话；使用 Google 登录 Agent-Jarvis 自身账号体系；第三方模型账号授权也需要；授权方式采用官方 OAuth 优先、API Key 兜底；首批授权范围仅模型 Provider；本地大模型通过 OpenAI 兼容地址接入；技术栈采用 Next.js 全栈；服务端使用 SQLite 加密字段保存 Provider 凭据；模型设置使用独立设置页；保存最近会话；视觉风格为科幻控制台。

### INPUT-2026-09-08-003

用户要求：测试计划必须根据产品需求说明书、架构设计说明书、模块任务开发说明书制定；各说明书必须经过多角色评审、反馈、迭代后，方可进入下一阶段。

### INPUT-2026-09-08-004

用户要求按计划先更新项目说明书，然后严格按项目流程开始执行。

### INPUT-2026-09-10-005

用户要求重新审视当前前端 UI 设计，确认本次 L3 UI 基础设施变更覆盖全部现有前端界面；视觉采用浅色、默认紫色主题。用户明确约束：使用 shadcn/ui、Tailwind 与 HSL 三元组 Design Token；现有左下角菜单中的子菜单名称为「模型」，Provider 的启用与优先级选择已实现，不能另行发散为聊天内 Provider 选择器；当前 P3 技能上传相关变更不纳入本次 L3 验收。用户要求严格执行项目 R1-R4 与证据流程，并要求按 AIPM 定义的产品 owner、架构、模块开发、测试四角色逐层审查，L3 不得在产品分析后直接拍板进入实现。

### INPUT-2026-09-10-006

用户在 P2 路线选择中明确决定：一次性重写所有已批准前端界面，而非分阶段迁移；仍保留深色主题，浅色紫色为默认主题。

### INPUT-2026-09-10-007

用户要求提升流程效率，并明确：规则必须做成可验证的脚本、由机器核验；同时保证「实现的真实、测试的准确、每个需求都有清晰的回应与实现」。经讨论确认的目标清单：

1. **流程重量随撤回代价缩放**——按「双向门 / 单向门」（可逆性）分档，而不是按改动落在哪个目录分级。L1 目前跑完整 R1–R4，实测为改 3 句话付出 225 行流程文字，不可持续。
2. **每个变化点必须声明「门」与「发现方式」**——发现方式只有三种合法答案：某条机器检查、某个真实入口操作、或「发现不了」；填「发现不了」即为风险登记项，必须按单向门处理。
3. **能机器化的规则不得只写进文档**——新增规则必须同时交付可执行的检查命令。
4. **闭合五个已暴露的检测盲区**：① ID 撞号（TASK-044..048 实际发生过，g1–g4 全部放行）② `known_warnings` 与门禁实际状态不符（LV-AXE、g3 两条错了一整天）③ `check-specs` 与 `migrate_specs --dry-run` 判定不一致 ④ 长跑 dev server 与新增构建配置失去同步（导致「对话框菜单挤在左上角」）⑤ `review r1..r4` 对无 CP 表的 CR **静默跳过**。
5. 用户同时确认：一个 CR 一条分支、合并到 main；不采用四条常驻角色分支。

### INPUT-2026-09-10-008

用户原始输入（按对话顺序，原文保留；括号内为该条回答的问题，便于解读编号）：

1. 「讨论，如果jarvis采用DeepSeek harness架构代码是否可以？或者openclaw架构代码？」
2. 「我说的不是开发流程上的。而是Jarvis业务agent上的，可以参考DeepSeek harness还是openclaw比较合适？」
3. 「因为当前哪怕上传了skill，Jarvis也没有管理skill的能力，也没有联网搜索的能力。有技能，记忆那部分我采用本地的知识库（这也是后面会创建的核心功能）。你再联网分析下。另外，openclaw的token消耗有点大，怎么借鉴轻量化，但消耗多的token？」
4. 「A。先做哪个按你的建议。Jarvis的交互底层可以采用DeepSeek harness，也可以借鉴openclaw的主动唤醒，但不消耗大量token。我们可以进入需求与产品设计说明分析，讨论下功能。」（A = 读 DeepSeek Harness 源码、抄其结构自行实现，不安装其包）
5. 「1， 删；2.可以读多个技能；3.联网搜索，用你建议的，开源的，好用的工具，我没有api；4. 免审批；5. 默认关，要有开关。6.可见。7. 1.2条。」（1 删除 REQ-F-021 独立路由调用；2 取消「仅当轮」硬边界；3 搜索后端不用 API key；4 只读工具免审批；5 主动唤醒默认关并有开关；6 token 用量可见；7 执行过程可见性走「对话内步骤流 + 状态灯扩展」）
6. 「1，什么是docker？可以你帮我起这个服务吗？2. x在菜单里。3. 最大步数为10,4.要。另外不是说嵌入DeepSeek harness吗？没看到这个功能需求。」（2 token 用量放 ☰ 菜单；3 工具循环最大 10 步；4 要 `read_url` 工具）
7. 「再次审视下需求。」「看是否完整，清晰，齐全。」
8. 「1. 破，2.重建，3.不支持，4.提进。仅分析需求清单，产品设计清单是否准确，齐全。」（1 状态灯四态可破；2 刷新后步骤流重建；3 usage 拿不到时显示「不支持」——后由第 9 条改为本地估算；4 web_search 提进 A 期）
9. 「1. 放宽；2. 提醒后停止；3.带。4.提醒，5. 本地估算。」（1 面板 50% 上限放宽；2 触顶提醒后停止；3 回答带来源引用；4 Provider 不支持工具调用时提醒；5 token 拿不到 usage 时本地估算）
10. 「使用新模型，分析下当前的需求，是否已满足嵌入DeepSeek harness？」
11. 「读法2，方案A」（读法二 = 采用 DeepSeek Harness 的架构思想自行实现，不嵌入其包）

### INPUT-2026-09-11-009

用户原话（按对话顺序）：

1. 「起服务。另外模型要联网的时候，要让模型可以调用起来联网搜索。B期可以开始。」
2. 「开始B期」
3. 「R1 通过，标记形态可以，按流程合并」（对 CR-20260911-context-compaction 的 R1 终裁：通过；CP-4 极轻分隔标记形态：确认；处置：按 `docs/WORKFLOW.md` 合并）
4. 「继续，一直把BCDE期都做完。」
5. 「把任务先都完成。我还有其他需求要继续。」（第 4–5 条合读为对 C、D 期**推进与按流程合并的总授权**；各期具体设计决策由助手代拟并在各 CR 的「R1 人工终裁」节列为待追认清单。文档中的分期只定义到 D 期（CR-20260910-agent-tooling 附录 C），**「E 期」无定义**，C、D 完成后向用户确认，不代拟。）
6. 「没有E期。继续把当前的任务做完。」（2026-09-11，对第 4 条中「E 期」的答复：**没有 E 期**；附录 C 的 A/B/C/D 四期至此全部合并——B: CR-20260911-context-compaction、C: CR-20260911-knowledge-base、D: CR-20260911-proactive-wake。C、D 各 CR「R1 人工终裁」节的代拟决策清单仍待用户追认。）

其中「起服务」与「让模型可以调用起来联网搜索」已由 `CR-20260911-tool-availability` 处置（SearXNG 本机就绪 + `未探测` 不再等同 `不支持`）。本条输入的未尽部分为 **B 期：会话上下文分层与完整压缩**——A 期（`CR-20260910-agent-tooling`）只做了工具结果保留窗口（REQ-F-041，N=2），用户与助手的**文本历史仍是全量回放**，超预算时直接抛请求级错误要求用户开新对话。

用户在被告知「B 期的压缩阈值最好有真实 token 数据支撑，建议先用一用再开」后，仍选择直接开始 B 期。故阈值由架构按本机实测数据定初值，并按 DEC-018 `maxRatio` 的先例保留「被实现证伪则修订并回写」的出口。

## 状态

- 当前状态：ACCEPTED
- 日期：2026-09-08
- 来源：用户对话
