# 产品需求说明书

责任人：产品 owner

## 产品目标

交付 Agent-Jarvis 的第一个产品功能：一个本机运行的底部悬浮大模型对话 UI。当前版本**以单一本地管理员身份运行**，用户在底部悬浮输入条发起文本对话，查看流式回复，随时停止，开启新对话，并保存最近会话。可用模型 Provider 的**启用与使用优先级在「配置」中管理**，对话浮窗不做 Provider/模型选择。

## 用户范围

- 主要用户：本机使用 Agent-Jarvis 的**单一本地管理员**（当前版本不区分多用户，无账号登录）。
- 管理职责：该管理员在「配置」中启用模型 Provider、设定使用优先级、维护凭据。

## 功能需求

| ID | 名称 | 优先级 | 描述 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| REQ-F-001 | Google 登录 | MUST | 用户必须通过 Google 登录进入 Agent-Jarvis 受保护页面。 | 未登录访问主页、设置页或对话 API 时被阻断；登录后可访问主页、设置页和对话入口。 | DEFERRED（暂缓，CR-20260909-minimal-floating-chat）：当前版本以单一本地管理员身份运行，「账号登录」为占位入口，Google 登录待后续 CR 恢复。 |
| REQ-F-002 | 底部悬浮输入条 | MUST | 页面底部必须常驻紧凑悬浮输入条，作为大模型对话入口。 | 默认页面加载后底部输入条可见，可输入文本并触发发送；悬浮条仅含**状态灯、输入框、单个动作按钮**三个元素；输入框始终可输入，不依赖 Provider 状态。 | APPROVED |
| REQ-F-003 | 展开式对话面板 | MUST | 发送消息后，悬浮输入条展开为底部对话面板。 | 发送消息**触发**面板展开；用户可随后经 REQ-F-019 手动收起，再次发送重新展开。面板展开时展示用户消息与模型回复区域；对话状态**仅由状态灯表达，无文字**；面板高度上限为视口 50%。 | APPROVED（CR-20260909-collapsible-panel 将「发送后必须展开」放宽为「触发展开、可手动收起」，2026-09-09 用户确认） |
| REQ-F-004 | 文本流式回复 | MUST | 对话必须支持模型文本流式输出。 | 调用对话接口时前端逐段显示模型回复，而不是只在完成后一次性显示；会话活动中每轮请求由服务端携带该会话**已有全部消息 + 一段固定 system prompt**。 | APPROVED |
| REQ-F-005 | 停止生成 | MUST | 用户必须能中断正在生成的回复。 | 生成中显示「停止」；点击后**服务端停止请求模型、当前会话即结束**；被中断的回复保留在记录区并带**行内「已停止」标记**；落库 `status=stopped`。 | APPROVED |
| REQ-F-006 | Provider 启用与优先级 | MUST | 用户必须能在「配置」中启用/停用多个模型 Provider，并明确设定它们的使用优先级。对话浮窗不提供 Provider 或模型的选择/切换控件。 | ①「配置」将已启用 Provider 呈现为**有序列表**，用户可通过「上移/下移」调整顺序，列表位置即优先级（第 1 位最高）；②新启用的 Provider 追加到列表末尾；③仅启用一个 Provider 时排序控件隐藏或禁用；④对话请求**不携带** Provider/模型参数，服务端在「已启用且连接有效」的 Provider 中取优先级最高者解析实际使用的 Provider/模型；⑤最高优先级 Provider 连接无效时自动落到下一个连接有效的；⑥无任何连接有效的 Provider 时状态灯灭、发送被拦截并在输入框上方提示。 | APPROVED（重写，CR-20260909-minimal-floating-chat；P3 已实现，TEST-025/026） |
| REQ-F-007 | 独立模型设置页 | MUST | 系统必须提供独立设置页管理 OpenAI、DeepSeek、本地模型 Provider。 | `/settings/models` 可新增、编辑、启用、停用、测试 Provider，并提供已启用 Provider 的**优先级有序管理**（上移/下移）。 | APPROVED |
| REQ-F-008 | Provider 认证模式 | MUST | Provider 必须区分 OAuth、API Key、Local、Unsupported 四种认证模式。 | 设置页能显示每个 Provider 的认证模式；OAuth 仅用于官方支持的 Provider，OpenAI/DeepSeek 首版使用 API Key 兜底。 | APPROVED |
| REQ-F-009 | 凭据服务端加密 | MUST | API Key 等敏感凭据必须由服务端加密保存，浏览器不得获得明文。 | 数据库存储值不是明文；Provider 查询接口只返回脱敏状态。 | APPROVED |
| REQ-F-010 | OpenAI 接入 | MUST | 系统必须接入 OpenAI Provider。 | 配置 OpenAI API Key 和模型后，可通过统一对话接口获得流式文本回复。 | APPROVED |
| REQ-F-011 | DeepSeek 接入 | MUST | 系统必须接入 DeepSeek Provider。 | 配置 DeepSeek API Key 和模型后，可通过 OpenAI 兼容接口获得流式文本回复。 | APPROVED |
| REQ-F-012 | 本地模型接入 | MUST | 系统必须支持本地 OpenAI-compatible 模型服务。 | 配置本地 base URL 和模型名后，可通过统一对话接口获得流式文本回复。 | APPROVED |
| REQ-F-013 | 最近会话保存 | MUST | 系统必须按当前管理员身份保存最近会话和消息。 | 刷新页面后，最近会话及其消息可恢复；恢复触发条件收窄——**仅当最近会话仍处于活动状态**（用户既未「停止」也未「新对话」）时刷新才恢复它，否则呈现空会话。 | APPROVED |
| REQ-F-014 | 克制的浅色简约视觉 | SHOULD | 对话 UI 应采用克制、专业的浅色简约风格（视觉方向参考 `gens.team/ai-insights`：留白充足、单一强调色、统一圆角、卡片式分组），默认浅色并可切换深色，优先可读性与呼吸感而非装饰或信息密度。 | 浅色为默认且可切换深色并持久化；**单一强调色（蓝色系）**用于主按钮与交互焦点，其余为中性灰阶；交互元素（按钮、输入框、卡片）使用统一圆角，分组靠细线或留白而非重边框；布局以充足留白构建层级，非紧凑控制台密度；无阴影或仅极轻阴影；文本在桌面和移动宽度下不溢出；正文文本、控件边界、**状态灯四态**在两套主题下均达 WCAG 2.2 AA（正文 4.5:1、非文本 3:1）；**收起/展开控件（REQ-F-019）为真实 `<button>`，焦点可见、与背景非文本对比 ≥ 3:1、触控目标 ≥ 24px，图标形态入 `docs/UI_STANDARD.md`**。 | APPROVED |
| REQ-F-015 | 极简首页 | SHOULD | 当前版本首页不展示知识库概览，只保留标题与两个弹窗入口，把注意力留给底部悬浮对话。 | 首页仅渲染产品标题、「配置」与「账号登录」两个弹窗按钮和底部悬浮对话；两个弹窗可打开、关闭、Esc 退出且焦点回到触发按钮；当前版本「账号登录」为**占位入口**（对应 REQ-F-001 暂缓）。 | APPROVED |
| REQ-F-016 | 回复 Markdown 渲染 | SHOULD | 助手回复应按 Markdown 渲染，使代码、列表和强调可读。 | 代码块、行内代码、粗体、斜体、有序/无序列表、标题按语义元素渲染；仅 http/https/mailto 链接可点击；模型输出中的原始 HTML 不被执行；流式中未闭合代码围栏不破版；**请求级错误**（回复第一段文本到达前失败）在**输入框上方一行红字**展示，内容脱敏（不含 API Key、不含 Provider 内部细节），不写入对话上下文，不结束会话，不阻塞继续输入。 | APPROVED |
| REQ-F-017 | 开始新对话 | MUST | 用户必须能主动开启一个不继承既有上下文的新会话。 | 「非生成中且输入框为空」时动作按钮显示为「新对话」（无历史消息时也是）；点击后清空浮窗记录区、丢弃当前 `conversationId`；在下一条消息发送前不产生新的会话记录；点击「新对话」后刷新页面呈现空会话，不自动恢复上一会话。 | APPROVED |
| REQ-F-018 | 连接状态指示灯 | MUST | 悬浮条以单个状态灯表达当前可对话性与生成状态，不使用文字。 | 首页加载时对全部已启用 Provider 执行**一次连接探测**，状态灯反映探测结果；灯有四种可辨识状态——**检测中**（加载探测进行中）、**灭**（无已启用 Provider 或全部探测失败，不可对话）、**常亮**（至少一个 Provider 连接有效，可对话）、**闪烁**（正在生成回复）；四态在两套主题下互相可区分且与背景对比达 WCAG 2.2 AA 非文本对比 3:1。**（CR-20260909-collapsible-panel 补充：折叠状态下回复完成时状态灯做一次短促瞬时提示后落定，属四态的显示表现，不新增可辨识状态。）** | APPROVED |
| REQ-F-019 | 收起对话面板 | SHOULD | 对话面板展开后，用户可主动将其收起为输入条形态，会话状态不变；可再次展开，记录区原样恢复。收起/展开偏好按浏览器记住。 | ①存在对话记录时，状态灯所在行显示收起/展开控件（展开态朝下、收起态朝上），无记录时不显示；②点击收起：记录区从视图与 DOM 移除，面板回到「状态灯 + 输入框 + 单按钮」形态，`conversationId` 不变、`jarvis:chat-session-ended` 不置位、消息不清空；③点击展开：记录区恢复且滚动到底部，消息与折叠前一致；④折叠状态下收到流式回复保持折叠，状态灯转「生成中」，回复完成或失败时状态灯做一次短促瞬时提示后落定，全程不自动展开、不显示未读计数；⑤折叠状态下发送新消息自动展开；⑥收起/展开切换与发送即时写入 `localStorage`（每浏览器），刷新时存在可恢复会话则按存储偏好渲染（未设过默认展开），折叠偏好为真但无记录时按①不显示控件、呈现输入条形态，不产生「折叠了却空白」的中间态；⑦收起/展开为纯 UI 状态，不改 `conversationId`、不结束会话、与「新对话」正交。<br>**实现约束**：`expanded` 拆为 `hasTranscript`（派生自消息数）与 `userCollapsed`（持久 boolean），`showTranscript = hasTranscript && !userCollapsed`；瞬时切换无高度动画。 | APPROVED |

## 验收澄清（CR-20260908-floating-chat-functional-fixes，非新增需求）

- REQ-F-005：「不再追加输出」必须在服务端可观察——停止后服务端停止向模型请求，落库消息状态为 `stopped`。
- REQ-F-006：「切换」必须在浮窗 UI 上可观察——存在多个已启用 Provider 时渲染切换控件，且所选 Provider/模型随对话请求发送。
- REQ-F-007：「新增、编辑、启用、停用、测试」为五个独立可观察动作，各自需要独立验收证据；「编辑」不得产生重复配置。
- REQ-F-013：「恢复」必须在真实入口可观察——刷新后通过 `GET /api/conversations/[id]/messages` 取回消息并在浮窗重建。
- REQ-F-008：`oauth` 认证模式在本版本仅作展示，不可保存为可用 Provider。

### CR-20260908-ui-light-theme-minimal-home 澄清

- REQ-F-007「独立设置页」在本版本同时存在两个真实入口：首页「配置」弹窗与 `/settings/models` 深链页面，二者渲染同一 `ModelSettings` 组件。主题开关位于该弹窗的「外观」分区。
- REQ-F-002/003：展开后的对话面板高度上限为视口 65%，不得占满全屏（避免退化为通用聊天工具）。**（已被 CR-20260909-minimal-floating-chat 收敛为 50%，见下。）**
- 首页「知识库概览」区域在当前版本不展示，属已批准的暂缓项，不构成需求删除。

### CR-20260909-minimal-floating-chat 澄清

**会话生命周期**
- 会话自**首条用户消息**创建，至用户点击**「停止」或「新对话」**结束。
- 活动中：多轮续接，每次发送沿用同一 `conversationId`；回复正常完成后会话仍活动，可继续追问。
- 已结束：`conversationId` 丢弃；记录区在「点击新对话」或「发送下一条消息」时清空；下一条消息创建新会话。
- 已结束/已停止的会话及其消息保留在本地存储，本版本无浏览入口，且不被刷新恢复逻辑捞取。

**请求级错误 vs 流中错误**
- 请求级错误（回复第一段文本到达前失败：连接失败/鉴权失败/Provider 4xx-5xx/超时）→ 输入框上方一行红字，脱敏，不写入上下文，不结束会话，不阻塞输入。
- 流中错误（回复已开始后中断）→ 该助手气泡标记「生成失败」，落库 `status=error`（现有行为）。

**面板高度**：展开后对话面板高度上限由视口 65% 收敛为 **50%**（覆盖 CR-20260908-ui-light-theme-minimal-home 的 65% 澄清）。

**动作按钮状态机**（单个按钮）

| 场景 | 按钮 | 行为 |
|---|---|---|
| 生成中 | 停止 | abort，服务端停止请求模型，会话结束，落库 `stopped` |
| 输入框有字 | 发送 | 发送，Enter 等效 |
| 输入框空（有/无历史均同） | 新对话 | 清空记录区，丢弃 `conversationId` |

**优先级解析**：见 REQ-F-006 验收 ④⑤⑥。状态灯反映「是否存在任一连接有效的 Provider」；发送时取连接有效的 Provider 中优先级最高者。

**REQ-F-001 暂缓边界**：当前版本以单一本地管理员身份运行，不实现账号登录；「账号登录」按钮保留为占位入口。恢复 Google 登录时同步恢复 REQ-F-001 与 REQ-NF-002。

### CR-20260909-collapsible-panel 澄清

**折叠状态语义**（REQ-F-019）：收起/展开是纯 UI 态，不影响 `conversationId`、`jarvis:chat-session-ended`，不触发或结束会话；与「新对话」「停止」正交。持久化用 `localStorage['jarvis:chat-collapsed']`（每浏览器）；`localStorage` 服务端不可读，SSR 首帧按默认（展开）渲染，hydration 后按存储校正——只接受一帧内校正，不接受持久错误态（与 DEC-011 同类处理）。

**折叠期间回复完成提示**：仅状态灯一次短促瞬时提示，不引入未读角标、消息计数或第五种持久灯态。

**折叠时的请求级错误红字**：折叠状态下记录区不在 DOM，但请求级错误红字**照常显示在输入框上方**（它关联「发送」动作，输入条始终可见）。

**折叠可访问性**：折叠时 `aria-live` 记录区随之移除，屏幕阅读器用户在折叠期间不朗读流式回复——与视力用户对等（都需展开查看内容），非缺陷。

## 非功能需求

| ID | 名称 | 优先级 | 描述 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| REQ-NF-001 | 本机运行 | MUST | 第一版面向本机开发运行。 | 应用可通过本机命令启动，SQLite 数据保存于本地项目运行目录。 | APPROVED |
| REQ-NF-002 | 认证隔离 | MUST | 用户只能访问自己的 Provider 配置和会话。 | API 测试证明用户 A 不能读取或调用用户 B 的 Provider 与会话。 | DEFERRED（暂缓，CR-20260909-minimal-floating-chat）：当前版本为单一本地管理员，无隔离对象；恢复 Google 登录时同步恢复本条。 |
| REQ-NF-003 | 官方授权边界 | MUST | 第三方模型账号授权只使用官方支持方式。 | 系统不实现网页登录模拟、cookie 复用、抓包 token 或密码代登录。 | APPROVED |
| REQ-NF-004 | 真实入口验证 | MUST | 交付前必须通过真实 Next.js 应用入口验证主路径。 | 冒烟测试启动真实应用并走通（管理员身份免登录进入后）模型设置和底部浮窗对话主路径。 | APPROVED |

## 非目标

- 首版不实现图片、语音、多模态输入。
- 首版不实现工具调用和 Agent 编排。
- 首版不实现知识固化入口。
- 首版不实现公网生产部署。
- 首版不实现非官方第三方网页登录自动化。
- 首版不实现多用户与账号隔离（随 REQ-F-001 Google 登录一同暂缓）。
- 首版不实现对话历史浏览（已结束会话保留在本地但无浏览入口）。
- 首版不实现超长上下文的截断或摘要；超出模型上下文上限时由 Provider 报错，走流中错误路径。
- 折叠状态下不显示未读消息角标、消息计数或「有新回复」持久标记（CR-20260909-collapsible-panel）；折叠期间回复完成仅由状态灯瞬时提示，用户需手动展开查看内容。

## 多角色评审

| 角色 | 检查重点 | 反馈 | 处理结果 | 结论 |
|---|---|---|---|---|
| 产品 owner | 目标、范围、非目标、验收标准 | 第三方账号授权需要纳入需求，但不能误写为 OpenAI/DeepSeek 官方 OAuth 已可用于模型调用。 | 增加 REQ-F-008 与 REQ-NF-003，写明官方 OAuth 优先、API Key 兜底。 | APPROVED |
| 架构角色 | 技术可行性与外部 API 边界 | OpenAI/DeepSeek 官方模型 API 当前以 Bearer/API Key 调用为主。 | 将 OpenAI/DeepSeek 首版认证模式定义为 API Key 托管，本地模型定义为 Local。 | APPROVED |
| 测试角色 | 可测试性 | 每条 MUST 需求需要可观察验收标准。 | 为所有 MUST 需求补充验收标准。 | APPROVED |

### CR-20260909-minimal-floating-chat 评审

评审立场：各角色以本说明书职责 + 行业惯例为立场，独立评审，允许 CONDITIONAL / 反对。

| 角色 | 立场与检查重点 | 反馈（本角色视角） | 处理结果 | 结论 |
|---|---|---|---|---|
| 产品 owner | 守「已批准需求不得被静默削减」「每条 MUST 有可观察验收」「变更须用户确认」 | ①「用户范围」原文即「本机个人用户 + 同一用户配置 Provider」，单管理员与之一致，不算收缩范围。②浮窗删芯片/状态文字/模型框/链接**不是删能力**——Provider 选择迁到「配置」且升级为优先级（REQ-F-006），能力净增。③**坚持**优先级机制（有序列表 + 上移/下移 + 解析规则 + fallthrough）写进 REQ-F-006 验收，不下放 UI 规范——否则「对话用哪个 Provider」这个用户可观察行为将无验收锚点。④REQ-F-001/NF-002 只能标 DEFERRED 不能删，且必须保留模块/测试覆盖行，恢复路径可追溯。⑤全部 15 个变化点已于 2026-09-09 逐轮取得用户确认。 | REQ-F-006 按④重写；REQ-F-017/018 新增；DEFERRED 保留覆盖行；用户确认记入批准状态 | APPROVED |
| 架构角色 | 守「技术可行、可维护、信任模型清晰」；行业惯例：本地单用户工具 OAuth 是摩擦项 | ①`JARVIS_TEST_USER_ID` 旁路已在 `auth-guard.ts` 存在且被 TEST-001/002 覆盖，作当前身份来源无新增信任面。②**但该 env 语义是「测试旁路」，用作产品形态开关名不副实**——生产构建（`next start`）下不生效，当前「单管理员」实际只在 `npm run dev` 成立。要求本 CR 明确「生产形态待 P3 决」，不得让说明书读起来像生产已支持免登录。③优先级解析、会话生命周期、状态灯探测三处需成文为 DEC。 | ②记入 REQ-F-001 验收注与 DEC-001；架构说明书补「技术栈与部署形态」节；DEC-010/011/012 立项（待评审） | CONDITIONAL：生产形态须在架构说明书显式标注为待定 |
| 测试角色 | 守 AI_STANDARD 原则 12（真实入口对应）、原则 15（验收逐条断言） | ①原 REQ-F-006「浮窗切换器」验收随需求作废，须有替代覆盖，否则「对话用哪个 Provider」无断言。②REQ-F-005 语义从「停止后可续」变为「停止即结束」，是**行为回归风险点**，必须有断言证明「停止后下一条消息是新会话」。③REQ-F-018「每次加载探测」若在测试里打真实网络会让 CI 不稳定。④REQ-F-001 正向验收（TEST-022 人工）随 REQ-F-001 暂缓，但不能因此假装 REQ-F-001 已验证——`gate g3` 对 TEST-022 的阻断应转为「需求暂缓」而非「待执行」。 | ①→TEST-025；②→TEST-029；③→TEST-027 用 mock；④测试说明书「未完成项」明确措辞 | APPROVED |
| 开发角色 | 守「任务可实现、依赖清晰、无隐藏返工」 | ①`providers` 表加列需向后兼容迁移（`node:sqlite` 无迁移框架）。②「新对话后刷新空白」跨 SSR/hydration，是本 CR 最不确定的实现点，不宜在 P1 冻结方案。③浮窗收敛涉及删除多个已测元素，须同步更新 TEST-011/012 断言避免测试与实现漂移。 | ①②记入架构 DEC-010/011 待决点；③TEST-011/012 本 CR 同步改写 | APPROVED |

### CR-20260909-collapsible-panel 评审

评审立场：各角色以本职说明书 + 行业惯例独立评审，允许 CONDITIONAL / 反对。完整评审见 `project/06_changes/CR-20260909-collapsible-panel.md`。

| 角色 | 反馈（本角色视角） | 处理结果 | 结论 |
|---|---|---|---|
| 产品 owner | ①范围内，SHOULD 合理。②REQ-F-003「发送后**必须**展开」→「触发展开、可手动收起」是一次 MUST 验收放宽，须记为用户确认变更。③**反对「折叠 + 完全沉默」**——折叠后回复完成，唯一信号是与「空闲就绪」同色的常亮绿灯，用户无法区分「可以聊」和「答案在等你」。 | 加状态灯完成瞬时提示（甲案，非角标非持久新态）；REQ-F-003 放宽记为用户确认变更；新增非目标锁定「无未读角标/计数」 | APPROVED |
| 架构角色 | ①**`expanded` 状态必须解耦**为 `hasTranscript`（派生）+ `userCollapsed`（持久），否则 `rollbackOptimistic`/`handleNewConversation`/发送路径要各自打补丁。②`localStorage` 服务端不可读，SSR 首帧只能按默认渲染 + client 校正——与 DEC-011（`sessionStorage`）同类。③纯 MOD-CHAT-UI，与 DEC-005「保持主界面可见」方向一致。④a11y：折叠时 `aria-live` 记录区移除，屏幕阅读器折叠期间不朗读流式回复——与视力用户对等，非缺陷。 | REQ-F-019 实现约束写明状态解耦；新增 DEC-013（含 SSR/hydration 处理）；澄清小节记 a11y | APPROVED |
| 模块开发角色 | ①**任务不能是「加个按钮」**——必须含 `expanded`→`showTranscript` 重构，写死在 TASK-030。②持久化集中为一个 `setUserCollapsed` helper（try/catch，镜像 `markSessionEnded`），键 `jarvis:chat-collapsed`。③**决策 D**：`max-height` 到 `auto` 不能 transition——v1 瞬时切换无动画。 | 条件并入 TASK-030 描述与 DEC-013 | APPROVED |
| 测试角色 | ①**`expanded`→`showTranscript` 是行为相邻重构**，会碰 REQ-F-003 的「发送后展开」e2e 和 TEST-030「新对话清空 + 刷新空白」——两者须在重构后重跑，作为 CR 回归门。②REQ-F-019 七条验收 → 七组独立断言。③**持久化跨刷新必须真实浏览器**（原则 12），因 SSR 首帧展开 / client 校正折叠这条路径 jsdom 测不出。④陈旧偏好（`collapsed=true` 但无记录）须显式断言不出现「折叠了却空白」的中间态。 | TEST-031 承载折叠行为；测试说明书点名 TEST-030 / REQ-F-003 e2e 重验；持久化跨刷新走 e2e | APPROVED |

## 批准状态

- 当前状态：APPROVED
- 用户确认：2026-09-08，用户要求按该计划更新说明书并严格按流程执行。
- 用户确认：2026-09-09，用户逐轮确认 CR-20260909-minimal-floating-chat 的需求变化点（单管理员形态、浮窗收敛、Provider 优先级、状态灯四态、会话生命周期、浅色简约视觉），并要求落地后查看各流程变化点与角色评审意见。
- 用户确认：2026-09-09，用户提出并逐轮确认 CR-20260909-collapsible-panel（对话面板一级折叠、折叠时安静、偏好记住、发送即展开、状态栏箭头控件、折叠期间回复完成状态灯瞬时提示【甲案】、v1 无动画），并要求经角色评审后修正说明书。
