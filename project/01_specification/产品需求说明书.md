# 产品需求说明书

责任人：产品 owner

## 产品目标

交付 Agent-Jarvis 的第一个产品功能：一个本机运行的底部悬浮大模型对话 UI。当前版本**以单一本地管理员身份运行**，用户在底部悬浮输入条发起文本对话，查看流式回复，随时停止，开启新对话，并保存最近会话。可用模型 Provider 的**启用与使用优先级在「配置」中管理**，对话浮窗不做 Provider/模型选择。

## 用户范围

- 主要用户：本机使用 Agent-Jarvis 的**单一本地管理员**（当前版本不区分多用户，无账号登录）。
- 管理职责：该管理员在「配置」中启用模型 Provider、设定使用优先级、维护凭据。

## 功能需求

| ID | 名称 | 优先级 | 描述 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| REQ-F-001 | Google 登录 | MUST | 用户必须通过 Google 登录进入 Agent-Jarvis 受保护页面。 | 未登录访问主页、设置页或对话 API 时被阻断；登录后可访问主页、设置页和对话入口。 | DEFERRED（暂缓，CR-20260909-minimal-floating-chat）：当前版本以单一本地管理员身份运行，「账号登录」为占位入口，Google 登录待后续 CR 恢复。 |
| REQ-F-002 | 底部悬浮输入条 | MUST | 页面底部必须常驻紧凑悬浮输入条，作为大模型对话入口。 | 默认页面加载后底部输入条可见，可输入文本并触发发送；悬浮条仅含**状态灯、输入框、单个动作按钮**三个元素；输入框始终可输入，不依赖 Provider 状态。 | APPROVED |
| REQ-F-003 | 展开式对话面板 | MUST | 发送消息后，悬浮输入条展开为底部对话面板。 | 发送消息**触发**面板展开；用户可随后经 REQ-F-019 手动收起，再次发送重新展开。面板展开时展示用户消息与模型回复区域；对话状态**仅由状态灯表达，无文字**；面板高度上限**分情形**：纯对话为视口 50%；本轮记录区含步骤流（REQ-F-035）时为 75%（用户 2026-09-10 终裁 5 确认「75% + 分情形」）。 | APPROVED（CR-20260909-collapsible-panel 将「发送后必须展开」放宽为「触发展开、可手动收起」，2026-09-09 用户确认）；**APPROVED（CR-20260910-agent-tooling 重写高度上限，覆盖 65%→50% 两次澄清；R1 已终裁 2026-09-10）** |
| REQ-F-004 | 文本流式回复 | MUST | 对话必须支持模型文本流式输出。 | 调用对话接口时前端逐段显示模型回复，而不是只在完成后一次性显示；会话活动中每轮请求由服务端按 REQ-NF-007 / REQ-F-041 / REQ-F-042 规则组装上下文：**稳定前缀 + 易变后缀 + （若已压缩）摘要 + 其后的会话消息（含工具往返，受保留窗口约束）**。 | APPROVED（CR-20260910-agent-tooling 重写：不再是「已有全部消息 + 一段固定 system prompt」；R1 已终裁 2026-09-10） |
| REQ-F-005 | 停止生成 | MUST | 用户必须能中断正在生成的回复。 | 生成中显示「停止」；点击后**服务端停止请求模型、当前会话即结束**；被中断的回复保留在记录区并带**行内「已停止」标记**；落库 `status=stopped`；「停止」在工具循环任一阶段（生成中 / 工具执行中）立即生效，已执行工具调用与已生成文本一并落库（REQ-F-029 ⑤）。 | APPROVED（CR-20260910-agent-tooling 补循环中途停止语义；R1 已终裁 2026-09-10） |
| REQ-F-006 | Provider 启用与优先级 | MUST | 用户必须能在「配置」中启用/停用多个模型 Provider，并明确设定它们的使用优先级。对话浮窗不提供 Provider 或模型的选择/切换控件。 | ①「配置」将已启用 Provider 呈现为**有序列表**，用户可通过「上移/下移」调整顺序，列表位置即优先级（第 1 位最高）；②新启用的 Provider 追加到列表末尾；③仅启用一个 Provider 时排序控件隐藏或禁用；④对话请求**不携带** Provider/模型参数，服务端在「已启用且连接有效」的 Provider 中取优先级最高者解析实际使用的 Provider/模型；⑤最高优先级 Provider 连接无效时自动落到下一个连接有效的；⑥无任何连接有效的 Provider 时状态灯灭、发送被拦截并在输入框上方提示；⑦解析到最高优先级 Provider 后按 REQ-F-040 判定工具可用性，**优先级不因工具能力跳过**。 | APPROVED（重写，CR-20260909-minimal-floating-chat；P3 已实现，TEST-025/026）；**APPROVED（CR-20260910-agent-tooling 补 ⑦；R1 已终裁 2026-09-10）** |
| REQ-F-007 | 独立模型设置页 | MUST | 系统必须提供独立设置页管理 OpenAI、DeepSeek、本地模型 Provider。 | `/settings/models` 可新增、编辑、启用、停用、测试 Provider，并提供已启用 Provider 的**优先级有序管理**（上移/下移）。 | APPROVED |
| REQ-F-008 | Provider 认证模式 | MUST | Provider 必须区分 OAuth、API Key、Local、Unsupported 四种认证模式。 | 设置页能显示每个 Provider 的认证模式；OAuth 仅用于官方支持的 Provider，OpenAI/DeepSeek 首版使用 API Key 兜底。 | APPROVED |
| REQ-F-009 | 凭据服务端加密 | MUST | API Key 等敏感凭据必须由服务端加密保存，浏览器不得获得明文。 | 数据库存储值不是明文；Provider 查询接口只返回脱敏状态。 | APPROVED |
| REQ-F-010 | OpenAI 接入 | MUST | 系统必须接入 OpenAI Provider。 | 配置 OpenAI API Key 和模型后，可通过统一对话接口获得流式文本回复。 | APPROVED |
| REQ-F-011 | DeepSeek 接入 | MUST | 系统必须接入 DeepSeek Provider。 | 配置 DeepSeek API Key 和模型后，可通过 OpenAI 兼容接口获得流式文本回复。 | APPROVED |
| REQ-F-012 | 本地模型接入 | MUST | 系统必须支持本地 OpenAI-compatible 模型服务。 | 配置本地 base URL 和模型名后，可通过统一对话接口获得流式文本回复。 | APPROVED |
| REQ-F-013 | 最近会话保存 | MUST | 系统必须按当前管理员身份保存最近会话和消息。 | 刷新页面后，最近会话及其消息可恢复；恢复触发条件收窄——**仅当最近会话仍处于活动状态**（用户既未「停止」也未「新对话」）时刷新才恢复它，否则呈现空会话；消息记录含工具调用与结果（数据形状由架构定），恢复**包括步骤流**（REQ-F-035 ④）；`GET /api/conversations/[id]/messages` 返回含工具记录且向后兼容，已有会话行不动。 | APPROVED（CR-20260910-agent-tooling 补工具记录持久化与重建；R1 已终裁 2026-09-10） |
| REQ-F-014 | 克制的浅色简约视觉 | SHOULD | 对话 UI 应采用克制、专业的浅色简约风格（视觉方向参考 `gens.team/ai-insights`：留白充足、单一强调色、统一圆角、卡片式分组），默认浅色并可切换深色，优先可读性与呼吸感而非装饰或信息密度。 | 浅色为默认且可切换深色并持久化；**单一强调色（蓝色系）**用于主按钮与交互焦点，其余为中性灰阶；交互元素（按钮、输入框、卡片）使用统一圆角，分组靠细线或留白而非重边框；布局以充足留白构建层级，非紧凑控制台密度；无阴影或仅极轻阴影；文本在桌面和移动宽度下不溢出；正文文本、控件边界、**状态灯五态**（REQ-F-036）在两套主题下均达 WCAG 2.2 AA（正文 4.5:1、非文本 3:1）；**收起/展开控件（REQ-F-019）与左下角 ☰ 菜单触发器（REQ-F-015 / CR-20260909-corner-menu）为真实 `<button>`，焦点可见、与背景非文本对比 ≥ 3:1、触控目标 ≥ 24px；☰ 触发器 `aria-haspopup` + `aria-expanded`；图标形态入 `docs/UI_STANDARD.md`**；步骤流（步骤行、展开控件，REQ-F-035）纳入同一视觉基础与可访问性要求。 | APPROVED（CR-20260910-agent-tooling：四态→五态、步骤流纳入；R1 已终裁 2026-09-10） |
| REQ-F-015 | 全屏动态展示屏首页 | SHOULD | 当前版本首页是一块**全屏动态展示屏**，其上浮着底部悬浮对话与左下角 ☰ 悬浮菜单。展示屏默认呈现**标题视图**（产品名 + 一句话简介）；对话触发的内容（首个：技能洞察 HTML 报告，REQ-F-026）在展示屏上呈现，可由对话指令切换回标题视图（REQ-F-027）。 | 首页渲染一块全屏 `DisplayScreen`（默认标题视图，含产品名——取代原 header `<h1>`，header 不再单独存在）；底部悬浮对话（REQ-F-002/003）与左下角 ☰ 触发器 + 浮层菜单（内联浅色/深色 + 「模型」+ 「账号登录」，Esc/light-dismiss/焦点回归行为不变）层叠**在展示屏之上**（层叠刻度：DisplayScreen 基底 / 底部对话 20 / ☰ 30）；「模型」「账号登录」弹窗行为不变；当前版本「账号登录」为**占位入口**（对应 REQ-F-001 暂缓）。 | APPROVED（CR-20260909-corner-menu 将 header 两按钮改为 ☰ 菜单；**CR-20260909-display-screen 将「极简空首页」重写为「全屏动态展示屏」，2026-09-09 用户确认**） |
| REQ-F-016 | 回复 Markdown 渲染 | SHOULD | 助手回复应按 Markdown 渲染，使代码、列表和强调可读。 | 代码块、行内代码、粗体、斜体、有序/无序列表、标题按语义元素渲染；仅 http/https/mailto 链接可点击；模型输出中的原始 HTML 不被执行；流式中未闭合代码围栏不破版；**请求级错误**（回复第一段文本到达前失败）在**输入框上方一行红字**展示，内容脱敏（不含 API Key、不含 Provider 内部细节），不写入对话上下文，不结束会话，不阻塞继续输入。 | APPROVED |
| REQ-F-017 | 开始新对话 | MUST | 用户必须能主动开启一个不继承既有上下文的新会话。 | 「非生成中且输入框为空」时动作按钮显示为「新对话」（无历史消息时也是）；点击后清空浮窗记录区、丢弃当前 `conversationId`；在下一条消息发送前不产生新的会话记录；点击「新对话」后刷新页面呈现空会话，不自动恢复上一会话。 | APPROVED |
| REQ-F-018 | 连接状态指示灯 | MUST | 悬浮条以单个状态灯表达当前可对话性与生成状态，不使用文字。 | 首页加载时对全部已启用 Provider 执行**一次连接探测**，状态灯反映探测结果；灯有五种可辨识状态——**检测中**（加载探测进行中）、**灭**（无已启用 Provider 或全部探测失败，不可对话）、**常亮**（至少一个 Provider 连接有效，可对话）、**闪烁 / 生成文本中**（正在生成回复）、**执行工具中**（REQ-F-036，第五态，与「生成文本中」可辨识区分）；五态在两套主题下互相可区分且与背景对比达 WCAG 2.2 AA 非文本对比 3:1。**（CR-20260909-collapsible-panel 补充：折叠状态下回复完成时状态灯做一次短促瞬时提示后落定，属四态的显示表现，不新增可辨识状态。）**（该补充由 CR-20260910-agent-tooling 作废：用户 2026-09-10 拍板破四态。） | APPROVED（CR-20260910-agent-tooling 四态→五态；R1 已终裁 2026-09-10） |
| REQ-F-019 | 收起对话面板 | SHOULD | 对话面板展开后，用户可主动将其收起为输入条形态，会话状态不变；可再次展开，记录区原样恢复。收起/展开偏好按浏览器记住。 | ①存在对话记录时，状态灯所在行显示收起/展开控件（展开态朝下、收起态朝上），无记录时不显示；②点击收起：记录区从视图与 DOM 移除，面板回到「状态灯 + 输入框 + 单按钮」形态，`conversationId` 不变、`jarvis:chat-session-ended` 不置位、消息不清空；③点击展开：记录区恢复且滚动到底部，消息与折叠前一致；④折叠状态下收到流式回复或 `tool_call` / `tool_result` 事件保持折叠，状态灯转「生成文本中」或「执行工具中」（REQ-F-036），回复完成、触顶或失败时状态灯做一次短促瞬时提示后落定，全程不自动展开、不显示未读计数；⑤折叠状态下发送新消息自动展开；⑥收起/展开切换与发送即时写入 `localStorage`（每浏览器），刷新时存在可恢复会话则按存储偏好渲染（未设过默认展开），折叠偏好为真但无记录时按①不显示控件、呈现输入条形态，不产生「折叠了却空白」的中间态；⑦收起/展开为纯 UI 状态，不改 `conversationId`、不结束会话、与「新对话」正交。<br>**实现约束**：`expanded` 拆为 `hasTranscript`（派生自消息数）与 `userCollapsed`（持久 boolean），`showTranscript = hasTranscript && !userCollapsed`；瞬时切换无高度动画。 | APPROVED（CR-20260910-agent-tooling 重写 ④ 折叠态长任务；R1 已终裁 2026-09-10）；**APPROVED（CR-20260911-display-console-ux CP-8：实现约束「瞬时切换无高度动画」改为「允许 150–200ms 过渡、reduced-motion 下无动画」，并与 REQ-F-054 悬停态正交；R1 已终裁 2026-09-10）** |
| REQ-F-020 | 技能上传与注册 | SHOULD | 用户可把一个技能**文件夹或 zip 压缩包**交给底部对话框（拖放或点选上传）；Jarvis 读取其内容，调用大模型生成一份 `SKILL.md`，将内容原样存入 `.data/skills/<name>/` 并注册，补充为可被对话调用的系统能力。 | ①**底部对话框**是唯一入口（不是左下角 ☰ 菜单），支持两种投递方式——**拖放**与**点选上传**（真实 `<button>` 触发），每种方式都接受**文件夹**与 **zip 压缩包**；②系统追加一条「生成 SKILL.md」指令，调用当前优先 Provider 产出 `SKILL.md`（frontmatter `name`/`description` + 正文 `instructions`）写入该技能目录；③注册成功后对话内出现「已注册技能：&lt;name&gt; — &lt;description&gt;」确认，**不回显 `SKILL.md` 全文**；④生成结果解析不出 frontmatter 时，`name` 回退为文件夹名、`description` 为「（未生成描述）」，并在对话内提示用户可补充；⑤扩展名不在白名单（`.md .mdx .txt .json .jsonl .csv .yaml .yml .toml .xml .py .js .ts .tsx .html .sh`）的文件、二进制文件、超大文件不纳入技能内容，**且必须在注册回执中列出被排除的文件与原因**，不得静默丢弃；⑥当前只做单一技能，注册机制对多技能开放；⑦**投递失败必须可见**——落到对话框的拖放若不是可识别的技能文件夹或 zip，必须阻止浏览器默认行为并在对话内说明原因，不得静默无反应；⑧**点选上传收敛为一个可见入口**（CR-20260915-unified-upload-entry）：控制台常驻单个「上传」按钮，点击展开菜单给出「文件夹」/「zip 压缩包」两个选项，各自仍触发对应的隐藏 `<input type=file>`——不改变①的两种投递方式（拖放/点选）与两种内容类型，键盘可达性不因合并而降低（菜单本身支持方向键/Enter/Esc 标准导航）。 | APPROVED（R1 2026-09-09 拍板；**①⑤ 由 CR-20260910-skill-intake 重写：新增 zip 与点选上传、白名单放宽、被排除文件回执、⑦ 失败可见；⑧ 由 CR-20260915-unified-upload-entry 新增**） |
| REQ-F-021 | 按发送路由技能 | SHOULD | 已注册 ≥1 个技能时，每次发送前系统做一次独立的最小路由判定，决定本轮是否启用某技能。 | ①路由是**独立的最小 LLM 调用**，不占用主对话模型上下文；②路由输入 = 本轮用户消息 + 已注册技能的 `name`/`description` 列表，输出结构化 `{skill: <name\|null>}`；③命中某技能则**仅本轮**注入，后续发送不自动延续（参考 ChatGPT 对话内技能用法）；④无可用 Provider 时路由不阻塞，按普通对话继续（fail-open），不报错打断；⑤未注册任何技能时不发起路由调用。 | **SUPERSEDED**（由 REQ-F-030 取代：独立路由调用删除，技能由模型经工具自主选择；CR-20260910-agent-tooling，R1 已终裁 2026-09-10。原状态 APPROVED（R1，2026-09-09 用户拍板；CR-20260909-skills）） |
| REQ-F-022 | 技能内容注入 | SHOULD | 命中技能的那一轮，把技能的 `SKILL.md` 与文件夹内全部白名单文本文件拼入该轮 system 上下文。 | ①注入内容 = `SKILL.md` 正文 + 各白名单文本文件（带文件名分隔）；②注入总量上限 32KB，超出按文件顺序截断并在回复内注明被截断；③注入**仅作用于命中的那一轮**，不写入会话持久上下文；④不改变 REQ-F-004「每轮携带会话已有全部消息 + 固定 system prompt」，技能内容作为该轮**附加 system 段**。 | **SUPERSEDED**（由 REQ-F-030 ①②④ 取代：正文按需 `read_skill`、无「仅当轮」硬边界；CR-20260910-agent-tooling，R1 已终裁 2026-09-10。原状态 APPROVED（R1，2026-09-09 用户拍板；CR-20260909-skills）） |
| REQ-F-023 | 技能 HTML 产出捕获 | SHOULD | 技能轮的回复若含 ` ```html ` 代码块，捕获最后一个作为洞察产物；否则在对话内明确告知，不静默。 | ①从完成的回复取**最后一个** ` ```html ` 围栏块；②捕获成功：内容存入 `insights`（REQ-F-024），对话内提示「已生成洞察」（展示屏 UI 属 CR-20260909-display-screen）；③回复中无 html 围栏：对话内显示「本轮未产出 HTML」；④html 围栏存在但流式结束时未闭合：显示「HTML 不完整」，不写入 `insights`；⑤非技能轮不做捕获。<br>**CR-20260910-agent-tooling 重写**：捕获改为 `save_insight(html)` 工具——模型显式调用；无「技能轮」概念，①⑤作废；HTML 完整性由工具参数校验，不完整则工具失败回喂模型（④改）；③**保留「不静默」原则**（用户 2026-09-10 终裁 1）——模型本轮读取过技能却未调用 `save_insight` 时，对话内明确告知「本轮未产出洞察」，不静默；**`skill-html-unsandboxed` 信任面扩大且用户明示接受**（终裁 3 选 C）——`read_url` 使 HTML 来源从「用户自己拖入的技能文件夹」扩到「任意可被搜索到的网页」，攻击链为 `web_search → read_url → 页面内提示注入 → 模型调 save_insight → 攻击者 HTML 进入无沙箱同源 iframe`；缓解仍只有 REQ-F-025 ② 的不可关闭提示条；风险登记为 `skill-html-unsandboxed-web-source`（`test-results.json known_warnings`），iframe 沙箱化仍是未来 CR 的强制出口义务且优先级因本 CR 提高。 | APPROVED（R1，2026-09-09 用户拍板；CR-20260909-skills）；**APPROVED（CR-20260910-agent-tooling 重写；R1 已终裁 2026-09-10）** |
| REQ-F-024 | 洞察结果存储与读回 | SHOULD | 技能产出的 HTML 按会话存储，并提供读回入口供展示屏使用。 | ①`insights` 表含 `id`、`conversation_id`、`kind`、`html`、`created_at`；②`kind` 为扩展点，F1 固定写 `"skill"`；③同一会话多次产出按 `created_at` 追加，不覆盖；④提供 `GET /api/insights?conversationId=<id>`（返回该会话洞察列表，最新在前）作为 F1 的真实读回入口（原则 13）；⑤首页/展示屏渲染属 CR-20260909-display-screen；⑥写入者由捕获逻辑改为 `save_insight` 工具（CR-20260910-agent-tooling），`kind` 扩展点与追加语义不变。 | APPROVED（R1，2026-09-09 用户拍板；CR-20260909-skills）；**APPROVED（CR-20260910-agent-tooling 补 ⑥；R1 已终裁 2026-09-10）** |
| REQ-F-025 | 技能 HTML 渲染安全边界（当前非目标） | MUST | 当前版本不对技能生成的 HTML 做沙箱隔离，以显式告知替代技术隔离。 | ①技能生成的 HTML 由展示屏以 `<iframe srcdoc>` 直接渲染，**不加 `sandbox` 属性**（用户 2026-09-09 明确接受该风险）；②展示屏（CR-20260909-display-screen）**渲染任一技能 HTML 时**在其顶部显示一条**不可关闭**提示条（无关闭按钮、Esc 无效、切走 insight 视图才消失），说明内容由大模型生成且未隔离；③该风险登记为 known warning `skill-html-unsandboxed`，「沙箱化」列为未来 CR 的出口义务；④不因此放宽 REQ-NF-003（仍不做网页登录模拟、cookie 复用、抓包 token、密码代登录）。 | APPROVED（R1，用户明确接受风险；CR-20260909-skills；②由 CR-20260909-display-screen 落地） |
| REQ-F-026 | 动态展示屏内容与持久化 | SHOULD | 展示屏的内容由**全局单一**状态驱动，跨刷新恢复，随对话动态更新，并对未来内容类型开放。 | ①展示屏内容由一个**全局单行**状态 `display_state` 决定（不是每会话一份）；②`display_state` 有 `kind` 字段作扩展点，F2 支持 `kind ∈ {home, insight}`，其他 kind 预留；③技能轮产出洞察（`insights` 新行，REQ-F-023/024）后，`display_state` 自动切为 `{kind:"insight", ref: <该会话最新洞察>}`，展示屏渲染该 HTML（`<iframe srcdoc>` + REQ-F-025 提示条）；④刷新页面后展示屏按 `display_state` 恢复上次内容，不因刷新回到标题视图；⑤`kind:"home"` 时渲染标题视图。 | APPROVED（R1，2026-09-09 用户拍板；CR-20260909-display-screen） |
| REQ-F-027 | 对话指令控制展示屏 | SHOULD | 用户可用对话消息把展示屏切回标题视图。 | ①每次发送的路由判定（REQ-F-021，同一次独立调用）除 `skill` 外并行输出 `display` 字段；②用户消息表达"显示首页 / 回到首页 / 显示标题"类意图时 `display:"home"` → `display_state` 置 `{kind:"home"}`、展示屏回标题视图；③路由判不出展示意图 → `display:null` → 展示屏**不变**（fail-open）；④`display` 切换与 `skill` 命中互不阻塞（一条消息可以既触发技能又不改展示，或只切展示）。 | **SUPERSEDED**（由 REQ-F-032 取代：`show_home()` / `show_insight(id)` 工具；路由 `display` 字段随 REQ-F-021 删除而失去宿主；CR-20260910-agent-tooling，R1 已终裁 2026-09-10。原状态 APPROVED（R1，2026-09-09 用户拍板；CR-20260909-display-screen）） |
| REQ-F-028 | 技能可见性 | SHOULD | 用户随时能看到系统里有哪些技能，以及某一轮回复是否用了技能。 | ①左下角 ☰ 菜单可查看**已注册技能列表**（名称 + 描述；无技能时显示「尚未注册技能」）；②某轮发送命中技能时，对话内出现「本轮使用技能：&lt;name&gt;」提示；③未命中技能的轮次不出现该提示；④技能列表读 `GET /api/skills`，注册成功后无需刷新页面即可看到新技能。<br>**CR-20260910-agent-tooling 重写**：①列表**可管理**（删除、重命名，REQ-F-031）；②③删除——「本轮使用技能」提示由步骤流的 `read_skill` 行承担（REQ-F-035 ⑥）；④不变。 | APPROVED（R1，2026-09-10 用户拍板；CR-20260910-skill-intake）；**APPROVED（CR-20260910-agent-tooling 重写 ①②③；R1 已终裁 2026-09-10）** |
| REQ-F-029 | 工具调用循环 | MUST | 一次发送内，模型可多次调用已注册工具，服务端执行并回喂结果，直到模型给出最终文本或触顶。 | ①单轮内工具调用上限 **100** 步（CR-20260911-display-console-ux CP-1 由 10 提升，用户 2026-09-11 直接授权；代码已先行同步）；②触顶：终止循环，对话内出现提醒「已达 100 步上限，已停止」，**已执行工具结果与已生成文本保留**并落库 `status=truncated`；`truncated` 进入可重放集合（**= REQ-F-013 活动会话恢复所捞取的状态集合**）；触顶**不结束会话**，用户可继续追问；③工具执行失败（异常 / 超时）不终止循环——失败信息作为该工具结果回喂，由模型决定重试或改道；单工具超时上限由架构定；④**每次 `tool_call` 计 1 步**；同一工具同参数（参数按规范化 JSON 判等）连续失败 2 次后，本轮拒绝第 3 次同参调用；⑤「停止」（REQ-F-005）在循环任一阶段立即生效；⑥全部工具当前分三类——**只读**（`list_skills` / `read_skill` / `search_skills` / `web_search` / `read_url`）、**改展示态**（`show_home` / `show_insight`）、**追加洞察行**（`save_insight`）；均不写文件、不执行命令，**不引入执行审批**；新增任何写文件 / 执行命令类工具的 CR 必须把审批机制列为出口义务；⑦Provider 不支持工具调用时按 REQ-F-040 处理。 | APPROVED（CR-20260910-agent-tooling，R1 人工终裁 2026-09-10 完成）；**APPROVED（CR-20260911-display-console-ux CP-1 改 ①② 上限；R1 已终裁：用户 2026-09-13，INPUT-2026-09-13-027）** |
| REQ-F-030 | 技能作为工具 | MUST | 技能不再按轮注入，而是作为三个工具供模型按需读取。 | ①`list_skills()` 返回名录；`read_skill(name)` 返回 `SKILL.md` + 白名单文本文件正文（白名单与 32KB 上限沿用 REQ-F-020，上限语义从「每轮注入」改为「单次 read 结果」）；`search_skills(query)` 按 name / description 关键字检索；②system 稳定前缀只含技能名录（name + 一行 description），不含正文；③名录超出 REQ-NF-007 子预算时只列前 N 条并提示模型用 `search_skills`；④一轮内可 read 多个技能；read 结果作为工具结果进入上下文，随 REQ-F-041 淡出，**无「仅当轮」硬边界**；⑤未注册任何技能时三个工具不注册（REQ-NF-008 ④）；⑥REQ-F-021 独立路由调用删除、REQ-F-022 注入方式作废。 ⑦**人的一侧**：每次 `list_skills` / `search_skills` / `read_skill` 在对话内步骤流各占一行（REQ-F-035 ①），用户能看出本轮用了哪个技能、读了哪个文件；技能名录本身不另设界面。 | APPROVED（CR-20260910-agent-tooling，R1 人工终裁 2026-09-10 完成、P3/P4 完成；状态回写遗漏，2026-09-11 补正） |
| REQ-F-031 | 技能管理 | SHOULD | 用户可在 ☰ 技能列表删除与重命名技能。 | ①每项提供「删除」「重命名」；②删除需二次确认，成功后目录与 DB 行同步移除，列表免刷新更新；③重命名做唯一性校验（复用 `SkillNameConflictError`），目录名同步；④历史会话中已进入上下文的技能内容不受影响；⑤仍不做编辑正文、版本管理、技能市场。 | APPROVED（CR-20260910-agent-tooling，R1 人工终裁 2026-09-10 完成、P3/P4 完成；状态回写遗漏，2026-09-11 补正） |
| REQ-F-032 | 展示屏工具化 | SHOULD | 展示屏由工具控制，取代路由字段。 | ①`show_home()`：`display_state` 置 `{kind:"home"}`；②`show_insight(insightId)`：置 `{kind:"insight", ref}`，**id 可为任意历史洞察**（用户 2026-09-10 终裁 2 选 B，非目标「不做展示内容历史回看/切换」「只支持回首页意图」随之改写）；id 须经 `conversations` 关联校验属主，非法 / 不存在 / 不属当前用户的 id 按工具失败回喂；③REQ-F-027 路由 `display` 字段作废；④两工具仅改 UI 状态，免审批；⑤`show_document(id)`（CR-20260915-document-display）：置 `{kind:"document", refId:id}`，id 复用 `search_documents`/`read_document` 的文档标识格式，同一条 `resolveWithinRoots` 校验路径；资料库来源的文档沿用与 `read_document` 相同的采纳闸（未采纳/已拒绝一律拒绝并说明去处）；展示屏渲染的是**原件**而不是提取文本——浏览器原生能内嵌渲染的格式（PDF/HTML/纯文本类）走沙箱化 `iframe`，其余格式（如 `.docx`）给新标签页打开/下载原文件的回退，不假装能预览。 | APPROVED（CR-20260910-agent-tooling，R1 人工终裁 2026-09-10 完成、P3/P4 完成；状态回写遗漏，2026-09-11 补正；⑤ CR-20260915-document-display，R1 用户 2026-09-15 总授权终裁） |
| REQ-F-033 | 联网搜索工具 | MUST | 模型可通过 `web_search(query)` 检索网页。 | ①经配置的 SearXNG 兼容端点 `GET /search?q=&format=json` 取结果，返回 title / url / content 列表，条数受 REQ-NF-007 子预算约束；②未配置搜索后端或「联网」总开关关闭时该工具**不注册**；③配置了但不可用时按 REQ-NF-011；④只发送查询词，不发送会话历史、凭据、用户身份（REQ-NF-009）；⑤结果内容按子预算截断。 | APPROVED（CR-20260910-agent-tooling，R1 人工终裁 2026-09-10 完成、P3/P4 完成；状态回写遗漏，2026-09-11 补正） |
| REQ-F-034 | 网页读取工具 | MUST | 模型可通过 `read_url(url)` 读取网页正文。 | ①抓取正文后**先摘要再入上下文**；原文不进入上下文、不落库；②目标地址受 REQ-NF-009 ③ 约束；③抓取超时与体积上限由架构定；④步骤流显示的是摘要不是原文；⑤「联网」总开关关闭时不注册。 | APPROVED（CR-20260910-agent-tooling，R1 人工终裁 2026-09-10 完成、P3/P4 完成；状态回写遗漏，2026-09-11 补正） |
| REQ-F-035 | 对话内步骤流 | MUST | 工具执行过程在对话内可见。 | ①每次 `tool_call` 在记录区出现一行紧凑步骤（工具名 + 参数摘要）；`tool_result` 到达后该行更新为完成 / 失败并可展开查看结果摘要；②默认折叠详情；③SSE 新增 `tool_call` / `tool_result` 事件；④刷新后活动会话的步骤流按落库记录**重建**（REQ-F-013）；⑤步骤行与展开控件为真实 DOM / `<button>`，满足 REQ-F-014 / NF-006 可访问性；⑥REQ-F-028 ②「本轮使用技能」提示删除，由步骤流的 `read_skill` 行承担。 | APPROVED（CR-20260910-agent-tooling，R1 人工终裁 2026-09-10 完成、P3/P4 完成；状态回写遗漏，2026-09-11 补正） |
| REQ-F-036 | 状态灯执行阶段 | SHOULD | 状态灯能区分「执行工具中」与「生成文本中」。 | ①新增第五态「执行工具中」；②折叠态下由灯态承担执行可见性；③五态在两套主题下互相可辨识且 WCAG 2.2 AA 非文本对比 ≥3:1；④显式取代 REQ-F-018「四态」与 CR-20260909-collapsible-panel「不新增可辨识状态」约束（用户 2026-09-10 拍板）。 | APPROVED（CR-20260910-agent-tooling，R1 人工终裁 2026-09-10 完成、P3/P4 完成；状态回写遗漏，2026-09-11 补正） |
| REQ-F-037 | token 用量可见 | SHOULD | 用户能看到当前会话的 token 消耗。 | ①☰ 菜单显示当前会话累计 input / output token；②累计包含该会话内**全部**内部模型调用（主循环每一步、摘要调用等）；③Provider 返回 `usage` 时用真实值，流式请求携带 `stream_options.include_usage`；④Provider 不返回 usage 时用服务端本地估算并**标注「估算」**；⑤新对话清零；⑥不做费用换算。 | APPROVED（CR-20260910-agent-tooling，R1 人工终裁 2026-09-10 完成、P3/P4 完成；状态回写遗漏，2026-09-11 补正） |
| REQ-F-038 | 搜索后端配置入口 | MUST | 用户可配置联网能力与搜索后端。 | ①☰ 新增「搜索」项，与「模型」并列，**不进「模型」弹窗**；②含「联网」总开关（**默认开**，用户 2026-09-10 终裁 4；但搜索后端 URL 未配置时 `web_search` 仍不注册，故实际出网以填入 URL 为准）、SearXNG 兼容端点 URL、启用 / 停用、「测试连接」（判据：`GET /search?q=&format=json` 返回 2xx 且 JSON 含 `results` 字段——SearXNG 默认未开 json 输出，须专测该点）；③URL 服务端保存，非敏感；**含 userinfo（`user:pass@`）的 URL 拒绝保存**；④总开关关闭 → `web_search` 与 `read_url` 均不注册；仅 URL 未配置 → 只 `web_search` 不注册。 | APPROVED（CR-20260910-agent-tooling，R1 人工终裁 2026-09-10 完成、P3/P4 完成；状态回写遗漏，2026-09-11 补正） |
| REQ-F-039 | 回答来源引用 | SHOULD | 使用了联网结果的回答附来源。 | ①本轮使用了 `web_search` / `read_url` 结果时，最终回答附来源列表（URL + 标题）；②来源 URL **必须来自本轮工具结果集合**，服务端校验，不在集合内的剔除并在步骤流标记；③按 REQ-F-016 渲染为可点击链接；④未使用联网工具的轮次不附来源。 | APPROVED（CR-20260910-agent-tooling，R1 人工终裁 2026-09-10 完成、P3/P4 完成；状态回写遗漏，2026-09-11 补正） |
| REQ-F-040 | Provider 工具能力探测与降级 | MUST | 不支持工具调用的 Provider 不导致失败。 | ①Provider 新增「支持工具调用」能力属性；②REQ-F-007「测试」动作顺带探测（发送带工具定义的最小请求）并保存结果，配置页标注；③解析到的最高优先级 Provider 不支持时，**本轮降级为纯对话**（不注册任何工具）并在对话内提醒，每会话一次；④REQ-F-006 优先级规则不变——**不因工具能力跳过** Provider；⑤状态灯「常亮」语义不变。 | APPROVED（CR-20260910-agent-tooling，R1 人工终裁 2026-09-10 完成、P3/P4 完成；状态回写遗漏，2026-09-11 补正） |
| REQ-F-042 | 自动上下文压缩 | MUST | 长对话接近输入预算上限时，把最早的若干轮文本压成一段摘要，使会话可以继续而不必开新对话。 | ①**按预算触发，绝不按轮数**——实测短会话按轮数压缩反而更贵（EV-2026-09-11 §1：18 条会话倒亏 19%、10 条倒亏 11%）；②摘要由**当前对话所用的同一个 Provider / 模型**生成（用户 2026-09-11 决策），一次非流式调用，`max_tokens` 有界；③最近 K 轮保持逐字，K 由架构定；④触发时**静默**——不在记录区插入系统消息（用户决策 Q1）；⑤**用户可感知的代价**：被压轮次的细节由逐字变为摘要，模型对早前内容的回忆会变粗；这是本需求的已知取舍，须在 REQ-F-043 的可展开摘要中保持可查证；⑥**原始消息一律保留在库**，压缩只影响发送给模型的上下文；⑦与 REQ-F-041 的工具结果保留窗口是**两条独立规则**，不合并。 | APPROVED（CR-20260911-context-compaction，R1 已终裁 2026-09-11） |
| REQ-F-043 | 压缩可见与可展开 | SHOULD | 用户能看出压缩发生过，并能查看摘要内容。 | ①在压缩边界渲染一条**极轻的分隔标记**——不是系统消息气泡，不打断阅读流；②默认折叠，展开控件为真实 `<button>` + `aria-expanded`；③展开后显示该段摘要全文；④标记随会话刷新重建（与 REQ-F-035 ④ 同机制）；⑤**不提供撤销压缩**——原文仍在库中，但本版本不提供把它重新放回上下文的入口。 | APPROVED（CR-20260911-context-compaction，R1 已终裁 2026-09-11） |
| REQ-F-060 | 主动唤醒 | SHOULD | Jarvis 可以在用户空闲时主动给出一条简短提醒，但默认不开、开了也花不了大钱。 | ①**默认关**（用户 2026-09-10 终裁 5）；②☰ 菜单「主动唤醒」项：开关、间隔（分钟，1–1440，默认 30）、每日 token 上限（默认 20,000）、今日已用量，同屏可见；③设置服务端保存于键值表，非法值拒绝并说明；④**触发条件**：开关开 + 页面未隐藏 + 没有正在进行的对话流 + 用户自上次输入/发送起安静满一个完整间隔；触发由客户端定时器发出，**能否花钱由服务端判定**（开关与日上限）；⑤「现在唤醒」按钮：立即唤醒一次，忽略开关但不忽略日上限，用于先看一次效果。 | APPROVED（CR-20260911-proactive-wake，R1 总授权，决策清单已于 2026-09-11 追认通过） |
| REQ-F-061 | 唤醒产出的呈现与隔离 | MUST | 唤醒的产出不打断、不误导、不自激。 | ①模型判断无事可提（回复 NOOP 或空）→ **什么都不显示、不落库**；②有内容 → 落为一条 `system` 消息行（状态 `wake`，前缀「主动提醒：」），转录区以系统行显示（不是助手气泡），状态灯闪一次；刷新后由落库记录重建在同一位置；③**绝不回放进模型上下文**：`wake` 行不在回放集合；下一次唤醒的提示也不含早前的提醒、工具行与摘要；④`GET /api/conversations/[id]/messages` 返回该行且字段与既有行一致。 | APPROVED（CR-20260911-proactive-wake，R1 总授权，决策清单已于 2026-09-11 追认通过） |
| REQ-F-070 | 本地跟踪对象 | MUST | 用户在本机维护一份被跟踪的对象清单，看板与模型共用它。 | ①一个对象 = 一个 Markdown 文件（frontmatter 记 kind / 标题 / 定位 / 容量 / 下一步 / 采集状态 / 变更 / 已读时间 / 采集源 / 证据），存放于 `JARVIS_ENTITIES_PATH`（默认 `.data/entities`），**无数据库表**；②三种 kind：友商、**规则与准入方**（标准组织与监管接入机构合为一类，因并网标准即由其发布）、客户；状态字段按 kind 分化，客户**不做健康分**（无交付数据）；③每个对象另有**采集健康度**：`fresh` / `stale` / `failed_fetch` / `parse_failed` / 未接入，与内容变化分开显示；`stale` 由距上次成功采集的时长推算，硬失败压过时长；④提供列表、读取、新建、改字段、管源、删除的本机接口；⑤列表顺序固定（先 kind 后名称），**不按新鲜度重排**；⑥**采集可执行**：对一个已登记的采集源可立即抓取一次，结果写回该对象的采集状态——抓不到记 `failed_fetch`、抓到但取不出正文记 `parse_failed`、取到正文即 `fresh`；首次采集只建立基线**不计为变化**；内容变化时写入一行**计算得出**的变更摘要（新增/移除行数 + 引用首条新增），**不由模型撰写**；采集失败**不推进**上次成功采集时间；⑦**巡检间隔编辑不丢字**（CR-20260915-sweep-interval-edit）：输入框旁常驻显示合法范围（30–1440 分钟）；停顿约 500ms 或按 Enter 立即保存，保存失败原样显示错误；编辑期间若收到后台巡检刷新，只更新其它字段，不覆盖尚未提交的间隔值；⑧**删除即归档**（CR-20260915-board-card-lifecycle）：看板卡片本身就是删除入口，删除前需二次确认；删除不是硬删——原文件移入归档目录，`listEntities` 天然不再列出它，但保留一份可以人工找回，不留「点错就永久丢失」的坑；⑨**新增用同形卡片**（CR-20260915-board-card-lifecycle）：三条泳道的「新增」不再是列表下方常驻的一行表单，而是一张与其它卡片同形（虚线边框区分）的 `+` 卡片，点击后原地展开成表单，提交或取消后收回。 | APPROVED（CR-20260911-home-dashboard，R1 逐轮拍板 2026-09-11；⑦ CR-20260915-sweep-interval-edit，R1 用户 2026-09-15 终裁；⑧⑨ CR-20260915-board-card-lifecycle，R1 用户 2026-09-15 总授权终裁） |
| REQ-F-071 | 知识看板呈现 | SHOULD | 跟踪对象与知识库存量在一块看板上呈现。 | ①上区三条泳道：友商用卡片网格，规则与准入方、客户用紧凑列表；②下区知识库总览：内容缺口（搜索无结果，按次数排）与按文档类型分布，并显式显示无归属条目数；③卡片展开为该对象的详情与**采集源管理**；工具与技能的调用**不放进卡片**，改为把问题预填进对话框；④**点开卡片即算已读**，不设独立的标记按钮；未读由「变更时间晚于已读时间」派生；⑤顶部汇总显示未读数、采集异常数、待采纳数；⑥空看板不假装有数据，并说明如何添加。 | APPROVED（CR-20260911-home-dashboard，R1 逐轮拍板 2026-09-11） |
| REQ-F-072 | 实体写入与证据 | MUST | 模型不能绕过用户改变看板上的事实。 | ①**模型只能提议**：新对象写入待采纳区，字段改动写入待采纳的修改队列，采纳前都不影响看板；②任何字段写入**必须带来源链接**，没有来源一律拒绝；③**直写判据在工具里**：仅当来源与该对象**已登记的采集源同域**时才直接生效，模型不得自称来源可信；未登记任何源的对象，任何来源都不能直写；④二手值落盘时记录 值 + 来源 + 定位 + 时间，按字段去重；⑤**提议在对话里也能被处理**（CR-20260915-entity-proposal-card）：`entity_pending` 事件带够标识（新对象带 `name`；字段修改带 `id`/`field`/`value`）时，转录区渲染一张可操作的卡片——采纳/忽略按钮调用的是与看板完全相同的路由，是同一条审批闸的另一个入口，不是绕过它的新路径；标识不足时（如 `extract_fields` 一次提议多个字段，凑不出一个能用的单一 id）退回纯文字通知，指向看板。 | APPROVED（CR-20260911-home-dashboard，R1 逐轮拍板 2026-09-11；⑤ CR-20260915-entity-proposal-card，R1 用户 2026-09-15 总授权终裁） |
| REQ-F-050 | 洞察追加与参数容错 | MUST | 长报告可以分块拼成**一份**洞察；工具参数解析失败时模型能知道错在哪。 | ①`save_insight` 支持指定已有洞察 id 追加：HTML 拼到该洞察正文末尾，`display_state` 指向不变、展示屏刷新；不指定 id 仍为新建；②洞察正文有上限（架构定，建议 512KB），超限拒绝并回喂；③追加成功的结果回喂累计长度与已含章节数，模型据此决定是否继续；④参数解析兼容嵌套 `{"arguments":{...}}` 包装；⑤解析失败或缺必填字段时，工具结果明确列出「收到的键名」与「缺少的字段」，不得一律回复「不完整或为空」；⑥`looksLikeCompleteHtml` 的判定文案区分「被截断」与「为空 / 缺字段」。 | APPROVED（CR-20260911-display-console-ux CP-2/3，R1 已终裁：用户 2026-09-13，INPUT-2026-09-13-027）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-051 | 模型输出截断可见 | MUST | 模型输出触及 Provider 输出上限时，用户与模型都能知道，而不是收到一段悄悄坏掉的内容。 | ①每次 Provider 调用携带输出上限参数，取该 Provider 允许的最大输出（按 kind 定默认值；本期**不加 Provider 列**以免 schema 变更，覆盖字段待需要时另立）；②适配器读取 `finish_reason === "length"`，把该轮标记为输出截断；③被截断的工具调用**不执行**，改为向模型回喂「工具参数在第 N 字符被输出上限截断，请缩短或分块提交」，步骤流该行显示「参数被截断」；④纯文本回复被截断时对话内出现提示（不静默）；⑤截断标记与 REQ-F-029 ② 的「触顶」是两种不同状态，不混用 `truncated`。 | APPROVED（CR-20260911-display-console-ux CP-4，R1 待终裁） ；**④ 经 CR-20260912-turn-budget-continue 修订（R1 已终裁 2026-09-12）**：截断的告知由适配器移交 `agent-loop`——适配器改发 `length_capped` 类型信号，循环据此**自动续写**（上限 3 次，另受本轮累计成本上限约束），**只在停止续写时**才向用户出文案。原文案「可以让我继续」等于让用户为一次完整的额外轮次买单，而实测 11 轮里 5 轮被截断。① ② ③ 不变；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-052 | 展示屏基础样式与主题 | SHOULD | 任何洞察 HTML 在展示屏上都有可读的排版，并跟随应用主题。 | ①iframe 文档由展示屏包一层基础样式：正文字号与行高、标题层级、表格边框与斑马纹、代码块、链接、引用、图片 `max-width:100%`；②基础样式的颜色取自应用主题 token，iframe 根元素带与宿主一致的 `data-theme`，主题切换时同步；③洞察自带 `<style>` 或为完整 `<html>` 文档时，自带样式优先，基础样式只做兜底不覆盖；④长报告在 iframe 内滚动，不被裁切；⑤REQ-F-025 提示条与无沙箱决策不变。 | APPROVED（CR-20260911-display-console-ux CP-5，R1 已终裁：用户 2026-09-13，INPUT-2026-09-13-027）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-053 | ☰ 菜单改为侧边抽屉 | SHOULD | 左下角 ☰ 打开的不再是固定宽度浮层，而是从左侧滑出的抽屉，内容分组、可滚动、不与对话面板重叠。 | ①触发器位置与图标不变；打开后从左侧滑出宽 280–320px（手机为 `min(320px, 100vw - 32px)`）的全高抽屉，带遮罩；②抽屉内容区独立滚动，任何视口下**完整落在视口内**（不裁切、不出屏）；抽屉为**模态**：遮罩覆盖全视口并接管点击，对话面板在其后不可交互——因此二者在窄视口上的**视觉重叠是设计的一部分**，不作为缺陷（原浮层的问题是非模态浮层与仍可交互的对话框相撞）；层叠刻度 30 不变；③内容分组：外观 / 模型 / 账号 / 技能 / 知识库 / 搜索与用量，组标题样式统一，行高统一（约 40px），图标 + 文字；④「模型」「账号登录」「搜索设置」为入口行，各自打开对话框（搜索设置由内联表单改为对话框——**待终裁**）；⑤技能列表每项一行：名称 + 单行省略描述，重命名 / 删除收进行尾更多菜单或悬停显示；⑥Esc、遮罩点击、焦点回归触发器、`aria-expanded` 契约保持，抽屉为 `role="dialog"` + `aria-modal`，焦点困在抽屉内；⑦REQ-F-015 中「浮层菜单」的描述随之改为「侧边抽屉」，其余不变。 | APPROVED（CR-20260911-display-console-ux CP-6/7，R1 已终裁：用户 2026-09-13，INPUT-2026-09-13-027）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-054 | 对话面板悬停态 | SHOULD | 鼠标移到展示屏时对话面板自动收缩，回到面板或准备输入时自动展开，不改变手动收起偏好。 | ①新增临时状态 `autoHidden`，与 REQ-F-019 的 `userCollapsed` 正交：`showTranscript = hasTranscript && !userCollapsed && !autoHidden`；②指针离开对话面板且停留在展示屏 ≥ 400ms（初值，架构可调）→ `autoHidden = true`，记录区收缩为 REQ-F-019 ② 的输入条形态（状态灯 + 工具条 + 输入行）；③指针进入面板、输入框获焦、输入框按键、发送消息 → 立即 `autoHidden = false`；④流式回复或工具执行进行中不自动收缩；已收缩时按 REQ-F-019 ④ 不自动展开；⑤仅在 `(hover: hover) and (pointer: fine)` 设备启用，触屏不启用；⑥`userCollapsed` 为真时悬停不展开（手动优先）；⑦`autoHidden` 不持久化、不写 `localStorage`；⑧收缩 / 展开有 150–200ms 高度过渡，`prefers-reduced-motion` 下无动画；⑨收缩后不显示未读计数、不留消息预览。⑩**回到记录区时停在最新一条**：`showTranscript` 由假转真时（收起后再展开、或首次挂载已有历史）记录区滚动到底部——收起会把记录区从 DOM 移除（REQ-F-019 ②），再展开是重新挂载，滚动位置从 0 起算；悬停自动收缩（`autoHidden`）不移除 DOM，位置自然保留。 | APPROVED（CR-20260911-display-console-ux CP-8，R1 已终裁：用户 2026-09-13，INPUT-2026-09-13-027）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-055 | PDF 正文读取 | MUST | `read_url` 遇到 PDF 时提取文字正文，而不是把字节当网页处理。 | ①按 `content-type` 或文件头识别 PDF（声明为 `application/octet-stream` 但内容是 PDF 的也算）；②提取文字正文，解压 FlateDecode 内容流、按 `TJ` 字距判定词间空格（小字距不得拆词）、经 `ToUnicode` CMap 还原 CID 字体（中文 PDF 的必要条件）；③**加密 PDF 报失败**并说明如何处理，不返回解不出的内容；④**无文字层的扫描件报失败**并说明不做 OCR，**不得返回空的成功**；⑤未被任何 CMap 覆盖的字形串按可读性丢弃，不得把噪音混入正文；⑥正文按既有单条工具结果预算截断并注明；⑦PDF 体积上限独立于网页上限（网页 2 MiB，PDF 24 MiB）。 | APPROVED（CR-20260911-web-reading，R1 已终裁）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-056 | 抓取失败可区分 | MUST | 读取失败时，模型能分清「需要浏览器」与「站点拒绝 / 页面不存在」。 | ①出站请求携带完整浏览器请求头（`user-agent`、`accept-language`、`accept` 含 `application/pdf`）——这是诚实标识而非伪装，且实测**不足以**通过 JS 人机校验；②识别人机校验：`cf-mitigated: challenge`、`server` 为 cloudflare/akamai 的 403、以及校验页文案（**中英文都须识别**，实测返回的是中文「正在进行安全验证 / 请稍候」）；③被判为人机校验或无标记的 403/429 → 触发 REQ-F-057 的浏览器回退；④其余失败（404/500 等）**不**触发浏览器，且结果文本明确告知模型「换来源，重复请求同一地址不会成功」。 | APPROVED（CR-20260911-web-reading，R1 已终裁）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-057 | 浏览器读取通道 | SHOULD | 被拦截的页面可以用真实浏览器引擎再读一次。 | ①仅在 REQ-F-056 ③ 判定为被拦截时触发，普通页面不走此路；②通过后返回渲染后的正文并**标注经浏览器读取**；③**仍停在校验页时报失败**，绝不把校验页当正文返回（否则模型会把安全提示当来源总结）；④为独立开关，默认开，可在「搜索设置」中关闭，关闭时给出如何开启的说明；⑤**明示边界**：对 Cloudflare / Akamai 一类能识别自动化浏览器的校验无效（实测有头无头均停在校验页），本需求不覆盖指纹伪装，该做法明确列为非目标。 | APPROVED（CR-20260911-web-reading，R1 已终裁）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-058 | 本机搜索后端就绪 | MUST | 联网检索所依赖的搜索后端在本机可用，且这一点可被验证。 | ①搜索后端为本机运行的 SearXNG 兼容服务，**仅绑定回环地址**；②其配置须开启 `json` 输出并关闭 bot limiter——两者缺一 JSON 接口即不可用，且这是最常见的配置失败；③地址落库于 `app_settings.search.base_url`，缺失时 `web_search` 不注册（REQ-NF-008 ④ 不变）；④「搜索设置」中的「测试连接」与 `web_search` 共用同一套失败分类（REQ-NF-011 ③ 不变）；⑤不得依赖公共 SearXNG 实例——实测 8 个实例均不提供 JSON 接口。 ⑥**人的一侧**：`☰ → 搜索设置`（REQ-F-053 抽屉内）显示端点地址、启用状态与「测试连接」的失败分类结果，用户不必读日志就能知道后端是否就绪。 | APPROVED（CR-20260911-web-reading，R1 已终裁）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-044 | 本地知识库 | MUST | 用户拥有一个本机的知识库，作为跨会话的长期记忆。 | ①一条知识 = 一个 Markdown 文件（三行 frontmatter：title / source / created），存放于 `JARVIS_KNOWLEDGE_PATH`（默认 `.data/knowledge`）；**无数据库表**；②用户手工放进目录的 `.md` 文件同样是条目（标题取首个标题行，来源记为 file）；③☰ 菜单「知识库」列出全部条目（标题 / 来源 / 名称）并可删除（删除需确认）；④列表在页面首次打开即正确（SSR），并随 `jarvis:knowledge-changed` 事件刷新；⑤知识跨会话可用：任一会话都能检索到此前存入的条目。 | APPROVED（CR-20260911-knowledge-base，R1 总授权，决策清单已于 2026-09-11 追认通过） |

> **CR-20260911-home-dashboard 扩写 REQ-F-044 ①**：条目 frontmatter 增加 `entity`（主分类，空即无归属）、`doc_type`（次分类）、`url`（原始链接）。存储形态为**存抽取正文 + 存原始链接 + 不存原件**。无归属桶显式计数，不隐藏。旧条目缺这三行仍可读。
>
> **CR-20260911-home-dashboard 扩写 REQ-F-045 ④**：`search_knowledge` 无结果时把查询词记入缺口日志（有上限），看板按次数排序展示。这是需求侧的缺口信号。

| REQ-F-045 | 知识检索工具 | MUST | 模型能按需从知识库取用内容，而不是把知识库整体注入提示词。 | ①`search_knowledge(query, limit?)`：关键词检索，返回条目名称、标题与含命中词的片段；中文按字二元、其余按词切分，BM25 排序；②`read_knowledge(name)` 返回条目全文，受 REQ-NF-013 ③ 子预算截断；③两者**只读免审批**（用户 2026-09-10 终裁 4），且**仅当知识库非空时注册**（REQ-NF-008 ④）；④新增 / 删除文件后立即可检索到 / 检索不到，不需重启。**同义改写检不到属已知边界**（词面匹配），由真实使用发现。 | APPROVED（CR-20260911-knowledge-base，R1 总授权，决策清单已于 2026-09-11 追认通过） |
| REQ-F-046 | 知识固化入口 | MUST | 值得长期记住的内容能方便地进入知识库，且模型不能绕过用户写入。 | ①**拖入文件**：把单个 `.md` / `.txt` 拖到对话框即存为条目（文件夹 / zip 仍是技能），转录区给出收据或拒绝原因；②**存回复**：每条完成的助手回复带「存入知识库」按钮（真实 `<button>` + 可访问名），一键以来源 conversation 入库；③**模型提议**：`save_knowledge(title, content)` 只能把条目写入**待采纳区**——不进检索，转录区提示「已放入待采纳区」，☰「知识库」顶部显示「待采纳（N）」并提供采纳 / 忽略；采纳后才成为条目；④**不自动写入**：不把会话摘要或回复自动灌入知识库，入库只经 ①②或经采纳的 ③；⑤**归档而非硬删**（CR-20260915-knowledge-library-merge）：删除一条已入库条目不是硬删——原文件移入归档目录，`listKnowledge` 天然不再列出它，但保留一份可以人工找回，与 REQ-F-070 ⑧ 的实体归档同一条安全线；⑥**模型可改分类**（CR-20260915-knowledge-library-merge）：`classify_knowledge(name, doc_type)` 直接改动一条已入库条目的类型，立即生效、不进待采纳区——这是整理性质的元数据修改，不是需要来源佐证的事实变更，与 ③ 的审批语义不冲突（③ 管"是否收录"，⑤⑥ 管"收录之后怎么整理"）。 | APPROVED（CR-20260911-knowledge-base，R1 总授权，决策清单已于 2026-09-11 追认通过；⑤⑥ CR-20260915-knowledge-library-merge，R1 用户 2026-09-15 总授权终裁） |
| REQ-F-041 | 工具结果保留窗口 | MUST | 工具结果不无限累积进上下文。 | ①发送给模型的上下文中，只有最近 N 轮的工具结果以原文保留；更早轮次的工具结果替换为一行摘要标记（工具名 + 参数摘要 + 「结果已省略」）；N 由架构定（建议 2）；②用户与助手文本消息不受本条影响（完整压缩属 B 期）；③落库记录不变，只影响发送给模型的上下文；④步骤流重建读库，不受影响。 | APPROVED（CR-20260910-agent-tooling，R1 人工终裁 2026-09-10 完成、P3/P4 完成；状态回写遗漏，2026-09-11 补正）。**CR-20260912-turn-budget-continue 曾提出修订 ①，2026-09-12 撤回**：该提议基于「轮内工具结果从不收窄」的误判，而轮内收窄由 DEC-080 的 `fitToolLoopContext` 在每次 provider 调用前执行，与本条各管一段（本条管跨轮回放）。本条不改 |
| REQ-F-080 | 本机生产运行与单管理员模式 | SHOULD | 应用可以用生产构建在本机运行，且在没有登录的前提下仍然可用。 | ①提供 `build:local` / `start:local` 两条脚本，构建产物落在**独立目录**，不得与运行中的 `next dev` 共用（沿用 DEC-009 的隔离结论）；②生产构建的常驻内存显著低于 `next dev`（实测 126 MB 对 1,564 MB），代价是没有热更新、改代码后须重新构建，此代价必须写入文档；③新增独立开关 `JARVIS_SINGLE_ADMIN_ID` 作为身份来源：设置后每个请求都归属该用户，**不复用也不放宽** `JARVIS_TEST_USER_ID`（后者是测试旁路，「拒绝生产」是其存在意义）；④身份来源优先级为 真实会话 → 测试旁路（仅非生产）→ 单管理员；配了真实登录时以真实身份为准；⑤配置预检须报告当前处于哪种身份模式，并在两个 id 不一致时告警（不一致会让 dev 与生产看到两套数据）。 | APPROVED（CR-20260912-local-production，R1 已终裁）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-090 | 报告列宽与居中 | SHOULD | 展示屏上的报告是一列居中的正文，不贴屏幕边缘。 | ①列宽为视口宽度的 **0.68**，水平居中，左右留白相等；②1024 px 以下让出整宽，避免窄屏下不可读；③该约束**不得**放在会被洞察自带样式覆盖的样式层——模型生成的完整 HTML 常带未分层的 `body { margin: 0 }`，未分层声明无条件压过分层声明，会取消居中而保留宽度限制，把正文钉在左边；④实现须靠选择器特异性取胜，**不得使用 `!important`**，文档内部的排版仍归它自己控制；⑤REQ-F-052「自带样式优先」不变：本条只约束最外层的列宽与位置。 | APPROVED（CR-20260912-latency-and-report-layout，R1 已终裁）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-091 | 保留窗口无条件生效 | MUST | 旧工具结果不因「尚未超预算」而继续原样重放。 | ①REQ-F-041 ① 的「只有最近 N 轮的工具结果以原文保留」**没有预算前提**，实现必须无条件套用，不得只作溢出兜底；②收窄后仍超出预算时抛请求级错误，不做循环压缩（DEC-026 ⑥ 后半段不变）；③用户与助手的文本消息不受影响（REQ-F-041 ② 不变）；④落库记录不变，只影响发送给模型的上下文（REQ-F-041 ③ 不变）。 | APPROVED（CR-20260912-latency-and-report-layout，R1 已终裁）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-101 | 渲染隔离与轮内预算 | MUST | 模型与网页来源的 HTML 不在本应用的源里执行；一次发送内的工具结果不会悄悄涨到超出预算。 | ①展示屏渲染洞察的 iframe **必须**带 `sandbox`；②该 sandbox **不得**包含 `allow-same-origin`——它与 `allow-scripts` 同时出现即等价于没有沙箱，而这正是 `skill-html-unsandboxed-web-source` 记录的注入链最后一环所依赖的条件；③工具循环**每次调用 Provider 前**重算当前上下文规模，未超预算时**不做任何收窄**（与 REQ-F-041 ① 的跨轮无条件收窄刻意相反：该条管「轮」不管「步」，而一次发送内的结果全部属于当前任务）；④超预算时逐级收窄——只保留最近 N 条工具结果原文，其余替换为省略标记，**`tool_call_id` 必须保留**以维持合法的 assistant/tool 配对；⑤收窄到最紧仍放不下时停止循环，复用 REQ-F-029 ② 的「触顶」语义（已完成结果与已生成文本保留、落库 `status=truncated`、不结束会话），且**不得静默**——首次收窄与最终停止各发一条说明，停止时带上估算值与限额；⑥单条工具结果的 token 上限取自**当前 Provider 的真实上下文窗口**与已申报的份额，不得使用与模型无关的常量。 | APPROVED（CR-20260912-sandbox-and-budget，R1 已终裁）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-102 | 展示屏三态互相可达 | MUST | 标题页、知识看板、洞察三者之间可以来回切换，且对话能到达其中任意一个。 | ①「回到首页」必须**真的**回到标题页——清持久态之外还要把会话阶段改回开场，并清掉「本次会话已播过开场」的标记；不清标记则下一次渲染立刻弹回看板，工具报告成功而屏幕不变；②对话可以打开知识看板；执行时先把持久态清回首页，否则残留的洞察会盖住看板；③看板**仍然不是持久状态**——它是本次会话的阶段，通过一条不落库的瞬时消息到达（`CR-20260912-display-stage` 对持久化方案的否决不被推翻）；④**鼠标移动不算开始工作**：页面加载时指针通常已在窗口内，把指针到达当作开始工作会让开场实际不可见；只有点击或焦点落进控制台才算，3 秒上限不变；⑤洞察仍然压过上述两个阶段，持久态优先级不变。 | APPROVED（CR-20260912-stage-reach，R1 已终裁）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-110 | 本地原文档 | MUST | 对话能检索并阅读用户自己放在本机文件夹里的原件（规格书、标准、论文等），在原地只读。 | ①用户在 ☰ 菜单里配置若干文件夹（绝对路径）；**不做文件选择器**——浏览器的目录选择器给的是沙箱句柄而非 Node 可读路径，做出来会「看起来成功了却什么都找不到」；②提供检索、阅读、列目录三个工具，且**无条件注册**：未配置目录时明确回复去哪配置，而不是从工具表里消失（按「有没有内容」决定是否注册会让能力对模型完全不可见，连「是空的」都说不出来）；③**零新增运行依赖**：PDF、docx、检索排序、配置存储全部复用项目已有实现；④单条结果按当前模型的真实窗口截断；工具预算不足时文档工具让位于看板读工具，但**必须出现在「未加载」名录里**，被说明与被静默丢弃是两回事；⑤**只读**：本层不复制、不改写、不移动、不删除用户的任何文件；移除目录只是不再查找它。该性质须可被机器断言（`documents.ts` 不导出任何写函数），而不只是写在注释里。**例外仅一处**（CR-20260914-insight-dual-output，REQ-F-190）：报告归档会在用户指定的归档目录里**新建**文件，该目录必须落在某个已配置的文档目录之内，且永不覆盖既有文件；写入实现不在本层，本层的无写入口性质不变；⑥读不了时分情形说明原因（目录未配置 / 目录为空 / 无检索结果 / 标识不存在 / 越界 / 扫描件 PDF / 旧 .doc / 加密 PDF），不得一律回「失败」；⑦遍历与抽取有上限，且超出时如实说明还剩多少未读入。 | APPROVED（CR-20260912-local-documents，R1 已终裁）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-120 | 运行时状态可见 | MUST | 发送失败时用户知道发生了什么、该做什么；模型知道自己跑在哪个 Provider 与模型上。 | ①发送失败时**先探一次服务再说话**：探不通说明服务没有响应并给出重启指引，探得通说明是传输中断可直接重试——两者下一步不同，不得用同一句话盖过去；服务端已经答复过的错误照原样传达，不被本条的措辞覆盖；②失败提示**带时间**，并说明本轮没有任何内容被保存；不得把浏览器的原始网络错误直接抛给用户；③提供一种带看护的本机启动方式：服务被外部停止后自动拉起并留下带时间戳的事件记录；但**不得无限重启**——连续短时间内退出应判为「起不来」并停止且说明原因，而跑过一段时间后的退出应判为「被停掉」并重置该计数；④每轮发送的提示中包含当前 Provider 名、类型与模型 id，使模型能回答「现在接的是什么模型」；该信息进**易变后缀**而非稳定前缀——它会随换模型而变，写进前缀会使前缀缓存失效（REQ-NF-008 ① 不变）。 | APPROVED（CR-20260912-runtime-visibility，R1 已终裁）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-130 | 超预算时收紧而非拒绝 | MUST | 一轮上下文超出预算时，缩短工具结果后继续，而不是让这一轮失败。 | ①超预算时**逐级收紧**：先给每条工具结果封顶，再放弃较早轮次的原文，最后才省略全部工具正文；第一级装得下就停，不做多余削减；②每一级都是**确定性截断而非摘要**——不得为此额外调用模型（DEC-026 ⑥ 禁止的循环压缩约束不变）；③收紧只改工具结果的正文：`tool_call_id` 必须保留（丢行会让 assistant/tool 配对非法），**用户与助手的文字永不被削**；④收紧后必须在本轮内如实说明削了几条、保留几轮，以及可以让其重新读取；不得静默；⑤只有**对话自身的文字**就超出预算时才抛请求级错误，且文案须说明是对话文字过长，而非含糊的超限——两种情形用户能做的事不同。 | APPROVED（CR-20260912-context-degrade，R1 已终裁）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-140 | 技能产出的宿主适配 | MUST | 为别的宿主写的技能，也能在这里把报告交付到展示屏上。 | ①`read_skill` 的结果**末尾**附本机产出约定：唯一出口是 `save_insight`、没有文件系统产出与 Word、一份报告始终一个 `insightId`、写错结构用整篇重写、技能自带的 `<style>` 要写进第一块、片段即可无须 `<html>` 骨架、章节编号整篇统一。放在技能正文之后，使其不被正文截断削掉；**不得改写用户的技能文件**——那是用户的目录，重装技能会抹掉改动；②`save_insight` 除新建与追加外须支持**整篇重写**，否则模型发现开头缺失时只能把开头追加到结尾；追加仍为默认，重写必须同时给出 `insightId`；③同一会话中再次**新建**洞察时，回喂须点名已有洞察的 id、体量与后果（拆成两份、展示屏只显示较新一份）；首次新建不带此提醒；④报告落地后对话面板自动让开——二者同为居中布局且实测重叠 78%，用户不应靠自行发现「把鼠标移开」才能读到刚要的东西。收起发生在本轮结束时（REQ-F-054 ④ 禁止流式进行中收缩），手动收起优先级不变，指针或焦点回到面板即恢复。 | APPROVED（CR-20260912-skill-report-bridge，R1 已终裁）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-150 | 技能按需分页读取 | MUST | 大技能的全部内容都够得着，而不是被截断成任意的一部分。 | ①读取技能的**入口**返回 SKILL.md 正文与该技能的文件清单（含每个文件的体量），不再把全部文件拼接后截断——入口大小必须与参考文件的体量无关；清单须提示这些文件尚未读取，需要时单独取；②可按清单中的相对路径**单独读取**其中一个文件，按本轮预算截断并说明；大文件不得再把小文件挤出可达范围（改前哪一份进得去由字母序决定）；③按路径读取须带守卫：先解析真实路径再判是否落在技能目录内，**不得**用字符串前缀判断；非可读格式一并拒绝；拒绝时不回显真实路径；④控制台在展示步骤流时的高度上限须留出足够空间，使其背后的报告顶部始终可读（REQ-F-003「不占满屏幕」不变，本条只收紧数值）。 | APPROVED（CR-20260912-skill-paging，R1 已终裁）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-170 | 知识写入带归属 | MUST | 经对话写入的知识条目必须带归属对象、来源与类型，且写进去的这三个字段必须读得回来。 | ①`save_knowledge` 增 `entity`（必填）、`source_url`、`doc_type` 三个参数并透传至存储层；②`entity` 取已存在的实体 name 或字面量 `__通用__`，传入不存在的对象时**拒绝写入**且错误文本回列现有对象名（禁止「未写入」这类无信息文本）；③`__通用__` 在知识看板中显示为**具名分组**，不与「未分类」合并；④`read_knowledge` 返回体在正文前带元数据头（entity / doc_type / source_url）；⑤`search_knowledge` 每条命中带 `entity`、**不带** source_url（命中列表可多条，全量头不划算）；⑥既有无归属条目**不回填**，保持现状。 | APPROVED（CR-20260912-knowledge-attribution，R1 已终裁 2026-09-12）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-171 | 知识可枚举与按对象检索 | MUST | 模型必须能列出知识库里有什么，并能按用户给对象起的名字找回条目。 | ①新增 `list_knowledge`，返回 name / title / entity / doc_type，支持按 entity 过滤与分页，并返回按对象分组的计数；②条目数超阈值时只返回分组计数 + 首页，不全量展开；③检索索引覆盖 `entity` 与 `docType`，**用对象的中文名可命中该对象下正文为外文的条目**；④`list_knowledge` 在零条目时**仍注册**（模型须能回答「库是空的」）——本条是对 REQ-F-045 ③ 的**局部例外**，不改 REQ-F-045 ③ 对既有工具的约束；⑤工具优先级为 `essential`，且须在证据中记录 8k 窗口下因它而未装载的工具名。 ⑥**人的一侧**：知识看板按对象分组显示条目计数，与 `list_knowledge` 的分组计数同源（REQ-F-071），用户不经模型也能看到库里有什么、哪些没有归属。 | APPROVED（CR-20260912-knowledge-attribution，R1 已终裁 2026-09-12）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-180 | 证据管线一轮内闭合 | MUST | 给一个 URL 到字段提议可见必须能在一轮对话内完成；模型无法把没有出处的内容冒充成有出处。 | ①`extract_fields` 可读取待采纳区的条目，`ingest_url` → `extract_fields` 在同一轮内成立；②待采纳区语义不变——条目采纳前仍不参与检索，抽出的字段仍是**提议**、仍须人工采纳；③工具失败时回传可执行原因（条目所在状态、对象不存在时回列现有对象名），**禁止「未写入」「未入库」这类无信息文本**；④`propose_entity_update` 的字段分两类：事实字段必须带 `quote` 且逐字存在于条目正文，结论字段允许无 `quote` 但必须带 `kind=推断`；⑤**无 `quote` 的值一律落为结论字段并强制标记，判定不交给模型自报**——否则模型可把编造内容报成结论字段绕开校验，护栏退化为自愿；⑥`kind=推断` 的字段在知识看板中**不得与有据可查的字段视觉等同**；⑦`ingest_url` 落盘正文剔除页面脚本数据与连续空行，`title` 不留截断残句。 | APPROVED（CR-20260912-ingest-extract-chain，R1 已终裁 2026-09-12）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-F-190 | 洞察双产物与归档 | MUST | 报告能以 Markdown 留在本地文档库里，此后可被检索、阅读、编辑，而不是只活在这个应用的数据库里。 | ①正文仍以 **HTML 为唯一作者产物**，Markdown 由同一份 HTML 派生——不让模型写第二遍，否则两份正文迟早对不上；②提供归档动作与 `archive_insight` 工具，格式 `md`（默认）或 `html`，两条路径共用同一实现；③归档目录由用户显式配置，且**必须落在某个已配置的文档目录之内**（写的范围不比读更大）；未配置时说清下一步该做什么，不猜目录、不静默；④**永不覆盖**：同名文件自动加序号；只新建，不改写、不移动、不删除任何既有文件；⑤归档的 Markdown 带元数据头（title、insight\_id、conversation\_id、created\_at、archived\_at、source=`jarvis://insight/<id>`），归档后立刻可被 `search_documents` / `read_document` 读到；⑥HTML→Markdown 覆盖模型实际产出的标签子集（h1–h6、p、ul/ol、strong/em、code、pre、a、img、table、blockquote、hr）；**认不出的标签只脱壳、不丢字**；表格单元格里的竖线转义；⑦**人的一侧**：展示屏报告上方有「归档到本地文档库」按钮，点后就地显示写到了哪儿或为什么没写成；归档目录在 ☰「本地文档」里设置。 | APPROVED（CR-20260914-insight-dual-output，R1 已终裁：用户 2026-09-13 授权按业界最佳实践代为决策，INPUT-2026-09-13-027） |
| REQ-F-200 | 设置界面上动态屏 | MUST | 改设置不必离开正在看的东西：面板开在动态屏上，而不是弹窗盖住报告或看板。 | ①**入口在对话框**：控制台上有「模型 / 技能 / 工具」三个入口，点击把对应面板唤到动态屏上；☰ 抽屉里的既有入口保留，两处唤起的是**同一个组件**，不另写一份；②面板呈现在动态屏上并可「返回」；面板状态是**瞬时的、不落库**——它是「此刻在看什么」，不是「这台机器该显示什么」，落库会让它跨会话粘住；模型设置在服务未就绪时**明说打不开**，不画一个存不了的空表单；③新增**工具面板**：列出系统登记的全部工具，标明本轮是否注册，**未注册的也列出来**（「没注册」与「不存在」对用户是两件事），并给出此刻的判定条件（技能数、知识条数、联网开关、Provider）；判定必须与 `runChatTurn` 走**同一个** `buildRegistry` 与同一份 `ToolContext`，不另写近似逻辑；④**非首页视图全部真悬浮，不再为控制台预留底边**（本条第三次修订，CR-20260918-unified-floating-console）：`DisplayScreen.tsx` 的 `insight`/`document`/`settings`/`board` 四个容器改为与首页一致的 `fixed inset-0`，不再读 `--jarvis-console-h` 让位。2026-09-15 版本（见下方历史）已经把记录区展开时的遮挡范围收窄到控制台自身 768px 宽的 footprint、不再连带遮住两侧无关内容；投入本次修订前的调查确认，这一步收窄的效果与「是否还保留让位」无关——即便让位量降到 0，遮挡范围依旧只是控制台自己的 footprint，2026-09-14 版本要防的「通栏让出一大块」不会复发。收拢态与展开态下，非首页视图与首页今天的实际表现完全一致。⑤**控制台入口合并为一行**（CR-20260918-unified-floating-console）：状态灯、上传、「模型 / 技能 / 工具 / 资料库」四个面板入口、本轮用量，全部在 `.floating-chat__status` 一行内，不再分两行分别呈现。 | APPROVED（CR-20260914-settings-on-display 首版 R1；④ 由 CR-20260915-console-menu-consolidation 第一次修订（收窄发布值）、CR-20260918-unified-floating-console 第二次修订（非首页视图全悬浮，不再让位），R1 均已终裁——本次 R1：用户 2026-09-18「按流程开始第一波的 CR」总授权，INPUT-2026-09-18-001 第 5 条；⑤ 由 CR-20260918-unified-floating-console 新增，同一轮 R1，INPUT-2026-09-18-001 第 6a 条） |
| REQ-F-210 | 能力不足与失败时按优先级下沉 | MUST | 一个 Provider 办不了的事，不必整轮陪着它失败——排在后面的那个可以接手。 | ①**能力下沉**：本轮需要工具而队首 Provider 已被探测为不支持工具调用（`tool_support = "no"`）时，按优先级取下一个未被判过 `no` 的 Provider 执行；全都不支持才降级为纯对话（REQ-F-040 ③ 不变，只是往后挪一步）。`unknown` 仍按「先试试看」处理，不触发下沉；②**失败下沉**：**第一次**模型调用在**尚未产出任何可见内容**时失败（超时 / 连接失败 / 上游错误），自动换链上的下一个重试一次；已经吐字或已经执行过工具则**不换**——重试的代价是重复计费与重复副作用，半句话被另一个模型接着写比失败更糟；③每轮**最多换一次**，且**换了必须说出来**（能力下沉在开场说、失败下沉在本轮结束时说），静默换模型比换错模型更坏；④用户在本轮**显式指定**了 Provider 时一律不下沉——那是他自己的选择；⑤**人的一侧**：换模型的那句话出现在对话记录区里，用户能看到这一轮实际由谁作答。 | APPROVED（CR-20260914-provider-failover，R1 已终裁：用户 2026-09-14「若有时候 DeepSeek 作为优先级高的模型能力不足的地方，可由优先级不高的模型如 openai 执行，不一定全都是一个模型解决」） |
| REQ-F-220 | 资料库与采纳流 | MUST | 一份成体系的研究资料能随版本进来，且由人一条条决定哪些算数——不是全信，也不是全不信。 | ①**资料库是随版本走的原始资料层**：合集 / 分组 / 清单（`清单.csv` 给编号、标题、来源、URL、取回方式），与用户自配的文档目录（REQ-F-110）**并列而不合并**；②**默认全部待采纳**：没有任何一份因为被放进来就自动生效；③**逐文件审批**，界面在动态屏（承 REQ-F-200 ①），可按组批量提交，但登记里留下的是逐条判断（谁、何时、什么结论）；④**可撤回**：撤回后立刻回到不可见——闸是活的，不是一次性的；⑤**人的一侧**：面板给出总数 / 待采纳 / 已采纳 / 已拒绝四个数，并写明「只有已采纳的能在对话里查阅」。 | APPROVED（CR-20260915-library-adoption，R1 已终裁：用户 2026-09-15「把资料库目录加入版本，并与知识库链接起来。里面的内容改为待采纳，审批通过后，可正式纳入，并支持对话查阅」，INPUT-2026-09-15-028） |
| REQ-F-230 | 采纳后方可对话查阅 | MUST | 审批要在对话里真的生效：没过审的读不到，过审的读得到，被挡住时人知道该去哪。 | ①已采纳的资料对 `search_documents` / `read_document` / `list_documents` 可见；②待采纳与已拒绝的不可见，但**被挡住的必须报数并指出入口**——「没有结果」与「有 N 份还没审」对用户是两件事；③采纳时在知识库留一张**索引卡**（标题、来源、取回方式、`documentId`），使 `search_knowledge` 命中后能转 `read_document` 取全文——这就是资料库与知识库之间的那根线；④**网页存档（`.html`）纳入可读格式**，取正文不取源码（否则资料库里 139 份网页在对话里等于不存在）；⑤**人的一侧**：被挡住时那句话里含数量与「动态屏 → 资料库」这个去处。 | APPROVED（CR-20260915-library-adoption，R1 已终裁：同 REQ-F-220） |
| REQ-F-241 | 待采纳内容默认收拢 | MUST | 菜单打开时不该像打开另一个要处理的收件箱：待采纳的知识提议默认只给计数，不默认铺开内容。 | ①☰ 菜单的知识库板块，待采纳队列**默认收拢**，只显示一个计数徽标（如「待采纳（3）」）；②点击徽标**就地展开**同一套采纳/忽略控件——不是跳转到另一个面板或页面，那样会把一件事拆成两个入口；③展开状态不跨会话持久化，每次打开菜单都从收拢态开始（与其它菜单折叠项同一口径）；④队列为空时徽标本身不显示（与此前一致，不引入新的空态文案）。 | APPROVED（CR-20260915-console-menu-consolidation，R1 已终裁：用户 2026-09-15「菜单里不用显示待采纳内容」，INPUT-2026-09-15-028 之后续一轮反馈；总授权见 INPUT-028「全实现」） |
| REQ-F-242 | 统一浏览 | MUST | 资料库已采纳原件与知识库真实条目分属两个来源，用户应该有一处能一起看，而不必分别打开两个面板拼凑全貌。 | ①「资料库」动态屏面板新增**浏览**模式，与既有**审批**模式（默认打开）并列，互不影响彼此已取到的数据；②浏览列表合并两类卡片：已采纳的资料库原件（结构化字段：类型/大小/更新时间/原文链接）与真实知识条目（source 不是资料库索引卡的那些）——**索引卡本身不重复出现**，已采纳原件只以库卡的身份出现一次；③**分页**：每页固定条数，与既有 `/api/library` 审批接口的"不分页"设计并存但不互相影响（该接口的不分页是"本目录全选"场景的既有设计，浏览场景相反需要分页，两条路由各自成立）；④列表附**类型统计**（复用既有 `byType`/"未分类"分桶口径）；⑤排序近似"模型推荐的优先级"：挂了归属对象的条目排前，组内按更新时间新旧排——不是逐条调用模型打分，258+ 条量级下不现实，如实记为启发式近似而非模型评分；⑥**知识看板（board）正文内嵌入同一套浏览视图**（CR-20260918-library-in-board）：与 ☰ 面板共用同一份组件实现（`LibraryBrowseView`/`loadBrowseFromApi`），不是另起一套渲染逻辑；看板原「知识库总览」板块改名「资料库」，REQ-F-170 ②③ 的「无归属/通用」计数与「内容缺口」板块原样保留，仅「按类型」统计被同一组件自带的、覆盖面更大的类型统计取代；用户不必离开看板去 ☰ 菜单才能看到可浏览的资料库内容。 | APPROVED（CR-20260915-knowledge-library-merge，R1 用户 2026-09-15 总授权终裁：INPUT-2026-09-15-031 第 5 条"支持对话修改分类"引出重新分类需要一处能看见分类结果的地方，随之扩展为统一浏览；⑥ CR-20260918-library-in-board，R1 用户 2026-09-18"按流程开始第一波的 CR"总授权终裁，INPUT-2026-09-18-001 第 3 条） |
| REQ-F-244 | 对象卡片消息清单 | MUST | 友商/准入方/客户卡片展开后，显示该对象采集源检测到的变更历史，而不只是最新一条；每条消息可点击进入其来源链接。 | ①卡片展开态渲染一个消息列表区块，与技术参数、采集源分列；②列表按时间倒序，每条消息文本是一个可点击的链接，`href` 指向该条变化的来源 URL、新标签页打开；③没有历史时显示明确的空状态文案，不留白也不报错；④既有的 `change`/`changeAt`（卡片折叠态的最新一条摘要）行为不变，消息列表是新增的展开视图，不是替代。 | APPROVED（CR-20260918-change-history-and-sources，R1 已终裁：INPUT-2026-09-18-001 第 1 条） |
| REQ-F-245 | 采集源弹窗配置 + 登记工具 | MUST | 采集源的管理离开卡片本身、收进弹窗；模型能在对话中登记一个发现的来源为采集源。 | ①卡片展开态不再直接显示采集源的增删表单，改为一个标明当前来源数的按钮；②点击该按钮打开一个弹窗，弹窗内是既有的来源列表（可立即采集/移除）与添加表单，行为与改动前的卡片内联版本完全一致；③新增模型可调用的 `add_source` 工具，登记一个 URL 为指定对象的采集源，不核实其内容、不构成任何字段的直接写入；④弹窗内新增"自动配置来源"按钮，点击后把一句预填问题交给对话框（复用既有 `onAsk` 机制），不在弹窗内直接发起模型调用；⑤`propose_entity` 的工具描述追加提示，引导模型在对象被采纳后主动查找并登记来源。 | APPROVED（CR-20260918-change-history-and-sources，R1 已终裁：INPUT-2026-09-18-001 第 2、6b 条） |
| REQ-F-243 | 友商对比看板 | MUST | 已跟踪的友商，其产品参数/规格按维度对齐显示在同一张表格里，支持对话唤起；逐维度的评价由模型在对话中现场给出，不在表格里预置或持久化一份写死的结论。 | ①存在一个可由对话唤起的展示屏视图，列出全部已跟踪的 `kind=competitor` 对象；②对比维度取自各友商已登记的技术参数（`params`）名称并集，缺该维度的友商显示占位符而非报错或留空；③视图不提供参数的增删改入口——参数仍只能通过知识看板既有入口维护；④视图不预置或持久化任何"评价总结"字段，用户询问某维度谁更优时由模型基于当下的 `params` 数据现场作答。 | APPROVED（CR-20260918-competitor-board，R1 已终裁：INPUT-2026-09-18-001 第 7 条 + INPUT-2026-09-18-002 确认与第 1 条的行业硬核指标对比页不是同一个） |
| REQ-F-250 | 行业技术指标对比 | MUST | 友商、规则与准入方、客户三类跟踪对象的技术参数/门槛要求，按维度对齐显示在同一张表格里，支持对话唤起；与「友商看板」（REQ-F-243）不是同一个页面——这一页范围更广，不限于友商，且逐维度不预置评价。 | ①存在一个可由对话唤起的展示屏视图，列出全部已跟踪的 `kind∈{competitor,authority,customer}` 对象；②对比维度取自各对象已登记的技术参数（`params`）名称并集，缺该维度的对象显示占位符而非报错或留空；③列头标注每个对象的 `kind`，便于区分"产品规格"与"门槛要求"；④视图不提供参数的增删改入口；⑤视图不显示 `Param.status`（是否满足）——是否显示留作开放问题，见 CR 文档「非目标 / 开放问题」，本条验收不要求它出现，也不禁止未来的修订加上它。 | APPROVED（CR-20260918-industry-spec-comparison，R1 已终裁：INPUT-2026-09-18-001 第 1 条 + INPUT-2026-09-18-002 确认与第 7 条「友商看板」不是同一个页面、包含客户） |
| REQ-F-260 | 组织架构/研发阵型看板 | MUST | 已跟踪对象（公司）下的人员——姓名、岗位、团队/阵型、简介、头像——可结构化登记并按公司/团队分组显示在一个可由对话唤起的展示屏视图里；数据随时间动态更新，不是一次性静态洞察。 | ①存在一个可由对话唤起的展示屏视图，按公司分区、同公司内按团队/阵型分组显示已登记人员；没有任何数据时如实显示空态说明，不报错、不留白；②人员信息的写入必须附来源链接，来源不在该公司已登记采集源之列时进入待采纳区，用户采纳后才生效——与技术参数（REQ-F-072）同一条证据纪律；③头像以外部链接形式显示，链接缺失或加载失败时以姓名首字兜底，不留破图标；④"对话创建一份公司洞察、固化显示在动态屏"由既有 `save_insight`/`show_insight`（REQ-F-032）机制覆盖，本条不重复规定。 | APPROVED（CR-20260918-org-chart-board，R1 已终裁：INPUT-2026-09-18-001 第 8 条 + INPUT-2026-09-18-002 第④点确认为结构化、可动态跟踪的数据） |
| REQ-F-270 | 每用户实体/知识库数据私有隔离 | MUST | 每个真实用户（真实登录会话或单管理员本机身份）的实体（`entities.ts`）与知识库（`knowledge.ts`）数据各自存放在该用户私有的文件根下，任何用户之间不共享同一个数据池——与 `store.ts`（SQLite，conversations/providers/skills）已有的按 `userId` 隔离模式对齐。 | ①两个不同 `userId` 各自创建的实体互不可见（一方的 `GET /api/entities` 看不到另一方创建的对象）；②同一 `userId` 的数据在跨请求间保持一致（不因请求顺序或并发而串号）；③本条不要求资料库（`library.ts`，团队共享参考资料，见 REQ-F-046 附近条目）与本机文档目录（REQ-F-110）具备同等隔离——两者的共享模型与本条讨论的对象不同，如实排除。 | APPROVED（CR-20260918-per-user-data-isolation，R1 已终裁：INPUT-2026-09-18-001 第 9a 条调查后，用户通过 AskUserQuestion 明确裁定"Each person needs their own private data"） |
| REQ-F-271 | 现有单管理员真实数据首次真实登录时自动继承 | MUST | 本机单管理员模式（`JARVIS_SINGLE_ADMIN_ID`）下已经存在的实体/知识库真实数据，在满足 REQ-F-270 的私有隔离形态上线后，于该管理员本人的首次真实请求时自动"过户"到他自己的私有根下，不需要任何人工复核脚本或额外操作。 | ①迁移只对判定为 owner 的身份触发（`JARVIS_SINGLE_ADMIN_ID` 精确匹配，或未来真实登录场景下 `JARVIS_OWNER_EMAIL` 大小写不敏感匹配），任何其他身份的首次请求只得到一个空的私有根，不触碰既有数据；②迁移是幂等的——同一用户的第二次及以后请求不重复执行、不报错；③迁移按 entities/knowledge 两个子域独立判定，进程在两次子迁移之间意外终止后，下次请求只补完尚未完成的那一半，不会因为"整体未完成"而重来已成功的部分。 | APPROVED（CR-20260918-per-user-data-isolation，R1 已终裁：用户通过 AskUserQuestion 明确裁定"Automatic on first login"，权衡自动化风险后的直接产品决策） |
| REQ-F-280 | 本地文档/资料库展示改用结构化转换后的内容 | MUST | 本地文档（REQ-F-110）与资料库（REQ-F-220/230）的 PDF/DOCX 类文件，在展示屏/浏览器里查看时改用 markitdown 转 Markdown、再转 HTML 后的结构化内容显示，不再直传原始二进制字节；只做标题/列表/表格等结构还原，不做语义改写；HTML/纯文本类文件不受影响，保持原样直传。原始字节仍可通过显式参数下载。 | ①打开一份此前无法在浏览器里正常显示的真实 PDF（Microsoft Edge 把该文件类型的直传响应当高风险下载拦截），确认改为结构化内容显示后不再触发该拦截；②抽查转换结果与原文逐段核对，标题层级/列表/表格结构保留，没有语义改写的痕迹；③转换失败时有明确报错与获取原始字节的途径，不静默退回旧行为。 | APPROVED（CR-20260921-markitdown-display，R1 已终裁：用户提出正式需求 INPUT-2026-09-21-001，通过 AskUserQuestion 明确裁定直接引入 markitdown 到生产运行时、且只整理排版不做模型改写） |
| REQ-F-290 | 文档展示可选由用户指定的技能经模型重新排版 | SHOULD | 在 REQ-F-280 的结构转换之上，用户可在「本地文档」设置里从已注册技能中指定一个"排版技能"；指定后，PDF/DOCX 打开时把抽取出的 Markdown 交给当前 Provider，以该技能的 SKILL.md 为排版规则重新整理后显示。未指定或指向的技能已删除时，行为与 REQ-F-280 完全一致（并在页顶说明后一种情况）。同一份文件内容只排版一次，之后命中缓存不再调用模型。 | ①注册并指定排版技能后打开一份真实 PDF，显示的是模型排版后的版本（有标题层级、段落不再被 PDF 硬换行切碎）；②第二次打开同一份文件不再产生模型调用（命中缓存）；③模型对某一段的输出明显缩水时，该段保留原文并有可见标注，不静默丢内容；整体失败时退回结构转换并在页顶说明；④"不使用"即刻停用，无需改代码或重启；⑤人工抽查排版结果与原文逐段一致、无杜撰（模型路线的核心风险，机器无法断言，必须人看）。 | APPROVED（CR-20260921-format-skill，R1 已终裁：INPUT-2026-09-21-002 推翻 INPUT-2026-09-21-001 时"只整理排版不改内容"的裁定，用户经两轮 AskUserQuestion 明确接受模型调用的时长/费用/理解偏差风险、要求复用现有 SKILL.md 上传/注册机制、并选择按文件内容缓存） |
| REQ-F-300 | 对话内生成并注册技能 | SHOULD | 用户在对话里描述想要的技能，模型在回复里给出完整的 SKILL.md（frontmatter 的 name/description + 正文）供用户审阅；用户明确说「注册」后，模型才把这份 SKILL.md 注册为技能。注册出来的技能与经「技能」上传入口注册的在磁盘与技能表里毫无区别。本条只覆盖"生成并注册一份 SKILL.md"：不含附属文件，不含在对话里修改/删除已有技能（后者仍用 REQ-F-031 的 ☰ 入口）。 | ①用户说"帮我生成一个 XX 技能"，模型给出完整 SKILL.md 而不注册；②用户说「注册」后技能出现在 ☰「技能」列表与 `GET /api/skills`，`read_skill` 能读回正文；③用户不说注册或说"再改改"时不注册（反例必须真实试一次）；④重名时得到可读的冲突说明；⑤没有任何技能时也能用此路径创建第一个。 | APPROVED（CR-20260921-chat-skill-register，R1 已终裁：INPUT-2026-09-21-003；用户经 AskUserQuestion 裁定先预览确认再注册、只 SKILL.md、这次只做生成并注册） |
| REQ-NF-050 | 本地读取的边界 | MUST | 模型能读的只有用户明确配置的目录，且这条边界守得住。 | ①一切路径先经真实路径解析（`realpath`）再判是否落在根内——顺序反过来，`..` 与符号链接都能走出去；②包含判定**不得**用字符串前缀：根 `/srv/docs` 不包含 `/srv/docs-private`；③遍历时直接跳过符号链接——读取时守卫也会拒，但列出来本身就泄露了它的存在，且链接成环会让遍历无界；④拒绝越界时**不回显目标的真实位置**；给模型的输出一律用「目录名/相对路径」，绝对路径不出现在其中。 | APPROVED（CR-20260912-local-documents，R1 已终裁）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-NF-060 | 单轮成本护栏与可见性 | MUST | 一轮对话累计花掉多少必须看得见，且不能没有上限。 | ①**单轮累计**（本轮全部 provider 调用之和，不是单次请求）的 input+output 达到阈值时**停止继续发起工具调用**，改用已获得的材料作答，并在步骤流中说明原因——单次请求的预算已由 DEC-080 守住，缺的是**跨迭代的累计上限**：`MAX_TOOL_STEPS` 允许 100 次调用，每次可达 76.8k，无人叫停；②输出因长度上限被截断时**自动续写至模型自然结束**，不再要求用户输入「继续」；③自动续写须有**硬止损**（续写次数或本轮累计 output token，取先到），触及止损时停下并说明；④界面显示**本轮**累计的 input / output token，而不只是 REQ-F-037 的会话累计值——实测一次深研究提问累计 1,028,825 input + 35,261 output（约 1.6 美元）而用户无从看见，正是因为它只并进了会话总数；⑤触及 ① 的阈值或 ③ 的止损时向用户给出预警。本条是对 REQ-F-037 的**增补**而非替代。 | APPROVED（CR-20260912-turn-budget-continue，R1 已终裁 2026-09-12；②–⑤ 依据不变，① 的口径于 2026-09-12 更正为「单轮累计」）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-NF-061 | 流式超时看沉默，不看总时长 | MUST | 长回答不该因为「写得久」被掐断——这个产品做的就是长回答。 | ①对 Provider 的流式请求，超时判据是**距上一个字节的间隔**，每收到一块即重置；总时长不设上限——总量由 `max_tokens` 与本轮累计天花板（REQ-NF-060 ①）负责；②真停住时的错误文本要说「多久没动静」，不说「超时」——前者能让人判断是对方卡住还是自己网断；③首次请求（尚未开始收流）同样由该看门狗管；④连接测试（`/models`）仍按总时长算——那是一次一发一收的探测，没有「还在产出」这回事；⑤**人的一侧**：真停住时对话记录区里出现的那句话写明「静默了多少秒」（如「Provider stream went quiet for 60s.」），用户据此能分清是对方卡住还是自己断网；写得久但一直在出字的回答**不再中断**，这是用户直接看得到的变化。 | APPROVED（CR-20260914-idle-timeout，R1 已终裁：2026-09-14 实测——一份正在流式产出的长讲义写到 13,499 字时被整条掐断，用户 2026-09-14「继续」授权下修复） |

## 非功能需求

| ID | 名称 | 优先级 | 描述 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| REQ-NF-001 | 本机运行 | MUST | 第一版面向本机开发运行。 | 应用可通过本机命令启动，SQLite 数据保存于本地项目运行目录。 | APPROVED |
| REQ-NF-002 | 认证隔离 | MUST | 用户只能访问自己的 Provider 配置和会话。 | API 测试证明用户 A 不能读取或调用用户 B 的 Provider 与会话。 | DEFERRED（暂缓，CR-20260909-minimal-floating-chat）：当前版本为单一本地管理员，无隔离对象；恢复 Google 登录时同步恢复本条。 |
| REQ-NF-003 | 官方授权边界 | MUST | 第三方模型账号授权只使用官方支持方式。 | 系统不实现网页登录模拟、cookie 复用、抓包 token 或密码代登录。 | APPROVED |
| REQ-NF-004 | 真实入口验证 | MUST | 交付前必须通过真实 Next.js 应用入口验证主路径。 | 冒烟测试启动真实应用并走通（管理员身份免登录进入后）模型设置和底部浮窗对话主路径。 | APPROVED |
| REQ-NF-005 | 不可信归档处理边界 | MUST | 用户上传的 zip 是不可信二进制输入，解析必须有界且不得越出技能目录。 | ①**路径**：条目名含 `..` 段、绝对路径、盘符前缀或反斜杠分隔时拒绝整个归档；落盘前每个文件路径仍须经 `path.resolve` 越界校验（双重防护）。②**资源上限**：条目数、单条解压后字节、总解压后字节、压缩比四项各有上限，任一超限**整体拒绝**并返回可读原因（不得部分写入）。③**格式**：仅接受压缩方法 `0`(store) 与 `8`(deflate)；通用位标记的加密位置位即拒绝；解析失败/文件截断按拒绝处理。④解析器为纯函数、不接触文件系统，可独立施加对抗性测试。 | APPROVED（R1，2026-09-10 用户拍板；CR-20260910-skill-intake，DEC-018） |
| REQ-NF-006 | 前端 UI 视觉基础 | MUST | 全部已批准的前端界面必须共享一致、克制的浅色紫色视觉语言；默认主题为浅色紫色。该变更只更新视觉基础与组件呈现，不新增或改变业务能力。 | ①已批准的展示屏、底部悬浮对话、左下角菜单、菜单内「模型」、账号占位入口、模型设置与现有弹窗均纳入同一视觉基础；②「模型」仍是 Provider 启用与优先级配置入口，对话浮窗不新增 Provider/模型选择器，保持 REQ-F-006 的自动优先级语义；③Design Token 使用可追溯的语义化 HSL 三元组，组件不散落同义视觉常量；④所有已覆盖界面满足 `UI-GOV-001` 的 WCAG 2.2 AA、键盘、焦点和响应式要求；⑤P3 技能上传功能不构成本需求的验收范围或完成证据；⑥保留 REQ-F-014 的深色主题切换，浅色紫色只是默认主题，两套主题均须满足相同的可访问性与行为要求；⑦步骤流与状态灯第五态纳入本视觉基础（CR-20260910-agent-tooling）。 | APPROVED（R1，2026-09-10 用户终裁；CR-20260910-ui-foundation）；**APPROVED（CR-20260910-agent-tooling 补 ⑦；R1 已终裁 2026-09-10）** |
| REQ-NF-007 | 每轮上下文预算 | MUST | 单轮发送给模型的输入有硬上限与子预算。 | ①总输入硬上限按 Provider 上下文窗口比例（架构定）；②子预算：技能名录、工具定义、单条工具结果、来源列表各有上限；③工具结果超子预算 → 截断并在结果尾注明；④总输入超上限 → **三级顺序**：先按 REQ-F-041 收窄工具结果 → 再按 REQ-F-042 压缩文本历史（须过 REQ-NF-012 ① 的净省校验）→ 仍超才报请求级错误（REQ-F-016 红字）。**压缩是一次有界操作，不循环重压**（CR-20260911-context-compaction 重写 ④，原为「收窄后仍超即报错」）；⑤验收以 REQ-F-037 的 token 数为度量，基准场景（1 技能 + 连续 3 轮搜索）记入证据。 | APPROVED（CR-20260910-agent-tooling，R1 人工终裁 2026-09-10 完成、P3/P4 完成；状态回写遗漏，2026-09-11 补正） |
| REQ-NF-008 | 提示词前缀稳定性与工具动态注册 | MUST | system prompt 可缓存，工具定义不白占 token。 | ①system prompt 分**稳定前缀**（身份、工具定义、技能名录）与**易变后缀**（display 状态等）；稳定前缀同会话内字节级不变，除非技能 / 工具集合变化；②当前时间不进入稳定前缀；③工具与技能 description 强制简短（上限架构定）；④工具按可用性动态注册，注册条件**以 REQ-F-038 ④ 为准**：无技能 → 无技能工具；「联网」总开关关闭 → `web_search` 与 `read_url` 均不注册；总开关开但 URL 未配置 → 只 `web_search` 不注册；Provider 不支持工具调用 → 无工具；未注册的工具定义不进 prompt。⑤工具集合在**一次发送内**不得变化（REQ-NF-011 ② 的「本轮不再调用」实现为执行体短路，不注销工具定义），以免打破 ① 的前缀稳定。 ⑥**人的一侧：无**（纯内部装配规则，用户不感知）。工具注册与否的后果由别处呈现：步骤流里出现或不出现某个工具（REQ-F-035 ①），以及 Provider 不支持工具时的降级提示（REQ-F-040 ③）。 | APPROVED（CR-20260910-agent-tooling，R1 人工终裁 2026-09-10 完成、P3/P4 完成；状态回写遗漏，2026-09-11 补正） |
| REQ-NF-009 | 出网边界 | MUST | 联网能力的数据流与目标地址受限。 | ①联网工具是 Jarvis **唯一**主动出网点（Provider 调用除外）；②出网只携带查询词 / 目标 URL，不携带会话历史、凭据、用户身份、系统提示；③`read_url` 目标：仅 http / https；解析后 IP 不得为 loopback、链路本地、私网段、云元数据地址；**重定向每跳重校验**；④「联网」总开关关闭 → 两工具不注册；⑤搜索后端 URL 允许为本机地址（自托管 SearXNG），该例外仅适用于 `web_search` 的后端调用，不适用于 `read_url` 目标；⑥REQ-NF-003 不变：SearXNG 为无账号聚合查询，不构成登录模拟。 | APPROVED（CR-20260910-agent-tooling，R1 人工终裁 2026-09-10 完成、P3/P4 完成；状态回写遗漏，2026-09-11 补正） |
| REQ-NF-010 | 内核可扩展性 | MUST | 新增能力不改对话核心。 | ①本期**实际做成注册式**的是三处：**工具**、**技能来源**、**循环策略（loop）**；**模型适配与上下文预算 / 压缩策略只留接口位，本期不重构 `adapters.ts`**（用户 2026-09-10 终裁 6 选 A——无痛点指向 adapters，重构只为架构整齐，会放大本期范围）；②新增一个工具 = 注册一个描述符（name、description、参数 JSON schema、执行体、可用性判定），**不修改对话核心**；③loop 作为扩展点为 Code mode 预留（本 CR 非目标）；④模式参考 DeepSeek Harness 插件化，**不引入其包**；⑤本条 ①②③ 的验收为 grep 守卫（对话核心不 import 具体工具模块）+「注册一个假工具即可被循环调用」测；「预算 / 压缩 / loop 注册式」的设计合理性为代码审查项，不作机器断言（R1 测试角色意见）。 | APPROVED（CR-20260910-agent-tooling，R1 人工终裁 2026-09-10 完成、P3/P4 完成；状态回写遗漏，2026-09-11 补正） |
| REQ-NF-012 | 压缩的成本与失败边界 | MUST | 压缩是省 token 的手段，不得自己变成开销或新的失败点。 | ①**净省校验**——只有当被压内容的 token 显著大于「摘要预算 + 生成成本」时才真的压缩，否则放弃并维持原样（倍数由架构定）；②**增量压缩**——新摘要由「上一份摘要 + 新老化的轮次」生成，不从头重压整个会话；③**fail-open**——摘要调用失败 / 超时 / 返回空时放弃压缩。判据是：**压缩失败后的行为必须与「从未尝试压缩」完全一致，不得更差**。即：上下文本来放得下的，照常完成对话；本来就超预算的，仍按 REQ-F-041 收窄后走 REQ-F-016 的请求级错误——那是压缩引入之前就有的行为，不是压缩带来的新失败点。压缩**绝不成为新的对话失败原因**；④摘要生成的 token 计入 REQ-F-037 的会话累计，不隐藏成本。 | APPROVED（CR-20260911-context-compaction，R1 已终裁 2026-09-11） |
| REQ-NF-013 | 知识库边界 | MUST | 知识库不得成为路径、体积或预算上的漏洞。 | ①条目名称只允许字母 / 数字 / `-` / `_`（其余字符折为 `-`，路径穿越在名称层即消失）；非法名称一律按「不存在」处理，不触碰文件系统；②单条内容 ≤ 64KB（超出拒绝并说明），空内容拒绝；③`read_knowledge` / `search_knowledge` 结果受 REQ-NF-007 子预算截断并留可见标记；工具结果随 REQ-F-041 保留窗口淡出；④**零新增运行依赖**、无出网。 | APPROVED（CR-20260911-knowledge-base，R1 总授权，决策清单已于 2026-09-11 追认通过） |
| REQ-NF-014 | 浏览器通道边界 | MUST | 浏览器读取不得成为绕过既有出站防护的通道，也不得无界消耗资源。 | ①启动前先过 REQ-NF-009 ③ 的地址守卫，**地址被拒时不得启动浏览器**；②浏览器自行解析 DNS，故其发出的**每一个请求（含子资源）**都必须再过同一守卫，不通过即中止——这比普通路径更严；③无头运行，单次读取有超时上限，校验页等待有独立且更短的预算（不得为等一个注定失败的校验耗掉整轮）；④浏览器引擎以动态 `import()` 懒加载，普通对话轮不得为其付出加载成本；⑤不得引入指纹伪装 / stealth 补丁。 | APPROVED（CR-20260911-web-reading，R1 已终裁）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |
| REQ-NF-020 | 唤醒成本边界 | MUST | 主动唤醒不得成为 token 的无底洞或新的失败点。 | ①**单次有界**：一次非流式调用；输入 = 固定指令 + 最近对话尾巴按 1,500 token 从头截断（实测 9 个真实会话单次 215–1,611 token，EV-2026-09-11-proactive-wake §1）；输出 `max_tokens` 200；不进工具循环、不注册工具；②**日上限**：今日唤醒 input+output 达上限即跳过且不调模型，次日归零；上限与已用量同屏显示；③**失败静默**：调用失败/超时不打断、不报错，但把输入 token 计入日用量（请求可能已计费）；唤醒 token 同时计入会话累计（REQ-F-037 ②，标注估算）；④**零新增运行依赖、无 schema 变更**。 | APPROVED（CR-20260911-proactive-wake，R1 总授权，决策清单已于 2026-09-11 追认通过） |
| REQ-NF-030 | 看板边界 | MUST | 看板不得成为路径、体积或预算上的漏洞。 | ①对象名称只允许字母 / 数字 / `-` / `_`，其余折为 `-`，路径穿越在名称层即消失；非法名称按「不存在」处理；②对象正文 ≤ 64KB；待采纳的修改队列有条数上限；③实体工具的返回受子预算截断并留可见标记；④采集源必须是 http/https 且不得包含凭据；⑤**零新增运行依赖、无 schema 变更**。 | APPROVED（CR-20260911-home-dashboard，R1 逐轮拍板 2026-09-11） |
| REQ-NF-011 | 搜索后端不可用降级 | SHOULD | 搜索后端故障不打断对话。 | ①`web_search` 调用失败（连接失败 / 超时 / 非 2xx / JSON 非法）→ 作为工具失败回喂模型（REQ-F-029 ③），不弹请求级错误；②连续失败 ≥2 次 → 本轮后续不再调用，步骤流标记「搜索后端不可用」；③「搜索」配置的「测试连接」可复现该故障。 | APPROVED（CR-20260910-agent-tooling，R1 人工终裁 2026-09-10 完成、P3/P4 完成；状态回写遗漏，2026-09-11 补正） |
| REQ-NF-040 | 免登录模式的边界 | MUST | 免登录运行不得成为任何人都能打开的门。 | ①单管理员模式要同时满足两个条件才生效：`JARVIS_SINGLE_ADMIN_ID` 被显式设置，**且** `NEXTAUTH_URL` 的主机名是回环地址（`localhost` / `127.0.0.1` / `::1`）；②指向真实域名或局域网地址时**拒绝生效**，请求回落为 401，强制走真实登录；③`NEXTAUTH_URL` 缺失或无法解析时同样拒绝——判断不了就要求登录；④主机名按精确匹配判定，`localhost.evil.com` 一类前缀域名必须被拒绝；⑤本条不放宽 REQ-NF-002 认证隔离：单管理员模式下仍然只有那一个用户，跨用户数据边界不变。 | APPROVED（CR-20260912-local-production，R1 已终裁）；状态于 2026-09-13 按 DEC-220 ② 推进（CR-20260913-spec-hygiene） |

## 非目标

- 首版不实现图片、语音、多模态输入。
- ~~首版不实现工具调用和 Agent 编排。~~ **（作废：CR-20260910-agent-tooling，REQ-F-029；R1 已终裁 2026-09-10）**
- ~~首版不实现知识固化入口。~~ **（作废：CR-20260910-agent-tooling；已由 CR-20260911-knowledge-base 实现：REQ-F-046 三个入口）**
- 首版不实现公网生产部署。
- 首版不实现非官方第三方网页登录自动化。
- 首版不实现多用户与账号隔离（随 REQ-F-001 Google 登录一同暂缓）。
- 首版不实现对话历史浏览（已结束会话保留在本地但无浏览入口）。
- ~~首版不实现超长上下文的截断或摘要；超出模型上下文上限时由 Provider 报错，走流中错误路径。~~ **（作废：CR-20260910-agent-tooling——REQ-F-041 工具结果保留窗口 + REQ-NF-007 预算；完整压缩属 B 期；R1 已终裁 2026-09-10）**
- 折叠状态下不显示未读消息角标、消息计数或「有新回复」持久标记（CR-20260909-collapsible-panel）；折叠期间回复完成仅由状态灯瞬时提示，用户需手动展开查看内容。
- 技能系统（CR-20260909-skills）首版只做单一技能的上传/~~路由/注入/HTML 捕获~~**读取（工具化，CR-20260910-agent-tooling：路由与注入作废，HTML 捕获改 `save_insight` 工具）**；不做技能市场、技能编排/串联、技能版本管理、技能编辑 UI。
- 技能生成的 HTML 不做沙箱隔离、不做内容安全策略过滤、不做脚本剥离（REQ-F-025，用户 2026-09-09 明确接受风险）。
- ~~技能路由不保证判定准确；误判由 fail-open（当普通对话）与「仅当轮」边界兜底，不做用户可纠正的路由覆写 UI（首版）。~~ **（改写：CR-20260910-agent-tooling）** 技能选择由模型经工具自主完成，不保证与用户预期一致；用户可用自然语言显式指定（「用技能 X」）；不做路由覆写 UI。
- F1（CR-20260909-skills）不做技能 HTML 的首页/展示屏渲染——归 CR-20260909-display-screen（用户 msg 54「两个CR」）。
- 动态展示屏（CR-20260909-display-screen）首版只做全局单一展示 + `home`/`insight` 两种 `kind`；不做多屏/分屏、~~不做展示内容历史回看/切换~~**（改写：CR-20260910-agent-tooling 终裁 2 选 B——`show_insight(id)` 可切到任意历史洞察；仍不做展示屏内的历史列表 UI，切换只经对话）**、不做展示屏手动编辑或拖拽布局、不做展示屏内容导出。
- 展示屏不对技能生成的 HTML 做沙箱、CSP、脚本剥离（REQ-F-025，用户接受风险）；唯一缓解是不可关闭提示条。
- ~~路由的 `display` 字段只支持"回首页"意图；不做"显示第 N 份报告""显示配置页"等更细的展示指令~~ **（改写：CR-20260910-agent-tooling 终裁 2 选 B——路由字段作废，改为 `show_home()` / `show_insight(id)` 工具，「显示第 N 份报告」类意图由 `show_insight` 承担；仍不做「显示配置页」等指向非 `insight` 内容的指令，`kind` 扩展点为未来 CR 预留）**。
- 技能归档只支持 **zip**（压缩方法 store / deflate）；不做 rar / 7z / tar / tar.gz，不做分卷、不做加密归档（CR-20260910-skill-intake）。
- ~~仍不做技能的编辑、删除、重命名、版本管理与技能市场——☰ 菜单里的技能列表是**只读**的（REQ-F-028）。~~ **（部分作废：CR-20260910-agent-tooling——删除与重命名由 REQ-F-031 实现；编辑正文、版本管理、技能市场仍不做；R1 已终裁 2026-09-10）**
- 不做上传进度条、断点续传、后台队列；注册是一次同步请求。
- （CR-20260910-agent-tooling）采用 DeepSeek Harness 的**内核结构**（插件化、注册式、loop 可换），**不引入**其 Standard 模式的 subagents / planning / goals / workflows / shell / 文件编辑 / sandbox；不安装 `@deepseek-ai/dsh` 包。
- （CR-20260910-agent-tooling）Code mode（模型写程序一次做完多步）为未来候补；前提是执行沙箱与审批机制。
- （CR-20260910-agent-tooling）不做执行审批机制（本 CR 无写文件 / 执行命令类工具）；引入此类工具的 CR 以审批机制为出口义务。
- ~~（CR-20260910-agent-tooling）B 期「会话上下文分层与完整压缩」~~ **（B 期由 CR-20260911-context-compaction 实现：REQ-F-042/043、REQ-NF-012）**；~~C 期「本地知识库」~~ **（C 期由 CR-20260911-knowledge-base 实现：REQ-F-044/045/046、REQ-NF-013）**；~~D 期「主动唤醒（默认关 + 开关 + 空闲 token 上限）」~~ **（D 期由 CR-20260911-proactive-wake 实现：REQ-F-060/061、REQ-NF-020）**。附录 C 的四期至此全部落地；**文档未定义 E 期**。
- （CR-20260911-context-compaction）**不做压缩撤销**——原文保留在库中，但本版本不提供把已压缩内容重新放回上下文的入口。
- （CR-20260911-context-compaction）**不做跨会话记忆**——压缩只在单个会话内进行，不把旧会话的摘要带进新会话。**（CR-20260911-knowledge-base：跨会话记忆由知识库承担——REQ-F-044 ⑤；但仍不自动把摘要灌入知识库——REQ-F-046 ④）**
- （CR-20260911-knowledge-base）**不做语义 / 向量检索**——检索是词面 BM25，同义改写检不到；条目上千或需要语义近似时另立 CR 评估（会引入依赖，L3）。
- （CR-20260911-knowledge-base）**不做知识条目的在线编辑与版本**——文件在 `JARVIS_KNOWLEDGE_PATH` 目录里，用任何编辑器改；不做知识分享 / 导入导出 UI。
- （CR-20260911-knowledge-base）**不做模型直接写入知识库**——模型只能提议，用户采纳后才入库。
- （CR-20260911-proactive-wake）**唤醒不带工具循环、不联网、不读知识库**——只看最近对话尾巴；需要更「聪明」的唤醒时另立 CR 并重估成本。
- （CR-20260911-proactive-wake）**不做服务端常驻调度**——页面关着就不唤醒；不做多设备/多标签页去重之外的任何后台机制。
- （CR-20260911-proactive-wake）**不做唤醒历史面板**——提醒就是会话里的 system 行，随会话看。
- （CR-20260911-context-compaction）**不做手动压缩入口**——用户 2026-09-11 选择「自动、静默」，不提供「立即压缩」动作。
- （CR-20260910-agent-tooling）不做 token 费用换算；不做搜索结果缓存；不做技能编辑正文 / 版本 / 市场。

## 变更响应 · CR-20260908-floating-chat-functional-fixes

### 验收澄清

- REQ-F-005：「不再追加输出」必须在服务端可观察——停止后服务端停止向模型请求，落库消息状态为 `stopped`。
- REQ-F-006：「切换」必须在浮窗 UI 上可观察——存在多个已启用 Provider 时渲染切换控件，且所选 Provider/模型随对话请求发送。
- REQ-F-007：「新增、编辑、启用、停用、测试」为五个独立可观察动作，各自需要独立验收证据；「编辑」不得产生重复配置。
- REQ-F-013：「恢复」必须在真实入口可观察——刷新后通过 `GET /api/conversations/[id]/messages` 取回消息并在浮窗重建。
- REQ-F-008：`oauth` 认证模式在本版本仅作展示，不可保存为可用 Provider。

### CR-20260908-ui-light-theme-minimal-home 澄清

- REQ-F-007「独立设置页」在本版本同时存在两个真实入口：首页「模型」弹窗（原「配置」，CR-20260909-corner-menu 更名）与 `/settings/models` 深链页面，二者渲染同一 `ModelSettings` 组件。~~主题开关位于该弹窗的「外观」分区~~ **（作废：CR-20260909-corner-menu 将主题开关移出该弹窗、内联进左下角 ☰ 菜单；「模型」弹窗此后只含 Provider 设置，与深链页对齐）**。
- REQ-F-002/003：展开后的对话面板高度上限为视口 65%，不得占满全屏（避免退化为通用聊天工具）。**（已被 CR-20260909-minimal-floating-chat 收敛为 50%，见下。）**
- 首页「知识库概览」区域在当前版本不展示，属已批准的暂缓项，不构成需求删除。

### CR-20260909-minimal-floating-chat 澄清

**会话生命周期**
- 会话自**首条用户消息**创建，至用户点击**「停止」或「新对话」**结束。
- 活动中：多轮续接，每次发送沿用同一 `conversationId`；回复正常完成后会话仍活动，可继续追问。
- 已结束：`conversationId` 丢弃；记录区在「点击新对话」或「发送下一条消息」时清空；下一条消息创建新会话。
- 已结束/已停止的会话及其消息保留在本地存储，本版本无浏览入口，且不被刷新恢复逻辑捞取。

**请求级错误 vs 流中错误**
- 请求级错误（回复第一段文本到达前失败：连接失败/鉴权失败/Provider 4xx-5xx/超时）→ 输入框上方一行红字，脱敏，不写入上下文，不结束会话，不阻塞输入。
- 流中错误（回复已开始后中断）→ 该助手气泡标记「生成失败」，落库 `status=error`（现有行为）。

**面板高度**：展开后对话面板高度上限由视口 65% 收敛为 **50%**（覆盖 CR-20260908-ui-light-theme-minimal-home 的 65% 澄清）。

**动作按钮状态机**（单个按钮）

| 场景 | 按钮 | 行为 |
|---|---|---|
| 生成中 | 停止 | abort，服务端停止请求模型，会话结束，落库 `stopped` |
| 输入框有字 | 发送 | 发送，Enter 等效 |
| 输入框空（有/无历史均同） | 新对话 | 清空记录区，丢弃 `conversationId` |

**优先级解析**：见 REQ-F-006 验收 ④⑤⑥。状态灯反映「是否存在任一连接有效的 Provider」；发送时取连接有效的 Provider 中优先级最高者。

**REQ-F-001 暂缓边界**：当前版本以单一本地管理员身份运行，不实现账号登录；「账号登录」按钮保留为占位入口。恢复 Google 登录时同步恢复 REQ-F-001 与 REQ-NF-002。

### CR-20260909-collapsible-panel 澄清

**折叠状态语义**（REQ-F-019）：收起/展开是纯 UI 态，不影响 `conversationId`、`jarvis:chat-session-ended`，不触发或结束会话；与「新对话」「停止」正交。持久化用 `localStorage['jarvis:chat-collapsed']`（每浏览器）；`localStorage` 服务端不可读，SSR 首帧按默认（展开）渲染，hydration 后按存储校正——只接受一帧内校正，不接受持久错误态（与 DEC-011 同类处理）。

**折叠期间回复完成提示**：仅状态灯一次短促瞬时提示，不引入未读角标、消息计数或~~第五种持久灯态~~。**（「不引入第五种持久灯态」由 CR-20260910-agent-tooling 作废——用户 2026-09-10 拍板破四态，新增「执行工具中」为第五态，见 REQ-F-036；「不引入未读角标 / 消息计数」不变。）**

**折叠时的请求级错误红字**：折叠状态下记录区不在 DOM，但请求级错误红字**照常显示在输入框上方**（它关联「发送」动作，输入条始终可见）。

**折叠可访问性**：折叠时 `aria-live` 记录区随之移除，屏幕阅读器用户在折叠期间不朗读流式回复——与视力用户对等（都需展开查看内容），非缺陷。

### CR-20260909-corner-menu 澄清

**左下角 ☰ 菜单**（REQ-F-015）：header 不再有功能按钮，标题独占顶部。☰ 触发器 `position: fixed` 于左下角，层叠高于底部悬浮对话（`z-index` > 20），对话折叠或展开时均可点击。浮层菜单为**非模态** disclosure（Esc 关 + 焦点回触发器 + 点击外部关闭）；「模型」「账号登录」两项仍打开各自的模态 `<dialog>`。浮层机制（原生 `popover` 属性 vs 自写 disclosure）留 P3。

**主题开关内联**：浅色/深色切换从「模型」（原「配置」）弹窗的「外观」分区移出，作为 ☰ 浮层内的内联控件——点击**不打开任何弹窗**，当场切换并写 `localStorage['jarvis-theme']`（`ThemeToggle` 组件与持久化机制不变，仅换宿主）。

**「配置」→「模型」**：首页该入口的按钮/菜单项文案由「配置」改为「模型」，打开的仍是同一个 Provider 设置弹窗（内部标题不强制改）。「配置」一词在配置预检（`config:check`）、`ConfigWarning` 等其它上下文不受影响。

**dev 指示器**：`next.config.mjs` 设 `devIndicators: { position: "bottom-right" }`，把 Next.js dev 模式指示器挪到右下角，避免与左下角 ☰ 冲突。这是构建配置，不在应用组件里做 `NODE_ENV` 分支；生产构建无 Next 指示器。

**可发现性取舍**：设置/账号入口在左下角属非行业惯例（惯例在右上角/汉堡菜单）。鉴于当前为单一本地管理员（配置一次即可），可发现性成本可接受——记为**有意取舍**，非遗漏。

## 变更响应 · CR-20260910-agent-tooling

### 来源与决策依据

INPUT-2026-09-10-008（用户原话 11 条）；`project/05_evidence/EV-2026-09-10-agent-tooling-requirements.md`（仓库代码事实、联网调研、16 项决策时间线、三轮审视缺口）。级别 L3 重型（含单向门 CP-5 / CP-7 / CP-10 / CP-16）。变化点 CP-1..CP-25 见 CR「变化点登记」表。

本节只记录**为什么这样变**；「现在是什么」已直接写入上方基线表（REQ-F-029..041、REQ-NF-007..011 新增行；REQ-F-003/004/005/006/013/014/018/019/023/024/028、NF-006 重写；REQ-F-021/022/027 SUPERSEDED；非目标作废 4 行、改写 1 行、新增 5 行）。

### 既有条款澄清（不改语义）

- REQ-NF-001 本机运行：讲部署形态（单进程、本机启动、SQLite 本地）；联网工具的出网是数据流，本条仍成立。
- REQ-NF-003 官方授权边界：SearXNG 为无账号、无 cookie 的聚合查询，不构成「网页登录模拟」；本条不放宽。
- REQ-NF-004 真实入口验证：冒烟扩到两条 agent 主路径——「注册技能 → 提问 → 步骤流 `read_skill` → 回答」「配置搜索 → 提问 → 步骤流 `web_search` / `read_url` → 回答附来源」，以及「未配置搜索」路径。
- REQ-F-016：工具执行错误既非请求级错误也非流中错误——它是**工具结果**，在步骤流内显示；工具结果摘要按 Markdown 渲染，链接仍仅 http / https / mailto 可点击；模型输出中的原始 HTML 不执行。
- REQ-F-020 ②：生成 `SKILL.md` 的独立 LLM 调用**不进工具循环**、不注册工具、不计入 REQ-F-037 的会话累计（它不属于任何会话）。

### 关键取舍（用户拍板，2026-09-10）

| 取舍 | 决定 | 为什么 |
|---|---|---|
| 参考对象 | 读 DeepSeek Harness 抄结构自行实现，不装包；OpenClaw 只借「主动唤醒」并推到 D 期 | dsh 同栈同许可、插件化正好补 Jarvis 缺的内核层；OpenClaw 的 token 开销是结构性的（官方文档自述） |
| 独立路由调用 | 删除 | 每轮省一次模型调用；模型经工具自主选择更准 |
| 「仅当轮注入」 | 取消 | 工具化后 read 结果随保留窗口自然淡出 |
| 搜索后端 | SearXNG（无 key、零 npm 依赖） | 用户无 API key；架构约束「新增运行依赖属 L3」 |
| 工具审批 | 只读工具免审批 | 当前工具集全部只读或仅改 UI 状态；写 / 执行类工具的审批为未来 CR 出口义务 |
| 状态灯四态 | 显式破坏为五态 | 长任务下四态不够；折叠态需要灯承担执行可见性 |
| 面板 50% | 有步骤流时 75% | 10 步步骤流塞进半屏会把回复挤出可视区 |
| 触顶 | 提醒后停止，成果保留 | 不白烧已跑的 10 步 |
| Provider 不支持工具 | 提醒 + 降级纯对话，不跳过 | 用户明确「提醒」而非「跳过」 |
| token 拿不到 | 本地估算并标注 | 否则 REQ-NF-007 在本地模型上无验收手段 |
| `web_search` 分期 | 提进 A 期 | A 期只做技能工具化时用户可感知收益仅「能删技能」；搜索是工具循环 / 步骤流 / 预算三者的最佳压测用例 |

### 分期

| 期 | 内容 | 状态 |
|---|---|---|
| A（本 CR） | REQ-F-029..041 + REQ-NF-007..011 + 上述重写 / 作废 / 澄清 | R1 已终裁 2026-09-10，P3/P4 完成（2026-09-11 状态回写补正） |
| B | 会话上下文分层与完整压缩 | 另立 CR |
| C | 本地知识库（检索工具 + 知识固化入口） | 另立 CR |
| D | 主动唤醒（默认关 + 开关 + 空闲态 token 上限） | 另立 CR |

### 待架构裁决项（P2）

单工具超时；`read_url` 体积上限与摘要方式；REQ-F-041 的 N；REQ-NF-007 各子预算数值；REQ-NF-008 description 长度上限；工具记录的数据形状与迁移；Provider 能力字段；搜索配置存储位置；SSE `tool_call` / `tool_result` 事件形状；token 本地估算方法。

## 变更响应 · CR-20260911-display-console-ux

### 来源与决策依据

- 用户原话：INPUT-2026-09-11-010；分析证据：`EV-2026-09-11-display-console-ux.md`（本机开发库 `save_insight` 调用时间线、三种视口截图测量、源码走读）。
- 四个用户可见现象对应的缺陷分层：截断不可见（适配器）→ 参数解析静默失败（注册表）→ 分块只能覆盖（展示工具）→ 无基础样式（展示屏）；菜单浮层无高度约束与分组；面板缺少悬停态；步数上限过低。

### 既有条款澄清（不改语义）

- REQ-F-024 ③「多次产出按 created_at 追加，不覆盖」说的是**行**不覆盖；REQ-F-050 ① 补的是「追加到同一份」的工具语义，两者并存：新建仍新建一行，追加只更新既有行的 `html`。
- REQ-F-026 ③「展示屏渲染该 HTML」：渲染方式由「原样 srcdoc」细化为「基础样式外壳 + 原样内容」（REQ-F-052），无沙箱决策（DEC-015）不变。
- REQ-F-019 ④「折叠态不自动展开」继续成立；REQ-F-054 的自动展开只作用于 `autoHidden`，从不改 `userCollapsed`。
- REQ-F-029 ④ 同参连续失败 2 次熔断、REQ-NF-007 预算、REQ-F-041 保留窗口、REQ-F-042 压缩在 100 步上限下继续作为成本边界。

### 非目标

- 不做 iframe 沙箱化（仍是既有出口义务，`skill-html-unsandboxed-web-source`）。
- 不做洞察历史浏览 / 切换 UI（REQ-F-032 ② 的 `show_insight` 仍是唯一切换入口）。
- 不改技能模板，不把产物形态改为 Markdown（备选 C，待终裁；若采纳另立 CR）。
- 不做报告导出、打印样式。
- 不为触屏设备设计悬停态的替代手势。

### 待用户终裁项（R1）

| # | 事项 | 助手建议 |
|---|---|---|
| 1 | 追加语义形态：`save_insight` 加 `insightId` 参数，还是新增 `append_insight` 工具 | 加参数——工具面不膨胀，模型已熟悉 `save_insight` |
| 2 | 是否采纳备选 C（Markdown 产物、服务端套模板） | 本期不采纳；先看 HTML 路径修完后的真实效果 |
| 3 | 搜索设置是否从内联表单改为对话框入口 | 改——与「模型」同形，抽屉只放入口 |
| 4 | 悬停延迟 400ms / 过渡 150–200ms 初值 | 接受为初值，架构按实测调整并回写 |
| 5 | 收缩后是否留最后一条消息的单行预览 | 不留——与 REQ-F-019 ② 形态一致，少一种中间态 |

### 待架构裁决项（P2）

各 Provider kind 的 `max_tokens` 默认值与 Provider 行覆盖字段；洞察正文上限；追加时的 HTML 拼接方式（片段直接拼接 vs 完整文档取 `<body>` 内容）；基础样式的注入方式（`srcDoc` 包裹 vs `<link>`）；抽屉组件选型（shadcn Sheet 与 DEC-014 手写 disclosure 的取舍，含 jsdom 可测性）；`autoHidden` 计时器与 `pointerleave` 的 `relatedTarget` 判定；DEC-022 / MOD-TOOLS / CP-1 数值表中「10 步」的同步。

## 变更响应 · CR-20260911-web-reading

### 来源与决策依据

- 用户原话：INPUT-2026-09-11-014；实测证据：`EV-2026-09-11-web-reading.md`。
- 三项报告的根因彼此独立，**其中两项推翻了最自然的猜测**：搜索是 `search.base_url` 缺失导致工具不注册（代码完好）；403 与 User-Agent 无关，是 Cloudflare / Akamai 的 JS 人机校验（加完整浏览器头、乃至驱动真实 Chromium 有头运行，实测均停在校验页）。PDF 则是最严重的一项——旧实现把 PDF 当网页解码，返回 38.9 万字符噪音**并报成功**。

### 既有条款澄清（不改语义）

- REQ-F-034 ①「只有摘要进入上下文」对 PDF 同样成立：原始字节不入库，只有提取出的正文入库。
- REQ-NF-009 ①「联网工具是唯一主动出网点」不变：浏览器通道属于 `read_url` 的一条分支，不是新的出网点，且受 REQ-NF-014 ② 约束（比普通路径更严，子资源也校验）。
- REQ-NF-003「官方授权边界」不变：本机 SearXNG 为无账号、无 cookie 的聚合查询；浏览器通道不登录、不携带凭据、不做指纹伪装。
- REQ-F-040 / REQ-NF-008 ④ 的注册条件不变：`search.base_url` 缺失时 `web_search` 仍不注册，本 CR 修的是**该键为何缺失**。

### 非目标

- 不做指纹伪装、stealth 浏览器或验证码求解——用户点名的 opencompute.org / tesla.com / iea.org **本 CR 之后仍然读不到**，工具会明确这么说并给出替代路径（换来源，或把正文贴给助手 / 存入知识库）。
- 不做 PDF 的 OCR、版面还原、表格结构化；不做多栏阅读顺序推断。
- 不做浏览器登录态、不保存 cookie、不做交互式操作。
- 不把浏览器通道用作普通页面的默认路径（实测对普通页面无优势且更慢）。

### 待用户复核项（R1 已终裁，但如实提请）

`playwright-core` 提为运行依赖是本 CR 唯一的单向门，用户以「都做吧」明确授权。**实测收益有限**：5 个样本站中只救回 1 个（tsmc.com），对用户点名的三个站点全部无效。若不认可这个取舍，可按 CR 的「回滚方式」**单独撤销依赖**而保留 PDF、搜索、请求头、失败分类全部能力。

## 变更响应 · CR-20260912-context-degrade

### 来源与决策依据

- 用户原话：INPUT-2026-09-12-018（贴出实际收到的错误）；实测证据：`EV-2026-09-12-context-degrade.md`。
- 根因是**份额本身不相容**：`singleToolResult` 0.15 × 4 = `totalInput` 0.6。读四份文档按构造就装不下。
- 而 `applyRetentionWindow`（保护最近 2 轮）与 `planCompaction`（保护最近 3 轮）都以「最近的最重要」为前提，大段阅读恰好都在那几轮，于是谁都不去动它们。

### 既有条款的关系

- **REQ-F-041 语义不变**：保留窗口仍是「最近 N 轮原文保留」，本条只在其之后追加收紧级别。
- **REQ-F-042 不变**：压缩仍保护最近 3 轮。让压缩去摘要刚说过的话是更严重的退化。
- **DEC-026 ⑥ 继续成立**：它禁止的是循环压缩（要额外调模型、不保证终止），而逐级截断两条都不沾。

### 一处如实说明

`CR-20260912-sandbox-and-budget` 把单条结果上限从常量 12,000 改为按真实窗口计算的 19,200。那次修的是「上限与模型无关」这个缺陷，是对的，但**同时把这个不相容关系放大了六成**，加剧了本故障。缺陷在它之前就存在（12,000 × 7 同样超限），但这一点如实记录。

### 非目标

- 不改 `BUDGET_SHARES` 的声明值。改份额会影响每一条工具输出的长度，影响面远大于本故障，值得单独评估。
- 不按相关性分配削减额度。一条大 PDF 与一条小搜索结果适用同一上限，前者被削得多；按相关性分配需要额外判断，本期不做。
- 不让压缩去处理最近几轮。

## 变更响应 · CR-20260912-ingest-extract-chain

### 变化点影响矩阵

标准档，R1 已由用户于 2026-09-12 终裁（依据：用户选定收口路线 A）。

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 extract 可读 pending | REQ-F-180 ①② | 缺陷修复 | 新增 REQ-F-180 |
| CP-2 失败回传可执行原因 | REQ-F-180 ③ | 缺陷修复 | 新增 REQ-F-180 |
| CP-3 字段分两类 | REQ-F-180 ④⑤⑥ | 新增 | 新增 REQ-F-180 |
| CP-4 抓取正文去噪 | REQ-F-180 ⑦ | 缺陷修复 | 新增 REQ-F-180 |

**⑤ 是 R1 产品角色补强的**：原验收只写「分两类」，未规定由谁判定；若由模型自报归类，模型可把编造内容报成结论字段绕开 quote 校验，护栏退化为自愿。

## 变更响应 · CR-20260912-knowledge-attribution

### 变化点影响矩阵

标准档（全 CP 双向门，但 CP-2、CP-5 的发现方式是真实入口操作）。R1 已由用户于 2026-09-12 终裁。**无数据迁移**（已裁定不回填历史条目）、**零新增运行依赖**、**无新增出网面**。

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 `save_knowledge` 三字段 + 对象校验 | REQ-F-170 ①② | 缺陷修复 | 新增 REQ-F-170 |
| CP-2 `read_knowledge` 带元数据头 | REQ-F-170 ④ | 新增 | 新增 REQ-F-170 |
| CP-3 `search_knowledge` 命中带 entity | REQ-F-170 ⑤ | 小改 | 新增 REQ-F-170 |
| CP-4 索引覆盖 entity / docType | REQ-F-171 ③ | 缺陷修复 | 新增 REQ-F-171 |
| CP-5 新增 `list_knowledge` | REQ-F-171 ①②④ | 新增 | 新增 REQ-F-171 |
| CP-6 `list_knowledge` 优先级与预算 | REQ-F-171 ⑤ | 小改 | 新增 REQ-F-171 |

**与既有条文的关系**：REQ-F-045 ③「零条目时读取工具不注册」**不改**——REQ-F-171 ④ 只为新增的 `list_knowledge` 开一处局部例外。该条对既有工具的连带后果（`total=0` 时 `search_knowledge` 不注册，`misses` 在冷启动阶段记不到东西）须单独立 CR 由用户终裁，不在本 CR 夹带。

**本 CR 明确不做**：跨语言检索（中文查询命中外文正文）需要同义词表或向量召回，量级远大于本 CR，登记不做。

## 变更响应 · CR-20260912-latency-and-report-layout

### 来源与决策依据

- 用户原话：INPUT-2026-09-11-013；实测证据：`EV-2026-09-11-chat-latency.md`。
- 两件事根因无关：延迟主因在 Provider 侧（自有开销 80 ms，DeepSeek 首字节 432–6,248 ms 波动），报告靠左是样式层次问题。

### 既有条款澄清（不改语义）

- REQ-F-041 ①②③ 的语义**一字未改**。REQ-F-091 是对其实现的补正——原实现把「只有最近 N 轮保留原文」打了折扣，只在超预算时才执行。
- REQ-F-052「洞察自带样式优先」不变；REQ-F-090 只接管最外层列宽与位置。
- DEC-026 ⑥ 的「收窄后仍超限即报错、不做循环压缩」保留。

### 非目标

- 不为对话延迟做缓存、连接预热或预取——实测自有开销 80 ms，这些手段改善不了首字时间，只增加复杂度。
- 不改变洞察内部的排版、配色或字体，那仍归文档自己。
- 不调整压缩阈值（REQ-F-042 归 B 期决策，本 CR 不动）。

### 一处如实说明

本 CR 为**事后补记**：代码 2026-09-11 已改，CR 与证据 2026-09-12 才写，期间代码注释引用了并不存在的证据文件 `EV-2026-09-11-chat-latency`，以及一个并不存在的功能需求编号（F 系列 059）。违反 AI_STANDARD 原则 8。虚构编号已替换为真实分配的 REQ-F-090，疏漏由助手主动上报。

## 变更响应 · CR-20260912-local-documents

### 来源与决策依据

- 用户原话：INPUT-2026-09-12-016 及两条澄清；实测证据：`EV-2026-09-12-local-documents.md`。
- 缺的不是一个工具而是一整层：改动前 18 个工具里没有任何一个碰得到用户磁盘（对 `src/lib/tools/` grep `readFile`/`readdir` 零命中）。

### 两层的分工（不可再合并）

| | 本地原文档（REQ-F-110） | 知识库（REQ-F-044/045/046） |
|---|---|---|
| 是什么 | 用户自己的文件，**事实来源** | 模型提炼、用户采纳的结论，**沉淀** |
| 谁放进去的 | 用户，放在自己的文件夹里 | 模型提议，用户采纳后才可检索 |
| 原件 | 原件就是全部 | 不保留原件 |
| Jarvis 的权限 | **只读** | 读写（经采纳） |

用户已明确否定把前者并入后者：「我说的知识库，不是规格化的知识数据库，是本地的原文档」。本条与 REQ-F-044/045/046 **并列而非替代**，后者语义一字未改。

### 非目标

- 不提供通用的 `read_file(path)`。没有根目录概念就没有边界可守。
- 不引入解析库。项目已有零依赖的 PDF 提取与 zip 读取，docx 就是 zip 装 XML。
- 不做 OCR。扫描件 PDF 会说明「没有文字层」，而不是返回乱码。
- 不写用户的文件。这一层没有写入口，连删除目录配置都不碰磁盘。
- **本条不解决「模型应该先查本地」**。工具在名录里，但没有任何东西告诉模型优先级——那属于身份提示层（用户所说的 agent.md），另立 CR。

### 一处如实说明

用户给的分层是「Agent 对话 → agent.md → skill 层 → 工具层 → 本地原文档 / 联网知识」。本 CR 补的是**最后一环**，agent.md 那一环仍然缺：身份提示至今只有一句英文常量，未写任何工作顺序。所以现在的状态是「能读，但不一定会主动去读」。

## 变更响应 · CR-20260912-local-production

### 来源与决策依据

- 用户原话与背景：INPUT-2026-09-12-012；实测证据：`EV-2026-09-12-local-production.md`。
- 直接起因是四次服务事故：`next dev` 内存增长到 3.8 GB 后编译 worker 被系统杀掉，GET 仍返回 200 而写路由 500，用户看到的是「删不掉 Provider」「优先级不生效」。

### 既有条款澄清（不改语义）

- REQ-F-001「Google 登录」保持 DEFERRED。本条不是恢复登录，也不是取消登录，而是让已选择的「本机单管理员」形态也能在生产构建下运行。
- REQ-NF-002「认证隔离」不变：单管理员模式下系统里仍只有那一个身份，用户之间的数据边界没有放宽。
- DEC-009「构建目录隔离」在此复用：生产构建固定用 `.next-prod`，因此可以与 `next dev` 并排运行而互不破坏。

### 非目标

- 不做内存看门狗或自动重启——治标不治本，且会掩盖真实的内存压力。
- 不做多用户、不做权限分级、不恢复 Google 登录。
- 不为生产模式做热更新替代方案；「改完要重新构建」是这条路径的固有代价，只做文档说明。
- 不放宽 `JARVIS_TEST_USER_ID` 的任何行为。

### 一处如实更正

助手在提出方案时估计「生产模式内存约为 dev 的三分之一」。实测为**十二分之一到二十一分之一**，原估计偏保守。以实测为准，见 EV §2。

## 变更响应 · CR-20260912-proposal-id-collision

### 变化点影响矩阵

标准档，R1 已由用户于 2026-09-12 终裁（依据：用户选定收口路线 A）。

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 id 去碰撞 | REQ-F-046（既有条文，行为修正） | 缺陷修复 | 无新增条文 |
| CP-2 汇总取实际落盘数 | REQ-F-046（既有条文，行为修正） | 缺陷修复 | 无新增条文 |

本 CR **不新增需求条文**：既有条文从未允许提议静默互相覆盖，也从未允许把入参条数当成落盘条数上报。属既有行为的缺陷修复，落点在 DEC-180 与 TASK-260。

## 变更响应 · CR-20260912-runtime-visibility

### 来源与决策依据

- 用户原话：INPUT-2026-09-12-017；实测证据：`EV-2026-09-12-runtime-visibility.md`。
- 两件事看起来无关，其实是同一个：运行时的状态对需要它的两方都不可见——用户判断不出服务是否还活着，模型说不出自己接的是谁。

### 排查结论：代码层没有缺陷

逐项排除了客户端误中断、进程崩溃、Provider 挂死、响应头被拖住、WSL 网卡；600 次请求零失败；1,200 次请求内存纹丝不动。真正原因是整机 90% 内存占用下服务被系统停掉。

**本条需求不修根因**（腾内存不在代码范围），只做两件事：让后果可见，让服务能自己回来。

### 既有条款的关系

- **REQ-F-016 不变**：请求级错误仍然回滚乐观更新、在输入框上方显示一行红字、不动会话。改的只是那行字说什么。
- **REQ-NF-008 ① 不变**：运行时信息进易变后缀，稳定前缀的字节稳定性不受影响。这是选易变后缀而非身份提示的全部理由。
- **REQ-F-040 不变**：Provider 降级时运行时那行随之更新，正是它必须可变的原因之一。

### 非目标

- 不解决内存本身。整机 90% 占用是真正的原因，而最大消耗方是开发工具链自身，不是 Jarvis。
- 不给模型加自省工具。多一次工具往返只为一句本该在提示里的事实，且模型未必想到去调。
- 不把看护塞进 `start:local`。保留一条「就起一个、不自动重启」的路子给排查用。
- **不是 agent.md 层**。本条只补了「运行时是什么」这一项事实，没有写工作顺序（本地优先、联网兜底）。那一环仍缺。

## 变更响应 · CR-20260912-sandbox-and-budget

### 来源与决策依据

- 用户原话：INPUT-2026-09-12-015；实测证据：`EV-2026-09-12-sandbox-and-budget.md`。
- 三件事都不是新发现的风险。前两件档案里早有记载，本 CR 补的是「现在修的代价很小」与「当初接受风险时的前提已经变了」。

### 既有条款的修订与澄清

- **修订 REQ-F-025 ②**：提示条文案由「未做安全隔离」改为说明内容已隔离但**未经核实**。沙箱化之后原文案已不属实，但删掉提示条更错——隔离的是权限，内容可信度一点没变。提示条仍不可关闭、仍无关闭控件。
- **REQ-F-029 ② 语义不变**：轮内预算放不下时复用它已定义的「触顶」行为，不新造一种停止状态。
- **REQ-F-041 ①②③ 语义不变**：该条管跨轮，本条管轮内，两者不冲突（见 REQ-F-101 ③ 括注）。
- **REQ-NF-009 不变**：本 CR 不新增任何出网面。

### 非目标

- 不解决提示注入本身。攻击者网页仍可让模型在对话里复述其内容或据其作答；本 CR 只切断「攻击者 HTML 取得本应用同源权限」这一环。该路径的发现方式仍为**发现不了**，如实留在风险登记里。
- 不把「联网」与 `save_insight` 做成互斥——那是 2026-09-10 已被用户否决的选项。
- 不把 `MAX_TOOL_STEPS` 调回 10。用户 2026-09-11 明确要求提到 100，且步数与 token 不是一回事：三次大页读取同样能超预算。
- 不做空 `sandbox=""`。当下同样零损失，但会挡掉将来模型生成图表的可能；若洞察日后会承载用户私有数据，应再评估收紧。

## 变更响应 · CR-20260912-skill-paging

### 来源与决策依据

- 用户原话：INPUT-2026-09-12-020；实测证据：`EV-2026-09-12-skill-paging.md`。
- 真实技能 20 个文件 160 KB，入口只给 32 KB 拼接块，**128 KB 永远看不到**，且看得到的那部分由字母序决定。评审标准、工作流、报告模板这三份恰恰决定报告质量，改前大概率落在被截断的部分里。

### 既有条款的修订

- **修订 REQ-F-030 ②**：技能正文的读取方式由「整份读取」改为「入口 + 按需逐文件」。「正文不进稳定前缀、要用时才读」的原意不变，只是「要用时才读」的粒度从整份细到单个文件。
- **REQ-F-003 语义不变**：控制台不占满屏幕这一条照旧，本条只把步骤流分支的数值从 75vh 收紧到 58vh。

### 非目标

- 不提高 32KB 注入上限。技能实测 160 KB，要装下得提到五倍，直接吃掉每轮上下文预算；且下一个更大的技能又会撞上。
- 不做相关性排序。清单按路径字母序列出并给出体量，由模型自己判断先读哪个。
- 不在本期横向错开报告列与控制台。需要处理面板展开/收起时报告是否跟着位移，属更大的布局改动。
- 不改写技能库里的旧技能。本条让它们**可被完整读到**，没有改写其内容。

### 一处必须同步删除的旧实现

`resolveSkillForTurn` 及其专用的 `TRUNCATION_NOTICE`、`truncateToBytes` 在本条改动后生产代码已无人调用，按交付纪律在同一变更内删除。对应的三处既有测试**迁移而非删除**——它们守的事情仍成立，只是问法换了。

## 变更响应 · CR-20260912-skill-report-bridge

### 来源与决策依据

- 用户原话：INPUT-2026-09-12-019；实测证据：`EV-2026-09-12-skill-report-bridge.md`。
- 三件事是一条链：技能不知道这里怎么出东西 → 模型临场搭桥 → 出来的东西又被面板盖住。

### 既有条款的修订与澄清

- **修订 REQ-F-050 ①**：洞察写入由「新建 / 追加」两种扩为三种，增加整篇重写。追加仍为默认，既有分块流程不受影响。
- **REQ-F-052 语义不变**：洞察自带样式优先本就成立，本条只是让模型知道可以带。
- **REQ-F-054 机制不变**：悬停收缩/展开照旧，本条只新增一个收起时机（报告落地），且明确它发生在流式结束之后。

### 非目标

- 不改写用户的技能文件。重装技能会抹掉改动，且每来一个新技能都要再改一次。
- 不把约定写进稳定前缀。它只在读技能时有用，进前缀是每轮都付的固定成本。
- 不在本期让报告列与对话面板彻底错开中轴。那是更大的布局改动，先用「落地即让开」解决默认状态下的遮挡。
- 不改技能注入上限。167,755 字符对 32KB 上限意味着约五分之四进不了模型，提高上限会挤占每轮预算，需单独评估。

### 一处如实说明

排查中查出 `ChatDelta` 的 `insight` 事件**全仓无人发出**——类型在、前端分支在、「已生成洞察」提示也在，但从工具化改造起就没有代码产生它。本 CR 让三个写入路径都发出它，该分支随之复活。这个洞是被真实入口断言逼出来的：新加的「面板让开」挂在它上面，跑 e2e 才发现不触发。

## 变更响应 · CR-20260912-stage-reach

### 来源与决策依据

- 用户原话：INPUT-2026-09-12-016 的两条运行反馈；实测证据：`EV-2026-09-12-stage-reach.md`。
- 两条反馈是同一个根因：展示屏的会话阶段在渲染顺序上无条件压过持久态，且没有任何路径改回去。修一处，三种表现一起好。
- 第三种表现由端到端测试暴露：干净 `main` 上三条 e2e 失败，失败点全是同一句「回首页后标题页应出现」。该回归此前未被发现，因为合并时只跑了单元测试与门禁。

### 既有条款的修订

- **修订 REQ-F-026**：展示屏三态之间的可达性写成明确条款。原文只描述了三态存在与优先级，没写「能不能回去」，实现因此只做了单向。
- **修订 REQ-F-032**：展示屏控制工具增加一个（打开知识看板）。
- **`CR-20260912-display-stage` 的核心判断保留**：看板不进 `display_state`。该 CR 否决持久化的理由（跨会话粘住、与 `show_home` / `show_insight` 打架）仍然成立，本 CR 不推翻它。

### 非目标

- 不给展示屏加轮询。`CR-20260909-display-screen` CP-9 明确禁止，本 CR 沿用事件驱动。
- 不给开场加最短驻留时间。那会让明确的点击产生「点了没反应」的延迟。
- 不解决看板内容为空的问题。知识库已采纳条目为 0，看板打开后大部分区域是空的——那是本地原文档层与知识可达性的事，另立 CR。
- 不改动画本身。扫描线与呼吸网格都在，坏的是开场页的存活时间。

## 变更响应 · CR-20260912-turn-budget-continue

### 变化点影响矩阵

标准档，R1 已由用户于 2026-09-12 终裁（依据：用户选定收口路线 A）。

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 本轮累计成本上限 | REQ-NF-060 ① | 新增 | 新增 REQ-NF-060（**原写「修订 REQ-F-041 ①」，该修订已于 2026-09-12 撤回**：轮内收窄归 DEC-080，REQ-F-041 管跨轮回放） |
| CP-2 占位保留一行摘要 | 无需求条文（`narrowToolResults` 的行为细节，落 DEC-170 ②） | 小改 | 无（原写「修订 REQ-F-041 ①」，已撤回） |
| CP-3 输出截断自动续写 | REQ-NF-060 ②③ | 新增 | 新增 REQ-NF-060 |
| CP-4 轮内成本护栏 | REQ-NF-060 ①⑤ | 新增 | 新增 REQ-NF-060 |
| CP-5 单轮花费可见 | REQ-NF-060 ④ | 新增 | 新增 REQ-NF-060 |

**两条性质不同，故一修订一新增**：REQ-NF-007 ①④ 已经要求兜住总输入上限，实测超 13 倍且未报请求级错误，属**实现未达条文**，按缺陷修复处理不另立条文；REQ-F-041 ① 按「轮」定义，照条文逐字执行也不会裁，属**条文未覆盖**，故修订。REQ-F-042 ⑦「两条独立规则不合并」不受影响。

## 变更响应 · CR-20260915-board-card-lifecycle

### 变化点影响矩阵

快车道，R1 已由用户于 2026-09-15 终裁（用户「全实现，按你建议的顺序全实现」总授权，本 CR 是该顺序里 CR-B 之后的第三条，对应 INPUT-2026-09-15-029 第 2/3/4/5a 条）。

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 删除即归档 + 卡片自带删除入口 | REQ-F-070 ⑧（新增子款） | 修订 | 修订 REQ-F-070（新增 ⑧） |
| CP-2 新增改为同形 `+` 卡片 | REQ-F-070 ⑨（新增子款） | 修订 | 修订 REQ-F-070（新增 ⑨） |

**为什么三条泳道只登记一次，不逐条各登记一遍**：友商/规则与准入方/客户共用同一个 `lane`/`card` 渲染函数（`KnowledgeDashboard.tsx`），本 CR 改的是这两个共享函数本身——三条泳道因此同时获得同一套行为，不是改三遍。用户原话第 4 条「规则与准入，参考上一条」、第 5a 条「客户，参考2.3.」也是照这个「同一套行为覆盖三条泳道」的意思读的。

## 变更响应 · CR-20260915-console-menu-consolidation

### 变化点影响矩阵

标准档，R1 已由用户于 2026-09-15 终裁（用户「全实现，按你建议的顺序全实现」总授权，本 CR 是该顺序里 CR-H 之后的第一条）。

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 控制台只发布收拢态高度 | REQ-F-200 ④（修订） | 修订 | 修订 REQ-F-200 ④ |
| CP-2 菜单待采纳默认收拢 | REQ-F-241 | 新增 | 新增 REQ-F-241 |
| CP-3 流程纪律落到三份文档 | 无需求条文（纯流程文档） | 小改 | 无——`docs/CONTROLS.md`/`docs/WORKFLOW.md`/`CLAUDE.md` 不是受控说明书里的 REQ 层 |

**为什么是修订而不是新增 REQ**：REQ-F-200 ④ 的产品意图（「控制台不压住展示屏」）没有变，变的是**怎么做到**——旧实现的「怎么做」本身就是缺陷（通栏让位），条文里混着实现细节，缺陷就藏在条文里，只能改条文本身，不能另开一条新需求绕过它。

## 变更响应 · CR-20260915-document-display

### 变化点影响矩阵

快车道，R1 已由用户于 2026-09-15 终裁（用户「全实现，按你建议的顺序全实现」总授权，本 CR 是该顺序里 CR-C 之后的第四条，对应 INPUT-2026-09-15-029 第 7/8 条）。

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 `show_document` 工具 + 原件字节路由 | REQ-F-032 ⑤（新增子款） | 修订 | 修订 REQ-F-032（新增 ⑤） |
| CP-2 展示屏渲染文档原件 | REQ-F-032 ⑤（同一子款） | 修订 | 同上，不重复占号 |

**第 8 条第三句「从资料库的材料里调出内容支持回复」不产生变化点**：`search_documents`/`read_document` 早在 `CR-20260915-library-adoption` 就已把资料库并入文档根（见该 CR CP-5），采纳闸也已生效——这句话描述的能力在本 CR 开始前就已经成立，这里只用既有测试（`TEST-420`①②④⑤）复核一遍，不算新增或修订。

## 变更响应 · CR-20260915-entity-proposal-card

### 变化点影响矩阵

快车道，R1 已由用户于 2026-09-15 终裁（用户「全实现，按你建议的顺序全实现」总授权，本 CR 是该顺序里 CR-D 之后的第五条，对应 INPUT-2026-09-15-029 第 2 条后半「支持对话阅读，编辑」）。

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 对话内提议卡 | REQ-F-072 ⑤（新增子款） | 修订 | 修订 REQ-F-072（新增 ⑤） |

**「阅读」与「沟通」不产生变化点**：`read_entity` 工具在本 CR 之前就已存在，模型早就能在对话里读出一个跟踪对象的全部内容；「沟通」（围绕一张卡片的持续对话）已由 INPUT-2026-09-15-031 决策 10 裁定不做。本 CR 只补「编辑」缺的那一半——不是编辑能力本身（`propose_entity`/`propose_entity_update` 也已存在），是编辑提议出现后，用户能不能不离开对话框就处理掉它。

## 变更响应 · CR-20260915-knowledge-library-merge

### 变化点影响矩阵

标准档（CP-3 只能由真实入口操作确认），R1 已由用户于 2026-09-15 终裁（INPUT-2026-09-15-031 第 5 条「模型落。支持对话修改分类。」+ INPUT-2026-09-15-032「1. 删除当前所有笔记。2.原件；3. 全实现」）。

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 重新分类 + 归档化删除 | REQ-F-046 ⑤⑥（新增子款） | 修订 | 修订 REQ-F-046（新增 ⑤⑥） |
| CP-2 统一浏览视图 | REQ-F-242 | 新增 | 新增 REQ-F-242 |
| CP-3 归档合并前遗留笔记 | 无新增条文 | — | 一次性数据迁移，落 CR 文档与证据文档，不改变任何 REQ 的条文本身 |

**为什么 CP-3 不产生新 REQ**：REQ 描述的是系统应当具备的持续行为，CP-3 是发生过一次、不重复的数据操作，产品行为上等价于"合并之前的旧笔记不再出现在检索/浏览结果里"——这一点已经被 REQ-F-046（归档即消失于列表）与 REQ-F-242（浏览只看当前条目）覆盖，无需单独立条。

## 变更响应 · CR-20260915-library-adoption

### 变化点影响矩阵

标准档，R1 已由用户于 2026-09-15 终裁（INPUT-2026-09-15-028：入库范围「全部 253 MB 入库」、审批粒度「逐文件、界面在动态屏」两项当日答复）。

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 资料库进版本库 | REQ-F-220 ① | 新增 | 新增 REQ-F-220 |
| CP-2 采纳登记层 | REQ-F-220 ②④ | 新增 | 新增 REQ-F-220 |
| CP-3 审批界面在动态屏 | REQ-F-220 ③⑤ | 新增 | 新增 REQ-F-220（承 REQ-F-200 ① 的入口约定，不改其条文） |
| CP-4 对话查阅只见已采纳 | REQ-F-230 ①②⑤ | 新增 | 新增 REQ-F-230 |
| CP-5 采纳时写知识库索引卡 | REQ-F-230 ③ | 新增 | 新增 REQ-F-230（REQ-F-044..046 的采纳语义不变：知识库仍只收条目，卡片就是条目） |
| CP-6 网页存档纳入可读格式 | REQ-F-230 ④ | 新增 | 新增 REQ-F-230；REQ-F-110 ② 的「可读格式」清单由实现层扩展，条文未改 |

**为什么是新增而不是修订 REQ-F-110**：本地原档层的契约是「用户自己的文件，就地读，不复制不改写，配上即可读」。资料库要的恰恰是**配上也先读不到**，直到有人点头。把这条塞进 REQ-F-110 会让同一条需求同时说「全可读」和「先不可读」，读到的人只能靠猜。两层并列，各自说得清。

**非目标（如实登记）**：①采纳登记随 `.data/` 留在本机，不随版本库走——换一台机器克隆下来，资料在、审批结果不在，需重审；②资料库是否**发布**（`origin` 是公开仓库）不在本 CR 范围内，本 CR 不 push；③不做 OCR：扫描件 PDF 采纳后仍读不出文字，与 REQ-F-110 同口径。

## 变更响应 · CR-20260915-sweep-interval-edit

### 变化点影响矩阵

快车道，R1 已由用户于 2026-09-15 终裁（用户「全实现，按你建议的顺序全实现」总授权，本 CR 是该顺序里 CR-A 之后的第二条）。

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 巡检间隔编辑不丢字 | REQ-F-070 ⑦（新增子款） | 修订 | 修订 REQ-F-070（新增 ⑦） |

**为什么是修订而不是新增 REQ**：巡检间隔本就是 REQ-F-070 ⑥「采集可执行」下的既有能力（由 CR-20260911-scheduled-sweep 通过 TASK-129 落地），本 CR 修的是这项已有能力的**编辑体验缺陷**（编辑中被后台刷新覆盖、无 Enter 提交、范围提示不常驻），不是新增一项跟踪对象相关的能力，归在同一条 REQ 下补一个子款更贴合。

## 变更响应 · CR-20260915-unified-upload-entry

### 变化点影响矩阵

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 上传入口收敛为一个按钮 | REQ-F-020 ⑧（新增子款） | 修订 | 修订 REQ-F-020（新增 ⑧） |

**为什么是修订而不是新增 REQ**：控制台的技能上传能力本身（拖放/点选、文件夹/zip 两种内容）不变，本 CR 只改点选路径的**呈现方式**——两个并列常驻按钮收敛为一个入口 + 菜单，归在 REQ-F-020 下补一个子款，与①「两种投递方式」的既有条文衔接。

## 变更响应 · CR-20260918-change-history-and-sources

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 消息清单 | REQ-F-244（新增） | 新增 | 新增 REQ-F-244 |
| CP-2 add_source 工具 | REQ-F-245（新增） | 新增 | 新增 REQ-F-245 |
| CP-3 来源弹窗配置 | REQ-F-245（新增） | 新增 | 同上，不重复占号 |

**为什么 CP-2、CP-3 合并进同一条 REQ**：两者是同一件事（"来源怎么被添加"）的机制半与界面半，分开写会让两条 REQ 各自读起来都不完整——CP-2 没有 CP-3 就是一个只有对话能用、界面上摸不到的工具，CP-3 没有 CP-2 就是一个弹窗但按钮点了什么都不会发生。

## 变更响应 · CR-20260918-competitor-board

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 友商看板 | REQ-F-243（新增） | 新增 | 新增 REQ-F-243 |

**为什么是新增 REQ 而不是并进已有条目**：已跟踪对象的展示（REQ-F-070，看板三条泳道）与洞察报告展示（REQ-F-032）都描述的是"某一类内容怎么呈现"，但都不是"多个对象的同一批字段互相对齐比较"这件事——后者是一种新的呈现形态（表格式跨对象对比），没有已有条文可以自然容纳，故独立成条而不是给某个既有 REQ 加子款。

## 变更响应 · CR-20260918-industry-spec-comparison

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 行业技术指标对比 | REQ-F-250（新增） | 新增 | 新增 REQ-F-250 |

**为什么是新增 REQ 而不是并进 REQ-F-243（友商看板）**：两者形态相似（都是跨对象的维度对齐对比表），但覆盖范围不同——REQ-F-243 限定已跟踪友商，REQ-F-250 覆盖三类跟踪对象且用户已明确两者"不是同一个页面"（INPUT-2026-09-18-002），把它们并成一条会掩盖这个明确的产品边界。

## 变更响应 · CR-20260918-library-in-board

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 看板「资料库」板块 | REQ-F-242 ⑥（新增子款） | 修订 | 修订 REQ-F-242（新增 ⑥） |

item 4（对话检索资料库 + 展示屏直显原件）核实后确认已被 REQ-F-032 ⑤、REQ-F-110、REQ-F-230 三条既有 APPROVED 需求完整覆盖，不修订、不新增条目——详见 `project/06_changes/CR-20260918-library-in-board.md` 的「问题经过」。

## 变更响应 · CR-20260918-org-chart-board

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 组织架构数据模型 | REQ-F-260（新增） | 新增 | 新增 REQ-F-260 |
| CP-2 组织架构看板 | REQ-F-260（新增） | 新增 | 同上，不重复占号 |

**为什么是新增 REQ 而不是并进已有条目**：`params`（REQ-F-072）描述的是"一个对象自己的技术规格"，人员/组织架构是"这个对象内部由谁构成"——两者都挂在同一个跟踪对象上，但回答的是不同的问题，没有已有条文能自然容纳后者，故独立成条。

## 变更响应 · CR-20260918-per-user-data-isolation

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 每用户文件根解析与迁移模块 | REQ-F-270（新增）、REQ-F-271（新增） | 新增 | 新增 REQ-F-270、REQ-F-271 |
| CP-2 全部真实调用点接线 | REQ-F-270、REQ-F-271 | 派生 | 不重复占号，落实 REQ-F-270/271 的具体执行面 |

**为什么是两条新 REQ 而不是一条**：私有隔离（REQ-F-270，"数据存在哪、归谁"）与自动迁移（REQ-F-271，
"现有数据什么时候、怎么过户"）是两个可以独立成立的产品承诺——一个部署可以只要隔离、手动搬迁移数据
（走「问题经过」否决的方案 D），也可以两者都要（本 CR 的实际选择）。分开登记让未来任何一条只想改动
其中一半的 CR 有干净的挂靠点，不必牵动另一半的验收条件。

## 变更响应 · CR-20260921-chat-skill-register

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 对话内生成并注册技能（先预览、用户确认后注册） | REQ-F-300（新增） | 新增 | 新增 REQ-F-300 |

**为什么是新增 REQ 而不是修订 REQ-F-020**：REQ-F-020 约定的是"投递文件夹/zip → 生成 SKILL.md → 注册"这条入口
的行为（含白名单、回执、拖放细节）；本条是另一条入口（对话），共享的只是最后一步"注册"。单独占号让两条入口各自
的验收互不牵动。**为什么 SHOULD**：没有它技能仍能经上传入口注册，它是便利性增强，不是功能前提。

## 变更响应 · CR-20260921-format-skill

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 文档展示可选由技能经模型重新排版 | REQ-F-290（新增） | 新增 | 新增 REQ-F-290 |

**为什么是 SHOULD 而不是 MUST**：它是 REQ-F-280（MUST）之上的可选增强——不指定技能时系统完全按
REQ-F-280 工作。把它定为 MUST 会把"必须配置模型才能看文档"这一新前提悄悄塞进产品，而用户明确要求
"选不使用即刻停用"。**为什么单独占号而不改写 REQ-F-280**：两条的可逆性与成本模型不同（REQ-F-280 零模型
调用、REQ-F-290 每份新文件一次模型调用并接受理解偏差风险），分开登记让未来任何一条的调整不牵动另一条。

## 变更响应 · CR-20260921-markitdown-display

| 变化点 | 本层落点 | 分类 | 派生/修订 |
|---|---|---|---|
| CP-1 文档展示改用结构化转换 | REQ-F-280（新增） | 新增 | 新增 REQ-F-280 |

**为什么是新增 REQ 而不是修订 REQ-F-110/220/230**：这三条既有需求约定的是"本地文档/资料库这两类
内容存在、可被检索阅读"这件事本身，没有约定"以什么格式把字节交给浏览器"这个展示层细节——新增
REQ-F-280 单独登记这一层，避免为了一次展示方式的调整去改写三条内容层的既有需求文字。

## 批准状态

<!-- 2026-09-14 补记：本节的登记自 2026-09-10 起断了三天，以下各行由文档自身的表格推导补齐（某条 CR 在本文件里新增了哪些编号，就去表里找哪一行写着它）。`check-approval-log` 从此守住这件事。 -->
- 用户确认：2026-09-15，CR-20260915-unified-upload-entry（L2）R1：用户在需求清单第 6 条指出"上传文件夹，上传zip，还没合并在一起"（INPUT-2026-09-15-029），经与用户确认属 UI 层面的入口合并（两个并列按钮 → 一个入口 + 菜单，非内容分类改动）；该总授权即为本 CR 的推进授权。修订 **REQ-F-020**（新增 ⑧）。逐 CP 方案见「变更响应 · CR-20260915-unified-upload-entry」。
- 用户确认：2026-09-15，CR-20260915-entity-proposal-card（L2）R1：用户在需求清单第 2 条要求友商卡片「支持对话阅读，编辑」（阅读已由既有 `read_entity` 满足；「沟通」按 INPUT-031 决策 10 不做）；该总授权即为本 CR 的推进授权。修订 **REQ-F-072**（新增 ⑤）。逐 CP 方案见「变更响应 · CR-20260915-entity-proposal-card」。
- 用户确认：2026-09-15，CR-20260915-document-display（L2）R1：用户在需求清单第 7/8 条要求「指令打开对应的动态展示屏」「对话支持显示某篇资料库的资料，也支持展示洞察的报告」；该总授权即为本 CR 的推进授权（第 8 条「展示洞察的报告」已由既有 `show_insight` 满足，「从资料库调出内容支持回复」已由 `CR-20260915-library-adoption` 满足，均不在本 CR 变化点内）。修订 **REQ-F-032**（新增 ⑤）。逐 CP 方案见「变更响应 · CR-20260915-document-display」。
- 用户确认：2026-09-15，CR-20260915-board-card-lifecycle（L2）R1：用户在需求清单第 2/3/4/5a 条要求友商/规则与准入方/客户三条泳道的卡片可删除、支持对话阅读与编辑（「沟通」按 INPUT-031 决策 10 不做）、新增改用 `+` 卡片而非行内表单；该总授权即为本 CR 的推进授权。修订 **REQ-F-070**（新增 ⑧⑨）。逐 CP 方案见「变更响应 · CR-20260915-board-card-lifecycle」。
- 用户确认：2026-09-15，CR-20260915-sweep-interval-edit（L2）R1：助手在「全实现，按你建议的顺序全实现」总授权期间审查巡检间隔输入框时自行发现编辑竞态缺陷（编辑中若后台刷新落地，尚未提交的输入会被静默覆盖），该总授权即为本 CR 的推进授权。修订 **REQ-F-070**（新增 ⑦）。逐 CP 方案见「变更响应 · CR-20260915-sweep-interval-edit」。
- 用户确认：2026-09-15，CR-20260915-console-menu-consolidation（L2）R1：用户反馈 9「菜单里不用显示待采纳内容」、10「对话框展开的时候不用全屏往上，仅对话框往上伸缩即可」（随后澄清「不是重排，是没有对话框的部分内容也被遮挡了」）。修订 **REQ-F-200 ④**，新增 **REQ-F-241**。逐 CP 方案见「变更响应 · CR-20260915-console-menu-consolidation」。
- 用户确认：2026-09-15，CR-20260915-library-adoption（L2）R1：用户原话「把资料库目录加入版本，并与知识库链接起来。里面的内容改为待采纳，审批通过后，可正式纳入，并支持对话查阅」（INPUT-2026-09-15-028），并当日答复两项待裁定——入库范围选「全部 253 MB 入库」（知悉「进历史不可逆、每次克隆都要拉一遍」后仍如此选）、审批粒度选「逐文件审批，界面在动态屏」。新增 **REQ-F-220**、**REQ-F-230**。逐 CP 方案见「变更响应 · CR-20260915-library-adoption」。
- 用户确认：2026-09-15，CR-20260915-knowledge-library-merge（L2）R1：用户就"资料库并入知识库后"三项待裁定明确答复"1. 删除当前所有笔记。2.原件；3. 全实现"（INPUT-2026-09-15-032），并在 INPUT-2026-09-15-031 第 5 条裁定"模型落。支持对话修改分类。"；该总授权即为本 CR 的推进授权。修订 **REQ-F-046**（新增 ⑤⑥），新增 **REQ-F-242**。逐 CP 方案见「变更响应 · CR-20260915-knowledge-library-merge」。
- CR-20260908-floating-chat-functional-fixes（R1）：无新增编号（只改既有条目）。逐 CP 方案见「变更响应 · CR-20260908-floating-chat-functional-fixes」。
- CR-20260912-ingest-extract-chain（R1）：**REQ-F-180**（提议）。逐 CP 方案见「变更响应 · CR-20260912-ingest-extract-chain」。
- CR-20260912-knowledge-attribution（R1）：**REQ-F-170**（拒绝写入）、**REQ-F-171**（用对象的中文名可命中该对象下正文为外文的条目）。逐 CP 方案见「变更响应 · CR-20260912-knowledge-attribution」。
- CR-20260912-proposal-id-collision（R1）：无新增编号（只改既有条目）。逐 CP 方案见「变更响应 · CR-20260912-proposal-id-collision」。
- CR-20260912-turn-budget-continue（R1）：**REQ-F-041**（CR-20260912-turn-budget-continue 曾提出修订 ①，2026-09-12 撤回）、**REQ-F-051**（不加 Provider 列）、**REQ-NF-060**（单轮累计）。逐 CP 方案见「变更响应 · CR-20260912-turn-budget-continue」。

- 当前状态：APPROVED
- 用户确认：2026-09-12，CR-20260912-skill-paging（L2）R1：用户对上一条 CR 记录的两项遗留回「继续修改」。新增 REQ-F-150，修订 REQ-F-030 ②。**2 项按助手建议采纳、待追认**：清单不做相关性排序；横向冲突本期只做纵向缓解。
- 用户确认：2026-09-12，CR-20260912-skill-report-bridge（L2）R1：用户听完四条处置后回「全做」。新增 REQ-F-140，修订 REQ-F-050 ①。**2 项按助手建议采纳、待追认**：不改用户技能文件而在 read_skill 结果后附约定；面板与报告列仍共用中轴，用「落地即让开」绕过默认遮挡。
- 用户确认：2026-09-12，CR-20260912-context-degrade（L2）R1：用户贴出实际收到的上下文溢出错误并问怎么解决，即为修复授权。新增 REQ-F-130。**2 项按助手建议采纳、待追认**：不改份额声明值；削短按条等分而非按相关性。
- 用户确认：2026-09-12，CR-20260912-runtime-visibility（L2）R1：用户在三条建议中明确选了「2 和 3」（服务自愈、报错说人话），并追加「Jarvis 还没办法读模型」。新增 REQ-F-120。**2 项按助手建议采纳、待追认**：保留 `start:local` 另加 `serve:local`；看护阈值取 20 秒 / 3 次。
- 用户确认：2026-09-12，CR-20260912-local-documents（L3）R1：用户「好的，先做本地原档层」为直接授权。新增 REQ-F-110、REQ-NF-050。**3 项按助手建议采纳、待追认**：可读格式限定、不做文件选择器、索引不持久化。
- 用户确认：2026-09-12，CR-20260912-sandbox-and-budget（L2）R1：用户读完全项目审视报告后回复「都可以继续处理」，构成对发现 01/02/05 的推进授权。新增 REQ-F-101，修订 REQ-F-025 ②。**1 项按助手建议采纳、待追认**：沙箱档位取 `allow-scripts` 而非空 `sandbox=""`（可单独撤回，理由见该 CR「方案选项」）。
- 用户确认：2026-09-12，CR-20260912-stage-reach（L2）R1：用户两条运行反馈（对话进不了知识看板、首页没有动画）即为对现状的否决。新增 REQ-F-102，修订 REQ-F-026、REQ-F-032。**2 项按助手建议采纳、待追认**：开场只收窄触发条件不加最短驻留；`show_board` 执行时清回首页持久态。
- 用户确认：2026-09-11，CR-20260912-latency-and-report-layout（L2）R1：报告列宽 0.68 与居中为用户直接指定；延迟部分用户要求排查，助手实测后只实施唯一有收益的一项（保留窗口补正）。新增 REQ-F-090、REQ-F-091。**本 CR 为事后补记**，详见变更响应节。
- 用户确认：2026-09-12，CR-20260912-local-production（L2）R1：助手就四次 dev server 内存事故提出「改用生产构建」并说明两个前提（需重新构建、免登录旁路在生产不生效），用户回复「要。继续」。新增 REQ-F-080、REQ-NF-040。
- 用户确认：2026-09-11，CR-20260911-web-reading（L3）R1：用户报告「无 search 工具 / 403 反爬 / 无 PDF 阅读」三项缺失并要求「看看怎么修复」；在收到实测分析与两项待决（启动 Docker、Playwright 提为运行依赖）后回复「都做吧」。新增 REQ-F-055..058、REQ-NF-014。**如实记录**：用户点名的三个站点在本 CR 之后仍读不到，原因与替代路径见变更响应节「非目标」。
- **待确认**：2026-09-11，CR-20260911-display-console-ux（L2）R1：用户就四个运行反馈要求「需求分析与描述」（INPUT-2026-09-11-010），其中「步数上限扩到 100」已直接授权并实施；REQ-F-050..054、REQ-F-029 ①② 修改、REQ-F-019 实现约束修订：用户随后回复「继续」，按项目对总授权的既有处理（INPUT-2026-09-11-009 先例）记为**总授权推进、5 项待终裁按助手建议采纳、待追认**（追加用参数、本期不改 Markdown、搜索设置入口化、悬停初值可调、收缩后不留预览）。REQ 编号因与 C 期撞号由 044..048 重编为 050..054。
- 用户确认：2026-09-08，用户要求按该计划更新说明书并严格按流程执行。
- 用户确认：2026-09-09，用户逐轮确认 CR-20260909-minimal-floating-chat 的需求变化点（单管理员形态、浮窗收敛、Provider 优先级、状态灯四态、会话生命周期、浅色简约视觉），并要求落地后查看各流程变化点与角色评审意见。
- 用户确认：2026-09-09，用户提出并逐轮确认 CR-20260909-collapsible-panel（对话面板一级折叠、折叠时安静、偏好记住、发送即展开、状态栏箭头控件、折叠期间回复完成状态灯瞬时提示【甲案】、v1 无动画），并要求经角色评审后修正说明书。
- 用户确认：2026-09-09，用户提出并逐轮确认 CR-20260909-corner-menu（左下角 ☰ 悬浮菜单收纳「模型」「账号登录」、主题开关内联进菜单、「配置」更名「模型」、Next dev 指示器挪 `bottom-right`、浮层机制留 P3、桌面与移动同一套），经角色评审后要求按流程执行开发。
- 用户确认：2026-09-09，用户提出技能系统并逐轮（msg 49→54）拍板 CR-20260909-skills 的 R1 需求（拖文件夹到对话框注册技能、按发送独立路由、仅当轮注入、HTML 产出捕获入 `insights`、无沙箱隔离且接受风险、不回显 SKILL.md 全文、拆 F1 技能 / F2 展示屏两个 CR）。本 CR 为首个按 R1–R4 共识门禁模型执行的变更；R1 四角色评审 + 人工终裁完成，R2/R3/R4 机器门通过，P3 实现待用户授权。
- 用户确认：2026-09-10，CR-20260910-skill-intake（L2）R1 需求：技能投递支持**文件夹与 zip**两种形态、拖放与点选两条路径（决策「2. c」）；zip 用**零依赖自写解析**（决策「3. 自写」）；扩展名白名单**放宽**并要求被排除文件在回执列出（决策「4. 顺便放宽」）；缺陷修复与 zip 扩展**一起做**（决策「1. 一起做」）。新增 REQ-F-028（技能可见性）、REQ-NF-005（不可信归档处理边界）。四角色 R1 评审已闭环至全 APPROVED，待人工终裁。
- 用户确认：2026-09-09，CR-20260909-display-screen（F2，L3）R1 需求：首页重写为全屏动态展示屏（REQ-F-015）、`display_state` 全局单行 + `kind` 扩展点（REQ-F-026）、技能洞察自动上屏 + 跨刷新持久、「显示首页」对话指令回标题视图（REQ-F-027）、未沙箱化 `<iframe srcdoc>` + 不可关闭提示条落地 REQ-F-025 ②。R1 四角色评审完成（产品/测试 APPROVED，架构/模块 CONDITIONAL），待用户人工终裁。
- 用户确认：2026-09-10，CR-20260910-agent-tooling（L3 重型）R1 需求：用户逐轮拍板 16 项决策（INPUT-2026-09-10-008；EV-2026-09-10-agent-tooling-requirements §3）——读 DeepSeek Harness 抄结构自行实现（方案 A / 读法二）、删独立路由、技能工具化（一轮可读多个）、SearXNG 无 key 搜索、只读工具免审批、token 可见（拿不到 usage 时本地估算）、步骤流 + 状态灯五态、10 步触顶提醒后停止、`read_url`、刷新重建步骤流、面板分情形放宽、来源引用、Provider 不支持工具时提醒降级、`web_search` 提进 A 期。新增 REQ-F-029..041、REQ-NF-007..011；重写 12 条；作废 F-021/022/027。**R1 四角色评审完成（产品 / 架构 / 模块开发 / 测试全部 CONDITIONAL，零 REJECTED），派生 CP-26..CP-43；人工终裁 7 项已拍板**——保留 F-023「不静默」告知、`show_insight` 可指任意历史洞察并改写两条展示屏非目标、**明示接受 `read_url` 带来的无沙箱 iframe 信任面扩大**（登记 `skill-html-unsandboxed-web-source`，沙箱化仍为强制出口义务）、「联网」默认开、面板 75% 分情形、`adapters.ts` 本期只留接口位不重构、迁移框架纳入本期。R1 PASS，待 P2。
- 用户确认：2026-09-18，CR-20260918-library-in-board（L2）R1：用户在 INPUT-2026-09-18-001 第 3、4 条要求知识看板「知识库」栏目改名「资料库」并直接展示可浏览的原始资料（分页、类型统计），以及确认对话检索与展示屏直显资料库内容的能力现状；随 Wave 1 四条 CR 一并经用户"按流程开始第一波的 CR"总授权推进。修订 **REQ-F-242**（新增 ⑥）。逐 CP 方案见「变更响应 · CR-20260918-library-in-board」。
- 用户确认：2026-09-18，CR-20260918-change-history-and-sources（L2）R1：用户在 INPUT-2026-09-18-001 第 1、2、6b 条要求对象卡片显示消息清单、采集源改为弹窗配置且支持自动发现，范围经 INPUT-2026-09-18-002 排除第 1 条括号内的行业指标对比页（留给独立 CR）；该确认即为本 CR 的推进授权。新增 **REQ-F-244**、**REQ-F-245**。逐 CP 方案见「变更响应 · CR-20260918-change-history-and-sources」。
- 用户确认：2026-09-18，CR-20260918-competitor-board（L2）R1：用户在 INPUT-2026-09-18-001 第 7 条要求"新增一页友商看板，支持将友商的产品参数，规格，放在同一个表格上进行对比分析，并每个维度给出评价总结。同样支持对话控制"，并在 INPUT-2026-09-18-002 中确认该页与第 1 条的行业硬核指标对比页不是同一个；协调会话汇总本 CR 方案后与另外两条并行 Wave 2/3 CR 一并提请确认，用户选择「Approve all three as-is」。新增 **REQ-F-243**。逐 CP 方案见「变更响应 · CR-20260918-competitor-board」。
- 用户确认：2026-09-18，CR-20260918-industry-spec-comparison（L2）R1：用户在 INPUT-2026-09-18-001 第 1 条要求"友商/准入/客户卡片的技术指标移出，另立一页做行业技术指标对比"，并在 INPUT-2026-09-18-002 中确认该页与第 7 条"友商看板"不是同一个、包含客户；协调会话汇总本 CR 方案（含范围推断纳入规则与准入方、`Param.status` 是否入表的开放问题）后与另外两条并行 Wave 2/3 CR 一并提请确认，用户选择「Approve all three as-is」。新增 **REQ-F-250**。逐 CP 方案见「变更响应 · CR-20260918-industry-spec-comparison」。
- 用户确认：2026-09-18，CR-20260918-org-chart-board（L2，快车道）R1：用户在 INPUT-2026-09-18-001 第 8 条要求"新增公司组织架构和研发阵型等看板，支持对话查看一个公司的组织架构及研发阵型，具体到人名，岗位，简介。对话可以创建对一个公司的洞察，然后固化显示在动态屏。可以先以维谛为例"，并在 INPUT-2026-09-18-002 第④点确认为"动态跟踪，但是也有组织结构图，人名，岗位，简介，头像等"——结构化、可持续更新的数据；协调会话汇总本 CR 方案（人员挂在公司实体上、复用既有证据闸、头像外链不本地托管）后与另外两条并行 Wave 2/3 CR 一并提请确认，用户选择「Approve all three as-is」。新增 **REQ-F-260**。逐 CP 方案见「变更响应 · CR-20260918-org-chart-board」。
- 用户确认：2026-09-18，CR-20260918-per-user-data-isolation（重型档）R1：用户在 INPUT-2026-09-18-001 第 9a 条提出 Railway/多用户部署，协调会话调查代码现状（`entities.ts`/`knowledge.ts` 完全没有 `userId` 概念，一旦启用真实多用户登录即等同于所有用户共享同一份数据）后，用户通过 AskUserQuestion 分两步明确裁定：①每用户数据私有隔离，不共享池；②现有单管理员真实数据首次真实登录时自动继承，不走人工复核脚本。新增 **REQ-F-270**、**REQ-F-271**。逐 CP 方案见「变更响应 · CR-20260918-per-user-data-isolation」。
- 用户确认：2026-09-21，CR-20260921-markitdown-display（L2）R1：用户在排查 Railway 生产部署上资料库文档打不开（Microsoft Edge SmartScreen 按文件类型单独拦截 PDF，已用本机 localhost 复现、排除"新域名信誉"这个此前的误判）过程中提出正式需求（INPUT-2026-09-21-001），协调会话汇总方案（`/api/documents/raw` 对 PDF/DOCX 改用 markitdown 转换后的 HTML 渲染显示，原始字节仍可下载）后提请确认，用户选择「同意，按方案推进」。新增 **REQ-F-280**。逐 CP 方案见「变更响应 · CR-20260921-markitdown-display」。
- 用户确认：2026-09-21，CR-20260921-format-skill（L2）R1：用户在本机验证 CR-20260921-markitdown-display 的转换效果后反馈"可阅读性差"（排查根因：markitdown 对该真实 PDF 零标题识别，50 余页拍平成无层级段落），否决了不引入模型的启发式标题识别方案，要求改用模型重新排版并复用现有 SKILL.md 上传/注册机制（INPUT-2026-09-21-002）；协调会话就"推翻此前'只整理排版不改内容'裁定并接受模型调用成本与风险"、"是否字面复用现有技能机制"、"是否按文件内容缓存"三点经 AskUserQuestion 确认，并汇总方案（复用技能的注册与 SKILL.md 内容、不复用按对话轮路由；分块调用；缩水兜底；哈希缓存）提请终裁，用户选择「同意，按方案推进」。新增 **REQ-F-290**。逐 CP 方案见「变更响应 · CR-20260921-format-skill」。
- 用户确认：2026-09-22，CR-20260921-chat-skill-register（L2）R1：CR-20260921-format-skill 真实入口验证时发现用户没有可选的排版技能，用户提出"帮我在对话里具备生成技能的功能，并能支持注册"（INPUT-2026-09-21-003）；协调会话就先预览再注册/只 SKILL.md/是否含改删三点经 AskUserQuestion 确认，汇总方案（新增写工具 `register_skill`，`confirmed` 参数承载用户确认，注册完整复用 `registerSkill()`）后提请终裁，用户选择「同意，按方案推进」。新增 **REQ-F-300**。逐 CP 方案见「变更响应 · CR-20260921-chat-skill-register」。
