# 产品需求说明书

责任人：产品 owner

## 产品目标

交付 Agent-Jarvis 的第一个产品功能：一个本机运行的底部悬浮大模型对话 UI。当前版本**以单一本地管理员身份运行**，用户在底部悬浮输入条发起文本对话，查看流式回复，随时停止，开启新对话，并保存最近会话。可用模型 Provider 的**启用与使用优先级在「配置」中管理**，对话浮窗不做 Provider/模型选择。

## 用户范围

- 主要用户：本机使用 Agent-Jarvis 的**单一本地管理员**（当前版本不区分多用户，无账号登录）。
- 管理职责：该管理员在「配置」中启用模型 Provider、设定使用优先级、维护凭据。

## 功能需求

| ID | 名称 | 优先级 | 描述 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| REQ-F-001 | Google 登录 | MUST | 用户必须通过 Google 登录进入 Agent-Jarvis 受保护页面。 | 未登录访问主页、设置页或对话 API 时被阻断；登录后可访问主页、设置页和对话入口。 | DEFERRED（暂缓，CR-20260909-minimal-floating-chat）：当前版本以单一本地管理员身份运行，「账号登录」为占位入口，Google 登录待后续 CR 恢复。 |
| REQ-F-002 | 底部悬浮输入条 | MUST | 页面底部必须常驻紧凑悬浮输入条，作为大模型对话入口。 | 默认页面加载后底部输入条可见，可输入文本并触发发送；悬浮条仅含**状态灯、输入框、单个动作按钮**三个元素；输入框始终可输入，不依赖 Provider 状态。 | APPROVED |
| REQ-F-003 | 展开式对话面板 | MUST | 发送消息后，悬浮输入条展开为底部对话面板。 | 发送消息**触发**面板展开；用户可随后经 REQ-F-019 手动收起，再次发送重新展开。面板展开时展示用户消息与模型回复区域；对话状态**仅由状态灯表达，无文字**；面板高度上限**分情形**：纯对话为视口 50%；本轮记录区含步骤流（REQ-F-035）时为 75%（用户 2026-09-10 终裁 5 确认「75% + 分情形」）。 | APPROVED（CR-20260909-collapsible-panel 将「发送后必须展开」放宽为「触发展开、可手动收起」，2026-09-09 用户确认）；**REVIEWING（CR-20260910-agent-tooling 重写高度上限，覆盖 65%→50% 两次澄清；R1 待终裁）** |
| REQ-F-004 | 文本流式回复 | MUST | 对话必须支持模型文本流式输出。 | 调用对话接口时前端逐段显示模型回复，而不是只在完成后一次性显示；会话活动中每轮请求由服务端按 REQ-NF-007 / REQ-F-041 规则组装上下文：**稳定前缀 + 易变后缀 + 会话消息（含工具往返，受保留窗口约束）**。 | APPROVED；**REVIEWING（CR-20260910-agent-tooling 重写：不再是「已有全部消息 + 一段固定 system prompt」；R1 待终裁）** |
| REQ-F-005 | 停止生成 | MUST | 用户必须能中断正在生成的回复。 | 生成中显示「停止」；点击后**服务端停止请求模型、当前会话即结束**；被中断的回复保留在记录区并带**行内「已停止」标记**；落库 `status=stopped`；「停止」在工具循环任一阶段（生成中 / 工具执行中）立即生效，已执行工具调用与已生成文本一并落库（REQ-F-029 ⑤）。 | APPROVED；**REVIEWING（CR-20260910-agent-tooling 补循环中途停止语义；R1 待终裁）** |
| REQ-F-006 | Provider 启用与优先级 | MUST | 用户必须能在「配置」中启用/停用多个模型 Provider，并明确设定它们的使用优先级。对话浮窗不提供 Provider 或模型的选择/切换控件。 | ①「配置」将已启用 Provider 呈现为**有序列表**，用户可通过「上移/下移」调整顺序，列表位置即优先级（第 1 位最高）；②新启用的 Provider 追加到列表末尾；③仅启用一个 Provider 时排序控件隐藏或禁用；④对话请求**不携带** Provider/模型参数，服务端在「已启用且连接有效」的 Provider 中取优先级最高者解析实际使用的 Provider/模型；⑤最高优先级 Provider 连接无效时自动落到下一个连接有效的；⑥无任何连接有效的 Provider 时状态灯灭、发送被拦截并在输入框上方提示；⑦解析到最高优先级 Provider 后按 REQ-F-040 判定工具可用性，**优先级不因工具能力跳过**。 | APPROVED（重写，CR-20260909-minimal-floating-chat；P3 已实现，TEST-025/026）；**REVIEWING（CR-20260910-agent-tooling 补 ⑦；R1 待终裁）** |
| REQ-F-007 | 独立模型设置页 | MUST | 系统必须提供独立设置页管理 OpenAI、DeepSeek、本地模型 Provider。 | `/settings/models` 可新增、编辑、启用、停用、测试 Provider，并提供已启用 Provider 的**优先级有序管理**（上移/下移）。 | APPROVED |
| REQ-F-008 | Provider 认证模式 | MUST | Provider 必须区分 OAuth、API Key、Local、Unsupported 四种认证模式。 | 设置页能显示每个 Provider 的认证模式；OAuth 仅用于官方支持的 Provider，OpenAI/DeepSeek 首版使用 API Key 兜底。 | APPROVED |
| REQ-F-009 | 凭据服务端加密 | MUST | API Key 等敏感凭据必须由服务端加密保存，浏览器不得获得明文。 | 数据库存储值不是明文；Provider 查询接口只返回脱敏状态。 | APPROVED |
| REQ-F-010 | OpenAI 接入 | MUST | 系统必须接入 OpenAI Provider。 | 配置 OpenAI API Key 和模型后，可通过统一对话接口获得流式文本回复。 | APPROVED |
| REQ-F-011 | DeepSeek 接入 | MUST | 系统必须接入 DeepSeek Provider。 | 配置 DeepSeek API Key 和模型后，可通过 OpenAI 兼容接口获得流式文本回复。 | APPROVED |
| REQ-F-012 | 本地模型接入 | MUST | 系统必须支持本地 OpenAI-compatible 模型服务。 | 配置本地 base URL 和模型名后，可通过统一对话接口获得流式文本回复。 | APPROVED |
| REQ-F-013 | 最近会话保存 | MUST | 系统必须按当前管理员身份保存最近会话和消息。 | 刷新页面后，最近会话及其消息可恢复；恢复触发条件收窄——**仅当最近会话仍处于活动状态**（用户既未「停止」也未「新对话」）时刷新才恢复它，否则呈现空会话；消息记录含工具调用与结果（数据形状由架构定），恢复**包括步骤流**（REQ-F-035 ④）；`GET /api/conversations/[id]/messages` 返回含工具记录且向后兼容，已有会话行不动。 | APPROVED；**REVIEWING（CR-20260910-agent-tooling 补工具记录持久化与重建；R1 待终裁）** |
| REQ-F-014 | 克制的浅色简约视觉 | SHOULD | 对话 UI 应采用克制、专业的浅色简约风格（视觉方向参考 `gens.team/ai-insights`：留白充足、单一强调色、统一圆角、卡片式分组），默认浅色并可切换深色，优先可读性与呼吸感而非装饰或信息密度。 | 浅色为默认且可切换深色并持久化；**单一强调色（蓝色系）**用于主按钮与交互焦点，其余为中性灰阶；交互元素（按钮、输入框、卡片）使用统一圆角，分组靠细线或留白而非重边框；布局以充足留白构建层级，非紧凑控制台密度；无阴影或仅极轻阴影；文本在桌面和移动宽度下不溢出；正文文本、控件边界、**状态灯五态**（REQ-F-036）在两套主题下均达 WCAG 2.2 AA（正文 4.5:1、非文本 3:1）；**收起/展开控件（REQ-F-019）与左下角 ☰ 菜单触发器（REQ-F-015 / CR-20260909-corner-menu）为真实 `<button>`，焦点可见、与背景非文本对比 ≥ 3:1、触控目标 ≥ 24px；☰ 触发器 `aria-haspopup` + `aria-expanded`；图标形态入 `docs/UI_STANDARD.md`**；步骤流（步骤行、展开控件，REQ-F-035）纳入同一视觉基础与可访问性要求。 | APPROVED；**REVIEWING（CR-20260910-agent-tooling：四态→五态、步骤流纳入；R1 待终裁）** |
| REQ-F-015 | 全屏动态展示屏首页 | SHOULD | 当前版本首页是一块**全屏动态展示屏**，其上浮着底部悬浮对话与左下角 ☰ 悬浮菜单。展示屏默认呈现**标题视图**（产品名 + 一句话简介）；对话触发的内容（首个：技能洞察 HTML 报告，REQ-F-026）在展示屏上呈现，可由对话指令切换回标题视图（REQ-F-027）。 | 首页渲染一块全屏 `DisplayScreen`（默认标题视图，含产品名——取代原 header `<h1>`，header 不再单独存在）；底部悬浮对话（REQ-F-002/003）与左下角 ☰ 触发器 + 浮层菜单（内联浅色/深色 + 「模型」+ 「账号登录」，Esc/light-dismiss/焦点回归行为不变）层叠**在展示屏之上**（层叠刻度：DisplayScreen 基底 / 底部对话 20 / ☰ 30）；「模型」「账号登录」弹窗行为不变；当前版本「账号登录」为**占位入口**（对应 REQ-F-001 暂缓）。 | APPROVED（CR-20260909-corner-menu 将 header 两按钮改为 ☰ 菜单；**CR-20260909-display-screen 将「极简空首页」重写为「全屏动态展示屏」，2026-09-09 用户确认**） |
| REQ-F-016 | 回复 Markdown 渲染 | SHOULD | 助手回复应按 Markdown 渲染，使代码、列表和强调可读。 | 代码块、行内代码、粗体、斜体、有序/无序列表、标题按语义元素渲染；仅 http/https/mailto 链接可点击；模型输出中的原始 HTML 不被执行；流式中未闭合代码围栏不破版；**请求级错误**（回复第一段文本到达前失败）在**输入框上方一行红字**展示，内容脱敏（不含 API Key、不含 Provider 内部细节），不写入对话上下文，不结束会话，不阻塞继续输入。 | APPROVED |
| REQ-F-017 | 开始新对话 | MUST | 用户必须能主动开启一个不继承既有上下文的新会话。 | 「非生成中且输入框为空」时动作按钮显示为「新对话」（无历史消息时也是）；点击后清空浮窗记录区、丢弃当前 `conversationId`；在下一条消息发送前不产生新的会话记录；点击「新对话」后刷新页面呈现空会话，不自动恢复上一会话。 | APPROVED |
| REQ-F-018 | 连接状态指示灯 | MUST | 悬浮条以单个状态灯表达当前可对话性与生成状态，不使用文字。 | 首页加载时对全部已启用 Provider 执行**一次连接探测**，状态灯反映探测结果；灯有五种可辨识状态——**检测中**（加载探测进行中）、**灭**（无已启用 Provider 或全部探测失败，不可对话）、**常亮**（至少一个 Provider 连接有效，可对话）、**闪烁 / 生成文本中**（正在生成回复）、**执行工具中**（REQ-F-036，第五态，与「生成文本中」可辨识区分）；五态在两套主题下互相可区分且与背景对比达 WCAG 2.2 AA 非文本对比 3:1。**（CR-20260909-collapsible-panel 补充：折叠状态下回复完成时状态灯做一次短促瞬时提示后落定，属四态的显示表现，不新增可辨识状态。）**（该补充由 CR-20260910-agent-tooling 作废：用户 2026-09-10 拍板破四态。） | APPROVED；**REVIEWING（CR-20260910-agent-tooling 四态→五态；R1 待终裁）** |
| REQ-F-019 | 收起对话面板 | SHOULD | 对话面板展开后，用户可主动将其收起为输入条形态，会话状态不变；可再次展开，记录区原样恢复。收起/展开偏好按浏览器记住。 | ①存在对话记录时，状态灯所在行显示收起/展开控件（展开态朝下、收起态朝上），无记录时不显示；②点击收起：记录区从视图与 DOM 移除，面板回到「状态灯 + 输入框 + 单按钮」形态，`conversationId` 不变、`jarvis:chat-session-ended` 不置位、消息不清空；③点击展开：记录区恢复且滚动到底部，消息与折叠前一致；④折叠状态下收到流式回复或 `tool_call` / `tool_result` 事件保持折叠，状态灯转「生成文本中」或「执行工具中」（REQ-F-036），回复完成、触顶或失败时状态灯做一次短促瞬时提示后落定，全程不自动展开、不显示未读计数；⑤折叠状态下发送新消息自动展开；⑥收起/展开切换与发送即时写入 `localStorage`（每浏览器），刷新时存在可恢复会话则按存储偏好渲染（未设过默认展开），折叠偏好为真但无记录时按①不显示控件、呈现输入条形态，不产生「折叠了却空白」的中间态；⑦收起/展开为纯 UI 状态，不改 `conversationId`、不结束会话、与「新对话」正交。<br>**实现约束**：`expanded` 拆为 `hasTranscript`（派生自消息数）与 `userCollapsed`（持久 boolean），`showTranscript = hasTranscript && !userCollapsed`；瞬时切换无高度动画。 | APPROVED；**REVIEWING（CR-20260910-agent-tooling 重写 ④ 折叠态长任务；R1 待终裁）** |
| REQ-F-020 | 技能上传与注册 | SHOULD | 用户可把一个技能**文件夹或 zip 压缩包**交给底部对话框（拖放或点选上传）；Jarvis 读取其内容，调用大模型生成一份 `SKILL.md`，将内容原样存入 `.data/skills/<name>/` 并注册，补充为可被对话调用的系统能力。 | ①**底部对话框**是唯一入口（不是左下角 ☰ 菜单），支持两种投递方式——**拖放**与**点选上传**（真实 `<button>` 触发），每种方式都接受**文件夹**与 **zip 压缩包**；②系统追加一条「生成 SKILL.md」指令，调用当前优先 Provider 产出 `SKILL.md`（frontmatter `name`/`description` + 正文 `instructions`）写入该技能目录；③注册成功后对话内出现「已注册技能：&lt;name&gt; — &lt;description&gt;」确认，**不回显 `SKILL.md` 全文**；④生成结果解析不出 frontmatter 时，`name` 回退为文件夹名、`description` 为「（未生成描述）」，并在对话内提示用户可补充；⑤扩展名不在白名单（`.md .mdx .txt .json .jsonl .csv .yaml .yml .toml .xml .py .js .ts .tsx .html .sh`）的文件、二进制文件、超大文件不纳入技能内容，**且必须在注册回执中列出被排除的文件与原因**，不得静默丢弃；⑥当前只做单一技能，注册机制对多技能开放；⑦**投递失败必须可见**——落到对话框的拖放若不是可识别的技能文件夹或 zip，必须阻止浏览器默认行为并在对话内说明原因，不得静默无反应。 | APPROVED（R1 2026-09-09 拍板；**①⑤ 由 CR-20260910-skill-intake 重写：新增 zip 与点选上传、白名单放宽、被排除文件回执、⑦ 失败可见**） |
| REQ-F-021 | 按发送路由技能 | SHOULD | 已注册 ≥1 个技能时，每次发送前系统做一次独立的最小路由判定，决定本轮是否启用某技能。 | ①路由是**独立的最小 LLM 调用**，不占用主对话模型上下文；②路由输入 = 本轮用户消息 + 已注册技能的 `name`/`description` 列表，输出结构化 `{skill: <name|null>}`；③命中某技能则**仅本轮**注入，后续发送不自动延续（参考 ChatGPT 对话内技能用法）；④无可用 Provider 时路由不阻塞，按普通对话继续（fail-open），不报错打断；⑤未注册任何技能时不发起路由调用。 | **SUPERSEDED**（由 REQ-F-030 取代：独立路由调用删除，技能由模型经工具自主选择；CR-20260910-agent-tooling，R1 待终裁。原状态 APPROVED（R1，2026-09-09 用户拍板；CR-20260909-skills）） |
| REQ-F-022 | 技能内容注入 | SHOULD | 命中技能的那一轮，把技能的 `SKILL.md` 与文件夹内全部白名单文本文件拼入该轮 system 上下文。 | ①注入内容 = `SKILL.md` 正文 + 各白名单文本文件（带文件名分隔）；②注入总量上限 32KB，超出按文件顺序截断并在回复内注明被截断；③注入**仅作用于命中的那一轮**，不写入会话持久上下文；④不改变 REQ-F-004「每轮携带会话已有全部消息 + 固定 system prompt」，技能内容作为该轮**附加 system 段**。 | **SUPERSEDED**（由 REQ-F-030 ①②④ 取代：正文按需 `read_skill`、无「仅当轮」硬边界；CR-20260910-agent-tooling，R1 待终裁。原状态 APPROVED（R1，2026-09-09 用户拍板；CR-20260909-skills）） |
| REQ-F-023 | 技能 HTML 产出捕获 | SHOULD | 技能轮的回复若含 ` ```html ` 代码块，捕获最后一个作为洞察产物；否则在对话内明确告知，不静默。 | ①从完成的回复取**最后一个** ` ```html ` 围栏块；②捕获成功：内容存入 `insights`（REQ-F-024），对话内提示「已生成洞察」（展示屏 UI 属 CR-20260909-display-screen）；③回复中无 html 围栏：对话内显示「本轮未产出 HTML」；④html 围栏存在但流式结束时未闭合：显示「HTML 不完整」，不写入 `insights`；⑤非技能轮不做捕获。<br>**CR-20260910-agent-tooling 重写**：捕获改为 `save_insight(html)` 工具——模型显式调用；无「技能轮」概念，①⑤作废；HTML 完整性由工具参数校验，不完整则工具失败回喂模型（④改）；③**保留「不静默」原则**（用户 2026-09-10 终裁 1）——模型本轮读取过技能却未调用 `save_insight` 时，对话内明确告知「本轮未产出洞察」，不静默；**`skill-html-unsandboxed` 信任面扩大且用户明示接受**（终裁 3 选 C）——`read_url` 使 HTML 来源从「用户自己拖入的技能文件夹」扩到「任意可被搜索到的网页」，攻击链为 `web_search → read_url → 页面内提示注入 → 模型调 save_insight → 攻击者 HTML 进入无沙箱同源 iframe`；缓解仍只有 REQ-F-025 ② 的不可关闭提示条；风险登记为 `skill-html-unsandboxed-web-source`（`test-results.json known_warnings`），iframe 沙箱化仍是未来 CR 的强制出口义务且优先级因本 CR 提高。 | APPROVED（R1，2026-09-09 用户拍板；CR-20260909-skills）；**REVIEWING（CR-20260910-agent-tooling 重写；R1 待终裁）** |
| REQ-F-024 | 洞察结果存储与读回 | SHOULD | 技能产出的 HTML 按会话存储，并提供读回入口供展示屏使用。 | ①`insights` 表含 `id`、`conversation_id`、`kind`、`html`、`created_at`；②`kind` 为扩展点，F1 固定写 `"skill"`；③同一会话多次产出按 `created_at` 追加，不覆盖；④提供 `GET /api/insights?conversationId=<id>`（返回该会话洞察列表，最新在前）作为 F1 的真实读回入口（原则 13）；⑤首页/展示屏渲染属 CR-20260909-display-screen；⑥写入者由捕获逻辑改为 `save_insight` 工具（CR-20260910-agent-tooling），`kind` 扩展点与追加语义不变。 | APPROVED（R1，2026-09-09 用户拍板；CR-20260909-skills）；**REVIEWING（CR-20260910-agent-tooling 补 ⑥；R1 待终裁）** |
| REQ-F-025 | 技能 HTML 渲染安全边界（当前非目标） | MUST | 当前版本不对技能生成的 HTML 做沙箱隔离，以显式告知替代技术隔离。 | ①技能生成的 HTML 由展示屏以 `<iframe srcdoc>` 直接渲染，**不加 `sandbox` 属性**（用户 2026-09-09 明确接受该风险）；②展示屏（CR-20260909-display-screen）**渲染任一技能 HTML 时**在其顶部显示一条**不可关闭**提示条（无关闭按钮、Esc 无效、切走 insight 视图才消失），说明内容由大模型生成且未隔离；③该风险登记为 known warning `skill-html-unsandboxed`，「沙箱化」列为未来 CR 的出口义务；④不因此放宽 REQ-NF-003（仍不做网页登录模拟、cookie 复用、抓包 token、密码代登录）。 | APPROVED（R1，用户明确接受风险；CR-20260909-skills；②由 CR-20260909-display-screen 落地） |
| REQ-F-026 | 动态展示屏内容与持久化 | SHOULD | 展示屏的内容由**全局单一**状态驱动，跨刷新恢复，随对话动态更新，并对未来内容类型开放。 | ①展示屏内容由一个**全局单行**状态 `display_state` 决定（不是每会话一份）；②`display_state` 有 `kind` 字段作扩展点，F2 支持 `kind ∈ {home, insight}`，其他 kind 预留；③技能轮产出洞察（`insights` 新行，REQ-F-023/024）后，`display_state` 自动切为 `{kind:"insight", ref: <该会话最新洞察>}`，展示屏渲染该 HTML（`<iframe srcdoc>` + REQ-F-025 提示条）；④刷新页面后展示屏按 `display_state` 恢复上次内容，不因刷新回到标题视图；⑤`kind:"home"` 时渲染标题视图。 | APPROVED（R1，2026-09-09 用户拍板；CR-20260909-display-screen） |
| REQ-F-027 | 对话指令控制展示屏 | SHOULD | 用户可用对话消息把展示屏切回标题视图。 | ①每次发送的路由判定（REQ-F-021，同一次独立调用）除 `skill` 外并行输出 `display` 字段；②用户消息表达"显示首页 / 回到首页 / 显示标题"类意图时 `display:"home"` → `display_state` 置 `{kind:"home"}`、展示屏回标题视图；③路由判不出展示意图 → `display:null` → 展示屏**不变**（fail-open）；④`display` 切换与 `skill` 命中互不阻塞（一条消息可以既触发技能又不改展示，或只切展示）。 | **SUPERSEDED**（由 REQ-F-032 取代：`show_home()` / `show_insight(id)` 工具；路由 `display` 字段随 REQ-F-021 删除而失去宿主；CR-20260910-agent-tooling，R1 待终裁。原状态 APPROVED（R1，2026-09-09 用户拍板；CR-20260909-display-screen）） |
| REQ-F-028 | 技能可见性 | SHOULD | 用户随时能看到系统里有哪些技能，以及某一轮回复是否用了技能。 | ①左下角 ☰ 菜单可查看**已注册技能列表**（名称 + 描述；无技能时显示「尚未注册技能」）；②某轮发送命中技能时，对话内出现「本轮使用技能：&lt;name&gt;」提示；③未命中技能的轮次不出现该提示；④技能列表读 `GET /api/skills`，注册成功后无需刷新页面即可看到新技能。<br>**CR-20260910-agent-tooling 重写**：①列表**可管理**（删除、重命名，REQ-F-031）；②③删除——「本轮使用技能」提示由步骤流的 `read_skill` 行承担（REQ-F-035 ⑥）；④不变。 | APPROVED（R1，2026-09-10 用户拍板；CR-20260910-skill-intake）；**REVIEWING（CR-20260910-agent-tooling 重写 ①②③；R1 待终裁）** |
| REQ-F-029 | 工具调用循环 | MUST | 一次发送内，模型可多次调用已注册工具，服务端执行并回喂结果，直到模型给出最终文本或触顶。 | ①单轮内工具调用上限 **10** 步；②触顶：终止循环，对话内出现提醒「已达 10 步上限，已停止」，**已执行工具结果与已生成文本保留**并落库 `status=truncated`；`truncated` 进入可重放集合（**= REQ-F-013 活动会话恢复所捞取的状态集合**）；触顶**不结束会话**，用户可继续追问；③工具执行失败（异常 / 超时）不终止循环——失败信息作为该工具结果回喂，由模型决定重试或改道；单工具超时上限由架构定；④**每次 `tool_call` 计 1 步**；同一工具同参数（参数按规范化 JSON 判等）连续失败 2 次后，本轮拒绝第 3 次同参调用；⑤「停止」（REQ-F-005）在循环任一阶段立即生效；⑥全部工具当前分三类——**只读**（`list_skills` / `read_skill` / `search_skills` / `web_search` / `read_url`）、**改展示态**（`show_home` / `show_insight`）、**追加洞察行**（`save_insight`）；均不写文件、不执行命令，**不引入执行审批**；新增任何写文件 / 执行命令类工具的 CR 必须把审批机制列为出口义务；⑦Provider 不支持工具调用时按 REQ-F-040 处理。 | REVIEWING（CR-20260910-agent-tooling，R1 待四角色评审与人工终裁） |
| REQ-F-030 | 技能作为工具 | MUST | 技能不再按轮注入，而是作为三个工具供模型按需读取。 | ①`list_skills()` 返回名录；`read_skill(name)` 返回 `SKILL.md` + 白名单文本文件正文（白名单与 32KB 上限沿用 REQ-F-020，上限语义从「每轮注入」改为「单次 read 结果」）；`search_skills(query)` 按 name / description 关键字检索；②system 稳定前缀只含技能名录（name + 一行 description），不含正文；③名录超出 REQ-NF-007 子预算时只列前 N 条并提示模型用 `search_skills`；④一轮内可 read 多个技能；read 结果作为工具结果进入上下文，随 REQ-F-041 淡出，**无「仅当轮」硬边界**；⑤未注册任何技能时三个工具不注册（REQ-NF-008 ④）；⑥REQ-F-021 独立路由调用删除、REQ-F-022 注入方式作废。 | REVIEWING（CR-20260910-agent-tooling，R1 待四角色评审与人工终裁） |
| REQ-F-031 | 技能管理 | SHOULD | 用户可在 ☰ 技能列表删除与重命名技能。 | ①每项提供「删除」「重命名」；②删除需二次确认，成功后目录与 DB 行同步移除，列表免刷新更新；③重命名做唯一性校验（复用 `SkillNameConflictError`），目录名同步；④历史会话中已进入上下文的技能内容不受影响；⑤仍不做编辑正文、版本管理、技能市场。 | REVIEWING（CR-20260910-agent-tooling，R1 待四角色评审与人工终裁） |
| REQ-F-032 | 展示屏工具化 | SHOULD | 展示屏由工具控制，取代路由字段。 | ①`show_home()`：`display_state` 置 `{kind:"home"}`；②`show_insight(insightId)`：置 `{kind:"insight", ref}`，**id 可为任意历史洞察**（用户 2026-09-10 终裁 2 选 B，非目标「不做展示内容历史回看/切换」「只支持回首页意图」随之改写）；id 须经 `conversations` 关联校验属主，非法 / 不存在 / 不属当前用户的 id 按工具失败回喂；③REQ-F-027 路由 `display` 字段作废；④两工具仅改 UI 状态，免审批。 | REVIEWING（CR-20260910-agent-tooling，R1 待四角色评审与人工终裁） |
| REQ-F-033 | 联网搜索工具 | MUST | 模型可通过 `web_search(query)` 检索网页。 | ①经配置的 SearXNG 兼容端点 `GET /search?q=&format=json` 取结果，返回 title / url / content 列表，条数受 REQ-NF-007 子预算约束；②未配置搜索后端或「联网」总开关关闭时该工具**不注册**；③配置了但不可用时按 REQ-NF-011；④只发送查询词，不发送会话历史、凭据、用户身份（REQ-NF-009）；⑤结果内容按子预算截断。 | REVIEWING（CR-20260910-agent-tooling，R1 待四角色评审与人工终裁） |
| REQ-F-034 | 网页读取工具 | MUST | 模型可通过 `read_url(url)` 读取网页正文。 | ①抓取正文后**先摘要再入上下文**；原文不进入上下文、不落库；②目标地址受 REQ-NF-009 ③ 约束；③抓取超时与体积上限由架构定；④步骤流显示的是摘要不是原文；⑤「联网」总开关关闭时不注册。 | REVIEWING（CR-20260910-agent-tooling，R1 待四角色评审与人工终裁） |
| REQ-F-035 | 对话内步骤流 | MUST | 工具执行过程在对话内可见。 | ①每次 `tool_call` 在记录区出现一行紧凑步骤（工具名 + 参数摘要）；`tool_result` 到达后该行更新为完成 / 失败并可展开查看结果摘要；②默认折叠详情；③SSE 新增 `tool_call` / `tool_result` 事件；④刷新后活动会话的步骤流按落库记录**重建**（REQ-F-013）；⑤步骤行与展开控件为真实 DOM / `<button>`，满足 REQ-F-014 / NF-006 可访问性；⑥REQ-F-028 ②「本轮使用技能」提示删除，由步骤流的 `read_skill` 行承担。 | REVIEWING（CR-20260910-agent-tooling，R1 待四角色评审与人工终裁） |
| REQ-F-036 | 状态灯执行阶段 | SHOULD | 状态灯能区分「执行工具中」与「生成文本中」。 | ①新增第五态「执行工具中」；②折叠态下由灯态承担执行可见性；③五态在两套主题下互相可辨识且 WCAG 2.2 AA 非文本对比 ≥3:1；④显式取代 REQ-F-018「四态」与 CR-20260909-collapsible-panel「不新增可辨识状态」约束（用户 2026-09-10 拍板）。 | REVIEWING（CR-20260910-agent-tooling，R1 待四角色评审与人工终裁） |
| REQ-F-037 | token 用量可见 | SHOULD | 用户能看到当前会话的 token 消耗。 | ①☰ 菜单显示当前会话累计 input / output token；②累计包含该会话内**全部**内部模型调用（主循环每一步、摘要调用等）；③Provider 返回 `usage` 时用真实值，流式请求携带 `stream_options.include_usage`；④Provider 不返回 usage 时用服务端本地估算并**标注「估算」**；⑤新对话清零；⑥不做费用换算。 | REVIEWING（CR-20260910-agent-tooling，R1 待四角色评审与人工终裁） |
| REQ-F-038 | 搜索后端配置入口 | MUST | 用户可配置联网能力与搜索后端。 | ①☰ 新增「搜索」项，与「模型」并列，**不进「模型」弹窗**；②含「联网」总开关（**默认开**，用户 2026-09-10 终裁 4；但搜索后端 URL 未配置时 `web_search` 仍不注册，故实际出网以填入 URL 为准）、SearXNG 兼容端点 URL、启用 / 停用、「测试连接」（判据：`GET /search?q=&format=json` 返回 2xx 且 JSON 含 `results` 字段——SearXNG 默认未开 json 输出，须专测该点）；③URL 服务端保存，非敏感；**含 userinfo（`user:pass@`）的 URL 拒绝保存**；④总开关关闭 → `web_search` 与 `read_url` 均不注册；仅 URL 未配置 → 只 `web_search` 不注册。 | REVIEWING（CR-20260910-agent-tooling，R1 待四角色评审与人工终裁） |
| REQ-F-039 | 回答来源引用 | SHOULD | 使用了联网结果的回答附来源。 | ①本轮使用了 `web_search` / `read_url` 结果时，最终回答附来源列表（URL + 标题）；②来源 URL **必须来自本轮工具结果集合**，服务端校验，不在集合内的剔除并在步骤流标记；③按 REQ-F-016 渲染为可点击链接；④未使用联网工具的轮次不附来源。 | REVIEWING（CR-20260910-agent-tooling，R1 待四角色评审与人工终裁） |
| REQ-F-040 | Provider 工具能力探测与降级 | MUST | 不支持工具调用的 Provider 不导致失败。 | ①Provider 新增「支持工具调用」能力属性；②REQ-F-007「测试」动作顺带探测（发送带工具定义的最小请求）并保存结果，配置页标注；③解析到的最高优先级 Provider 不支持时，**本轮降级为纯对话**（不注册任何工具）并在对话内提醒，每会话一次；④REQ-F-006 优先级规则不变——**不因工具能力跳过** Provider；⑤状态灯「常亮」语义不变。 | REVIEWING（CR-20260910-agent-tooling，R1 待四角色评审与人工终裁） |
| REQ-F-041 | 工具结果保留窗口 | MUST | 工具结果不无限累积进上下文。 | ①发送给模型的上下文中，只有最近 N 轮的工具结果以原文保留；更早轮次的工具结果替换为一行摘要标记（工具名 + 参数摘要 + 「结果已省略」）；N 由架构定（建议 2）；②用户与助手文本消息不受本条影响（完整压缩属 B 期）；③落库记录不变，只影响发送给模型的上下文；④步骤流重建读库，不受影响。 | REVIEWING（CR-20260910-agent-tooling，R1 待四角色评审与人工终裁） |

## 非功能需求

| ID | 名称 | 优先级 | 描述 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| REQ-NF-001 | 本机运行 | MUST | 第一版面向本机开发运行。 | 应用可通过本机命令启动，SQLite 数据保存于本地项目运行目录。 | APPROVED |
| REQ-NF-002 | 认证隔离 | MUST | 用户只能访问自己的 Provider 配置和会话。 | API 测试证明用户 A 不能读取或调用用户 B 的 Provider 与会话。 | DEFERRED（暂缓，CR-20260909-minimal-floating-chat）：当前版本为单一本地管理员，无隔离对象；恢复 Google 登录时同步恢复本条。 |
| REQ-NF-003 | 官方授权边界 | MUST | 第三方模型账号授权只使用官方支持方式。 | 系统不实现网页登录模拟、cookie 复用、抓包 token 或密码代登录。 | APPROVED |
| REQ-NF-004 | 真实入口验证 | MUST | 交付前必须通过真实 Next.js 应用入口验证主路径。 | 冒烟测试启动真实应用并走通（管理员身份免登录进入后）模型设置和底部浮窗对话主路径。 | APPROVED |
| REQ-NF-005 | 不可信归档处理边界 | MUST | 用户上传的 zip 是不可信二进制输入，解析必须有界且不得越出技能目录。 | ①**路径**：条目名含 `..` 段、绝对路径、盘符前缀或反斜杠分隔时拒绝整个归档；落盘前每个文件路径仍须经 `path.resolve` 越界校验（双重防护）。②**资源上限**：条目数、单条解压后字节、总解压后字节、压缩比四项各有上限，任一超限**整体拒绝**并返回可读原因（不得部分写入）。③**格式**：仅接受压缩方法 `0`(store) 与 `8`(deflate)；通用位标记的加密位置位即拒绝；解析失败/文件截断按拒绝处理。④解析器为纯函数、不接触文件系统，可独立施加对抗性测试。 | APPROVED（R1，2026-09-10 用户拍板；CR-20260910-skill-intake，DEC-018） |
| REQ-NF-006 | 前端 UI 视觉基础 | MUST | 全部已批准的前端界面必须共享一致、克制的浅色紫色视觉语言；默认主题为浅色紫色。该变更只更新视觉基础与组件呈现，不新增或改变业务能力。 | ①已批准的展示屏、底部悬浮对话、左下角菜单、菜单内「模型」、账号占位入口、模型设置与现有弹窗均纳入同一视觉基础；②「模型」仍是 Provider 启用与优先级配置入口，对话浮窗不新增 Provider/模型选择器，保持 REQ-F-006 的自动优先级语义；③Design Token 使用可追溯的语义化 HSL 三元组，组件不散落同义视觉常量；④所有已覆盖界面满足 `UI-GOV-001` 的 WCAG 2.2 AA、键盘、焦点和响应式要求；⑤P3 技能上传功能不构成本需求的验收范围或完成证据；⑥保留 REQ-F-014 的深色主题切换，浅色紫色只是默认主题，两套主题均须满足相同的可访问性与行为要求；⑦步骤流与状态灯第五态纳入本视觉基础（CR-20260910-agent-tooling）。 | APPROVED（R1，2026-09-10 用户终裁；CR-20260910-ui-foundation）；**REVIEWING（CR-20260910-agent-tooling 补 ⑦；R1 待终裁）** |
| REQ-NF-007 | 每轮上下文预算 | MUST | 单轮发送给模型的输入有硬上限与子预算。 | ①总输入硬上限按 Provider 上下文窗口比例（架构定）；②子预算：技能名录、工具定义、单条工具结果、来源列表各有上限；③工具结果超子预算 → 截断并在结果尾注明；④总输入超上限 → 先按 REQ-F-041 收窄；仍超 → 请求级错误（REQ-F-016 红字），**不无限压缩**；⑤验收以 REQ-F-037 的 token 数为度量，基准场景（1 技能 + 连续 3 轮搜索）记入证据。 | REVIEWING（CR-20260910-agent-tooling，R1 待四角色评审与人工终裁） |
| REQ-NF-008 | 提示词前缀稳定性与工具动态注册 | MUST | system prompt 可缓存，工具定义不白占 token。 | ①system prompt 分**稳定前缀**（身份、工具定义、技能名录）与**易变后缀**（display 状态等）；稳定前缀同会话内字节级不变，除非技能 / 工具集合变化；②当前时间不进入稳定前缀；③工具与技能 description 强制简短（上限架构定）；④工具按可用性动态注册，注册条件**以 REQ-F-038 ④ 为准**：无技能 → 无技能工具；「联网」总开关关闭 → `web_search` 与 `read_url` 均不注册；总开关开但 URL 未配置 → 只 `web_search` 不注册；Provider 不支持工具调用 → 无工具；未注册的工具定义不进 prompt。⑤工具集合在**一次发送内**不得变化（REQ-NF-011 ② 的「本轮不再调用」实现为执行体短路，不注销工具定义），以免打破 ① 的前缀稳定。 | REVIEWING（CR-20260910-agent-tooling，R1 待四角色评审与人工终裁） |
| REQ-NF-009 | 出网边界 | MUST | 联网能力的数据流与目标地址受限。 | ①联网工具是 Jarvis **唯一**主动出网点（Provider 调用除外）；②出网只携带查询词 / 目标 URL，不携带会话历史、凭据、用户身份、系统提示；③`read_url` 目标：仅 http / https；解析后 IP 不得为 loopback、链路本地、私网段、云元数据地址；**重定向每跳重校验**；④「联网」总开关关闭 → 两工具不注册；⑤搜索后端 URL 允许为本机地址（自托管 SearXNG），该例外仅适用于 `web_search` 的后端调用，不适用于 `read_url` 目标；⑥REQ-NF-003 不变：SearXNG 为无账号聚合查询，不构成登录模拟。 | REVIEWING（CR-20260910-agent-tooling，R1 待四角色评审与人工终裁） |
| REQ-NF-010 | 内核可扩展性 | MUST | 新增能力不改对话核心。 | ①本期**实际做成注册式**的是三处：**工具**、**技能来源**、**循环策略（loop）**；**模型适配与上下文预算 / 压缩策略只留接口位，本期不重构 `adapters.ts`**（用户 2026-09-10 终裁 6 选 A——无痛点指向 adapters，重构只为架构整齐，会放大本期范围）；②新增一个工具 = 注册一个描述符（name、description、参数 JSON schema、执行体、可用性判定），**不修改对话核心**；③loop 作为扩展点为 Code mode 预留（本 CR 非目标）；④模式参考 DeepSeek Harness 插件化，**不引入其包**；⑤本条 ①②③ 的验收为 grep 守卫（对话核心不 import 具体工具模块）+「注册一个假工具即可被循环调用」测；「预算 / 压缩 / loop 注册式」的设计合理性为代码审查项，不作机器断言（R1 测试角色意见）。 | REVIEWING（CR-20260910-agent-tooling，R1 待四角色评审与人工终裁） |
| REQ-NF-011 | 搜索后端不可用降级 | SHOULD | 搜索后端故障不打断对话。 | ①`web_search` 调用失败（连接失败 / 超时 / 非 2xx / JSON 非法）→ 作为工具失败回喂模型（REQ-F-029 ③），不弹请求级错误；②连续失败 ≥2 次 → 本轮后续不再调用，步骤流标记「搜索后端不可用」；③「搜索」配置的「测试连接」可复现该故障。 | REVIEWING（CR-20260910-agent-tooling，R1 待四角色评审与人工终裁） |

## 非目标

- 首版不实现图片、语音、多模态输入。
- ~~首版不实现工具调用和 Agent 编排。~~ **（作废：CR-20260910-agent-tooling，REQ-F-029；R1 待终裁）**
- ~~首版不实现知识固化入口。~~ **（作废：CR-20260910-agent-tooling；C 期本地知识库将实现，本 CR 不实现；R1 待终裁）**
- 首版不实现公网生产部署。
- 首版不实现非官方第三方网页登录自动化。
- 首版不实现多用户与账号隔离（随 REQ-F-001 Google 登录一同暂缓）。
- 首版不实现对话历史浏览（已结束会话保留在本地但无浏览入口）。
- ~~首版不实现超长上下文的截断或摘要；超出模型上下文上限时由 Provider 报错，走流中错误路径。~~ **（作废：CR-20260910-agent-tooling——REQ-F-041 工具结果保留窗口 + REQ-NF-007 预算；完整压缩属 B 期；R1 待终裁）**
- 折叠状态下不显示未读消息角标、消息计数或「有新回复」持久标记（CR-20260909-collapsible-panel）；折叠期间回复完成仅由状态灯瞬时提示，用户需手动展开查看内容。
- 技能系统（CR-20260909-skills）首版只做单一技能的上传/~~路由/注入/HTML 捕获~~**读取（工具化，CR-20260910-agent-tooling：路由与注入作废，HTML 捕获改 `save_insight` 工具）**；不做技能市场、技能编排/串联、技能版本管理、技能编辑 UI。
- 技能生成的 HTML 不做沙箱隔离、不做内容安全策略过滤、不做脚本剥离（REQ-F-025，用户 2026-09-09 明确接受风险）。
- ~~技能路由不保证判定准确；误判由 fail-open（当普通对话）与「仅当轮」边界兜底，不做用户可纠正的路由覆写 UI（首版）。~~ **（改写：CR-20260910-agent-tooling）** 技能选择由模型经工具自主完成，不保证与用户预期一致；用户可用自然语言显式指定（「用技能 X」）；不做路由覆写 UI。
- F1（CR-20260909-skills）不做技能 HTML 的首页/展示屏渲染——归 CR-20260909-display-screen（用户 msg 54「两个CR」）。
- 动态展示屏（CR-20260909-display-screen）首版只做全局单一展示 + `home`/`insight` 两种 `kind`；不做多屏/分屏、~~不做展示内容历史回看/切换~~**（改写：CR-20260910-agent-tooling 终裁 2 选 B——`show_insight(id)` 可切到任意历史洞察；仍不做展示屏内的历史列表 UI，切换只经对话）**、不做展示屏手动编辑或拖拽布局、不做展示屏内容导出。
- 展示屏不对技能生成的 HTML 做沙箱、CSP、脚本剥离（REQ-F-025，用户接受风险）；唯一缓解是不可关闭提示条。
- ~~路由的 `display` 字段只支持"回首页"意图；不做"显示第 N 份报告""显示配置页"等更细的展示指令~~ **（改写：CR-20260910-agent-tooling 终裁 2 选 B——路由字段作废，改为 `show_home()` / `show_insight(id)` 工具，「显示第 N 份报告」类意图由 `show_insight` 承担；仍不做「显示配置页」等指向非 `insight` 内容的指令，`kind` 扩展点为未来 CR 预留）**。
- 技能归档只支持 **zip**（压缩方法 store / deflate）；不做 rar / 7z / tar / tar.gz，不做分卷、不做加密归档（CR-20260910-skill-intake）。
- ~~仍不做技能的编辑、删除、重命名、版本管理与技能市场——☰ 菜单里的技能列表是**只读**的（REQ-F-028）。~~ **（部分作废：CR-20260910-agent-tooling——删除与重命名由 REQ-F-031 实现；编辑正文、版本管理、技能市场仍不做；R1 待终裁）**
- 不做上传进度条、断点续传、后台队列；注册是一次同步请求。
- （CR-20260910-agent-tooling）采用 DeepSeek Harness 的**内核结构**（插件化、注册式、loop 可换），**不引入**其 Standard 模式的 subagents / planning / goals / workflows / shell / 文件编辑 / sandbox；不安装 `@deepseek-ai/dsh` 包。
- （CR-20260910-agent-tooling）Code mode（模型写程序一次做完多步）为未来候补；前提是执行沙箱与审批机制。
- （CR-20260910-agent-tooling）不做执行审批机制（本 CR 无写文件 / 执行命令类工具）；引入此类工具的 CR 以审批机制为出口义务。
- （CR-20260910-agent-tooling）B 期「会话上下文分层与完整压缩」、C 期「本地知识库」、D 期「主动唤醒（默认关 + 开关 + 空闲 token 上限）」不在本 CR。
- （CR-20260910-agent-tooling）不做 token 费用换算；不做搜索结果缓存；不做技能编辑正文 / 版本 / 市场。

## 变更响应 · CR-20260908-floating-chat-functional-fixes

### 验收澄清

- REQ-F-005：「不再追加输出」必须在服务端可观察——停止后服务端停止向模型请求，落库消息状态为 `stopped`。
- REQ-F-006：「切换」必须在浮窗 UI 上可观察——存在多个已启用 Provider 时渲染切换控件，且所选 Provider/模型随对话请求发送。
- REQ-F-007：「新增、编辑、启用、停用、测试」为五个独立可观察动作，各自需要独立验收证据；「编辑」不得产生重复配置。
- REQ-F-013：「恢复」必须在真实入口可观察——刷新后通过 `GET /api/conversations/[id]/messages` 取回消息并在浮窗重建。
- REQ-F-008：`oauth` 认证模式在本版本仅作展示，不可保存为可用 Provider。

### CR-20260908-ui-light-theme-minimal-home 澄清

- REQ-F-007「独立设置页」在本版本同时存在两个真实入口：首页「模型」弹窗（原「配置」，CR-20260909-corner-menu 更名）与 `/settings/models` 深链页面，二者渲染同一 `ModelSettings` 组件。~~主题开关位于该弹窗的「外观」分区~~ **（作废：CR-20260909-corner-menu 将主题开关移出该弹窗、内联进左下角 ☰ 菜单；「模型」弹窗此后只含 Provider 设置，与深链页对齐）**。
- REQ-F-002/003：展开后的对话面板高度上限为视口 65%，不得占满全屏（避免退化为通用聊天工具）。**（已被 CR-20260909-minimal-floating-chat 收敛为 50%，见下。）**
- 首页「知识库概览」区域在当前版本不展示，属已批准的暂缓项，不构成需求删除。

### CR-20260909-minimal-floating-chat 澄清

**会话生命周期**
- 会话自**首条用户消息**创建，至用户点击**「停止」或「新对话」**结束。
- 活动中：多轮续接，每次发送沿用同一 `conversationId`；回复正常完成后会话仍活动，可继续追问。
- 已结束：`conversationId` 丢弃；记录区在「点击新对话」或「发送下一条消息」时清空；下一条消息创建新会话。
- 已结束/已停止的会话及其消息保留在本地存储，本版本无浏览入口，且不被刷新恢复逻辑捞取。

**请求级错误 vs 流中错误**
- 请求级错误（回复第一段文本到达前失败：连接失败/鉴权失败/Provider 4xx-5xx/超时）→ 输入框上方一行红字，脱敏，不写入上下文，不结束会话，不阻塞输入。
- 流中错误（回复已开始后中断）→ 该助手气泡标记「生成失败」，落库 `status=error`（现有行为）。

**面板高度**：展开后对话面板高度上限由视口 65% 收敛为 **50%**（覆盖 CR-20260908-ui-light-theme-minimal-home 的 65% 澄清）。

**动作按钮状态机**（单个按钮）

| 场景 | 按钮 | 行为 |
|---|---|---|
| 生成中 | 停止 | abort，服务端停止请求模型，会话结束，落库 `stopped` |
| 输入框有字 | 发送 | 发送，Enter 等效 |
| 输入框空（有/无历史均同） | 新对话 | 清空记录区，丢弃 `conversationId` |

**优先级解析**：见 REQ-F-006 验收 ④⑤⑥。状态灯反映「是否存在任一连接有效的 Provider」；发送时取连接有效的 Provider 中优先级最高者。

**REQ-F-001 暂缓边界**：当前版本以单一本地管理员身份运行，不实现账号登录；「账号登录」按钮保留为占位入口。恢复 Google 登录时同步恢复 REQ-F-001 与 REQ-NF-002。

### CR-20260909-collapsible-panel 澄清

**折叠状态语义**（REQ-F-019）：收起/展开是纯 UI 态，不影响 `conversationId`、`jarvis:chat-session-ended`，不触发或结束会话；与「新对话」「停止」正交。持久化用 `localStorage['jarvis:chat-collapsed']`（每浏览器）；`localStorage` 服务端不可读，SSR 首帧按默认（展开）渲染，hydration 后按存储校正——只接受一帧内校正，不接受持久错误态（与 DEC-011 同类处理）。

**折叠期间回复完成提示**：仅状态灯一次短促瞬时提示，不引入未读角标、消息计数或~~第五种持久灯态~~。**（「不引入第五种持久灯态」由 CR-20260910-agent-tooling 作废——用户 2026-09-10 拍板破四态，新增「执行工具中」为第五态，见 REQ-F-036；「不引入未读角标 / 消息计数」不变。）**

**折叠时的请求级错误红字**：折叠状态下记录区不在 DOM，但请求级错误红字**照常显示在输入框上方**（它关联「发送」动作，输入条始终可见）。

**折叠可访问性**：折叠时 `aria-live` 记录区随之移除，屏幕阅读器用户在折叠期间不朗读流式回复——与视力用户对等（都需展开查看内容），非缺陷。

### CR-20260909-corner-menu 澄清

**左下角 ☰ 菜单**（REQ-F-015）：header 不再有功能按钮，标题独占顶部。☰ 触发器 `position: fixed` 于左下角，层叠高于底部悬浮对话（`z-index` > 20），对话折叠或展开时均可点击。浮层菜单为**非模态** disclosure（Esc 关 + 焦点回触发器 + 点击外部关闭）；「模型」「账号登录」两项仍打开各自的模态 `<dialog>`。浮层机制（原生 `popover` 属性 vs 自写 disclosure）留 P3。

**主题开关内联**：浅色/深色切换从「模型」（原「配置」）弹窗的「外观」分区移出，作为 ☰ 浮层内的内联控件——点击**不打开任何弹窗**，当场切换并写 `localStorage['jarvis-theme']`（`ThemeToggle` 组件与持久化机制不变，仅换宿主）。

**「配置」→「模型」**：首页该入口的按钮/菜单项文案由「配置」改为「模型」，打开的仍是同一个 Provider 设置弹窗（内部标题不强制改）。「配置」一词在配置预检（`config:check`）、`ConfigWarning` 等其它上下文不受影响。

**dev 指示器**：`next.config.mjs` 设 `devIndicators: { position: "bottom-right" }`，把 Next.js dev 模式指示器挪到右下角，避免与左下角 ☰ 冲突。这是构建配置，不在应用组件里做 `NODE_ENV` 分支；生产构建无 Next 指示器。

**可发现性取舍**：设置/账号入口在左下角属非行业惯例（惯例在右上角/汉堡菜单）。鉴于当前为单一本地管理员（配置一次即可），可发现性成本可接受——记为**有意取舍**，非遗漏。

## 变更响应 · CR-20260910-agent-tooling

### 来源与决策依据

INPUT-2026-09-10-008（用户原话 11 条）；`project/05_evidence/EV-2026-09-10-agent-tooling-requirements.md`（仓库代码事实、联网调研、16 项决策时间线、三轮审视缺口）。级别 L3 重型（含单向门 CP-5 / CP-7 / CP-10 / CP-16）。变化点 CP-1..CP-25 见 CR「变化点登记」表。

本节只记录**为什么这样变**；「现在是什么」已直接写入上方基线表（REQ-F-029..041、REQ-NF-007..011 新增行；REQ-F-003/004/005/006/013/014/018/019/023/024/028、NF-006 重写；REQ-F-021/022/027 SUPERSEDED；非目标作废 4 行、改写 1 行、新增 5 行）。

### 既有条款澄清（不改语义）

- REQ-NF-001 本机运行：讲部署形态（单进程、本机启动、SQLite 本地）；联网工具的出网是数据流，本条仍成立。
- REQ-NF-003 官方授权边界：SearXNG 为无账号、无 cookie 的聚合查询，不构成「网页登录模拟」；本条不放宽。
- REQ-NF-004 真实入口验证：冒烟扩到两条 agent 主路径——「注册技能 → 提问 → 步骤流 `read_skill` → 回答」「配置搜索 → 提问 → 步骤流 `web_search` / `read_url` → 回答附来源」，以及「未配置搜索」路径。
- REQ-F-016：工具执行错误既非请求级错误也非流中错误——它是**工具结果**，在步骤流内显示；工具结果摘要按 Markdown 渲染，链接仍仅 http / https / mailto 可点击；模型输出中的原始 HTML 不执行。
- REQ-F-020 ②：生成 `SKILL.md` 的独立 LLM 调用**不进工具循环**、不注册工具、不计入 REQ-F-037 的会话累计（它不属于任何会话）。

### 关键取舍（用户拍板，2026-09-10）

| 取舍 | 决定 | 为什么 |
|---|---|---|
| 参考对象 | 读 DeepSeek Harness 抄结构自行实现，不装包；OpenClaw 只借「主动唤醒」并推到 D 期 | dsh 同栈同许可、插件化正好补 Jarvis 缺的内核层；OpenClaw 的 token 开销是结构性的（官方文档自述） |
| 独立路由调用 | 删除 | 每轮省一次模型调用；模型经工具自主选择更准 |
| 「仅当轮注入」 | 取消 | 工具化后 read 结果随保留窗口自然淡出 |
| 搜索后端 | SearXNG（无 key、零 npm 依赖） | 用户无 API key；架构约束「新增运行依赖属 L3」 |
| 工具审批 | 只读工具免审批 | 当前工具集全部只读或仅改 UI 状态；写 / 执行类工具的审批为未来 CR 出口义务 |
| 状态灯四态 | 显式破坏为五态 | 长任务下四态不够；折叠态需要灯承担执行可见性 |
| 面板 50% | 有步骤流时 75% | 10 步步骤流塞进半屏会把回复挤出可视区 |
| 触顶 | 提醒后停止，成果保留 | 不白烧已跑的 10 步 |
| Provider 不支持工具 | 提醒 + 降级纯对话，不跳过 | 用户明确「提醒」而非「跳过」 |
| token 拿不到 | 本地估算并标注 | 否则 REQ-NF-007 在本地模型上无验收手段 |
| `web_search` 分期 | 提进 A 期 | A 期只做技能工具化时用户可感知收益仅「能删技能」；搜索是工具循环 / 步骤流 / 预算三者的最佳压测用例 |

### 分期

| 期 | 内容 | 状态 |
|---|---|---|
| A（本 CR） | REQ-F-029..041 + REQ-NF-007..011 + 上述重写 / 作废 / 澄清 | R1 待评审 |
| B | 会话上下文分层与完整压缩 | 另立 CR |
| C | 本地知识库（检索工具 + 知识固化入口） | 另立 CR |
| D | 主动唤醒（默认关 + 开关 + 空闲态 token 上限） | 另立 CR |

### 待架构裁决项（P2）

单工具超时；`read_url` 体积上限与摘要方式；REQ-F-041 的 N；REQ-NF-007 各子预算数值；REQ-NF-008 description 长度上限；工具记录的数据形状与迁移；Provider 能力字段；搜索配置存储位置；SSE `tool_call` / `tool_result` 事件形状；token 本地估算方法。

## 批准状态

- 当前状态：APPROVED
- 用户确认：2026-09-08，用户要求按该计划更新说明书并严格按流程执行。
- 用户确认：2026-09-09，用户逐轮确认 CR-20260909-minimal-floating-chat 的需求变化点（单管理员形态、浮窗收敛、Provider 优先级、状态灯四态、会话生命周期、浅色简约视觉），并要求落地后查看各流程变化点与角色评审意见。
- 用户确认：2026-09-09，用户提出并逐轮确认 CR-20260909-collapsible-panel（对话面板一级折叠、折叠时安静、偏好记住、发送即展开、状态栏箭头控件、折叠期间回复完成状态灯瞬时提示【甲案】、v1 无动画），并要求经角色评审后修正说明书。
- 用户确认：2026-09-09，用户提出并逐轮确认 CR-20260909-corner-menu（左下角 ☰ 悬浮菜单收纳「模型」「账号登录」、主题开关内联进菜单、「配置」更名「模型」、Next dev 指示器挪 `bottom-right`、浮层机制留 P3、桌面与移动同一套），经角色评审后要求按流程执行开发。
- 用户确认：2026-09-09，用户提出技能系统并逐轮（msg 49→54）拍板 CR-20260909-skills 的 R1 需求（拖文件夹到对话框注册技能、按发送独立路由、仅当轮注入、HTML 产出捕获入 `insights`、无沙箱隔离且接受风险、不回显 SKILL.md 全文、拆 F1 技能 / F2 展示屏两个 CR）。本 CR 为首个按 R1–R4 共识门禁模型执行的变更；R1 四角色评审 + 人工终裁完成，R2/R3/R4 机器门通过，P3 实现待用户授权。
- 用户确认：2026-09-10，CR-20260910-skill-intake（L2）R1 需求：技能投递支持**文件夹与 zip**两种形态、拖放与点选两条路径（决策「2. c」）；zip 用**零依赖自写解析**（决策「3. 自写」）；扩展名白名单**放宽**并要求被排除文件在回执列出（决策「4. 顺便放宽」）；缺陷修复与 zip 扩展**一起做**（决策「1. 一起做」）。新增 REQ-F-028（技能可见性）、REQ-NF-005（不可信归档处理边界）。四角色 R1 评审已闭环至全 APPROVED，待人工终裁。
- 用户确认：2026-09-09，CR-20260909-display-screen（F2，L3）R1 需求：首页重写为全屏动态展示屏（REQ-F-015）、`display_state` 全局单行 + `kind` 扩展点（REQ-F-026）、技能洞察自动上屏 + 跨刷新持久、「显示首页」对话指令回标题视图（REQ-F-027）、未沙箱化 `<iframe srcdoc>` + 不可关闭提示条落地 REQ-F-025 ②。R1 四角色评审完成（产品/测试 APPROVED，架构/模块 CONDITIONAL），待用户人工终裁。
- 用户确认：2026-09-10，CR-20260910-agent-tooling（L3 重型）R1 需求：用户逐轮拍板 16 项决策（INPUT-2026-09-10-008；EV-2026-09-10-agent-tooling-requirements §3）——读 DeepSeek Harness 抄结构自行实现（方案 A / 读法二）、删独立路由、技能工具化（一轮可读多个）、SearXNG 无 key 搜索、只读工具免审批、token 可见（拿不到 usage 时本地估算）、步骤流 + 状态灯五态、10 步触顶提醒后停止、`read_url`、刷新重建步骤流、面板分情形放宽、来源引用、Provider 不支持工具时提醒降级、`web_search` 提进 A 期。新增 REQ-F-029..041、REQ-NF-007..011；重写 12 条；作废 F-021/022/027。**R1 四角色评审完成（产品 / 架构 / 模块开发 / 测试全部 CONDITIONAL，零 REJECTED），派生 CP-26..CP-43；人工终裁 7 项已拍板**——保留 F-023「不静默」告知、`show_insight` 可指任意历史洞察并改写两条展示屏非目标、**明示接受 `read_url` 带来的无沙箱 iframe 信任面扩大**（登记 `skill-html-unsandboxed-web-source`，沙箱化仍为强制出口义务）、「联网」默认开、面板 75% 分情形、`adapters.ts` 本期只留接口位不重构、迁移框架纳入本期。R1 PASS，待 P2。
