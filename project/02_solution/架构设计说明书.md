# 架构设计说明书

责任人：架构角色

## 设计目标

基于已批准产品需求，采用 Next.js 全栈架构实现本机运行的底部悬浮大模型对话功能。服务端负责身份态、Provider 配置与优先级、凭据加密、流式模型代理和会话持久化；前端负责占位登录入口、独立模型设置页（含优先级管理）、底部悬浮对话 UI 和流式展示。当前版本（CR-20260909-minimal-floating-chat）以单一本地管理员身份运行，Google 登录（REQ-F-001）与多用户隔离（REQ-NF-002）暂缓。

## 架构角色定位与文档范围

**架构角色负责（本文档的权威边界）**：技术选型与版本、部署与运行形态、模块划分与边界、模块间接口契约、数据模型与迁移策略、状态流、外部依赖边界、跨模块的非功能约束（安全、性能、可测性）与架构决策记录（DEC）。

**架构角色不负责**：产品功能定义与验收标准（产品需求说明书）、具体实现代码（模块开发角色）、测试用例设计（测试角色）。当架构约束与产品需求冲突时，按 `docs/WORKFLOW.md` 失败回流：架构 → 产品需求 → 用户确认。

**本文档对每个产品需求变化点必须给出架构裁决**：不涉及架构（纯文档/纯实现）、既有决策已覆盖、需新增或修订 DEC。模块任务开发说明书据此逐点派生任务。

## 技术栈与部署形态

### 运行形态

单进程全栈：一个 Node.js 进程同时提供前端渲染与后端 API，无独立后端服务、无反向代理、无消息队列。数据落在项目运行目录下的单个 SQLite 文件。面向单机单用户（当前单管理员），并发规模为 1。

### 部署方式

| 环境 | 命令 | 构建目录 | 说明 |
|---|---|---|---|
| 交互开发 | `npm run dev` | `.next/` | 当前唯一实际使用形态（REQ-NF-001） |
| 生产构建（本机） | `npm run build` + `npm start` | `.next/` | 可用但当前无验收；`JARVIS_TEST_USER_ID` 旁路在此不生效（见 DEC-001 备注） |
| 验证构建 | `npm run build:verify` | `.next-verify/` | 构建目录隔离（DEC-009） |

非目标（产品需求说明书）：无容器化、无公网部署、无 CI/CD 发布管线、无多实例。发布控制见 `project/07_releases/版本发布控制说明书.md`（本机分发）。

### 前端技术

| 项 | 选择 | 依据 |
|---|---|---|
| 框架 | Next.js 15 App Router + React 19（RSC + 客户端组件） | 全栈同构、SSR 注水、Route Handler 与页面同仓 |
| 语言 | TypeScript 5.9 | — |
| 样式与主题 | 全局 CSS + CSS 自定义属性做 theme-aware，无 CSS 框架、无组件库 | DEC-006；零依赖，无主题闪烁 |
| 弹窗 | 原生 `<dialog>` + `showModal()` | DEC-007；平台原生遮罩/焦点陷阱/Esc |
| Markdown | 自写 `src/lib/markdown.tsx`，输出 React 元素不经 innerHTML | DEC-008；无注入面、零依赖 |
| 流式消费 | `fetch` + `ReadableStream` reader 解析 SSE；`AbortController` 停止 | DEC-004 |
| 状态管理 | React 组件内 `useState`，无全局状态库 | 规模不需要 |

### 后端技术

| 项 | 选择 | 依据 |
|---|---|---|
| API 层 | Next.js Route Handlers（与前端同进程） | 单体全栈 |
| 持久化 | `node:sqlite`（`DatabaseSync`，Node 内置，需 Node 22+；当前 Node 24） | DEC-002；零外部数据库、零 ORM；类型垫片 `src/types/node-sqlite.d.ts` |
| 凭据加密 | `node:crypto` aes-256-gcm，密钥 `JARVIS_SECRET_KEY` fail-fast | REQ-F-009；`src/lib/crypto.ts` |
| 身份 | NextAuth 4（Google Provider）**代码保留但暂缓**；当前 `src/lib/auth-guard.ts` 单管理员旁路 | DEC-001 |
| 模型调用 | 统一走 OpenAI-compatible `/chat/completions`（原生 `fetch`），流式归一为 `ChatDelta`，超时 60s，错误体归一 | DEC-003；`src/lib/adapters.ts` |
| 流式输出 | SSE（`text/event-stream`），`ChatDelta` 事件 | DEC-004 |

### 依赖清单与边界

运行依赖（`dependencies`）：`next`、`react`、`react-dom`、`next-auth`。**`openai` 已声明但全仓库无 import**——属可清理的冗余依赖，P3 处理（CONTROLS 对外部依赖为 L3 敏感，移除按 L1）。
开发依赖：`vitest` + `@testing-library/*` + `jsdom`（组件/单元）、`@playwright/test`（真实入口）、`typescript`、`vite`。
新增任何运行依赖属 L3，须先经 CR。本 CR 不新增依赖。

### 关键运行配置

`.env.local`（不入库）：`JARVIS_SECRET_KEY`（必需）、`JARVIS_DB_PATH`、`JARVIS_TEST_USER_ID`（当前单管理员形态使用）、`NEXTAUTH_URL`/`NEXTAUTH_SECRET`（暂缓项，占位）、`GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`（暂缓项，可空）。预检 `npm run config:check`（`scripts/check-config.mjs`）。

## 总体架构

```text
Browser UI
  -> Next.js App Router pages
  -> Auth.js Google session
  -> API routes
  -> SQLite persistence
  -> Provider adapters
  -> OpenAI / DeepSeek / Local OpenAI-compatible endpoints
```

## 架构决策

| 决策 ID | 覆盖需求 | 方案 | 替代方案 | 选择理由 | 风险 | 状态 |
|---|---|---|---|---|---|---|
| DEC-001 | REQ-F-001, REQ-NF-002 | 使用 Auth.js/NextAuth Google 登录保护页面和 API。 | 自建用户名密码。 | Google 登录符合用户确认，减少本地密码管理。 | 本机 OAuth callback 需要用户配置 Google OAuth client。 | DEFERRED（CR-20260909-minimal-floating-chat）：NextAuth 代码保留，当前版本以 `src/lib/auth-guard.ts` 的 `JARVIS_TEST_USER_ID` 旁路提供单一本地管理员身份（仅 `NODE_ENV!==production` 生效）。生产构建下的单管理员形态列 P3 待决（见 DEC-011 备注）。 |
| DEC-002 | REQ-F-007, REQ-F-009, REQ-F-013 | 使用 SQLite 保存用户、Provider、会话和消息，Provider secret 加密保存。 | 只用环境变量或内存保存。 | 能按用户隔离配置和恢复最近会话。 | 加密密钥丢失后无法解密旧凭据。 | APPROVED |
| DEC-003 | REQ-F-008, REQ-F-010, REQ-F-011, REQ-F-012 | 使用统一 Provider Adapter 抽象 OAuth/API Key/Local/Unsupported 与流式输出。 | 为每个 Provider 写独立 UI 和 API。 | 统一调用链，便于后续扩展官方 OAuth Provider。 | 各 Provider 流式事件格式不同，需要归一。 | APPROVED |
| DEC-004 | REQ-F-004, REQ-F-005 | 使用 Server-Sent Events 输出 `ChatDelta`，前端用 AbortController 停止生成。 | WebSocket。 | SSE 足够支持首版单向文本流，复杂度更低。 | 长连接错误需要明确状态。 | APPROVED |
| DEC-005 | REQ-F-002, REQ-F-003, REQ-F-014, REQ-F-015, REQ-F-017, REQ-F-018, REQ-F-019 | 极简首页（标题独占顶部 + **左下角 ☰ 悬浮菜单**收纳「模型」「账号登录」与内联主题切换，CR-20260909-corner-menu）+ 全局底部悬浮输入条，发送后展开为受限高度（≤50vh，CR-20260909 由 65vh 收敛）的底部面板。悬浮条仅含状态灯 + 输入框 + 单个动作按钮 + 收起/展开控件（REQ-F-019），**不含 Provider/模型切换控件**（选择迁移到「模型」，见 DEC-010）。 | 右下客服窗、命令面板或全屏聊天；header 放功能按钮。 | 符合用户确认的底下浮窗对话；限高避免退化为通用聊天工具；浮窗收敛把模型选择责任交给「模型」弹窗；应用外壳（对话 + 菜单）都收到底部，页面本体保持干净。 | 移动宽度下需要避免遮挡和文本溢出；☰ 菜单与底部对话的层叠见 DEC-014。 | APPROVED |
| DEC-006 | REQ-F-014 | 主题用 CSS 自定义属性做 theme-aware：`:root` 为浅色基线，`prefers-color-scheme: dark` 与 `[data-theme="dark"]` 覆盖；选择存 localStorage，由 root layout 的内联脚本在首帧前应用。 | 只做单一浅色主题；或引入主题库。 | 满足“浅色默认 + 深色开关”且零依赖、无主题闪烁。 | 两套调色板都必须各自通过对比度门槛。 | APPROVED |
| DEC-007 | REQ-F-007, REQ-F-015 | 弹窗用原生 `<dialog>` + `showModal()`；设置弹窗与 `/settings/models` 复用同一 `ModelSettings` 组件。 | 自建 modal 或引入组件库。 | 平台原生提供遮罩、焦点陷阱、Esc 与焦点回归，无需依赖。 | jsdom 不实现 showModal，测试需回退到 open 属性。 | APPROVED |
| DEC-008 | REQ-F-016 | 助手回复用自写极简 Markdown→React 渲染器，输出 React 元素而非 innerHTML。 | 引入 react-markdown/marked+DOMPurify。 | 避免新增外部依赖（CONTROLS L3），且不经 innerHTML 天然无 HTML 注入面。 | 仅支持常用子集，表格/图片按字面文本呈现。 | APPROVED |
| DEC-009 | REQ-NF-004 | `next.config.mjs` 用 `NEXT_DIST_DIR` 隔离构建目录：交互 dev 用 `.next/`，smoke/e2e/verify 各用独立目录。 | 共用 `.next/` + 约定不并行。 | `next build` 与 `next dev` 共用 `.next/` 会互相污染 webpack 运行时（`/api/auth/*` 报 vendor-chunks MODULE_NOT_FOUND）。 | 需要每个非交互入口显式设 env。 | APPROVED |
| DEC-010 | REQ-F-006, REQ-F-007 | Provider 使用优先级：为每个 Provider 增加持久化的 `priority`（整数，越小越优先）；「配置」以有序列表 + 上移/下移维护；`runChatTurn` 调用新增 `resolveActiveProvider(userId)`——在「已启用且连接有效」的 Provider 中按 `priority` 升序取第一个，最高优先级连接无效时 fallthrough 到下一个；对话请求不再携带 `providerId`/`model`。 | 浮窗下拉切换器（原方案）；「设为当前」单选。 | 用户要求浮窗零选择、「配置」明确优先级；有序列表比单选更贴合「主用 A、备用 B」的心智。 | 需要一次 schema 迁移；`connected` 判定用存储态（secret 可解密 / local），与 REQ-F-018 的实时探测分离——发送时取存储态最高优先者，探测只驱动灯。 | APPROVED（P3 落地，CR-20260909：`store.ts` `migrateProviderPriority` + `resolveActiveProvider`，TEST-025/026） |
| DEC-011 | REQ-F-005, REQ-F-013, REQ-F-017 | 会话生命周期：会话自首条消息创建，`停止` 与 `新对话` 均结束会话（前端丢弃 `conversationId`，下一条消息触发新建）；正常完成的会话保持活动可续接；刷新恢复逻辑仅在「最近会话仍活动」时注水。「新对话后刷新空白」需要一个持久化信号。 | 每次 `停止`/`新对话` 立即在服务端建空会话记录。 | 用户明确「未主动结束则保留原会话，主动结束后刷新空白」；空会话记录会污染 recent 列表。 | **P3 定选**：客户端 `sessionStorage` 标记 `jarvis:chat-session-ended`。`FloatingChat` 在 `useState` 初始值处（`restored = !sessionEnded() && initialMessages.length > 0`）读取它，因此 SSR 首帧就按 ended 判定，不渲染旧会话——无首帧闪现。标记在 `停止`/`新对话` 时置位，在下一次 `start` 事件（新会话建立）时清除。 | APPROVED（P3 落地，CR-20260909，TEST-029/030；EV-2026-09-09-minimal-floating-chat-impl） |
| DEC-012 | REQ-F-018 | 状态灯四态：`检测中`（加载探测未回）/`灭`（无已启用 Provider 或全部探测失败）/`常亮`（≥1 连接有效）/`闪烁`（生成中）。首页加载时对每个已启用 Provider 发一次 `testProviderConnection`（复用 MOD-ADAPTER 既有探测），结果注水到首页并驱动灯态与 `resolveActiveProvider` 的 `connected` 判定。 | 灯只反映存储的上次「测试」结果（不主动探测）。 | 用户要求「每次进页面自动探测一次」，接受每个 Provider 一次出站请求的成本换灯的真实性。 | P3 落地：新增 `GET /api/providers/probe`（并行 `Promise.all` + `testProviderConnection` 内建 10s 超时）；`FloatingChat` 首屏渲染 `检测中`，在 `useEffect` 里 fetch probe 后落定灯态——首屏不阻塞。 | APPROVED（P3 落地，CR-20260909，TEST-027；ui-contract LB-06） |
| DEC-013 | REQ-F-019, REQ-F-003 | 折叠面板状态模型（CR-20260909-collapsible-panel）：`FloatingChat` 的 `expanded` 单 boolean 拆为 `hasTranscript`（派生 = `messages.length > 0`）与 `userCollapsed`（持久 boolean），`showTranscript = hasTranscript && !userCollapsed`。`userCollapsed` 存 `localStorage['jarvis:chat-collapsed']`，经一个 `setUserCollapsed` helper（try/catch，镜像 `markSessionEnded`）读写；显式折叠/展开与「发送」（置 false）更新它。折叠控件为 `.floating-chat__status` 行内首个真实 `<button>`。折叠为**瞬时切换，无高度动画**。 | 直接加第四个驱动到 `expanded`；折叠动画用 `grid-template-rows: 0fr↔1fr`。 | `expanded` 已由「发送/新对话/hydrate」三处驱动 + `rollbackOptimistic` 里的 `setExpanded`，再加折叠会到处打补丁——派生 `showTranscript` 是唯一真源。动画在 flex/grid 子项上 CSS 脆弱，v1 不值当。 | `localStorage` 服务端不可读：SSR 首帧按默认（`userCollapsed=false`，展开）渲染，client `useState` 初值处读取后校正——与 DEC-011（`sessionStorage`）同一模式，接受一帧内校正，不接受持久错误态。`userCollapsed=true` 但 `hasTranscript=false` 时按 `showTranscript=false` 呈现输入条形态，控件不显示（无「折叠了却空白」中间态）。 | APPROVED |
| DEC-014 | REQ-F-015, REQ-F-014 | 左下角 ☰ 悬浮菜单（CR-20260909-corner-menu）：新增 `src/components/CornerMenu.tsx`（`"use client"`，MOD-SETTINGS-UI），`position: fixed` 于左下角，`z-index` 高于底部对话。触发器为真实 `<button aria-haspopup aria-expanded>`；浮层为**非模态** disclosure（Esc 关 + 焦点回触发器 + 点击外部 light-dismiss）。`page.tsx` 把 `<ThemeToggle/>`、`<SettingsDialog/>`、`<AccountDialog/>` 作 `children` 传入 `CornerMenu`——`CornerMenu` 不感知 props 形状；三者的模态 `<dialog>` 经 top-layer 逃逸，DOM 位置无关。`ThemeToggle` 仅换宿主（`SettingsDialog` → `CornerMenu`），持久化机制不变。dev 指示器：`next.config.mjs` `devIndicators: { position: "bottom-right" }`——一处构建配置，组件无 `NODE_ENV` 分支。**z-index 刻度**（成文化）：底部对话 = 20，☰ 触发器 + 浮层 = 30，模态 `<dialog>` 由浏览器 top-layer 管理。 | 原生 `popover` 属性（自带 light-dismiss/Esc，但 jsdom 支持差）；组件里按 `NODE_ENV` 偏移躲 Next 指示器（架构角色反对——不可靠探测 + 组件带环境分支）。 | 单个 ☰ + 浮层比两个散按钮的底部冲突小；`children` 传入使 `CornerMenu` 与业务解耦；`devIndicators` 配置比组件分支干净。 | 浮层机制（原生 `popover` vs 自写 disclosure）留 P3；☰ 与折叠条在窄视口下的具体避让 P3 定。 | APPROVED |
| DEC-015 | REQ-F-025 | 技能生成的 HTML **不做沙箱隔离**（CR-20260909-skills）：F2 展示屏将以 `<iframe srcdoc={html}>` **不带 `sandbox` 属性**渲染。**信任面**：该 iframe 与宿主同源，`<script>` 可读 `localStorage`（`jarvis-theme` / `jarvis:chat-collapsed` / `jarvis:chat-session-ended`）、可 `fetch` 任意同源 API（`DELETE /api/providers/[id]`、`POST /api/chat/stream`、`GET /api/insights`）、可读 DOM/cookie。HTML 由大模型生成、经不可信技能文件夹内容诱导，等价于"在应用内执行第三方脚本"。**风险等级：高**。**缓解（当前）**：仅显式告知——known warning `skill-html-unsandboxed` + F2 首次渲染前不可关闭提示条（REQ-F-025 ②③）。**出口义务**：后续 CR 必须给该 iframe 加 `sandbox`（至少去掉 `allow-same-origin`）或改用独立 origin/Worker 渲染，届时 DEC-015 转为被取代。 | ① `<iframe sandbox>`（架构 R1 建议，用户否决）；② 服务端 CSP/脚本剥离（改变产物语义，用户要"原样 HTML"）。 | 用户 2026-09-09（msg 54-1）明确接受风险、要求先不做安全。本机单管理员、无多租户、技能由本人上传——现实攻击面主要是"技能文件夹被投毒 → 生成恶意 HTML"，在当前使用形态下由用户自负。 | **高**：未沙箱化 HTML 可完全操纵同源会话与 Provider 配置。已登记 known warning + 出口义务，不隐藏。 | APPROVED（用户接受风险；高危项，带出口义务） |
| DEC-016 | REQ-F-021, REQ-F-027 | 技能路由是**独立最小 LLM 调用**（CR-20260909-skills；CR-20260909-display-screen 扩 `display` 字段）：`src/lib/skills.ts` `routeTurn(userMessage, skills)` 用 `resolveActiveProvider` 解析出的同一 Provider 发**一次非流式** `/chat/completions`，system prompt 仅"给定技能名+描述列表，判断本条用户消息该用哪个技能、以及是否要求回到首页，只输出 JSON `{\"skill\": <name>\|null, \"display\": \"home\"\|null}`"，`max_tokens` 小、超时 10s。**不复用主对话上下文**（不带历史消息）。**Fail-open**：无可用 Provider / 超时 / 非法 JSON / 未知技能名 → 返回 `{skill: null, display: null}`，按普通对话继续，绝不打断发送。触发时机：`POST /api/chat/stream` 收到发送后、启动主流式前，**当且仅当** `listSkills(userId)` 非空时调用一次；`display` 字段判定不依赖技能存在（无技能也可回首页）。**F1（skills）与 F2（display-screen）P3 合并实现**——`src/lib/skills.ts` 的该函数从第一行代码起即命名 `routeTurn` 并返回两个字段（F1 阶段 `display` 恒 null 且无消费者，F2 起 `DisplayScreen` 消费），代码里从不出现旧命名、无后续 rename。 | ① 复用主模型 + 在主 system prompt 里塞"可选技能"（污染每轮上下文、加 token）；② 纯前端关键词匹配（不够鲁棒，用户要"像 ChatGPT 那样"）；③ 用户显式 `/skill` 前缀（用户否决了"拖到技能区"、要对话内自动）；④ `skill` 与 `display` 各发一次 LLM 调用（无谓翻倍）。 | 独立调用把路由成本与主对话解耦：主对话上下文干净、路由失败不影响对话。一次小 completion 同时出 `skill` + `display` 两个判定，延迟/费用可接受（仅在有技能时）。 | 多一次出站调用（有技能时每次发送）；路由判定不保证准确——由 fail-open + "仅当轮"边界（REQ-F-022 ③）兜底，误触发最多是多注入一次技能内容或多切一次展示。 | APPROVED（CR-20260909-skills R2/R3/R4；`display` 字段 CR-20260909-display-screen R1 锁定） |
| DEC-017 | REQ-F-015, REQ-F-026, REQ-F-027 | **动态展示屏数据流**（CR-20260909-display-screen）：①**存储**——单表 `display_state(id TEXT PRIMARY KEY, kind TEXT NOT NULL, ref_id TEXT, updated_at TEXT NOT NULL)`，**全局单行**（固定 `id='singleton'`）；`getDisplayState()` 无行则返回默认 `{kind:'home'}`，`setDisplayState({kind, refId})` 用 `INSERT ... ON CONFLICT(id) DO UPDATE`（get-or-create）。非 per-conversation、非 per-user。②**渲染**——`src/components/DisplayScreen.tsx`（`"use client"`）：`kind:'home'` → 标题视图（产品名 + 简介，**取代原 header `<h1>`**）；`kind:'insight'` → `<iframe srcDoc={html} className="display-screen__frame">`（**无 `sandbox`**，DEC-015）+ 其上不可关闭 `.display-screen__notice` 提示条。③**内容流**——`FloatingChat` 在收到 SSE 尾 `insight` 事件、或 `routeTurn` 返回 `display:'home'` 后 `window.dispatchEvent(new Event('jarvis:display-changed'))`；`DisplayScreen` `useEffect` 监听该事件 → `GET /api/display` 重取并重渲染。**无轮询、无独立 SSE/WebSocket**。④**首帧**——`page.tsx`（RSC）读 `getDisplayState()` 作 `DisplayScreen` 初始 props，SSR 首帧即正确内容；client 再订阅事件（SSR/hydration 一致性同 DEC-011）。⑤**写入点**——`POST /api/chat/stream` 路由层：`onInsight` 回调里 `insertInsight` 后 `setDisplayState({kind:'insight', refId: insightId})`；`routeTurn` 返回 `display:'home'` 时 `setDisplayState({kind:'home'})`。 | ① `display_state` 每会话一行（用户心智是"一块屏"，且切会话不该丢展示）；② 展示屏用 SSE 推送（对话已有 SSE，再开一条长连接不值当）；③ 轮询 `GET /api/display`（无谓请求）。 | 全局单行贴合"Jarvis 的动态显示屏"心智；事件 + 重取比推送简单、比轮询省；首帧 SSR 走已验证的 DEC-011 模式。 | `<iframe srcdoc>` 无 sandbox 的信任面见 DEC-015（高危，用户接受，带出口义务）；全局单行在未来多用户场景需改为 per-user（当前单管理员无此问题）。 | APPROVED（CR-20260909-display-screen；机制 R1 锁定，本 DEC 为其誊写） |

## CR-20260909-corner-menu 方案（架构角色，逐变化点）

| CP | 部署 | 后端 | 数据库 | 前端 | 可行性 |
|---|---|---|---|---|---|
| CP-1 REQ-F-015 验收重写 | 无 | 无 | 无 | `page.tsx` 删 `home__actions` div，header 只 `<h1>`；加 `<CornerMenu>{children}</CornerMenu>` | 高 |
| CP-2 「配置」→「模型」 | 无 | 无 | 无 | `SettingsDialog` 触发按钮文案；`page.tsx`/测试/e2e 里的 "配置" 字面量 | 高 |
| CP-3 主题内联 | 无 | 无 | 无 | `SettingsDialog` 删 `<section aria-label="外观">` + `ThemeToggle` 导入；`ThemeToggle` 渲染进 `CornerMenu` 浮层（组件本身不改，`localStorage['jarvis-theme']` + `data-theme` 机制不变） | 高 |
| CP-4 `CornerMenu` 组件 | 无 | 无 | 无 | 新组件：☰ `<button>` + 受控 `open` state + 浮层 `<div role="menu">` + `document` click-outside + `keydown` Esc + 焦点回触发器。机制 P3 定 | 高 |
| CP-5 `next.config.mjs` | 配置变更（`devIndicators`），无部署形态变化 | 无 | 无 | —— | 高（1 行） |
| CP-6 测试回归 | 无 | 无 | 无 | 见测试说明书；`saveProviderThroughSettingsDialog` / appearance / account e2e 流程经 ☰ | 高 |

**架构总判**：**零部署形态 / 零后端 / 零数据库变更**（`devIndicators` 仅是 dev UI 位置）。纯 MOD-SETTINGS-UI + `page.tsx` chrome 改动，无新依赖。DEC-014 APPROVED，P3 可实现。

## CR-20260909 变化点架构裁决

对产品需求说明书 CR-20260909 的每个变化点给出架构裁决，模块任务开发说明书据此逐点派生任务。

| 需求变化点 | 架构裁决 | 涉及模块 | 数据/接口影响 | 关联 DEC |
|---|---|---|---|---|
| 产品目标 / 用户范围 措辞 | 不涉及架构 | — | — | — |
| REQ-F-001 → DEFERRED | 既有决策修订：NextAuth 保留，`auth-guard` 旁路即当前身份来源；生产形态待定 | MOD-AUTH | 无 | DEC-001（改） |
| REQ-NF-002 → DEFERRED | 单用户下隔离约束空转，`store` 按身份 id 存取的实现不动 | MOD-DB | 无 | DEC-001（改） |
| REQ-F-002 悬浮条三元素 | 新增：组件收敛，删除子元素 | MOD-CHAT-UI | 前端组件结构；请求体去 `providerId`/`model` | DEC-005（改） |
| REQ-F-003 状态仅灯 + 50vh | 既有决策修订（限高）+ 新增（删状态文字） | MOD-CHAT-UI | `globals.css` `max-height`；`ui-contract` RF-09 阈值 | DEC-005（改） |
| REQ-F-004 携带全部历史 + system prompt | 既有决策已覆盖（`runChatTurn` 现行行为，TASK-010） | MOD-CHAT | 无（补文档） | DEC-004 |
| REQ-F-005 停止 = 结束会话 | 新增：会话生命周期语义变更（原停止后会话续接） | MOD-CHAT, MOD-CHAT-UI | 前端 `conversationId` 生命周期；无 schema 变更 | DEC-011（新） |
| REQ-F-006 Provider 优先级 | 新增：数据模型 + 解析算法 + 配置 UI | MOD-DB, MOD-PROVIDER, MOD-CHAT, MOD-SETTINGS-UI | **schema 迁移**（Provider +`priority`）；`GET/PATCH /api/providers`；`/api/chat/stream` 参数可选化 | DEC-010（新） |
| REQ-F-007 优先级有序管理 | 新增：设置组件增排序交互 | MOD-SETTINGS-UI | `PATCH /api/providers/[id]` 承载 reorder | DEC-010（新） |
| REQ-F-013 恢复条件收窄 | 新增：首页注水前置条件（会话是否活动） | MOD-CHAT-UI, MOD-CHAT | 无 schema；需要「会话已结束」的持久化信号 | DEC-011（新） |
| REQ-F-014 浅色简约视觉 | 既有决策修订：调色板/圆角/间距 token 重定，主题机制（DEC-006）不变 | MOD-CHAT-UI, MOD-SETTINGS-UI | `globals.css` token；`ui-contract` 静态规则 | DEC-006（措辞） |
| REQ-F-015 占位登录 | 既有决策已覆盖（`AccountDialog` 在 `authenticated=true` 分支已不渲染登录跳转，TASK-016） | MOD-CHAT-UI | 无 | DEC-001 |
| REQ-F-016 请求级错误红字 | 新增：错误分类（请求级 vs 流中）+ 新 UI 元素 | MOD-CHAT-UI, MOD-CHAT | `/api/chat/stream` 首段前失败以 HTTP 错误响应（非 SSE `error` 事件）；前端区分渲染 | DEC-004（细化） |
| REQ-F-017 开始新对话 | 新增：按钮状态机 + 清空逻辑 + 刷新空白持久化信号 | MOD-CHAT-UI | 无 schema；`sessionStorage` 或服务端标志位（P3 定） | DEC-011（新） |
| REQ-F-018 状态灯四态 + 加载探测 | 新增：首页 SSR 阶段并/串行探测 + 灯态状态机 | MOD-CHAT-UI, MOD-ADAPTER | 首页 loader 增探测调用；复用 `testProviderConnection` | DEC-012（新） |

**schema 变更清单**（本 CR 唯一）：`providers` 表新增 `priority INTEGER NOT NULL DEFAULT`，迁移时按 `rowid` 顺序回填。迁移写在 `src/lib/store.ts` 的 `migrate()`，向前兼容（旧库自动加列）。P3 实现 CR 落定后 DEC-010 转 APPROVED。

## CR-20260909-collapsible-panel 方案（架构角色，逐变化点）

架构角色对本 CR 每个变化点给出 部署 / 前端 / 后端 / 数据库 四面裁决与可行性判断。**变化点编号 CP-1..CP-8。**

| 变化点 | 部署 | 后端 | 数据库 | 前端 | 可行性 |
|---|---|---|---|---|---|
| CP-1 REQ-F-019 收起面板（整体） | 无变更（单进程、`npm run dev`/`next build` 不变、无新路由、无新 env） | 无接触面（折叠是纯 UI 态，零 API 调用、零服务端写） | 无 schema/迁移/新表列 | 见 CP-3..CP-7 | 高 |
| CP-2 REQ-F-003 验收放宽 | 无 | 无 | 无 | `floating-chat--expanded` class 由 `showTranscript` 派生取代无条件 `setExpanded(true)` | 高 |
| CP-3 DEC-013 状态模型 | 无 | 无 | 无 | `FloatingChat` 的 `expanded: useState` 拆为 `userCollapsed: useState`（持久）+ 派生 `hasTranscript = messages.length>0` + `showTranscript = hasTranscript && !userCollapsed`。删除全部 `setExpanded` 调用点（`handleSubmit`/`handleNewConversation`/`rollbackOptimistic`） | 高（派生态是 React 标准做法，净减状态机复杂度） |
| CP-4 折叠偏好持久化 | 无 | 无 | 无（存浏览器 `localStorage`，per-browser 语义在单机场景正确；跨设备同步为非目标） | 模块级 `chatCollapsed()` / `setChatCollapsed(v)`（try/catch 包 `localStorage`，键 `jarvis:chat-collapsed`，镜像现有 `sessionEnded()`/`markSessionEnded()`）。`useState(() => chatCollapsed())` 初值。**SSR/hydration**：`localStorage` 服务端不可读 → `chatCollapsed()` catch 返回 `false`（展开），client mount 时 initializer 读到真值 → 一帧内校正（`showTranscript` 从 true→false）。**与 DEC-011（`jarvis:chat-session-ended`）完全同模式**，已有 e2e 先例（TEST-030）证明 React 19 正确收敛，dev 模式有 hydration warning、生产 build 无——接受，不额外做双 render/`suppressHydrationWarning` | 高（有同项目先例） |
| CP-5 折叠控件 | 无 | 无 | 无 | `.floating-chat__status` 行内、`__light`/`__sr` 之后加 `<button className="floating-chat__toggle" aria-expanded={showTranscript} aria-label={showTranscript ? "收起对话" : "展开对话"}>`，内含一个 `aria-hidden` 的箭头字形（CSS 旋转 `▾`）。仅 `hasTranscript` 时渲染。触控 ≥24px（padding），`:focus-visible` 环，非文本对比 ≥3:1 | 高 |
| CP-6 折叠期间行为 | 无 | 无 | 无 | ①保持折叠 + `lightState = isStreaming ? "busy" : …`——折叠不改此计算，自然满足。②**完成瞬时提示**：新增 `justFinished` state，流结束处（done/error/abort）若 `userCollapsed` 则 `setJustFinished(true)` + `setTimeout(…,1200)` 清；`lightState` 增分支 `justFinished ? "done" : probeState`；CSS `.floating-chat__light--done` = 一次性脉冲（`animation-iteration-count: 2`）。③折叠时发送 → `handleSubmit` 开头 `setChatCollapsed(false)`（发送 = 隐式展开意图，同时更新持久偏好） | 高 |
| CP-7 v1 无动画 | 无 | 无 | 无 | 不给 `.floating-chat--expanded` 的 `max-height`/`min-height` 加 `transition`；`showTranscript` 切换 = 条件渲染 `.floating-chat__messages`，瞬时。`prefers-reduced-motion` 用户无差别 | 高（不做事） |
| CP-8 回归重验 | 无 | 无 | 无 | 见测试说明书；`setExpanded` 删除后 `floating-chat.test.tsx` 与 `human-workflow.spec.ts` 的「发送后展开」断言按 `showTranscript` 派生逻辑重跑 | 高 |

**架构总判**：本 CR **零部署 / 零后端 / 零数据库变更**，纯 MOD-CHAT-UI 组件内改动，无新依赖。唯一技术风险是 CP-3 删除 `setExpanded` 触碰既有 e2e（CP-8 守卫）。CP-4 的 SSR/hydration 走已验证的 DEC-011 模式。DEC-013 维持 APPROVED，P3 可直接实现。

## CR-20260909-skills 方案（架构角色，逐变化点）

架构角色对 F1（技能系统）的每个变化点给出 部署 / 后端 / 数据库 / 前端 / 依赖 五面裁决与可行性判断。**变化点编号 CP-1..CP-14**（产品 CP-1..CP-6 + 架构派生 CP-7/CP-8 + 模块派生 CP-9..CP-12 + 测试派生 CP-13/CP-14）。

| 变化点 | 部署 | 后端 | 数据库 | 前端 | 依赖 | 可行性 |
|---|---|---|---|---|---|---|
| CP-1 技能上传与注册 | 新增运行目录 `.data/skills/<name>/`（与 `.data/` DB 同级，已在 `.gitignore`）；无新进程/端口 | 新增 `POST /api/skills`（multipart：文件夹内文件 + 相对路径）；服务端用 `node:fs/promises` 落盘到 `.data/skills/<slug>/`（`slug` = 文件夹名 sanitized，冲突则拒绝并提示改名）；随后触发 SKILL.md 生成（CP-9 Ⅱ）；成功后写 `skills` 表 | **新表 `skills`**（见下 schema 清单） | MOD-CHAT-UI 在底部对话框加**文件夹拖放**监听（`dragover`/`drop`，`DataTransferItem.webkitGetAsEntry()` 递归读目录，或 `<input type=file webkitdirectory>` 兜底），POST 到 `/api/skills`，回执后在记录区插一条"已注册技能：<name> — <description>"系统消息 | 无（`node:fs` 内置） | 高 |
| CP-2 按发送路由技能 | 无 | `POST /api/chat/stream` 在启动主流式前，`listSkills(userId)` 非空时调用 `routeTurn()`（DEC-016，一次非流式 completion）；结果决定本轮 `skill` 参数 | 无 | 无（纯服务端） | 无 | 高 |
| CP-3 技能内容注入 | 无 | `src/lib/skills.ts` `resolveSkillForTurn(name)`：读 `SKILL.md` 正文 + 遍历技能目录内扩展名 ∈ 白名单（`.md .txt .json .csv .yaml .yml .py .js .ts .tsx .html`）的文件，按文件名排序拼接（`\n\n=== <相对路径> ===\n` 分隔），累计 > 32KB 截断并在尾部追加 `\n\n[技能内容超过 32KB，已截断]`；返回字符串交 `runChatTurn` 作**该轮附加 system 段**（拼在固定 system prompt 之后、历史消息之前） | 无 | 无 | 无 | 高 |
| CP-4 技能 HTML 产出捕获 | 无 | `runChatTurn` 在助手消息**完整落库后**扫描其文本：取最后一个 ` ```html ` … ` ``` ` 闭合围栏内容 → 调 `onInsight?(html)`；无闭合围栏但有开围栏 → `onInsight` 不调用、回调 `onInsightMissing?("incomplete")`；完全无围栏 → `onInsightMissing?("none")`。仅当本轮 `skill` 非空时启用捕获 | 无（写库在路由处理器，CP-5） | MOD-CHAT-UI 据 `start`/`done` 后的一条元信息或 SSE 尾事件，在记录区插系统消息："已生成洞察，可在展示屏查看" / "本轮未产出 HTML" / "HTML 不完整" | 无 | 高 |
| CP-5 洞察结果存储与读回 | 无 | 路由处理器在 `onInsight` 回调里 `insertInsight({conversationId, kind:"skill", html})`；新增 `GET /api/insights?conversationId=<id>` → 该会话 `insights` 行按 `created_at` 倒序（属主校验，非属主 404，未登录 401） | **新表 `insights`**（见下 schema 清单） | 无（F1 只提供读回入口；渲染是 F2） | 无 | 高 |
| CP-6 安全隔离为当前非目标 | 无 | 无 | 无 | 无（提示条是 F2 的 DisplayScreen） | 无 | 高（F1 侧只落 known warning `skill-html-unsandboxed` + DEC-015；首渲提示条为 F2 交付） |
| CP-7 未沙箱化 `<iframe srcdoc>` → 高危 DEC | 无 | 无 | 无 | F2 渲染约束（本 CR 只立 DEC） | 无 | **DEC-015 立项**：记录信任面 + 风险等级高 + 出口义务（后续 CR 必须沙箱化）。用户接受风险 → 状态 APPROVED（高危项） |
| CP-8 路由为独立最小 LLM 调用 | 无 | 见 CP-2 + DEC-016：非流式、小 `max_tokens`、10s 超时、不带历史、fail-open | 无 | 无 | 无 | **DEC-016 立项**，APPROVED。可行性高 |
| CP-9 CP-1 拆 3 子任务 | —（模块层） | —（模块层） | —（模块层） | —（模块层） | — | 高（架构认可拆分：上传/存储、SKILL.md 生成、注册三关注点边界清晰，见模块说明书 TASK-033） |
| CP-10 HTML 捕获经 `onInsight?` 回调 | 无 | `runChatTurn` 签名加可选 `onInsight?(html: string)` 与 `onInsightMissing?(reason)`；**MOD-CHAT 只负责调用回调，不 import `insights`/`display`**——写库在 MOD-CHAT（路由处理器层）注入的回调实现里，保持 `runChatTurn` 无持久化副作用扩张 | 无 | 无 | 无 | 高（回调注入是既有 `onComplete(content,status)` 的同类扩展） |
| CP-11 路由 + 技能解析集中在 `src/lib/skills.ts` | 无 | **新增 MOD-SKILLS**（`src/lib/skills.ts`）：`listSkills` / `registerSkill` / `generateSkillDoc` / `routeTurn` / `resolveSkillForTurn`。`runChatTurn` 仅接收组装好的 `skill?` 字符串，不感知技能存储 | 无 | 无 | 无 | 高（模块边界清晰，见"模块边界"表新增行） |
| CP-12 拆 F1 / F2 两个 CR | —（流程） | —（流程） | —（流程） | —（流程） | — | 高（F1 = 本 CR：注册/路由/注入/捕获/存储+读回；F2 = CR-20260909-display-screen：全屏 DisplayScreen + `display_state` + "显示首页"，未起草） |
| CP-13 每个 CP 有真实入口测试 | —（测试层） | —（测试层） | —（测试层） | —（测试层） | — | 高（测试说明书 TEST-034..038 覆盖，见测试设计表） |
| CP-14 per-message 触发不延续 + 无技能路径回归 | 无 | 路由每次发送独立调用、`skill` 参数仅传给当轮 `runChatTurn`，天然"仅当轮"；无技能时不调用 `routeTurn`、`runChatTurn` 不带 `skill` → 行为与 CR 前完全一致 | 无 | 无 | 无 | 高（架构上"仅当轮"是默认，非额外机制） |

### schema 变更清单（CR-20260909-skills）

两张新表，写在 `src/lib/store.ts` 的 `migrate()`，用 `CREATE TABLE IF NOT EXISTS`，对旧库幂等：

```sql
CREATE TABLE IF NOT EXISTS skills (
  id           TEXT PRIMARY KEY,
  user_id      TEXT NOT NULL,
  name         TEXT NOT NULL,
  description  TEXT NOT NULL,
  dir_path     TEXT NOT NULL,
  created_at   TEXT NOT NULL,
  UNIQUE(user_id, name)
);
CREATE TABLE IF NOT EXISTS insights (
  id               TEXT PRIMARY KEY,
  conversation_id  TEXT NOT NULL,
  kind             TEXT NOT NULL,          -- F1 固定 "skill"；扩展点
  html             TEXT NOT NULL,
  created_at       TEXT NOT NULL
);
```

`skills.dir_path` 是 `.data/skills/<slug>/` 的相对路径。`insights` 不设外键（`node:sqlite` 默认不强制 FK），按 `conversation_id` 查询，属主校验在路由层经 `conversations` 表完成。F2 将新增 `display_state` 单行指针表，不在本 CR。

### 架构总判（CR-20260909-skills）

- **部署**：新增 `.data/skills/` 运行目录，无新进程/端口/env；`npm run dev` 形态不变。
- **后端**：新增 `POST /api/skills`、`GET /api/insights`；`POST /api/chat/stream` 增一个路由前置步骤（有技能时）。
- **数据库**：两张新表（`skills`、`insights`），幂等迁移，旧库自动建表。
- **前端**：MOD-CHAT-UI 增文件夹拖放 + 三类系统消息；无新组件（F1）。
- **依赖**：**零新增运行依赖**（`node:fs` 内置）。
- **信任面**：DEC-015（未沙箱化 HTML，高危，用户接受 + 出口义务）、DEC-016（独立路由调用，fail-open）。
- DEC-015 / DEC-016 APPROVED，P3 可实现。

## CR-20260909-display-screen 方案（架构角色，逐变化点）

架构角色对 F2（动态展示屏）的每个变化点给出 部署 / 后端 / 数据库 / 前端 / 依赖 五面裁决与可行性判断。**变化点编号 CP-1..CP-15**（产品 CP-1..CP-7 + 架构派生 CP-8..CP-11 + 模块派生 CP-12/CP-13 + 测试派生 CP-14/CP-15）。

| 变化点 | 部署 | 后端 | 数据库 | 前端 | 依赖 | 可行性 |
|---|---|---|---|---|---|---|
| CP-1 首页 = 全屏 DisplayScreen | 无 | `GET /api/display` 新增（读 `display_state`，属主/单管理员） | `display_state` 单行表（DEC-017） | `page.tsx` 首页重写：`<DisplayScreen initial={state}>` 作全屏基底，`<FloatingChat>` / `<CornerMenu>` 浮其上（z-index：DisplayScreen 基底 / FloatingChat 20 / CornerMenu 30） | 无 | 高（层叠关系与 corner-menu DEC-014 一脉相承） |
| CP-2 默认标题视图，header `<h1>` 迁入 | 无 | 无 | 无 | `DisplayScreen` `kind:'home'` 渲染标题视图（产品名 `<h1>` + 一句话简介）；`page.tsx` 删原 `<header><h1>`——标题只在展示屏 home 视图出现 | 无 | 高 |
| CP-3 技能洞察自动上屏 | 无 | `POST /api/chat/stream` 路由层：`onInsight` 写 `insights` 后 `setDisplayState({kind:'insight', refId})`（DEC-017 ⑤） | `display_state` 行更新 | `FloatingChat` 收到 SSE 尾 `insight` 事件 → `dispatchEvent('jarvis:display-changed')`；`DisplayScreen` 重取 `GET /api/display` → 渲染 `insights[refId].html` | 无 | 高 |
| CP-4 跨刷新持久 | 无 | `GET /api/display` 返回当前 `display_state` + 若 `kind:'insight'` 附 `insights` 行 html | `display_state` 持久 | `page.tsx` RSC 首帧读 `getDisplayState()` → SSR 即正确内容，刷新不回标题（DEC-017 ④） | 无 | 高（同 DEC-011 SSR 模式） |
| CP-5 「显示首页」指令回标题 | 无 | 路由层：`routeTurn` 返回 `display:'home'` → `setDisplayState({kind:'home'})`（DEC-017 ⑤） | `display_state` 行更新 | `FloatingChat` 据 `display:'home'`（SSE 尾事件或响应元信息）→ `dispatchEvent('jarvis:display-changed')` → `DisplayScreen` 重取 → home 视图 | 无 | 高 |
| CP-6 `kind` 扩展点 | 无 | `GET /api/display` 按 `kind` 分派；未知 `kind` → 回退 `home` | `display_state.kind` 自由文本 | `DisplayScreen` `switch(kind)`：F2 实现 `home`/`insight`，`default` → home 视图（不崩） | 无 | 高（扩展点是 switch + 默认分支，零成本） |
| CP-7 不可关闭安全提示条 | 无 | 无 | 无 | `kind:'insight'` 时 `<div className="display-screen__notice" role="note">` 在 `<iframe>` 上方：无 close 按钮、无 `onKeyDown` Esc 处理、`kind` 非 `insight` 时不渲染。文案固定（内容由大模型生成、未隔离） | 无 | 高 |
| CP-8 `display_state` 全局单行（DEC-017 ①） | 无 | `getDisplayState`/`setDisplayState` in `src/lib/display.ts`；固定主键 `'singleton'` + `ON CONFLICT DO UPDATE` | 单表单行 | 无 | 无 | 高 |
| CP-9 事件流无轮询/SSE（DEC-017 ③） | 无 | 无（`GET /api/display` 是普通拉取，由事件触发，非轮询） | 无 | `window` 自定义事件 `jarvis:display-changed`；`DisplayScreen` `useEffect` 加/removeEventListener | 无 | 高 |
| CP-10 `routeTurn` 扩 `display`（DEC-016 修订） | 无 | `routeTurn` 一次调用返回 `{skill, display}`；`display` fail-open（判不出 → null → 展示屏不变） | 无 | 无 | 无 | 高（F1 已把函数命名为 `routeTurn`；F2 只是让路由层读 `display` 字段并写 `display_state`） |
| CP-11 未沙箱化 iframe 实际渲染（DEC-015 从"立项"到"渲染 + 提示条"） | 无 | 无 | 无 | `<iframe srcDoc={html}>` **无 `sandbox`**；宿主页对该 iframe **不加额外 CSP**（用户接受）。DEC-015 出口义务：后续 CR 加 `sandbox` / 独立 origin | 无 | 高（渲染本身 1 行；风险由 DEC-015 承载） |
| CP-12 `page.tsx` 首页重写按 2 子项 | —（模块层） | —（模块层） | —（模块层） | —（模块层） | — | 高（架构认可拆分：Ⅰ `DisplayScreen` + `src/lib/display.ts` + `GET /api/display` + 事件流；Ⅱ `page.tsx` 布局重写 + z-index + TEST-021/032 回归。见模块说明书 TASK-037/038） |
| CP-13 client 组件 + SSR 首帧 + 事件派发 | 无 | 无 | 无 | `DisplayScreen` `"use client"`，props 带 `initial`（SSR 注水）；`FloatingChat` 在 `insight` 尾事件 / `display` 指令处 `dispatchEvent`——`FloatingChat` 只发事件、不 import `DisplayScreen`（解耦） | 无 | 高 |
| CP-14 每个可观察行为有真实入口测试 | —（测试层） | —（测试层） | —（测试层） | —（测试层） | — | 高（测试说明书 TEST-039..042，见测试设计表） |
| CP-15 REQ-F-015 重写回归门 | 无 | 无 | 无 | TEST-021（首页仅标题 + ☰）/ TEST-032（首页菜单）断言随首页结构反转——作回归门重写，须真覆盖"全屏 DisplayScreen + 浮层"新结构 | 无 | 高（回归门，见测试说明书） |

### 架构总判（CR-20260909-display-screen）

- **部署**：无变化（单进程、`npm run dev` 不变、无新 env）。
- **后端**：新增 `GET /api/display`；`POST /api/chat/stream` 路由层增两处 `setDisplayState` 写入（insight 落库后 / `display:'home'` 时）。
- **数据库**：一张新单行表 `display_state`（DEC-017，幂等迁移）。
- **前端**：`page.tsx` 首页重写（大改）+ 新增 `DisplayScreen` 组件 + `src/lib/display.ts`；`FloatingChat` 加 `jarvis:display-changed` 派发（只发事件，不 import DisplayScreen）。
- **依赖**：**零新增运行依赖**。
- **信任面**：DEC-015 在此 CR 从"立项"变"实际渲染"——`<iframe srcDoc>` 无 sandbox，唯一缓解是不可关闭常驻提示条；出口义务（后续 CR 沙箱化）保留。
- 新增 **MOD-DISPLAY**（见"模块边界"表新增行）；DEC-017 立项。DEC-016 修订（加 `display` 字段）。
- DEC-017 APPROVED，P3 可实现。

## 模块边界

| 模块 ID | 模块 | 职责 | 覆盖需求 |
|---|---|---|---|
| MOD-AUTH | 身份与会话 | 身份态读取、页面/API 访问控制；当前版本单一本地管理员形态（`JARVIS_TEST_USER_ID` 旁路），NextAuth Google 登录代码保留但暂缓 | REQ-F-001（暂缓）, REQ-NF-002（暂缓） |
| MOD-DB | 本地持久化 | SQLite schema、身份、Provider（含 `priority`）、会话、消息、加密字段 | REQ-F-006, REQ-F-009, REQ-F-013, REQ-NF-001 |
| MOD-PROVIDER | Provider 管理 | Provider CRUD、认证模式、连接测试、脱敏返回、**使用优先级维护与解析**（`resolveActiveProvider`） | REQ-F-006, REQ-F-007, REQ-F-008, REQ-F-009, REQ-NF-003 |
| MOD-ADAPTER | 模型适配 | OpenAI、DeepSeek、本地 OpenAI-compatible 流式调用归一、连接探测 | REQ-F-010, REQ-F-011, REQ-F-012, REQ-F-018 |
| MOD-CHAT | 对话 API | SSE 流式接口、按优先级解析 Provider、停止即结束会话、会话生命周期、会话消息保存、请求级与流中错误区分、**技能路由前置调用 + 技能内容注入当轮 system 段 + 技能 HTML 产出捕获（`onInsight` 回调）+ `insights` 写入 + `GET /api/insights` 读回**（CR-20260909-skills） | REQ-F-004, REQ-F-005, REQ-F-006, REQ-F-013, REQ-F-017, REQ-F-021, REQ-F-023, REQ-F-024, REQ-NF-001 |
| MOD-CHAT-UI | 悬浮对话 UI | 底部输入条（状态灯 + 输入框 + 单按钮）、展开面板（限高 ≤50vh）、**面板收起/展开 + 偏好持久化**、**状态灯四态**、**新对话动作**、**请求级错误红字行**、Markdown 渲染、浅色简约视觉与主题切换、**技能文件夹拖放上传 + 「已注册技能」/「本轮未产出 HTML」/「HTML 不完整」系统消息**（CR-20260909-skills）、**收到 `insight` 尾事件 / `display` 指令时派发 `jarvis:display-changed` 事件**（CR-20260909-display-screen，只发事件不 import DisplayScreen） | REQ-F-002, REQ-F-003, REQ-F-014, REQ-F-015, REQ-F-016, REQ-F-017, REQ-F-018, REQ-F-019, REQ-F-020, REQ-F-023, REQ-F-026 |
| MOD-SETTINGS-UI | 设置 UI | Provider 设置（首页「模型」弹窗与 `/settings/models` 深链共用组件）、**优先级有序管理（上移/下移）**、**左下角 ☰ 悬浮菜单（`CornerMenu`）**、外观主题开关（`ThemeToggle`，宿主为 ☰ 菜单）、认证模式展示、连接测试、**`page.tsx` 首页装配（全屏 `DisplayScreen` 基底 + `FloatingChat` + `CornerMenu` 浮层，z-index 层叠）**（CR-20260909-display-screen） | REQ-F-006, REQ-F-007, REQ-F-008, REQ-F-009, REQ-F-014, REQ-F-015 |
| MOD-SKILLS | 技能系统（CR-20260909-skills） | 技能注册（`registerSkill` / `listSkills`）、`SKILL.md` 生成（`generateSkillDoc`，LLM 调用 + frontmatter 解析 + 失败回退）、按发送路由（`routeTurn`，独立最小 LLM 调用，DEC-016，fail-open，返回 `{skill, display}`）、技能内容组装（`resolveSkillForTurn`，白名单 + 32KB 截断，仅当轮）。`src/lib/skills.ts`。不碰 `insights` / `display_state` / 会话持久化 | REQ-F-020, REQ-F-021, REQ-F-022, REQ-F-027 |
| MOD-DISPLAY | 动态展示屏（CR-20260909-display-screen） | 全屏展示屏渲染（`DisplayScreen`，`kind:home`/`insight` + 扩展点）、`display_state` 全局单行读写（`src/lib/display.ts` `getDisplayState`/`setDisplayState`，get-or-create）、`GET /api/display`、`jarvis:display-changed` 事件订阅、未沙箱化 `<iframe srcDoc>` 渲染 + 不可关闭安全提示条。不发起 LLM 调用、不碰会话消息 | REQ-F-015, REQ-F-026, REQ-F-027 |

## 接口契约

### Provider

```ts
type ProviderAuthMode = "oauth" | "api_key" | "local" | "unsupported";
type ProviderKind = "openai" | "deepseek" | "local";

type ProviderSummary = {
  id: string;
  name: string;
  kind: ProviderKind;
  authMode: ProviderAuthMode;
  baseUrl: string | null;
  defaultModel: string;
  enabled: boolean;
  connected: boolean;
  priority: number;          // CR-20260909：越小越优先；新启用者取当前最大 + 1
  secretPreview: string | null;
};
```

`resolveActiveProvider(userId)`（MOD-PROVIDER，CR-20260909）：返回 `enabled && connected` 的 Provider 中 `priority` 最小者及其 `defaultModel`，皆不满足时返回 `null`（→ 状态灯灭、发送被拦截）。`connected` 取 REQ-F-018 加载探测的当次结果。

### ChatDelta

```ts
type ChatDelta =
  | { type: "start"; conversationId: string; messageId: string }
  | { type: "delta"; text: string }
  | { type: "stopped" }
  | { type: "error"; message: string }
  | { type: "done"; messageId: string }
  // CR-20260909-skills：技能轮的产出元信息，附在 done 之后
  | { type: "insight"; insightId: string }
  | { type: "insight-missing"; reason: "none" | "incomplete" };
```

### 技能与洞察（CR-20260909-skills）

```ts
type SkillSummary = { id: string; name: string; description: string };

// MOD-SKILLS（src/lib/skills.ts）
function listSkills(userId: string): SkillSummary[];
function registerSkill(userId: string, folder: UploadedFile[]): Promise<{ id: string; name: string; description: string; docGenerated: boolean }>;
function generateSkillDoc(folder: UploadedFile[]): Promise<{ name: string; description: string; body: string } | null>; // null → 回退到文件夹名
function routeTurn(userMessage: string, skills: SkillSummary[]): Promise<{ skill: string | null; display: "home" | null }>; // DEC-016；任何失败 → { skill: null, display: null }（fail-open）。F1 只读 skill；F2（display-screen）读 display
function resolveSkillForTurn(name: string): Promise<string>; // SKILL.md + 白名单文本文件，≤32KB

// MOD-CHAT：runChatTurn 新增可选参数
type RunChatTurnInput = {
  /* …既有字段… */
  skill?: string;                                  // 命中技能的当轮附加 system 段；缺省即普通对话
  onInsight?: (html: string) => void | Promise<void>;      // 助手消息落库后，最后一个 ```html``` 闭合块
  onInsightMissing?: (reason: "none" | "incomplete") => void;
};
```

`insertInsight({ conversationId, kind: "skill", html })`（MOD-CHAT 路由层，非 `runChatTurn` 内部）→ `insights` 表。`GET /api/insights?conversationId=` 读回。

### 动态展示屏（CR-20260909-display-screen）

```ts
type DisplayKind = "home" | "insight" | (string & {});   // kind 扩展点
type DisplayState = { kind: DisplayKind; refId: string | null; updatedAt: string };

// MOD-DISPLAY（src/lib/display.ts）
function getDisplayState(): DisplayState;                 // 无行 → { kind:"home", refId:null }
function setDisplayState(next: { kind: DisplayKind; refId?: string | null }): void;  // get-or-create（固定主键）

// GET /api/display → { kind, refId, html }（kind:"insight" 时 html = insights[refId].html）
```

`routeTurn`（DEC-016）返回 `{ skill, display }`；路由层：`display === "home"` → `setDisplayState({kind:"home"})`；`onInsight` 落库后 → `setDisplayState({kind:"insight", refId: insightId})`。`FloatingChat` 两处触发后 `window.dispatchEvent(new Event("jarvis:display-changed"))`；`DisplayScreen` 监听 → `GET /api/display` 重取。

### API

| API | 方法 | 说明 | 覆盖需求 |
|---|---|---|---|
| `/api/providers` | GET | 返回当前身份 Provider 摘要（含 `priority`，按 `priority` 排序） | REQ-F-006, REQ-F-007, REQ-F-009 |
| `/api/providers` | POST | 新增或更新 Provider 配置（可含 `priority`；`PATCH /api/providers/[id]` 用于上移/下移调整优先级） | REQ-F-006, REQ-F-007, REQ-F-008, REQ-F-009 |
| `/api/providers/test` | POST | 测试单个 Provider 连通性（设置页「测试」按钮） | REQ-F-007, REQ-F-010, REQ-F-011, REQ-F-012 |
| `/api/providers/probe` | GET | 并行探测全部已启用 Provider，返回 `{ anyConnected, providers:[{id,ok}] }`，驱动悬浮条状态灯（CR-20260909） | REQ-F-018 |
| `/api/conversations/recent` | GET | 返回当前身份最近会话 | REQ-F-013 |
| `/api/chat/stream` | POST | 返回 SSE 流式模型回复；**请求体不再要求 `providerId`/`model`**，服务端按 `resolveActiveProvider` 解析（可选传入用于覆盖）；回复第一段到达前失败 → HTTP 错误响应（前端渲染为输入框上方红字，REQ-F-016）；`停止` → `stopped` 且会话结束（REQ-F-005）；**`listSkills` 非空时先调 `routeTurn` 判定本轮技能，命中则 `resolveSkillForTurn` 组装内容作当轮 system 段；助手消息落库后捕获最后一个 ```html``` 块 → 写 `insights`；SSE 尾部附一条技能产出元信息（`insight` / `insight-missing`）**（CR-20260909-skills） | REQ-F-004, REQ-F-005, REQ-F-006, REQ-F-016, REQ-F-017, REQ-F-021, REQ-F-023 |
| `/api/skills` | POST | multipart 上传技能文件夹（文件 + 相对路径）；落盘 `.data/skills/<slug>/`，调 `generateSkillDoc` 生成 `SKILL.md`（frontmatter 解析失败 → `name`=文件夹名、`description`=「（未生成描述）」），写 `skills` 表；返回 `{ id, name, description, docGenerated }`；slug 冲突 → 409 提示改名 | REQ-F-020 |
| `/api/skills` | GET | 返回当前身份已注册技能 `{ id, name, description }[]`（驱动前端确认与后续管理） | REQ-F-020 |
| `/api/insights` | GET | `?conversationId=<id>`：返回该会话 `insights` 行 `{ id, kind, html, createdAt }[]`（`created_at` 倒序）；非属主 404、未登录 401——F1 的真实读回入口（原则 13），F2 的 DisplayScreen 消费 | REQ-F-024 |
| `/api/display` | GET | 返回全局 `display_state`：`{ kind: "home" \| "insight" \| string, refId: string \| null, html: string \| null }`（`kind:"insight"` 时附 `insights[refId].html`）；无行 → `{ kind:"home", refId:null, html:null }`。未登录 401（CR-20260909-display-screen） | REQ-F-015, REQ-F-026 |

## 外部 API 证据

| 来源 | 关键事实 | 影响 |
|---|---|---|
| OpenAI 官方 API 文档 | OpenAI API 使用 API key/Bearer 认证，Responses API 支持 `stream: true`。 | OpenAI 首版采用服务端 API key 托管和 Responses 流式。 |
| DeepSeek 官方 API 文档 | DeepSeek API 使用 Bearer Auth，提供 OpenAI 兼容 `base_url=https://api.deepseek.com`，可设置 `stream`。 | DeepSeek 首版采用 OpenAI-compatible adapter。 |

## 多角色评审

| 角色 | 检查重点 | 反馈 | 处理结果 | 结论 |
|---|---|---|---|---|
| 产品 owner | 是否覆盖产品需求 | 架构必须保留第三方账号授权能力，但不能承诺非官方 OAuth。 | ProviderAuthMode 纳入 OAuth/API Key/Local/Unsupported。 | APPROVED |
| 架构角色 | 模块边界与接口 | SSE 足够首版文本流式；WebSocket 延后。 | DEC-004 固化为 SSE。 | APPROVED |
| 开发角色 | 可实现性 | 本地开发 OAuth 需要环境变量配置。 | 在风险中记录 Google OAuth client 依赖。 | APPROVED |
| 测试角色 | 可验证性 | 需要真实入口冒烟覆盖 UI、设置、流式对话。 | 测试说明书必须包含真实 Next.js 应用冒烟。 | APPROVED |

### CR-20260909-minimal-floating-chat 评审

| 角色 | 检查重点 | 反馈 | 处理结果 | 结论 |
|---|---|---|---|---|
| 架构角色（自审） | 文档是否明确本角色定位、技术栈与部署形态 | 首版架构说明书缺技术栈、部署方式、依赖边界的显式记录，只有 DEC 表；本角色定位未成文 | 新增「架构角色定位与文档范围」「技术栈与部署形态」两节；新增「CR-20260909 变化点架构裁决」表，对 14 个需求变化点逐点给出「不涉及架构 / 既有决策已覆盖 / 新增或修订 DEC」裁决 | APPROVED |
| 产品 owner | 架构是否仍覆盖暂缓需求的恢复路径 | NextAuth 代码保留、DEC-001 标 DEFERRED 而非删除，恢复只需配 OAuth 变量 + 移除旁路 | 记入 DEC-001 与 MOD-AUTH 职责 | APPROVED |
| 架构角色 | 新增 DEC-010/011/012 是否越出模块边界 | 优先级解析属 MOD-PROVIDER、会话生命周期属 MOD-CHAT、状态灯探测复用 MOD-ADAPTER，均在既有边界内；`priority` schema 迁移与「新对话刷新空白」持久化信号列为 P3 待决点并已在 DEC 中标注 | DEC-010/011/012 记为「待评审」，P3 实现 CR 落定后转 APPROVED | APPROVED |
| 开发角色 | `/api/chat/stream` 去掉必填 `providerId` 的兼容性 | 现有前端会传 `providerId`；改为「可选覆盖」向后兼容，服务端缺参时走 `resolveActiveProvider` | 记入接口契约表 | APPROVED |
| 测试角色 | 加载探测的出站请求成本与可测性 | 每 Provider 一次探测，串行 + 超时上限 P3 定；TEST-027 用 mock adapter 断言四态映射，不打真实网络 | 记入测试说明书 TEST-027 | APPROVED |
| 架构角色 | 请求级错误的传输层裁决 | 原 DEC-004 只定义 SSE `error` 事件；「回复第一段前失败」应以 HTTP 错误状态返回（连 SSE 流都未建立），前端据 `response.ok` 分流——这是 DEC-004 的细化，非新 DEC | 记入变化点裁决表 REQ-F-016 行与接口契约 `/api/chat/stream` | APPROVED |

### CR-20260909-collapsible-panel 评审

| 角色 | 反馈 | 处理结果 | 结论 |
|---|---|---|---|
| 架构角色 | REQ-F-019 的 `expanded` 状态过载是本 CR 的核心技术风险；`localStorage` 与 SSR 的一致性须与 DEC-011 保持同一模式 | 立 DEC-013：`expanded`→`hasTranscript` + `userCollapsed`，`showTranscript` 为唯一真源；SSR 首帧默认渲染 + hydration 校正；瞬时切换无动画 | APPROVED |
| 模块开发角色 | 任务须含状态重构而非 bolt-on；持久化集中一个 helper | 记入 DEC-013 与 TASK-030 | APPROVED |
| 产品 owner | 折叠纯 UI 态，不得影响会话生命周期 | DEC-013 明确与 `conversationId`/`jarvis:chat-session-ended`/「新对话」正交 | APPROVED |
| 测试角色 | 状态重构会碰既有 e2e | DEC-013 落地后 TEST-030 与「发送后展开」e2e 须重验（记入测试说明书） | APPROVED |

### CR-20260909-corner-menu 评审

| 角色 | 反馈 | 处理结果 | 结论 |
|---|---|---|---|
| 架构角色 | ☰ 菜单是首页 chrome，需明确模块归属、z-index 层级、dev 指示器处理方式 | DEC-014：`CornerMenu` 归 MOD-SETTINGS-UI；z-index 刻度成文（对话 20 / ☰+浮层 30 / 模态 top-layer）；`next.config.mjs devIndicators` 而非组件 `NODE_ENV` 分支 | APPROVED |
| 模块开发角色 | `CornerMenu` 应与业务解耦 | `page.tsx` 把三个组件作 `children` 传入，`CornerMenu` 不感知 props | APPROVED |
| 产品 owner | 「配置」弹窗删外观分区后是否还成立 | 与 `/settings/models` 深链（本就只有 ModelSettings）对齐，更一致 | APPROVED |
| 测试角色 | 浮层是新交互模式 | 需 `aria-haspopup`/`aria-expanded` + Esc + 焦点回归断言（TEST-032）；机制留 P3 不影响验收面 | APPROVED |

### CR-20260909-skills 评审（R2，架构说明书）

各角色以本职说明书 + 行业惯例对本节「CR-20260909-skills 方案」表 + DEC-015/016 独立评审。逐 CP 裁决见 CR 的 `## R2 评审矩阵`。

| 角色 | 反馈（本角色视角） | 处理结果 | 结论 |
|---|---|---|---|
| 架构角色（自审） | ①两张新表幂等迁移，`node:sqlite` 无框架，`CREATE TABLE IF NOT EXISTS` 足够；`insights` 不设 FK 与既有实践一致。②技能目录落 `.data/skills/`（文件系统）而非 BLOB 入库——文件夹可含多文件、便于人工检视，DB 只存元数据。③DEC-016 路由为独立非流式调用，不污染主上下文，fail-open 保证不打断——关键约束成文。④DEC-015 未沙箱化是**高危**且是新信任面，必须显式记录信任面清单 + 出口义务，不能只写"用户接受"。 | DEC-015 补全信任面枚举（`localStorage` 键、可打的 API）+ 出口义务；DEC-016 写明 10s 超时 / 小 `max_tokens` / 不带历史 | APPROVED（DEC-015 为高危项，带出口义务） |
| 产品 owner | 方案未引入 CR 未声明的用户可观察行为；"已注册技能：<name>"确认符合 msg 54-2"不回显全文"；`GET /api/skills` 为后续技能管理留口但 F1 不做管理 UI（非目标一致）。 | 确认无发散 | APPROVED |
| 模块开发角色 | ①`POST /api/skills` 一个端点承载"上传+落盘+生成 SKILL.md+写表"四件事，实现上须拆（见 TASK-033 3 子项），但架构上作为一个 REST 资源合理。②`runChatTurn` 加 `skill?` / `onInsight?` 是既有 `onComplete` 回调的同类扩展，`runChatTurn` 不 import `insights`——边界干净。 | 记入 TASK-033/034 | CONDITIONAL：`onInsight` 写库必须在路由层，`runChatTurn` 不得新增持久化依赖 |
| 测试角色 | ①`routeTurn` 是出站 LLM 调用，测试**严禁打真实网络**——单测须 mock，e2e 用本地 Provider。②`GET /api/insights` 使 `insights` 写入有真实消费者（原则 13 满足）。③DEC-015 的"未沙箱化"在 F1 无 UI 落点，测试只能断言 known warning 已登记 + DEC 可追溯。 | TEST-034 mock；TEST-037 读回；known warning 断言并入 P3 证据 | CONDITIONAL：`skill-html-unsandboxed` 须在 `test-results.json` known_warnings 登记 |

### CR-20260909-display-screen 评审（R2，架构说明书）

各角色对本节「CR-20260909-display-screen 方案」表 + DEC-017 + DEC-016 修订 + MOD-DISPLAY 独立评审。逐 CP 裁决见 CR 的 `## R2 评审矩阵`。R1 阶段架构 CONDITIONAL 的两条（提示条不可关闭常驻 / DEC-017 成文）在此闭环——REQ-F-025 ② 已收紧、DEC-017 已成文。

| 角色 | 反馈（本角色视角） | 处理结果 | 结论 |
|---|---|---|---|
| 架构角色（自审） | ①`display_state` 全局单行 + get-or-create（固定主键）——`node:sqlite` 无框架下最简。②DEC-017 内容流（事件 + 重取，无轮询/SSE）成文，首帧 SSR 走 DEC-011 模式。③`<iframe srcDoc>` 无 sandbox 是 DEC-015 的落地——信任面真实打开，提示条不可关闭常驻是唯一缓解、出口义务保留。④`routeTurn` `display` 字段复用 F1 同一次调用。 | DEC-017 立项；REQ-F-025 ② 已收紧 | APPROVED |
| 产品 owner | 方案无 CR 外行为；标题移入 home 视图符合"回到标题首页"表述；`kind` 扩展点未做过度设计（switch + default）。 | 确认 | APPROVED |
| 模块开发角色 | ①`page.tsx` 重写按 2 子项（CP-12）——认可。②`DisplayScreen` 与 `FloatingChat` 解耦（后者只派发事件）——边界干净。③`GET /api/display` 在 `kind:"insight"` 时附 html，省一次 `GET /api/insights` 往返——合理。 | 记入 TASK-037/038 | APPROVED |
| 测试角色 | ①`GET /api/display` 是真实入口，"刷新保持"可 e2e。②提示条不可关闭须断言（无 close 按钮 + Esc 无效）。③`display` fail-open 单测。④DEC-016 修订后 F1 的 TEST-034 仍只测 `skill`，`display` 归 TEST-042——无重叠。 | TEST-039..042 | APPROVED |

## 批准状态

- 当前状态：APPROVED
- CR-20260909-minimal-floating-chat：**P3 已落地**。DEC-010/011/012 全部转 APPROVED（schema 迁移 `providers.priority`、`sessionStorage` 持久化信号、`/api/providers/probe` 均已实现并测过）。冗余依赖 `openai` 已从 `package.json` 移除。
- CR-20260909-collapsible-panel：DEC-013 **APPROVED，P3 已落地**。`FloatingChat` 的 `expanded` 已拆为 `hasTranscript`（派生）+ `userCollapsed`（`localStorage['jarvis:chat-collapsed']`，`useState` 初值读取、SSR 首帧默认 + hydration 校正，同 DEC-011 模式）；瞬时切换无动画；折叠控件为状态栏内真实 `<button aria-expanded>`。零部署/后端/数据库变更（如「CR-20260909-collapsible-panel 方案」表所判）。
- CR-20260909-corner-menu：DEC-005 修订 + DEC-014 **APPROVED，P3 已落地**。`CornerMenu` 采用自写非模态 disclosure（P3 选型：jsdom 可测 + 全控制，未用原生 `popover`）；`page.tsx` 用 `children` 传三个组件，`CornerMenu` 与业务解耦；`next.config.mjs devIndicators.position = "bottom-right"`。z-index 刻度落地：`.corner-menu { z-index: 30 }` > `.floating-chat { z-index: 20 }`。零部署形态/后端/数据库变更（如「CR-20260909-corner-menu 方案」表所判）。
- CR-20260909-display-screen（R2）：新增 **DEC-017**（动态展示屏数据流：`display_state` 全局单行 get-or-create、`jarvis:display-changed` 事件 + `GET /api/display` 重取无轮询/SSE、SSR 首帧同 DEC-011、路由层两处 `setDisplayState` 写入）+ **DEC-016 修订**（`routeTurn` 加 `display` 字段）+ 新增 **MOD-DISPLAY**（`DisplayScreen` + `src/lib/display.ts`）+ 新单行表 `display_state` + `GET /api/display`。DEC-015 从"立项"变"实际渲染"（`<iframe srcDoc>` 无 sandbox + 不可关闭常驻提示条）。逐 CP 方案见「CR-20260909-display-screen 方案」表（CP-1..CP-15）。R2 评审：四角色 **全部 APPROVED**（R1 架构 CONDITIONAL 两条已闭环）。**状态：R1 四角色 APPROVED（1 轮闭环）+ R2 已成文，待用户 R1 拍板 + R3/R4**。
- CR-20260909-skills（R2）：新增 **DEC-015**（技能生成 HTML 未沙箱化，**高危**，用户接受风险，带"后续 CR 必须沙箱化"出口义务）+ **DEC-016**（技能路由为独立最小非流式 LLM 调用，fail-open；`routeTurn` 返回 `{skill, display}`，F1 只读 `skill`）。新增 **MOD-SKILLS**（`src/lib/skills.ts`）。两张新表 `skills` / `insights`（幂等迁移）。新增 `POST/GET /api/skills`、`GET /api/insights`；`POST /api/chat/stream` 增技能路由前置 + HTML 捕获。零新增运行依赖。部署仅新增 `.data/skills/` 运行目录。逐 CP 方案见「CR-20260909-skills 方案」表（CP-1..CP-14 全覆盖）。R2 评审：架构自审 APPROVED（DEC-015 高危带出口义务）、产品 APPROVED、模块 CONDITIONAL（`onInsight` 写库在路由层）、测试 CONDITIONAL（`skill-html-unsandboxed` 登记 known warning）。**状态：P2，待 R2/R3/R4 机器门**。
