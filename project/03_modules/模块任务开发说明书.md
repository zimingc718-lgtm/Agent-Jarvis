# 模块任务开发说明书

责任人：模块开发角色

## 模块任务总览

| 任务 ID | 模块 | 任务 | 状态 | 依赖 | 覆盖需求 | 覆盖测试 |
|---|---|---|---|---|---|---|
| TASK-001 | MOD-AUTH | 建立 Next.js 项目骨架和 Google 登录保护 | DONE | 无 | REQ-F-001, REQ-NF-001, REQ-NF-002 | TEST-001, TEST-002 |
| TASK-002 | MOD-DB | 建立 SQLite schema、用户隔离和凭据加密工具 | DONE | TASK-001 | REQ-F-009, REQ-F-013, REQ-NF-002 | TEST-003, TEST-004 |
| TASK-003 | MOD-PROVIDER | 实现 Provider CRUD、认证模式和连接测试接口 | DONE | TASK-002 | REQ-F-006, REQ-F-007, REQ-F-008, REQ-F-009, REQ-NF-003 | TEST-005, TEST-006 |
| TASK-004 | MOD-ADAPTER | 实现 OpenAI、DeepSeek、本地 OpenAI-compatible adapter | DONE | TASK-003 | REQ-F-010, REQ-F-011, REQ-F-012 | TEST-007 |
| TASK-005 | MOD-CHAT | 实现 SSE 流式对话 API、停止生成和消息保存 | DONE | TASK-002, TASK-004 | REQ-F-004, REQ-F-005, REQ-F-013 | TEST-008, TEST-009 |
| TASK-006 | MOD-SETTINGS-UI | 实现独立模型设置页 | DONE | TASK-003 | REQ-F-007, REQ-F-008, REQ-F-009 | TEST-010 |
| TASK-007 | MOD-CHAT-UI | 实现底部悬浮输入条、展开面板、模型切换和科幻视觉 | DONE | TASK-003, TASK-005 | REQ-F-002, REQ-F-003, REQ-F-006, REQ-F-014 | TEST-011, TEST-012 |
| TASK-008 | MOD-CHAT-UI | 实现真实入口冒烟路径 | DONE | TASK-001 至 TASK-007 | REQ-NF-004 | TEST-013 |
| TASK-009 | MOD-CHAT-UI | 停止生成改为真实 AbortController，服务端 abort 并落库 stopped | DONE | TASK-005, TASK-007 | REQ-F-005 | TEST-009 |
| TASK-010 | MOD-CHAT | 多轮会话续接：`runChatTurn` 复用 conversationId、回放历史+system prompt；新增 `GET /api/conversations/[id]/messages`；首页注水 | DONE | TASK-005 | REQ-F-013, REQ-NF-002 | TEST-014, TEST-015 |
| TASK-011 | MOD-PROVIDER | `saveProvider` upsert；新增 `PATCH/DELETE /api/providers/[id]`；`/api/providers/test` 改真实探测；拒绝 oauth 保存 | DONE | TASK-003 | REQ-F-006, REQ-F-007, REQ-F-008 | TEST-006, TEST-016 |
| TASK-012 | MOD-SETTINGS-UI | 设置页编辑/启用/停用/删除入口、类型切换保留输入、真实连接测试结果、保存中禁用 | DONE | TASK-011 | REQ-F-007 | TEST-010 |
| TASK-013 | MOD-DB, MOD-ADAPTER | 加密密钥 fail-fast；解密失败映射为可读状态；adapter 统一 `/chat/completions`、超时、错误体归一 | DONE | TASK-002, TASK-004 | REQ-F-009, REQ-F-010, REQ-F-011, REQ-F-012 | TEST-007, TEST-017 |
| TASK-014 | MOD-CHAT-UI | 可执行 UI 规范 `scripts/ui-contract.mjs`（静态+live），接入 TEST-012/TEST-018 与 `governance.py ui` | DONE | TASK-007 | REQ-F-014 | TEST-012, TEST-018 |
| TASK-015 | MOD-CHAT-UI | 主题系统：theme-aware token（浅色默认/深色覆盖）、`ThemeToggle`、root layout 无闪脚本 | DONE | TASK-014 | REQ-F-014 | TEST-019 |
| TASK-016 | MOD-CHAT-UI, MOD-SETTINGS-UI | 极简首页 + 原生 `<dialog>` 封装、`SettingsDialog`、`AccountDialog`（取代 `AuthActions`）；`/settings/models` 深链保留 | DONE | TASK-015 | REQ-F-015, REQ-F-007, REQ-F-001 | TEST-021 |
| TASK-017 | MOD-CHAT-UI | 助手回复 Markdown 渲染器 `src/lib/markdown.tsx` 并接入浮窗气泡 | DONE | 无 | REQ-F-016 | TEST-020 |
| TASK-018 | MOD-CHAT-UI | `ui-contract` 双主题化：两套调色板对比度、`LV-SURFACE`/`LV-CONTRAST-DARK`/`LV-REFLOW-dark`、展开面板限高规则 RF-09 | DONE | TASK-014, TASK-015 | REQ-F-014, REQ-F-003 | TEST-012, TEST-018 |
| TASK-019 | MOD-AUTH, MOD-DB | 配置诊断与阻断态：`runtime-config.getStorageConfig`、`api-guard.storageUnavailable`（8 个 store 路由 503）、`ConfigWarning` 具名阻断 UI、`scripts/check-config.mjs` 预检、`.gitignore`、`gate g3` 人工验证项支持 | DONE | TASK-001, TASK-013 | REQ-F-001, REQ-F-009, REQ-NF-001 | TEST-022, TEST-023 |
| TASK-020 | MOD-AUTH（构建流程） | 构建目录隔离：`next.config.mjs` 读 `NEXT_DIST_DIR`；smoke→`.next-smoke`、e2e→`.next-e2e`、`build:verify`→`.next-verify`；smoke/e2e 断言 `/api/auth/*` 可加载；WORKFLOW 记录禁止对交互 dev 直接 `next build` | DONE | TASK-001, TASK-008 | REQ-NF-004, REQ-F-001 | TEST-024 |
| TASK-021 | MOD-DB, MOD-PROVIDER, MOD-SETTINGS-UI | Provider 使用优先级：schema `ALTER TABLE providers ADD COLUMN priority INTEGER NOT NULL DEFAULT 1000000`，迁移后按 `rowid` 升序回写 0,1,2…；`saveProvider` 写 `priority`（新启用取当前最大 + 1）；`GET /api/providers` 按 `priority` 排序返回；`PATCH /api/providers/[id]` 支持上移/下移；`ModelSettings` 已启用列表渲染为有序列表 + 上移/下移按钮，单个时禁用排序 | DONE | TASK-011, TASK-012 | REQ-F-006, REQ-F-007 | TEST-025 |
| TASK-022 | MOD-PROVIDER, MOD-CHAT | `resolveActiveProvider(userId)`：在 `enabled && connected` 中按 `priority` 升序取第一个，逐个 fallthrough；`runChatTurn` 缺 `providerId` 时调用它；无结果时 `/api/chat/stream` 返回可读错误（前端红字），不建会话 | DONE | TASK-021, TASK-005 | REQ-F-006 | TEST-025, TEST-026 |
| TASK-023 | MOD-CHAT-UI | `FloatingChat` 收敛：删除模型芯片/Provider `<select>`、状态文字 `__state`、展开态可编辑模型输入框 `__model`、「Open model settings」链接；请求体不再发送 `providerId`/`model` | DONE | TASK-022 | REQ-F-002, REQ-F-003, REQ-F-006 | TEST-030, TEST-011 |
| TASK-024 | MOD-CHAT-UI, MOD-ADAPTER | 状态灯四态：`__light` 按 `检测中/灭/常亮/闪烁` 渲染，四态 class 有独立 token 且非文本对比 ≥ 3:1（两主题）。**探测不阻塞首屏（模块评审 CONDITIONAL）**——首屏渲染为「检测中」，对每个已启用 Provider 的连接探测（复用 `testProviderConnection`，带超时）在客户端 effect 或 RSC streaming 中完成后落定灯态 | DONE | TASK-021 | REQ-F-018, REQ-F-014 | TEST-027 |
| TASK-025 | MOD-CHAT, MOD-CHAT-UI | 会话生命周期。**按 3 个可独立提交的子项实现（模块评审 CONDITIONAL）**：①（MOD-CHAT）`停止` 服务端 abort + 落库 `stopped`，前端丢弃 `conversationId` 使会话结束 → TEST-029；②（MOD-CHAT-UI）动作按钮状态机（停止/发送/新对话），「新对话」清空记录区 + 丢弃 `conversationId` → TEST-030；③（MOD-CHAT-UI）「新对话后刷新空白」持久化信号（DEC-011，`sessionStorage` 标记 vs 服务端标志位，实现证据说明选型与 SSR 首帧处理）→ TEST-030 | DONE | TASK-023, TASK-009 | REQ-F-005, REQ-F-013, REQ-F-017 | TEST-029, TEST-030 |
| TASK-026 | MOD-CHAT-UI | 请求级错误红字行：`/api/chat/stream` 在首段前失败时前端在输入框上方渲染一行脱敏红字，不写入 messages、不结束会话、不禁用输入；展开面板 `max-height` 65vh → 50vh | DONE | TASK-023 | REQ-F-016, REQ-F-003 | TEST-028, TEST-030 |
| TASK-027 | MOD-CHAT-UI, MOD-SETTINGS-UI | 视觉简约化：`globals.css` 强调色收敛为单一蓝色系、统一圆角 token、增大留白刻度、弱化/移除阴影；不改主题机制（DEC-006）。**追加真实浏览器对比度回归（测试评审）**：改后两套调色板正文 4.5:1 / 非文本 3:1 须经 `--live` 复核 | DONE | TASK-015 | REQ-F-014 | TEST-012, TEST-018 |
| TASK-028 | MOD-CHAT-UI（UI 规范） | `scripts/ui-contract.mjs` 适配：删除引用 `__chip`/`__state`/`__model` 的规则与「模型芯片」规则（REQ-F-014）；RF-09 限高阈值 65vh→50vh；新增「状态灯四态各有独立 token 且非文本对比 ≥3:1（两主题）」规则；`docs/UI_STANDARD.md` 同步措辞 | DONE | TASK-024, TASK-027 | REQ-F-014, REQ-F-018, REQ-F-003 | TEST-012, TEST-018 |
| TASK-029 | MOD-AUTH（配置诊断） | `scripts/check-config.mjs` 适配单管理员：`JARVIS_TEST_USER_ID` 已设时 `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`/`NEXTAUTH_*` 由「缺失（红）」降为「暂缓（灰/可选）」，退出码不因暂缓项为非零；`JARVIS_SECRET_KEY` 仍必需 | DONE | TASK-019 | REQ-F-001, REQ-NF-001 | TEST-023 |
| TASK-031 | MOD-SETTINGS-UI | 左下角 ☰ 悬浮菜单（CR-20260909-corner-menu，DEC-014）：①新增 `src/components/CornerMenu.tsx`（`"use client"`）——☰ `<button aria-haspopup="menu" aria-expanded>` + 受控 `open` + 浮层 `<div role="menu">{children}</div>` + `document` mousedown 关闭 + `keydown` Esc 关闭 + 关闭时焦点回触发器；`position: fixed` 左下角、`z-index: 30`；②`page.tsx` 删 `home__actions`，header 只 `<h1>`，加 `<CornerMenu><ThemeToggle/><SettingsDialog .../><AccountDialog .../></CornerMenu>`；③`SettingsDialog` 删 `<section aria-label="外观">`（含 `<h3>外观</h3>` + `ThemeToggle`）与 `ThemeToggle` 导入，触发按钮文案「配置」→「模型」；④`next.config.mjs` 加 `devIndicators: { position: "bottom-right" }`；⑤`globals.css` `.corner-menu*` 样式（触控 ≥24px、焦点、非文本对比 ≥3:1）；⑥`ui-contract.mjs` 新增 corner-menu 规则。**按 2 子项提交（模块评审 CONDITIONAL）**：Ⅰ CornerMenu + ThemeToggle 迁移 + page.tsx + next.config.mjs；Ⅱ SettingsDialog 瘦身 + TEST-019/021 + appearance/account e2e 返工。 | DONE | TASK-016, TASK-015 | REQ-F-015, REQ-F-014 | TEST-032 |
| TASK-032 | 治理工具 | `tools/governance.py` 新增 `review r1\|r2\|r3\|r4` 子命令（CR-20260909-consensus-review-gates）：解析 CR 的 `## 变化点登记` 表 → CP 集合；r2/r3/r4 检查 `{全部 CP} ⊆ {对应层说明书 CR-<name> 节引用}`（`COVERAGE_GAP`）+ 该门 `## R{n} 评审矩阵` 结构合规（4 角色列、无 REJECTED、无空、CONDITIONAL 带条件，否则 `MATRIX_INVALID`）；r1 检查 CP 表有来源角色 + R1 拍板痕迹。无 CR 用 CP 格式时空过。`build_parser`/`run` 加派发。 | DONE | 无 | —（流程控制，不覆盖产品需求） | TEST-033 |
| TASK-030 | MOD-CHAT-UI | 收起对话面板（CR-20260909-collapsible-panel，DEC-013）：①**将 `FloatingChat` 的 `expanded` 单 boolean 重构为 `hasTranscript`（派生 = `messages.length > 0`）+ `userCollapsed`（持久 boolean），全组件用 `showTranscript = hasTranscript && !userCollapsed`，清理 `handleNewConversation` / `rollbackOptimistic` / 发送路径里的 `setExpanded`**；②新增 `setUserCollapsed` helper（try/catch，键 `jarvis:chat-collapsed`，镜像 `markSessionEnded`），显式折叠/展开与「发送」更新它；③`.floating-chat__status` 行内新增收起/展开 `<button>`（展开态朝下、收起态朝上），仅 `hasTranscript` 时渲染；④折叠时收到回复保持折叠，回复完成/失败时状态灯做一次瞬时提示；⑤折叠时发送自动展开；⑥**瞬时切换，无高度动画**；⑦`globals.css` 加控件样式（焦点可见、非文本对比 ≥3:1、触控 ≥24px） | DONE | TASK-023, TASK-025 | REQ-F-019, REQ-F-003 | TEST-031 |
| TASK-033 | MOD-SKILLS, MOD-DB, MOD-CHAT-UI | 技能上传→SKILL.md 生成→注册（CR-20260909-skills，CP-1/CP-9）。**按 3 个可独立提交子项实现**：Ⅰ（MOD-CHAT-UI + 路由）底部对话框文件夹拖放（`drop` + `webkitGetAsEntry` 递归，`<input webkitdirectory>` 兜底）→ `POST /api/skills` multipart（文件 + 相对路径）→ 服务端 `node:fs` 落盘 `.data/skills/<slug>/`（slug = sanitize(文件夹名)，冲突 409；**每个文件路径 `path.resolve` 后必须仍在该技能目录内，拒绝路径穿越**）；Ⅱ（MOD-SKILLS）`generateSkillDoc(folder)`：拼装文件夹内容 → 调 `resolveActiveProvider` 的 Provider 生成 `SKILL.md`（system prompt 明确输出格式：`---\nname: …\ndescription: …\n---\n\n<instructions 正文>`），解析 frontmatter；解析失败 → `name`=文件夹名、`description`=「（未生成描述）」、`SKILL.md` 写入原始返回 + 前缀注释，回执 `docGenerated:false` 供前端提示用户补充；Ⅲ（MOD-DB + MOD-SKILLS）`registerSkill` 写 `skills` 表（`UNIQUE(user_id,name)`）、`listSkills(userId)`；前端回执后记录区插「已注册技能：<name> — <description>」系统消息 | DONE | TASK-011, TASK-002 | REQ-F-020 | TEST-038, TEST-035 |
| TASK-034 | MOD-CHAT, MOD-CHAT-UI | 技能 HTML 产出捕获 + 存储 + 读回（CR-20260909-skills，CP-4/CP-5/CP-10）：①`runChatTurn` 加**一个**可选 `onFinal(finalText, status, conversationId) => ChatDelta[]`（P3 定选：合成为单回调，因为 `display:"home"` 与 HTML 捕获两类尾事件必须同时刻同顺序发出）——助手消息**完整落库后**调用一次，返回要补发的 SSE 尾事件。捕获判定用 MOD-SKILLS 纯函数 `captureSkillHtml(text)`：取**最后一个**闭合 ` ```html ` 块 / 有开围栏无闭合 → `"incomplete"` / 无围栏 → `"none"`。`runChatTurn` **不 import `insights`**；②`POST /api/chat/stream` 路由层实现 `onFinal`：仅当本轮 `skill` 非空且 `status==="complete"` 时捕获 → `insertInsight({conversationId, kind:"skill", html})` + `showInsight(id)` + 尾发 `{type:"insight", insightId}`；未捕获到 → 尾发 `{type:"insight-missing", reason}`；③新增 `GET /api/insights?conversationId=`（属主校验，倒序）；④`FloatingChat` 据 `insight`/`insight-missing` 尾事件在记录区插系统消息（「已生成洞察，可在展示屏查看」/「本轮未产出 HTML」/「HTML 不完整」） | DONE | TASK-005, TASK-035 | REQ-F-023, REQ-F-024 | TEST-036, TEST-037 |
| TASK-035 | MOD-SKILLS, MOD-CHAT | 技能路由 + 内容注入（CR-20260909-skills，CP-2/CP-3/CP-8/CP-11，DEC-016）：①`src/lib/skills.ts` `routeTurn(userMessage, skills)`——用 `resolveActiveProvider` 的 Provider 发**一次非流式** `/chat/completions`（system：给定技能名+描述，判定本条消息用哪个技能、以及是否要求回首页，只输出 `{"skill": name\|null, "display": "home"\|null}`；`max_tokens` 小、10s 超时、**不带历史**）；任何失败（无 Provider / 超时 / 非法 JSON / 未知名）→ 返回 `{skill:null, display:null}`（fail-open）。**F1 阶段只读 `skill`，`display` 字段由 F2（TASK-039）消费——函数从一开始即此签名，无 rename**；②`resolveSkillForTurn(name)`——读 `SKILL.md` 正文 + 遍历技能目录扩展名 ∈ 白名单（`.md .txt .json .csv .yaml .yml .py .js .ts .tsx .html`）的文件，按路径排序拼接（`\n\n=== <rel> ===\n` 分隔），累计 > 32KB 截断 + 尾注；③`runChatTurn` 加可选 `skill?: string`，非空则拼在固定 system prompt 之后、历史之前作**当轮附加 system 段**；④`POST /api/chat/stream` 在启动主流式前，`listSkills(userId)` 非空时调 `routeTurn` 一次，命中则 `resolveSkillForTurn` 组装并传 `skill` | DONE | TASK-022, TASK-033 | REQ-F-021, REQ-F-022 | TEST-034, TEST-035 |
| TASK-036 | MOD-DB | `display_state` 全局单行表 + 读写原语（CR-20260909-display-screen，CP-8，DEC-017 ①）：`migrate()` 加 `CREATE TABLE IF NOT EXISTS display_state(id TEXT PRIMARY KEY, kind TEXT NOT NULL, ref_id TEXT, updated_at TEXT NOT NULL)`；`src/lib/store.ts` 单行原语 `getDisplayState()`（`SELECT ... WHERE id='singleton'`，无行返回 `{kind:"home", refId:null}`）、`setDisplayState({kind, refId})`（`INSERT OR REPLACE INTO display_state (id, kind, ref_id, updated_at) VALUES ('singleton', ?, ?, ?)`）、`dumpDisplayStateRowsForTest()`；`src/lib/display.ts` 在其上提供 `resolveDisplayView()`（join `insights.html`）/ `showInsight(id)` / `showHome()`。get-or-create，单行 | DONE | TASK-002 | REQ-F-026 | TEST-039 |
| TASK-037 | MOD-DISPLAY | `DisplayScreen` 组件 + `GET /api/display` + 事件流 + 提示条（CR-20260909-display-screen，CP-1..CP-7/CP-9/CP-11/CP-13，DEC-017）：①`src/components/DisplayScreen.tsx`（`"use client"`，props `initial: DisplayState + html`）——`switch(kind)`：`home` → 标题视图（产品名 `<h1>` + 简介）；`insight` → `<div className="display-screen__notice" role="note">`（**不可关闭**：无按钮、无 Esc 处理）+ `<iframe srcDoc={html} className="display-screen__frame">`（**无 `sandbox`**，DEC-015）；`default` → home 视图；②`src/app/api/display/route.ts` `GET` → `getDisplayState()`，`kind:"insight"` 时附 `listInsights` 里 `ref_id` 那行的 `html`；③`useEffect`：`addEventListener("jarvis:display-changed", refetch)` → `GET /api/display` 更新 state；卸载 remove；④`globals.css` `.display-screen*`（全屏基底、`__notice` 非文本对比 ≥3:1、`__frame` 无边框铺满） | DONE | TASK-036, TASK-034 | REQ-F-015, REQ-F-026 | TEST-040, TEST-041 |
| TASK-038 | MOD-SETTINGS-UI, MOD-CHAT-UI | `page.tsx` 首页重写为全屏展示屏（CR-20260909-display-screen，CP-1/CP-2/CP-12/CP-15）。**按 2 子项提交**：Ⅰ（随 TASK-037）落 `DisplayScreen` + `src/lib/display.ts` + `GET /api/display` + `FloatingChat` 派发 `jarvis:display-changed`（此时 `page.tsx` 未改，DisplayScreen 未挂——可先单测组件）；Ⅱ `page.tsx`：删 `<header><h1>Agent-Jarvis</h1></header>`，RSC 读 `getDisplayState()` → `<DisplayScreen initial={...}>` 作全屏基底，`<FloatingChat>` / `<CornerMenu>` 浮其上；`globals.css` z-index 层叠（`.display-screen` 基底 / `.floating-chat` 20 / `.corner-menu` 30）；`ui-contract.mjs` 新增/改 首页结构规则；TEST-021/032 + appearance/account e2e 按新结构回归重写 | DONE | TASK-037 | REQ-F-015 | TEST-040, TEST-021, TEST-032 |
| TASK-039 | MOD-CHAT, MOD-SKILLS | `routeTurn` `display` 字段消费 + 路由层写 `display_state`（CR-20260909-display-screen，CP-3/CP-5/CP-10，DEC-016 修订）：①`routeTurn` 的 system prompt 与返回类型含 `display`（TASK-035 已从一开始即此签名，本任务是让路由层**读**它）；②`POST /api/chat/stream` 路由层的 `onFinal` 回调：捕获到 HTML → `insertInsight` 后 `showInsight(insightId)` + SSE 尾 `{type:"insight", insightId}`；`routeTurn` 返回 `display==="home"` → `showHome()` + SSE 尾 `{type:"display", kind:"home"}`；③`FloatingChat` 据 `insight` / `display` 尾事件 `window.dispatchEvent(new Event("jarvis:display-changed"))` | DONE | TASK-035, TASK-036 | REQ-F-027 | TEST-042, TEST-041 |
| TASK-040 | MOD-ZIP | 有界零依赖 zip 解析器（CR-20260910-skill-intake，CP-7/CP-8/CP-10，DEC-018）：新增 `src/lib/zip.ts`，**纯函数、不 import `node:fs`、不 import `store`**。①`readZipEntries(buffer, limits?) => {entries, skipped}`——尾部 65557 字节内反向搜 EOCD `0x06054b50` → 遍历 Central Directory `0x02014b50` → 按 `localHeaderOffset` 读 Local File Header `0x04034b50` 跳过 `nameLen+extraLen` 取数据段；method `0` 切片、method `8` 走 `zlib.inflateRawSync`；②**整体拒绝抛 `ZipError`**：无 EOCD / 偏移越界 / 数据截断 / 加密位(GPBF bit 0) / 条目名含 `..` 段或以 `/`·`\` 开头或含盘符 `X:` 或含 `\` 分隔 / 任一资源上限超出；③**逐条排除进 `skipped`**：method 非 `0`/`8`；④目录条目（名以 `/` 结尾）跳过不计数；⑤`DEFAULT_ZIP_LIMITS` = `{maxArchiveBytes: 20MiB, maxEntries: 2000, maxEntryBytes: 5MiB, maxTotalBytes: 50MiB, maxRatio: 100}`；⑥`deriveFolderName(entries, archiveFileName)`——恰有一个顶层目录且全部条目在其下则用它并剥前缀，否则用 zip 基名 | DONE | 无 | REQ-F-020, REQ-NF-005 | TEST-043 |
| TASK-041 | MOD-CHAT-UI | 三入口收敛 + 非法拖放可见（CR-20260910-skill-intake，CP-1/CP-2/CP-11）：①**单一** `submitSkillUpload(input)`，`input` 为 `{kind:"folder", folderName, files}` 或 `{kind:"archive", archive: File}`——构造 `FormData`、POST `/api/skills`、渲染回执（含 `excluded`）、错误处理**各只有一份**；②`handleDrop` 重写：`dataTransfer.types` 含 `Files` 时**立即 `preventDefault()`**（修本次 P6 根因：原实现在判空之后才 preventDefault，浏览器接管了 drop），再分类——恰一个目录 → folder；恰一个 `.zip` 文件 → archive；其余 → 系统消息「只能接收技能文件夹或 zip 压缩包，本次未处理：<原因>」；③两个隐藏 `<input type="file">`（`webkitdirectory` / `accept=".zip,application/zip"`）+ 悬浮条内一个真实可见 `<button>`「上传技能」触发选择，`onChange` 走同一 `submitSkillUpload`；④注册成功后 `dispatchEvent(new Event(SKILLS_CHANGED_EVENT))` | DONE | TASK-042 | REQ-F-020 | TEST-044 |
| TASK-042 | MOD-SKILLS, MOD-ZIP | 服务端 zip 接入 + 白名单放宽 + 被排除回执（CR-20260910-skill-intake，CP-3/CP-5/CP-6）：①`POST /api/skills` 支持 `archive` 字段——`readZipEntries` → `deriveFolderName` → 转 `UploadedFile[]`，之后与文件夹上传**同一条** `registerSkill` 路径；`ZipError` → 400 且**不写入任何文件**；②`SKILL_TEXT_EXTENSIONS` 由 11 项扩到 16 项（新增 `.mdx .toml .sh .jsonl .xml`）；③回执新增 `excluded: [{path, reason}]`，`reason ∈ {binary, too-large, not-injected, unsupported-zip-method}`——`binary`/`too-large` 未落盘，`not-injected` 已原样落盘但不进注入上下文，`unsupported-zip-method` 来自 `readZipEntries` 的 `skipped` | DONE | TASK-040 | REQ-F-020, REQ-NF-005 | TEST-045 |
| TASK-043 | MOD-SETTINGS-UI, MOD-CHAT, MOD-CHAT-UI | 技能可见性（CR-20260910-skill-intake，CP-4/CP-9/CP-12，REQ-F-028）：①新增 `src/components/SkillList.tsx`（`"use client"`，读 `GET /api/skills`，渲染只读「名称 — 描述」列表，空态「尚未注册技能」），由 `page.tsx` 作 `children` 挂进 `CornerMenu`——**不改 `CornerMenu` 本身**；②`SkillList` 监听 `SKILLS_CHANGED_EVENT` 重取（镜像既有 `jarvis:providers-changed`）；③`chat/stream/route.ts` 在 `onFinal` 返回的尾事件数组里加 `{type:"skill", name}`（命中技能时）；④`FloatingChat` 消费该事件插系统消息「本轮使用技能：<name>」，未命中不插；⑤**`src/lib/display-events.ts` 更名为 `src/lib/ui-events.ts`**，同时承载 `DISPLAY_CHANGED_EVENT`、`SKILLS_CHANGED_EVENT` 与 `DisplayView` 类型——两个 client 组件都不必 import 对方，也不拉入服务端模块；旧文件按原则 16 删除，两处 import 同步 | DONE | TASK-042 | REQ-F-028 | TEST-046 |
| TASK-044 | MOD-UI-FOUNDATION | L3 实施基线与 UI primitive：①从不含 P3 未验收改动的受控基线建立 L3 工作树；②新增 Tailwind v4/PostCSS、`components.json`、`src/lib/utils.ts`、本地 `src/components/ui/*` primitive 与最小依赖/lockfile；③将 `globals.css` 重置为 Tailwind 导入、HSL 三元组 token、`data-theme` variant、base reset 和可访问性规则；④无存储主题时固定浅色，不再按 OS 自动进入深色。 | DONE | P3 隔离基线 | REQ-NF-006, REQ-F-014 | TEST-047 |
| TASK-045 | MOD-DISPLAY, MOD-SETTINGS-UI | 一次性重写主页视觉壳：`DisplayScreen`、`page.tsx`、`CornerMenu`、`ThemeToggle` 与 `ConfigWarning` 改为 DEC-019 primitive 组合；保持 Display API/SSE 事件、左下角「模型」、z-index、`jarvis-theme` 和焦点行为。 | DONE | TASK-044 | REQ-F-002, REQ-F-006, REQ-F-014, REQ-F-015, REQ-NF-006 | TEST-048 |
| TASK-046 | MOD-SETTINGS-UI | 一次性重写 `Dialog`、`SettingsDialog`、`AccountDialog`、`ModelSettings` 与 `/settings/models`：统一 modal、表单、Provider 有序列表和状态反馈；不改 Provider API、认证模式、启用/停用或优先级语义。 | DONE | TASK-044 | REQ-F-006, REQ-F-007, REQ-F-008, REQ-F-014, REQ-NF-006 | TEST-049 |
| TASK-047 | MOD-CHAT-UI | 一次性重写 `FloatingChat` 与 Markdown 呈现：以 primitive、Lucide 和 Tooltip 替换视觉实现，保持流式 SSE、停止、新对话、折叠、状态灯、错误反馈、max-height 和无 Provider 自动降级；不接入或验收 P3 上传/技能 UI。 | DONE | TASK-044 | REQ-F-002, REQ-F-003, REQ-F-004, REQ-F-005, REQ-F-006, REQ-F-016, REQ-F-017, REQ-F-018, REQ-F-019, REQ-NF-006 | TEST-050 |
| TASK-048 | MOD-UI-FOUNDATION, MOD-CHAT-UI, MOD-SETTINGS-UI, MOD-DISPLAY | 最终集成与旧视觉删除：删除替代的全局 class 选择器、手绘图标和重复 reset；审查 `src/components/ui` 与业务组件 import 边界；从真实浏览器完成浅色紫色默认、深色、桌面/移动主要路径检查。仅全部 TASK-044..047 通过后合入/发布。 | DONE | TASK-045, TASK-046, TASK-047 | REQ-NF-006, REQ-NF-004 | TEST-047, TEST-048, TEST-049, TEST-050 |
| TASK-049 | MOD-GOVERNANCE | 门禁按 CR 切分 + 阶段聚合（CR-20260910-process-hardening，CP-3/CP-5）：①`check_g3` / `check_g35` / `check_review` / `gate` 新增可选 `cr` 参数，按该 CR `- 影响测试:` 声明的 TEST 集合收窄判定，**缺省分支（`cr=None`）语义完全不变**；`gate g4` 显式拒绝 `--cr`；②新增 `STAGE_GATES` 表与 `check_stage`，`check p1\|p2\|p3\|release` 一条命令跑完该阶段全部门禁，任一子门 FAIL 则整体 FAIL 并标出是哪一步；`check release` 同样拒绝 `--cr` | DONE | 无 | 无（流程控制） | TEST-051 |
| TASK-050 | MOD-GOVERNANCE | 变更记录脚手架（CR-20260910-process-hardening，CP-4/CP-10）：①`new-cr <名称>` 生成 13 个必需标签齐全、**全用半角冒号**的变更记录骨架，产物直接通过 `check-changes`，同名已存在时拒绝覆盖；②`matrix <CR>` 读该 CR 的 `## 变化点登记` 表，生成 R2/R3/R4 三张矩阵骨架（行数 = CP 数、列为四角色），格子填 `TODO`——`TODO` 不是合法裁决，未填完跑 `review` 必报 `MATRIX_INVALID`；已有矩阵默认跳过，`--force` 才重写 | DONE | TASK-049 | 无（流程控制） | TEST-052 |
| TASK-051 | MOD-GOVERNANCE | 说明书结构契约与迁移（CR-20260910-process-hardening，CP-1/CP-2/CP-8/CP-12）：①新增 `tools/migrate_specs.py`，把四本层级说明书重排为「基线节 + `变更响应 · <CR>` + 批准状态」，**幂等**（二次运行零 diff）；②评审 / 复盘节**移动**（非删除）到对应 CR 的 `## R{n} 评审意见`，带 `（迁移自 …）` 溯源标记；③新增 `check-specs` 子命令，机器校验节名白名单、同 CR 单节、说明书内无评审节；④向后兼容硬门：迁移前后 `review r1..r4` 对全部 CP-model CR 结果必须一致且全 PASS | DONE | TASK-049 | 无（流程控制） | TEST-053, TEST-054 |
| TASK-052 | MOD-GOVERNANCE | 基线抖动止血 + 单一验证入口（CR-20260910-process-hardening，CP-7/CP-9）：①`governed_hash` / `normalized_tsconfig` 在比对基线前把 `tsconfig.json` 的 `include` 归一化，丢弃 Next 每次构建写入的 `.next*/types/**/*.ts` 项——只归一化这一处抖动，该文件其余每个字节仍受控；②`package.json` 新增 `verify:all`（类型 + 单测 + 治理单测 + 视觉 + UI 契约 + 冒烟 + `check p3`）与 `governance:p2|p3|release|check-specs`，所有新命令挂在 `governance.py` 单一入口下 | DONE | 无 | 无（流程控制） | TEST-051 |
| TASK-053 | MOD-GOVERNANCE | 分档判据与 `check-doors`（CR-20260910-risk-scaled-gates，CP-1/CP-2/CP-3）：①三档判据（快车道/标准/重型）与「撤回代价」三问写入 `docs/CONTROLS.md`，`AI_STANDARD` 增原则 18；②`governance.py check-doors` **逐 CP** 校验「门」「发现方式」两列存在且取值合法（门 ∈ {单向,双向}，发现方式非空）；③`发现方式=发现不了` 而 `门=双向` 即阻断（`CHECK_DOORS_UNDETECTABLE_TWO_WAY`）；④含任一单向 CP 的 CR 必须有非占位的「回滚方式」（`CHECK_DOORS_MISSING_ROLLBACK`）。 | DONE | — | —（流程控制） | TEST-055 |
| TASK-054 | MOD-GOVERNANCE | ID 占用前置与 `check-ids`（CP-5，盲区 ①）：①CR 头部新增 `- 占用 ID:` 字段，`new-cr` 骨架同步；②`governance.py check-ids` 解析全部 CR 的占用区间，跨 CR 重叠即阻断（`CHECK_IDS_OVERLAP`）；③声明的 TASK-/TEST-/DEC- 在对应说明书表中不存在即阻断（`CHECK_IDS_DANGLING`）；④同一 ID 在同一张表出现多行定义即阻断（`CHECK_IDS_DUPLICATE`）。 | DONE | TASK-053 | —（流程控制） | TEST-056 |
| TASK-055 | MOD-GOVERNANCE | 17 个既有 CR 回填「占用 ID」（CP-9）：①脚本生成候选区间供人工逐条核对；②**只增字段，不改任何既有 ID 归属**；③不变量守卫——回填前后对全部 CP 模型 CR 跑 `review r1..r4`，结果逐字一致（沿用 TEST-054 硬门做法）。 | DONE | TASK-054 | —（流程控制） | TEST-056 |
| TASK-056 | MOD-GOVERNANCE | `known_warnings` 可核验化（CP-6/CP-10，盲区 ②）：①条目支持 `{text, check}`，谓词 `gate_red:<gate>`（断言该门确实失败）/ `dep_absent:<pkg>`（断言该包不在 `package.json`）/ `manual`（须带 `reviewed_at`，超 90 天报陈旧）；②`governance.py check-warnings` 执行谓词，断言失效即阻断（`CHECK_WARNINGS_STALE`）；③自由文本条目解析为 `UNVERIFIABLE` 并列出，不阻断；④迁移现有 4 条自由文本。 | DONE | TASK-053 | —（流程控制） | TEST-057 |
| TASK-057 | MOD-GOVERNANCE | dev server 一致性检查（CP-12，盲区 ④/⑤ 的运行时面）：`scripts/check-dev-server.mjs` 打**真实运行的服务器**，取首页引用的样式表，断言其含 Tailwind 工具类（`.fixed` / `.mx-auto` 等）与 token；不含即报「dev server 早于构建配置启动，需重启」。服务器未运行时**显式 SKIP 并说明**，不得静默通过。 | DONE | — | REQ-NF-006 | TEST-058 |
| TASK-058 | MOD-GOVERNANCE | 结构规则合流与如实报告（CP-4，盲区 ③/⑤）：①`check-specs` 复用 `migrate_specs.migrate_spec` 的产物比对，消除两套结构规则各说各话（`SPECS_NOT_CANONICAL`）；②`review r1..r4` 对无 CP 表的 CR 由**静默跳过**改为**如实报告** `SKIPPED_BY_LEVEL`，并断言其级别确为 L1。 | DONE | TASK-053 | —（流程控制） | TEST-059, TEST-060 |

## 关键接口

| 接口 | 所属模块 | 输入 | 输出 | 错误处理 |
|---|---|---|---|---|
| `getCurrentUser()` | MOD-AUTH | 请求上下文 | 当前用户或 null | 未登录返回 null，由页面/API 转换为重定向或 401 |
| `encryptSecret()` / `decryptSecret()` | MOD-DB | 明文凭据或密文字段 | 加密字段或明文 | 密钥缺失时返回配置错误 |
| `listProviders(userId)` | MOD-PROVIDER | 用户 ID | ProviderSummary[] | 只返回当前用户数据 |
| `testProviderConnection({baseUrl,secret})` | MOD-ADAPTER | base URL、可选 secret | `{ ok, message }` | 区分 401/403、404、超时（TimeoutError）、网络错误 |
| `saveProvider(userId, input)` | MOD-PROVIDER | 含可选 `id`、可选 `priority` | `{ id, created }` | 带 `id` 且属主则 UPDATE（空 secret 保留原密钥），否则 INSERT；未传 `priority` 且新启用时取「当前最大 + 1」 |
| `resolveActiveProvider(userId)` | MOD-PROVIDER | 身份 ID | `{ provider, model } \| null`（CR-20260909） | 在 `enabled && connected` 中按 `priority` 升序取首个；皆不满足返回 `null` |
| `reorderProvider(userId, id, direction)` | MOD-PROVIDER | 身份 ID、Provider ID、`"up" \| "down"` | boolean | 与相邻 Provider 交换 `priority`；仅属主生效；`PATCH /api/providers/[id]` 承载 |
| `setProviderEnabled / deleteProvider(userId, id)` | MOD-PROVIDER | 用户 ID、Provider ID | boolean | 仅属主生效，否则返回 false（路由转 404） |
| `PATCH / DELETE /api/providers/[id]` | MOD-PROVIDER | 属主 + id | `{ ok }` 或 404 | 未登录 401 |
| `sendProviderStream()` | MOD-ADAPTER | Provider、模型、消息、Abort signal、timeoutMs | AsyncIterable<ChatDelta> | 统一 `/chat/completions`；HTTP 错误体归一为 `error`；用户 abort→`stopped`；超时→`error` |
| `runChatTurn({conversationId?, systemPrompt?, ...})` | MOD-CHAT | 会话（可选）、Provider、模型、消息 | SSE（`start` 携带 conversationId） | 非属主会话 404；`ProviderSecretError`→400 可读；错误不写入 assistant 上下文 |
| `GET /api/conversations/[id]/messages` | MOD-CHAT | 属主 + id | `{ conversation, messages }` | 非属主 404，未登录 401 |
| `POST /api/chat/stream` | MOD-CHAT | 会话、消息（`providerId`/`model` 可选覆盖，CR-20260909） | SSE（`start` 携带 conversationId；技能轮尾部 `insight`/`insight-missing`） | 缺 Provider 参数时走 `resolveActiveProvider`；无可用 Provider → 可读错误 + 不建会话（前端红字）；`停止` → `stopped` 且会话结束；`listSkills` 非空时先 `routeTurn`（fail-open）→ 命中则注入当轮 system 段 |
| `listSkills(userId)` / `registerSkill(userId, folder)` | MOD-SKILLS | 身份 ID / 上传文件夹 | `SkillSummary[]` / `{id,name,description,docGenerated}` | `registerSkill` slug 冲突 → 抛（路由转 409）；`generateSkillDoc` 失败 → 回退文件夹名 + 「（未生成描述）」 |
| `routeTurn(userMessage, skills)` | MOD-SKILLS | 本轮用户消息、技能摘要列表 | `{ skill: string\|null, display: "home"\|null }`（DEC-016；F1 只读 `skill`，`display` 留 F2） | **fail-open**：无 Provider / 10s 超时 / 非法 JSON / 未知名 → `{ skill: null, display: null }`，绝不抛 |
| `resolveSkillForTurn(name)` | MOD-SKILLS | 技能名 | `string`（`SKILL.md` 正文 + 白名单文本文件，≤32KB） | 超 32KB 截断 + 尾注；技能目录缺失 → 抛（调用方在 `routeTurn` 命中后才调，理论不发生） |
| `insertInsight({conversationId,kind,html})` / `listInsights(conversationId)` | MOD-DB | 会话 ID、kind、html | `{id,…}` / `Insight[]`（`created_at` 倒序） | 无 FK；属主校验由路由层经 `getConversation` 完成 |
| `runChatTurn({…, skill?, onFinal?})` | MOD-CHAT | + 可选 `skill`（当轮 system 段）、`onFinal(finalText, status, conversationId) => ChatDelta[]` | 同前 + 尾事件 | `skill` 缺省 → 行为与 CR 前逐字节一致；捕获判定与写库都在路由层的 `onFinal` 实现里；`runChatTurn` 不 import `insights` |
| `GET /api/skills` / `GET /api/insights` | MOD-SKILLS / MOD-CHAT | 身份 / `?conversationId=` | `SkillSummary[]` / `Insight[]` | 未登录 401；`/api/insights` 非属主会话 404 |
| `readZipEntries(buffer, limits?)` | MOD-ZIP | zip 字节 + 可选上限 | `{ entries: ZipEntry[], skipped: [{path, reason}] }` | 抛 `ZipError` = **整体拒绝**（无 EOCD/截断/加密位/路径越界/超上限）；`skipped` = 逐条排除（不支持的压缩方法）。**纯函数，不碰 fs** |
| `deriveFolderName(entries, archiveFileName)` | MOD-ZIP | 解包条目 + zip 文件名 | `{ folderName, entries }` | 恰一个顶层目录且全部条目在其下 → 用它并剥前缀；否则用 zip 基名 |
| `submitSkillUpload(input)` | MOD-CHAT-UI | `{kind:"folder",folderName,files}` \| `{kind:"archive",archive}` | void（渲染系统消息） | 三个入口（拖文件夹 / 拖 zip / 点选）唯一的提交路径；回执含 `excluded` 时追加一条列出被排除项 |
| `getDisplayState()` / `setDisplayState({kind, refId?})` | MOD-DB | — / `{kind, refId}` | `DisplayStateRecord` / void | 全局单行（固定主键 `'singleton'`）；`getDisplayState` 无行 → `{kind:"home", refId:null}`；`setDisplayState` = `INSERT OR REPLACE` |
| `resolveDisplayView()` / `showInsight(id)` / `showHome()` | MOD-DISPLAY | — | `DisplayView` / void / void | `resolveDisplayView` 把 `kind:"insight"` 的指针 join 到 `insights.html`（行不存在 → `html:null`）；两个 show* 是路由层的写入意图函数 |
| `GET /api/display` | MOD-DISPLAY | — | `{ kind, refId, html }`（`kind:"insight"` 时 `html` = 该 `insights` 行） | 未登录 401；未知 `kind` → 前端回退 home |
| `routeTurn(userMessage, skills)` | MOD-SKILLS | 本轮用户消息、技能摘要 | `{ skill: string\|null, display: "home"\|null }` | fail-open（见上）；F1 读 `skill`，F2 读 `display` |

## 变更响应 · CR-20260909-collapsible-panel

### 技术设计

TASK-030 唯一，全部落 `src/components/FloatingChat.tsx` + `src/app/globals.css`。逐变化点给出实现方案、涉及符号、技术可行性。

| 变化点 | 实现方案 | 涉及符号 | 可行性判断 |
|---|---|---|---|
| CP-3 状态模型 | 删 `const [expanded, setExpanded] = useState(restored)`。新增 `const [userCollapsed, setUserCollapsed] = useState(() => chatCollapsed())`。派生（非 state）：`const hasTranscript = messages.length > 0`；`const showTranscript = hasTranscript && !userCollapsed`。`<section className={... showTranscript ? "floating-chat--expanded" : ""}>`；记录区 `{showTranscript ? <div className="floating-chat__messages">…</div> : null}`。删 `handleSubmit` 里 `setExpanded(true)`、`handleNewConversation` 里 `setExpanded(false)`、`rollbackOptimistic` 里 `setExpanded(remaining.length > 0)`（后者整体删除——`showTranscript` 自动跟随 `messages`）。 | `FloatingChat`：`userCollapsed`、`hasTranscript`、`showTranscript`；移除 `expanded`/`setExpanded` | **高**。派生态替代冗余 state 是 React 官方推荐；净删 3 处 `setExpanded` + 1 个 `rollbackOptimistic` 内联，状态机更简单。`restored` 变量仅剩控制 `messages`/`conversationId` 初值，保留。 |
| CP-4 持久化 | 模块级函数（紧邻 `sessionEnded`/`markSessionEnded`）：`function chatCollapsed(): boolean { try { return localStorage.getItem("jarvis:chat-collapsed") === "1"; } catch { return false; } }` 与 `function setChatCollapsed(v: boolean): void { try { v ? localStorage.setItem("jarvis:chat-collapsed","1") : localStorage.removeItem("jarvis:chat-collapsed"); } catch {} }`。`useState(() => chatCollapsed())` 初值。切换/发送时 `setUserCollapsed(x)` 与 `setChatCollapsed(x)` 成对调用（可包一个 `applyCollapsed(v)` helper 保证同步）。 | `chatCollapsed`、`setChatCollapsed`、`applyCollapsed` | **高**。完全镜像已上线的 `sessionEnded`/`markSessionEnded`（`FloatingChat.tsx:53-71`）。SSR 读不到 → catch → `false`，client mount 校正——DEC-011 同模式，TEST-030 e2e 已证明收敛。 |
| CP-5 折叠控件 | `.floating-chat__status` 内，`__sr` span 之后：`{hasTranscript ? <button type="button" className="floating-chat__toggle" aria-expanded={showTranscript} aria-label={showTranscript ? "收起对话" : "展开对话"} onClick={() => applyCollapsed(showTranscript)}><span aria-hidden="true" className="floating-chat__toggle-icon" /></button> : null}`。CSS：`.floating-chat__toggle { margin-left: auto; padding: 0.25rem 0.375rem; …}`（触控 ≥24px），`.floating-chat__toggle-icon` 用 border 画三角或 `▾` 字形 + `[aria-expanded="false"] .floating-chat__toggle-icon { transform: rotate(-90deg) }`。`:focus-visible` 复用全局焦点环。 | `applyCollapsed`；CSS `.floating-chat__toggle`、`.floating-chat__toggle-icon` | **高**。真实 `<button>`（满足 FK-03）；`aria-expanded` 是 disclosure 模式的标准 ARIA。 |
| CP-6 折叠期间 | ①保持折叠 + busy：`showTranscript` 不含 `isStreaming`，天然满足；`lightState` 计算已有 `isStreaming ? "busy"`。②完成瞬时提示：`const [justFinished, setJustFinished] = useState(false)`；在 `handleSubmit` 的 `finally` 里 `if (userCollapsed) { setJustFinished(true); const t = setTimeout(() => setJustFinished(false), 1200); }`（用 ref 存 timer 以便 unmount 清）。`lightState = isStreaming ? "busy" : justFinished ? "done" : probeState`；`LIGHT_LABEL` 加 `done: "回复已就绪"`。CSS `.floating-chat__light--done { background: var(--ok); animation: floating-chat-pulse 0.6s ease-in-out 2; }`。③折叠时发送自动展开：`handleSubmit` 首行 `applyCollapsed(false)`。 | `justFinished`、`finishTimerRef`；`LIGHT_LABEL.done`；CSS `--done` | **高**。`justFinished` 是纯瞬态，1.2s 自灭，不违反 REQ-F-018「不新增可辨识持久状态」。timer 清理用现有 `useEffect` cleanup 模式。 |
| CP-7 无动画 | 不加 `transition`；`.floating-chat__messages` 条件渲染（挂/卸），瞬时。 | —— | **高**（不做事） |
| CP-2 REQ-F-003 放宽 | 无独立代码——由 CP-3 的 `showTranscript` 派生天然实现「发送触发展开（因 `messages` 增）+ 可手动收起（`userCollapsed`）」。 | —— | **高** |
| CP-8 回归 | 见测试说明书；本任务提交前跑 `test:floating-chat` + `test:e2e` 确认「发送后展开」「新对话清空」不回归。 | —— | **高**（守卫已定） |

**模块总判**：技术可行性 **全部高**。单文件组件改动 + 一段 CSS，无新依赖、无跨模块。提交拆 2 步：①CP-3/CP-4 状态重构（`showTranscript` 派生 + 持久化 helper，行为等价，e2e 应全绿）②CP-5/CP-6 折叠控件 + 瞬时提示（新行为）。

## 变更响应 · CR-20260909-corner-menu

### 技术设计

TASK-031。落 `src/components/CornerMenu.tsx`（新）+ `src/components/SettingsDialog.tsx` + `src/app/page.tsx` + `src/app/globals.css` + `next.config.mjs` + `scripts/ui-contract.mjs`。

| CP | 实现方案 | 涉及符号 | 可行性 |
|---|---|---|---|
| CP-4 `CornerMenu` | `"use client"`；`const [open, setOpen] = useState(false)`；`triggerRef`。渲染 `<div className="corner-menu"><button ref={triggerRef} className="corner-menu__trigger" aria-haspopup="menu" aria-expanded={open} aria-label="打开菜单" onClick={() => setOpen(v => !v)}>☰</button>{open ? <div className="corner-menu__panel" role="menu">{children}</div> : null}</div>`。`useEffect`（`open` 时挂）：`document` `mousedown` 命中 `.corner-menu` 外 → `setOpen(false)`；`keydown` `Escape` → `setOpen(false)` + `triggerRef.current?.focus()`。 | `CornerMenu`；CSS `.corner-menu` / `__trigger` / `__panel` | 高（受控 disclosure，jsdom 可测；原生 `popover` 留 P3 可换） |
| CP-1 `page.tsx` | 删 `<div className="home__actions">…</div>`；`<header className="home__bar"><h1>…</h1></header>`（仅标题）；`{storeReady 或 auth.ok 分支外}` 加 `<CornerMenu><ThemeToggle /><SettingsDialog templates={templates} providers={savedProviders} storage={storage} /><AccountDialog authenticated={auth.ok} googleOAuth={googleOAuth} /></CornerMenu>`。CornerMenu 无条件渲染（未配置态也要能开菜单切主题/看账号）。 | `page.tsx` | 高 |
| CP-3 主题内联 | `SettingsDialog.tsx`：删 `import { ThemeToggle }`、删 `<section className="dialog__section" aria-label="外观"><h3>外观</h3><ThemeToggle /></section>`。`ThemeToggle` 组件零改动，由 `page.tsx` 直接渲染进 `CornerMenu`。 | `SettingsDialog` | 高 |
| CP-2 命名 | `SettingsDialog` 触发 `<button>配置</button>` → `<button>模型</button>`；弹窗标题 `"设置"` 可保留或改 `"模型 Provider"`（不强制）。 | `SettingsDialog` | 高 |
| CP-5 `next.config.mjs` | `const nextConfig = { distDir: …, devIndicators: { position: "bottom-right" } }`。 | `next.config.mjs` | 高（1 行，构建时校验位置枚举） |
| CP-6 ui-contract + 样式 | `.corner-menu { position: fixed; left; bottom; z-index: 30 }`；`.corner-menu__trigger`（≥24px、`:focus-visible` 全局环、非文本对比 ≥3:1）；`.corner-menu__panel`（浮层，开在触发器上方，`--surface` 底、细线、极轻阴影）；内部 `button` 排成菜单行。ui-contract 新增规则（见测试说明书）。 | `globals.css`、`ui-contract.mjs` | 高 |

**未越界性**：`CornerMenu` 归 MOD-SETTINGS-UI（该模块已负责 Provider + 外观开关）；`page.tsx` 用 `children` 传三个组件，`CornerMenu` 不 import 业务组件、不感知 props——与业务解耦。零 API/store/schema/依赖变更。

**提交拆分**：Ⅰ `CornerMenu` + `page.tsx` + `next.config.mjs` + CSS + `ThemeToggle` 迁移（此时 `SettingsDialog` 仍有外观分区 → 主题开关暂时两处，测试可先绿）；Ⅱ `SettingsDialog` 删外观分区 + TEST-019/021 + appearance/account e2e 返工 + ui-contract 规则。

## 变更响应 · CR-20260909-display-screen

### 变化点影响矩阵

模块开发角色对「架构设计说明书 · CR-20260909-display-screen 方案」的每个变化点逐点审视。**每个变化点在下表必有对应行。**

| 变化点 | 既有任务/模块影响 | 分类 | 派生/修订任务 |
|---|---|---|---|
| CP-1 首页 = 全屏 DisplayScreen | `page.tsx`（TASK-016/031）：布局重写；新增 MOD-DISPLAY | 大改 | TASK-037、TASK-038 Ⅱ |
| CP-2 默认标题视图 + header `<h1>` 迁入 | `page.tsx` 删 `<header>`；`DisplayScreen` home 视图承载标题 | 大改 | TASK-037 ①、TASK-038 Ⅱ |
| CP-3 技能洞察自动上屏 | `POST /api/chat/stream` 路由层（TASK-034 ②）：+ `setDisplayState` 调用 | 小改 | TASK-039 ② |
| CP-4 跨刷新持久 | `page.tsx` RSC：+ `getDisplayState()` 首帧读取 | 小改 | TASK-037 ②、TASK-038 Ⅱ |
| CP-5 「显示首页」指令 | `routeTurn`（TASK-035）：`display` 字段已在签名里；路由层 + `setDisplayState({kind:"home"})` | 小改 | TASK-039 ② |
| CP-6 `kind` 扩展点 | `DisplayScreen` `switch` + `default` 分支 | 新增 | TASK-037 ① |
| CP-7 不可关闭提示条 | `DisplayScreen` insight 分支 + `globals.css` | 新增 | TASK-037 ①④ |
| CP-8 `display_state` 全局单行 | 新增 `display_state` 表 + `src/lib/display.ts` | 新增 | TASK-036 |
| CP-9 事件流无轮询/SSE | `FloatingChat` +派发；`DisplayScreen` +监听 | 新增 | TASK-037 ③、TASK-039 ③ |
| CP-10 `routeTurn` 扩 `display` | `routeTurn`（TASK-035）签名从一开始即含 `display`；路由层读它 | 小改 | TASK-039 ① |
| CP-11 未沙箱化 iframe 渲染 | `DisplayScreen` insight 分支 `<iframe srcDoc>` 无 sandbox | 新增 | TASK-037 ① |
| CP-12 `page.tsx` 2 子项 | —（本表即响应） | 新增 | TASK-038 Ⅰ/Ⅱ |
| CP-13 client 组件 + SSR 首帧 + 事件派发 | `DisplayScreen` client + `page.tsx` RSC 注水；`FloatingChat` 只发事件不 import DisplayScreen | 新增 | TASK-037、TASK-039 ③ |
| CP-14 每个可观察行为有真实入口测试 | —（测试层，TEST-039..042） | 新增 | 无（测试角色） |
| CP-15 REQ-F-015 重写回归门 | TEST-021/032 + appearance/account e2e 断言反转 | 回归 | TASK-038 Ⅱ |

**既有任务状态变更汇总**：无既有任务废止。`page.tsx`（TASK-016/031 的产物）布局大改由 TASK-038 承接。`POST /api/chat/stream` 路由层在 F1 的 TASK-034 基础上加两处 `setDisplayState`（TASK-039）。**F1（TASK-033/034/035）与 F2（TASK-036..039）P3 合并实现**——`routeTurn` 从第一行代码即 `{skill, display}` 签名。

### 技术设计

TASK-036..039。落 `src/lib/store.ts`（`display_state` 表）+ `src/lib/display.ts`（新）+ `src/app/api/display/route.ts`（新）+ `src/components/DisplayScreen.tsx`（新）+ `src/app/page.tsx` + `src/app/globals.css` + `src/components/FloatingChat.tsx` + `src/app/api/chat/stream/route.ts` + `scripts/ui-contract.mjs`。

| 变化点 | 实现方案 | 涉及符号 | 可行性判断 |
|---|---|---|---|
| CP-8 `display_state` | `migrate()` 加建表；`store`：`getDisplayState()` = `db.prepare("SELECT kind, ref_id AS refId, updated_at AS updatedAt FROM display_state WHERE id='singleton'").get() ?? {kind:'home', refId:null}`；`setDisplayState({kind, refId=null})` = `db.prepare("INSERT OR REPLACE INTO display_state (id, kind, ref_id, updated_at) VALUES ('singleton', ?, ?, ?)").run(kind, refId, new Date().toISOString())`；`display.ts` 在其上 join `insights.html` 成 `resolveDisplayView()`。 | `store.migrate`/`getDisplayState`/`setDisplayState`、`display.resolveDisplayView` | **高**（`INSERT OR REPLACE` 是 SQLite 核心语法；单行无并发问题——单管理员） |
| CP-1/CP-2 全屏 + 标题视图 | `page.tsx`（RSC）：`const display = getDisplayState(); const html = display.kind==='insight' && display.refId ? listInsights(...)?.find(i=>i.id===display.refId)?.html ?? null : null;` → `<main className="home"><DisplayScreen initial={{...display, html}} /><FloatingChat .../><CornerMenu>...</CornerMenu></main>`。删 `<header className="home__bar">`。`DisplayScreen`：`kind==='home'` → `<section className="display-screen display-screen--home"><h1>Agent-Jarvis</h1><p>…简介…</p></section>`。 | `page.tsx`、`DisplayScreen` | **高**（布局是 CSS 层叠 + 条件渲染） |
| CP-3/CP-5/CP-9 内容流 | `DisplayScreen`：`const [state, setState] = useState(initial); useEffect(() => { const h = () => fetch('/api/display').then(r=>r.json()).then(setState); window.addEventListener('jarvis:display-changed', h); return () => window.removeEventListener('jarvis:display-changed', h); }, [])`。`FloatingChat`：SSE 读到 `{type:'insight'}` 或 `{type:'display'}` 事件时 `window.dispatchEvent(new Event('jarvis:display-changed'))`——**`FloatingChat` 不 import `DisplayScreen`**。 | `DisplayScreen` `useEffect`、`FloatingChat` SSE 分支 | **高**（自定义事件是浏览器标准；无轮询/长连接） |
| CP-4 跨刷新 | `page.tsx` RSC 每次请求读 `getDisplayState()` → SSR 首帧即上次内容；`DisplayScreen` `useState(initial)` 承接，不因 mount 重置。刷新 = 新 RSC 请求 = 新首帧。 | `page.tsx`、`DisplayScreen` | **高**（同 DEC-011 SSR 模式，已验证） |
| CP-6 `kind` 扩展点 | `DisplayScreen`：`function render() { switch (state.kind) { case 'insight': return <Insight/>; case 'home': default: return <Home/>; } }`。未知 kind 落 `default` → home，不崩。 | `DisplayScreen` | **高**（switch + default） |
| CP-7 提示条 | `<div className="display-screen__notice" role="note">此内容由大模型生成，未做安全隔离。</div>` 在 `<iframe>` 之前；**无 `<button>`、无 `onKeyDown`**——DOM 里不存在关闭途径。`globals.css`：`.display-screen__notice { position: sticky; top: 0; ... }` 非文本对比 ≥3:1。 | `DisplayScreen` insight 分支、`globals.css` | **高** |
| CP-10/CP-11 iframe + `display` 字段 | `<iframe srcDoc={state.html ?? ''} className="display-screen__frame" title="技能洞察" />`——**不写 `sandbox` 属性**。路由层（`chat/stream/route.ts`）：F1 已 `const { skill } = await routeTurn(...)`；F2 改为 `const { skill, display } = await routeTurn(...)`，`if (display === 'home') { setDisplayState({kind:'home'}); sseTail({type:'display', kind:'home'}); }`。 | `DisplayScreen`、`chat/stream/route.ts` | **高**（渲染 1 行；路由层多一个分支） |
| CP-12 `page.tsx` 2 子项 | Ⅰ：`DisplayScreen` + `display.ts` + `api/display` + `FloatingChat` 派发——组件与数据层可先单测（`page.tsx` 未挂载 DisplayScreen 时不影响现有首页）。Ⅱ：`page.tsx` 布局重写 + z-index + ui-contract + TEST-021/032 回归。 | `page.tsx` | **高**（拆分清晰，Ⅰ 不碰现有首页） |
| CP-13 SSR + 事件派发解耦 | `DisplayScreen` `"use client"` + props `initial`；`page.tsx`（RSC）注水。`FloatingChat` 只 `dispatchEvent`，`DisplayScreen` 只 `addEventListener`——两者零直接引用。 | `page.tsx`、`DisplayScreen`、`FloatingChat` | **高** |
| CP-14/CP-15 | 见测试说明书。 | — | —（测试角色） |

**模块总判**：技术可行性 **全部高**。零新增运行依赖。`page.tsx` 布局重写是最大改动，按 2 子项（Ⅰ 组件/数据层不碰现有首页；Ⅱ 布局 + 回归）。`FloatingChat` ↔ `DisplayScreen` 用自定义事件解耦，无直接引用。**F1/F2 P3 合并实现**：TASK-036（表）→ TASK-033/034/035（F1）→ TASK-037（DisplayScreen）→ TASK-039（路由层写 display_state）→ TASK-038 Ⅱ（page.tsx 重写 + 回归）。

## 变更响应 · CR-20260909-minimal-floating-chat

### 变化点影响矩阵

模块开发角色对「架构设计说明书 · CR-20260909 变化点架构裁决」的每一行逐点审视，标注对既有任务的影响（不变 / 小改 / 大改 / 废止）并派生新任务。**每个架构裁决点在下表必有对应行。**

| 架构裁决点 | 既有任务影响 | 分类 | 派生/修订任务 |
|---|---|---|---|
| 产品目标 / 用户范围 措辞 | 无 | 不变（纯文档） | 无 |
| REQ-F-001 → DEFERRED | TASK-001（Google 登录保护）：代码不动，语义降级为「暂缓，旁路即当前身份」 | 小改（仅状态/文档） | TASK-029（配置诊断适配） |
| REQ-NF-002 → DEFERRED | TASK-002（用户隔离）：`store` 按 id 存取实现不动 | 不变 | 无 |
| REQ-F-002 悬浮条三元素 | TASK-007（悬浮输入条 + 模型切换 + 科幻视觉）：模型切换子功能**废止**；输入条保留 | 大改 | TASK-023（收敛 FloatingChat） |
| REQ-F-003 状态仅灯 + 50vh | TASK-007 状态展示部分改写；TASK-018 RF-09 限高阈值 | 小改（限高）+ 大改（删状态文字） | TASK-023、TASK-026、TASK-028 |
| REQ-F-004 携带全部历史 + system prompt | TASK-010（`runChatTurn` 回放历史 + system prompt）：**行为已满足** | 不变（补验收澄清） | 无 |
| REQ-F-005 停止 = 结束会话 | TASK-009（真实 AbortController + 落库 stopped）：保留；新增「结束会话」语义 | 大改（语义变更） | TASK-025 |
| REQ-F-006 Provider 优先级 | TASK-003/011（Provider CRUD、`saveProvider` upsert）：+`priority` 字段与写入；TASK-005（对话 API）：+ 解析 | 大改（schema + 算法 + API） | TASK-021、TASK-022 |
| REQ-F-007 优先级有序管理 | TASK-006/012（设置页）：+ 排序交互 | 大改 | TASK-021 |
| REQ-F-013 恢复条件收窄 | TASK-010（首页注水）：+「会话是否活动」前置判断 | 小改 | TASK-025 |
| REQ-F-014 浅色简约视觉 | TASK-015（主题 token）：调色板/圆角/间距重定，机制不变；TASK-014/018（ui-contract）：规则调整 | 小改（token）+ 小改（规则） | TASK-027、TASK-028 |
| REQ-F-015 占位登录 | TASK-016（`AccountDialog`）：`authenticated=true` 分支**已满足** | 不变 | 无 |
| REQ-F-016 请求级错误红字 | TASK-013（adapter 错误体归一）：保留；前端新增错误分类与红字行 | 大改（新 UI + 错误分流） | TASK-026 |
| REQ-F-017 开始新对话 | 无既有任务 | 新增 | TASK-025 |
| REQ-F-018 状态灯四态 + 加载探测 | TASK-007 状态灯（原为静态装饰）→ 状态驱动；TASK-004（adapter 探测）：复用 | 大改（前端）+ 小改（复用探测） | TASK-024、TASK-028 |
| schema 变更：`providers.priority` | `src/lib/store.ts` `migrate()` | 大改（迁移） | TASK-021（含迁移与回填） |

**既有任务状态变更汇总**：TASK-007 的「模型切换」子功能废止（由 TASK-023 收敛），其余 TASK-001..020 代码不变，仅 TASK-001 语义降级、TASK-010 增前置判断（TASK-025 承接）。无既有任务需要回滚。

## 变更响应 · CR-20260909-skills

### 变化点影响矩阵

模块开发角色对「架构设计说明书 · CR-20260909-skills 方案」的每个变化点逐点审视，标注对既有任务/模块的影响（不变 / 小改 / 大改 / 新增）并派生任务。**每个变化点在下表必有对应行。**

| 变化点 | 既有任务/模块影响 | 分类 | 派生/修订任务 |
|---|---|---|---|
| CP-1 技能上传与注册 | 无既有任务；新增 MOD-SKILLS + `skills` 表 + `POST/GET /api/skills` + 前端拖放 | 新增 | TASK-033（3 子项） |
| CP-2 按发送路由技能 | `POST /api/chat/stream` 路由处理器：+ 前置 `routeTurn` 调用（有技能时） | 小改（新增一个前置分支） | TASK-035 ④ |
| CP-3 技能内容注入 | `runChatTurn`（TASK-005/010）：+ 可选 `skill?` 参数拼入当轮 system 段 | 小改（可选参数，缺省行为不变） | TASK-035 ②③ |
| CP-4 技能 HTML 产出捕获 | `runChatTurn`：+ 可选 `onFinal` 回调（同 `onComplete` 同类扩展；P3 合成为单回调） | 小改 | TASK-034 ① |
| CP-5 洞察存储与读回 | 新增 `insights` 表 + `insertInsight` + `GET /api/insights` | 新增 | TASK-034 ②③ |
| CP-6 安全非目标（REQ-F-025） | 无代码；`test-results.json` known_warnings 加 `skill-html-unsandboxed` | 新增（记录项） | 并入 TASK-034 交付证据 |
| CP-7 DEC-015 未沙箱化（REQ-F-025） | 无 F1 代码（F2 渲染约束）；DEC-015 立项 | 不变（本 CR 无实现） | 无（F2 = CR-20260909-display-screen 承接首渲提示条） |
| CP-8 DEC-016 独立路由调用 | `routeTurn` 实现须严格按 DEC-016（非流式、10s、不带历史、fail-open） | 新增 | TASK-035 ① |
| CP-9 CP-1 拆 3 子任务 | —（本表即响应） | 新增 | TASK-033 Ⅰ/Ⅱ/Ⅲ |
| CP-10 捕获回调，写库在路由层 | `runChatTurn` 不 import `insights`；写库在 `/api/chat/stream` 注入的回调里 | 约束（写进 TASK-034 描述） | TASK-034 ①② |
| CP-11 `src/lib/skills.ts` 集中 | 新增 MOD-SKILLS 模块，路由 + 技能解析全在此文件 | 新增 | TASK-035（模块归属） |
| CP-12 拆 F1/F2 | —（流程）；F2 = CR-20260909-display-screen（未起草） | 不变 | 无 |
| CP-13 每个 CP 有真实入口测试 | —（测试层，见测试说明书 TEST-034..038） | 新增 | 无（测试角色） |
| CP-14 per-message 触发 + 无技能回归 | `skill` 参数仅当轮传；无技能时 `routeTurn` 不调用、`runChatTurn` 不带 `skill` → 既有对话路径零改动 | 不变（既有路径）+ 新增（当轮语义） | TASK-035 ④（守卫见 TEST-013/TEST-038） |

**既有任务状态变更汇总**：无既有任务废止。`runChatTurn`（TASK-005/010）加 3 个可选参数，缺省行为完全不变（CP-14 守卫）。`POST /api/chat/stream` 路由处理器加"有技能时的前置 + 尾部元事件"，无技能时与 CR 前一致。`FloatingChat`（TASK-023）加拖放监听 + 系统消息渲染。

### 技术设计

TASK-033 / TASK-034 / TASK-035。落 `src/lib/skills.ts`（新）+ `src/lib/store.ts`（两张表 + `insertInsight`/`listInsights`/`registerSkill`/`listSkills`）+ `src/lib/chat.ts`（`runChatTurn` 可选参数）+ `src/app/api/skills/route.ts`（新）+ `src/app/api/insights/route.ts`（新）+ `src/app/api/chat/stream/route.ts` + `src/components/FloatingChat.tsx`。

| 变化点 | 实现方案 | 涉及符号 | 可行性判断 |
|---|---|---|---|
| CP-1 上传与注册 | Ⅰ 前端：`onDrop` 读 `e.dataTransfer.items` → 对每个 `webkitGetAsEntry()` 递归 `createReader().readEntries()` 收集 `File` + `entry.fullPath`；打包成 `FormData`（`files[]` + `paths[]`）POST `/api/skills`。兜底 `<input type="file" webkitdirectory hidden>`。Ⅰ 服务端：`slug = name.normalize().replace(/[^\w.-]+/g,"-")`；`fs.mkdir(.data/skills/<slug>, {recursive:false})`——`EEXIST` → 409 `{error:"技能名已存在，请改文件夹名"}`；逐文件 `fs.writeFile`（拒绝路径穿越：`path.resolve` 后必须仍在技能目录内）。Ⅱ `generateSkillDoc`：把文件夹内白名单文本（≤字节预算）塞进一个生成 prompt，非流式调用 → 解析返回文本的 `---\n…\n---` frontmatter（一个极简 YAML 子集解析：逐行 `key: value`，不引入 yaml 依赖）。Ⅲ `registerSkill`：`INSERT INTO skills`（`crypto.randomUUID()` id，`new Date().toISOString()`）。 | `src/app/api/skills/route.ts`、`skills.registerSkill`/`generateSkillDoc`/`listSkills`、`store` 的 `skills` 表 DDL、`FloatingChat` 的 `onDrop`/`onDragOver` | **中高**。目录拖放读取跨浏览器有差异（Chromium `webkitGetAsEntry` OK，Firefox 部分），`<input webkitdirectory>` 兜底可靠。frontmatter 极简解析避免新依赖。路径穿越防护是标准做法。 |
| CP-2 路由调用 | `/api/chat/stream` handler：解析 body 后 `const skills = listSkills(userId); let skillDoc: string | undefined; if (skills.length) { const { skill } = await routeTurn(lastUserMessage, skills); if (skill) skillDoc = await resolveSkillForTurn(skill); }`，再 `runChatTurn({ …, skill: skillDoc, onFinal })`。F1 只读 `skill` 字段；`display` 字段留给 F2（CR-20260909-display-screen）。 | `chat/stream/route.ts` | **高**（一个 `await` 前置，`routeTurn` 内部已 try/catch fail-open） |
| CP-3 内容注入 | `resolveSkillForTurn`：`fs.readFile(SKILL.md)` 取 `---` 之后正文；`fs.readdir(recursive)` 过滤白名单扩展；`for` 累加 `parts`，`Buffer.byteLength(acc) > 32*1024` 则 break + push 截断注记。`runChatTurn`：`const system = skill ? `${SYSTEM_PROMPT}\n\n${skill}` : SYSTEM_PROMPT;` | `skills.resolveSkillForTurn`、`chat.ts` 的 system 组装点 | **高**（纯文件读 + 字符串拼接） |
| CP-4 HTML 捕获 | `runChatTurn` 在 `onComplete`（助手消息落库）之后调用一次 `onFinal(content, status, conversationId)`；捕获判定在路由层实现里：`if (skillSegment && status === "complete") { const c = captureSkillHtml(content); … }`。MOD-SKILLS 的 `captureSkillHtml` = 取 `[...text.matchAll(/```html\r?\n([\s\S]*?)```/gi)]` 的最后一个；否则 `/```html\r?\n/` 命中判 `incomplete`；再否则 `none`。 | `chat.ts` 完成分支、`skills.captureSkillHtml`、`chat/stream/route.ts` | **高**。正则取最后一个闭合块；"有开无闭"用剩余检测。 |
| CP-5 存储 + 读回 | `store`：`insertInsight({conversationId,kind,html})` → `INSERT`；`listInsights(conversationId)` → `SELECT … ORDER BY created_at DESC`。`GET /api/insights`：`requireUserId` → 校验 `conversationId` 属主（既有 `getConversation`）→ `listInsights`。路由 `onFinal` 回调返回尾事件数组：捕获到 html 时 `const row = insertInsight({conversationId, kind:"skill", html}); showInsight(row.id); return [{type:"insight", insightId: row.id}]`。 | `store.insertInsight`/`listInsights`、`api/insights/route.ts` | **高**（既有 `getConversation` 属主校验可复用） |
| CP-6 安全非目标 | TASK-034 交付时在 `test-results.json` `known_warnings` 加 `{id:"skill-html-unsandboxed", note:"技能生成 HTML 未沙箱化渲染（F2），用户接受风险，沙箱化为后续 CR 出口义务，DEC-015"}`。 | `test-results.json` | **高** |
| CP-7 DEC-015 | 无 F1 代码。 | — | **高**（不做事） |
| CP-8 DEC-016 路由约束 | `routeTurn`：`AbortSignal.timeout(10_000)`；`body: { model, messages:[{role:"system",…},{role:"user", content: userMessage}], max_tokens: 64, stream:false }`；`try { const j = JSON.parse(extractJson(text)); const skill = skills.some(s=>s.name===j.skill) ? j.skill : null; const display = j.display === "home" ? "home" : null; return { skill, display } } catch { return { skill: null, display: null } }`。无 `resolveActiveProvider` 结果 → 直接 `return { skill: null, display: null }`。F1 阶段调用方忽略 `display`。 | `skills.routeTurn` | **高**（非流式 fetch + JSON 解析，失败路径全部返回 `{skill:null,display:null}`） |
| CP-9 3 子任务 | 见 CP-1 的 Ⅰ/Ⅱ/Ⅲ；各自可独立提交：Ⅰ（上传+落盘，返回原始文件树）、Ⅱ（SKILL.md 生成，可先桩 `description`）、Ⅲ（写表+list）。 | TASK-033 Ⅰ/Ⅱ/Ⅲ | **高** |
| CP-10 写库在路由层 | `runChatTurn` 签名只有回调，无 `import { insertInsight }`；写库代码在 `chat/stream/route.ts`。审查点：`chat.ts` 不得出现 `insights`/`insertInsight`。 | `chat.ts` / `chat/stream/route.ts` 边界 | **高**（约束明确，可代码审查 + grep 守卫） |
| CP-11 `src/lib/skills.ts` 集中 | 所有技能逻辑（list/register/generate/route/resolve）在单文件；`store` 只提供 `skills` 表的 CRUD 原语，`skills.ts` 组合。 | `src/lib/skills.ts` | **高** |
| CP-12 拆 CR | F1 到此为止；F2 起草时新建 `CR-20260909-display-screen.md`。 | — | **高** |
| CP-13 测试 | 见测试说明书。 | — | —（测试角色） |
| CP-14 per-message + 回归 | `skill` 是 `runChatTurn` 的单次入参，不存 state、不写会话——下一次发送重新走 `routeTurn`。无技能时 `skills.length===0` → 不调 `routeTurn`、`skill` 为 `undefined` → `runChatTurn` 与现状逐字节一致。守卫：TEST-013 冒烟 + TEST-038 e2e「第二次发送不重复触发」。 | `chat/stream/route.ts` 分支 | **高** |

**模块总判**：技术可行性 **CP-1 中高（目录拖放跨浏览器差异，有兜底）**，其余全高。零新增运行依赖（frontmatter 用极简自解析，不引 yaml）。跨模块面：MOD-SKILLS（新）↔ MOD-CHAT（`runChatTurn` 可选参数）↔ MOD-DB（两表）。`runChatTurn` 缺省行为不变是 CP-14 硬约束，有 grep + 测试守卫。提交顺序：TASK-033 Ⅰ→Ⅱ→Ⅲ → TASK-035 → TASK-034。

## 变更响应 · CR-20260910-process-hardening

### 变化点影响矩阵

| CP | 既有任务/文件影响 | 分类 | 派生任务 |
|---|---|---|---|
| CP-1 两段式结构 | 四本说明书重排（内容不减） | 结构变更 | TASK-051 ① |
| CP-2 评审归位 CR | 说明书删评审/复盘节，文字迁入 CR | 去重（移动，非删除） | TASK-051 ② |
| CP-3 阶段聚合命令 | `governance.py` `run`/`build_parser` 加派发 | 新增 | TASK-049 ② |
| CP-4 `new-cr` 脚手架 | 无既有任务 | 新增 | TASK-050 ① |
| CP-5 `--cr` 作用域 | `check_g3` / `check_g35` / `check_review` 加可选筛选；**缺省分支不变** | 小改（新增可选参数） | TASK-049 ① |
| CP-6 向后兼容硬门 | 无代码；验收条款 + TEST-054 | 约束 | TASK-051 ④ |
| CP-7 `tsconfig.json` 抖动 | `governance.py` 基线扫描归一化 | 小改 | TASK-052 ① |
| CP-8 脚本迁移 | 新增 `tools/migrate_specs.py` | 新增 | TASK-051 ① |
| CP-9 单一 CLI 入口 | 约束：新命令全挂 `governance.py`；`package.json` 加一条 | 约束 + 小改 | TASK-052 ② |
| CP-10 `matrix` 生成器 | 无既有任务 | 新增 | TASK-050 ② |
| CP-11 新命令单测 | `tests/test_governance.py` | 新增 | 无（测试角色） |
| CP-12 `check-specs` | `governance.py` 新子命令 | 新增 | TASK-051 ③ |

**既有任务状态变更汇总**：无既有任务废止、无既有任务改状态。`governance.py` 的既有判定分支（`check_g3` / `check_review` / 基线扫描）只增可选参数与归一化，**缺省路径行为不变**——这是 CP-5/CP-6 的硬约束，由 TEST-051/TEST-054 守卫。

### 技术设计

| CP | 实现方案 | 涉及符号 | 可行性 |
|---|---|---|---|
| CP-5 | `check_g3(root, cr=None)`：`required_tests` 若给 `cr` 则先按该 CR 的「影响测试」标签过滤；`check_review(root, level, cr=None)` 同理筛 `with_model`。`build_parser` 给 `gate`/`review` 加 `--cr`；`g4` 与 `check release` 显式 `if args.cr: error()` | `check_g3`/`check_g35`/`check_review`/`build_parser` | **高**（在既有集合上加一层筛选） |
| CP-3 | `STAGE_GATES = {"p1": [...], "p2": [...], "p3": [...], "release": [...]}`，`check_stage` 顺序执行并汇总 findings；任一 FAIL 即整体 FAIL | `STAGE_GATES`/`check_stage` | **高**（纯编排） |
| CP-4 | `new_cr(root, name)`：从模板字符串写文件；模板内 13 个标签用**半角冒号**；`os.path.exists` 则拒绝 | `new_cr`/`CR_TEMPLATE` | **高** |
| CP-10 | `build_matrices(root, cr, force)`：`parse_cp_registry` 取 CP → 对 R2/R3/R4 各拼一张表（表头四角色，每 CP 一行，四格填 `TODO`）→ 若 CR 内已有 `## R{n} 评审矩阵` 且非 `--force` 则跳过 | `build_matrices` | **高**（复用既有 `parse_cp_registry`） |
| CP-7 | 基线取哈希前，若文件是 `tsconfig.json` 则 `json.loads` → 对 `include` 排序并剔除 `.next*/types/**/*.ts` 形态项 → 重新序列化再哈希 | `sha256_file` 旁加 `normalized_bytes(path)` | **高**（一个文件的特例，边界清晰） |
| CP-1/CP-2/CP-8 | `migrate_specs.py`：按 `^## ` / `^### ` 切块 → 归类（基线 / 变更响应 / 评审 / 批准状态）→ 评审块按标题里的 CR 名路由到对应 `CR-*.md` 追加 `## R{n} 评审意见` → 其余按 CR 名排序重排 → 写回。**幂等**：已是新结构时二次运行无 diff | `migrate_specs.py` | **中高**（切块与路由是纯文本处理；幂等性由 TEST-053 守卫） |
| CP-12 | `check_specs(root)`：对四本说明书解析 `##` 标题 → 断言 ∈ 白名单 ∪ `变更响应 · CR-*`；断言每个 CR 名至多一个变更响应节；断言无评审/复盘节名 | `check_specs`/`SPEC_SECTION_WHITELIST` | **高** |
| CP-6/CP-11 | 见测试说明书 | — | —（测试角色） |

**模块总判**：技术可行性 **CP-1/CP-2/CP-8 中高**（纯文本重排，幂等性需守卫），其余全高。**零 `src/` 变更、零依赖**。提交顺序：TASK-052（基线抖动，先止血）→ TASK-049（作用域 + 阶段）→ TASK-050（脚手架）→ TASK-051（迁移 + 结构自检，最后做且带向后兼容硬门）。

## 变更响应 · CR-20260910-record-accuracy

| CP | 模块响应 | 判定 |
|---|---|---|
| CP-1 | 不落本层：证据文件的 known warning 由测试层修正。 | 不变 |
| CP-2 | 不落本层：同上。 | 不变 |
| CP-3 | **本层修正对象**：`## 批准状态` 段关于 CR-20260910-process-hardening 的那一行叙述——编号 TASK-044..047 改为 TASK-049..052、状态 TODO 改为 DONE，并注明 TASK-044..048 现归 CR-20260910-ui-foundation。任务表中 TASK-049..052 的定义行与技术设计行本身正确，不动。 | 小改（单行叙述） |

约束：本 CR 不新增、不删除、不重编号任何 TASK，也不改动任何 TASK 的状态字段。TASK-049..052 的 DONE 是 CR-20260910-process-hardening 的既有结论，此处只是让叙述与任务表一致。重编号会波及两个已 APPROVED 的评审矩阵，属 L2 以上，不在本 CR 范围。

## 变更响应 · CR-20260910-risk-scaled-gates

### 变化点影响矩阵

| CP | 受影响实现 | 判定 | 派生任务 |
|---|---|---|---|
| CP-1 | `docs/CONTROLS.md` 变更控制表、`docs/AI_STANDARD.md` | 大改 | TASK-053 ① |
| CP-2 | CR 模板 `cr_template()`、`governance.py` 新子命令 | 新增 | TASK-053 ②③④ |
| CP-3 | 流程约束，无独立实现 | 约束 | TASK-053 ①（由 R4 绑定 TEST 强制） |
| CP-4 | 五条检查分散在 TASK-054/056/057/058 与既有 `check_ledger` | 新增 | TASK-054, 056, 057, 058 |
| CP-5 | `cr_template()`、`new_cr()`、新 `check_ids()` | 新增 | TASK-054 |
| CP-6 | `load_test_results()` 的消费方、新 `check_warnings()` | 大改 | TASK-056 |
| CP-7 | `tools/governance.py` 单一 CLI 入口；`scripts/check-dev-server.mjs` 为运行时检查 | 约束 | TASK-053..058 全体 |
| CP-8 | 既有 `check_ledger`（**不动**）；`docs/WORKFLOW.md` 合并检查清单 | 不变（仅文档） | TASK-053 ① 附带 |
| CP-9 | 17 个 `project/06_changes/CR-*.md` | 新增字段 | TASK-055 |
| CP-10 | `project/05_evidence/test-results.json` 的 4 条自由文本 | 小改 | TASK-056 ④ |
| CP-11 | `tests/test_governance.py` | 新增 | TEST-055..060 的阳性用例 |
| CP-12 | `scripts/check-dev-server.mjs`、`package.json` 脚本别名 | 新增 | TASK-057 |

### 技术设计

| 任务 | 输入 / 输出 | 硬约束 | 完成判据 |
|---|---|---|---|
| TASK-053 | 输入：DEC-021 ①②。输出：`CONTROLS.md` 三档表 + 原则 18 + `check-doors`。 | 逐 CP 判定，不给整个 CR 打单一标签；判据不得留主观口子。 | `check-doors` 对全部 CP 模型 CR 通过；阳性用例能拦住「发现不了 + 双向门」。 |
| TASK-054 | 输入：17 个 CR 的 ID 使用现状。输出：`- 占用 ID:` 字段 + `check-ids`。 | 三个错误码 `CHECK_IDS_OVERLAP` / `_DANGLING` / `_DUPLICATE` 分别可触发。 | 阳性用例复现 TASK-044..048 撞号并被阻断。 |
| TASK-055 | 输入：TASK-054 的字段定义。输出：17 个 CR 回填。 | **只增字段，不改任何既有 ID 归属**；回填前后 `review r1..r4` 结果逐字一致。 | 不变量守卫通过；`check-ids` 全绿。 |
| TASK-056 | 输入：现有 9 条 `known_warnings`（4 字符串 + 5 对象）。输出：可核验结构 + `check-warnings`。 | 双形态兼容，历史条目不被强制改写；谓词必须真的执行门/查依赖，不得只做字符串匹配。 | 阳性用例复现「warning 称 g3 为红而 g3 实测绿」并被阻断。 |
| TASK-057 | 输入：运行中的 dev server URL。输出：`scripts/check-dev-server.mjs`。 | 真实入口（原则 12）；服务器未运行显式 SKIP；断言对象是**服务器实际返回的 CSS**，不是磁盘文件。 | 阳性用例复现「CSS 无 Tailwind 工具类」并被识别。 |
| TASK-058 | 输入：`migrate_specs.migrate_spec`。输出：`check-specs` 比对 + `review` 如实报告。 | 复用迁移器产物，不第二次实现结构规则。 | 阳性用例复现「变更响应节位置不合规而 `check-specs` 放行」并被阻断。 |

## 变更响应 · CR-20260910-skill-intake

### 变化点影响矩阵

模块开发角色对「架构设计说明书 · CR-20260910-skill-intake 方案」的每个变化点逐点审视。**每个变化点在下表必有对应行。**

| 变化点 | 既有任务/模块影响 | 分类 | 派生/修订任务 |
|---|---|---|---|
| CP-1 非法拖放必须可见 | `FloatingChat.handleDrop`（TASK-033 Ⅰ 产物）：`preventDefault` 位置错误 | **缺陷修复** | TASK-041 ② |
| CP-2 显式上传入口 | TASK-033 Ⅰ 描述含「`<input webkitdirectory>` 兜底」但 P3 未实现 | **缺陷修复（欠账）** | TASK-041 ③ |
| CP-3 zip 成为合法来源 | `POST /api/skills`（TASK-033 Ⅰ）：+ `archive` 分支 | 小改（新分支，复用同一 registerSkill 路径） | TASK-042 ① |
| CP-4 技能可见性 | 无既有任务；新增组件 + 尾事件 | 新增 | TASK-043 |
| CP-5 被排除文件回执 | `POST /api/skills` 过滤逻辑：从静默丢弃改为收集并回执 | **缺陷修复** | TASK-042 ③ |
| CP-6 白名单放宽 | `SKILL_TEXT_EXTENSIONS` 常量 11 → 16 项 | 小改（常量） | TASK-042 ② |
| CP-7 零依赖有界解析（DEC-018） | 新增 MOD-ZIP | 新增 | TASK-040 ①③④⑤⑥ |
| CP-8 不可信归档边界（REQ-NF-005） | `registerSkill` 既有 `path.resolve` 校验保留为第二道 | 新增（解析层）+ 不变（落盘层） | TASK-040 ② |
| CP-9 模块归属 | 上传入口 MOD-CHAT-UI；技能列表 MOD-SETTINGS-UI | 新增 | TASK-041 / TASK-043 ① |
| CP-10 解析器纯函数独立模块 | 约束：`zip.ts` 不 import `node:fs` / `store` | 约束（写进 TASK-040 描述 + grep 守卫） | TASK-040 |
| CP-11 三入口收敛单一提交 | `FloatingChat` 现有 `handleDrop` 内联提交逻辑 → 提出为 `submitSkillUpload` | 大改（重构） | TASK-041 ① |
| CP-12 `skill` 尾事件 | `chat/stream/route.ts` `onFinal`（TASK-034 ②）：+ 一个 variant | 小改 | TASK-043 ③④ |
| CP-13 zip 对抗性单测 | —（测试层） | 新增 | 无（测试角色） |
| CP-14 拖放与三入口测试 | —（测试层） | 新增 | 无（测试角色） |
| CP-15 可见性与回执断言 + 回归门 | —（测试层） | 新增 + 回归 | 无（测试角色） |

**既有任务状态变更汇总**：无既有任务废止。TASK-033（技能上传注册）的 Ⅰ 子项由 TASK-041/042 **修正并补齐**（`preventDefault` 顺序 + 未实现的兜底入口 + 被排除回执）—— 记为缺陷修复，不是新功能。TASK-034 ② 的尾事件数组加一个 variant。`src/lib/display-events.ts` 更名 `ui-events.ts`（TASK-043 ⑤），影响 `DisplayScreen.tsx` 与 `FloatingChat.tsx` 两处 import。

### 技术设计

TASK-040..043。落 `src/lib/zip.ts`（新）+ `src/lib/ui-events.ts`（由 `display-events.ts` 更名）+ `src/components/SkillList.tsx`（新）+ `src/lib/skills.ts` + `src/app/api/skills/route.ts` + `src/app/api/chat/stream/route.ts` + `src/components/FloatingChat.tsx` + `src/app/page.tsx` + `src/app/globals.css` + `scripts/ui-contract.mjs`。

| 变化点 | 实现方案 | 涉及符号 | 可行性判断 |
|---|---|---|---|
| CP-7/CP-10 zip 解析 | `readZipEntries`：`const eocd = findEocd(buf)`（自 `buf.length-22` 起向前最多 65557 字节找 `0x06054b50`）→ `cdOffset`/`cdCount` → 循环读 CD 条目（签名校验、`flags=readUInt16LE(8)`、`method=readUInt16LE(10)`、`compSize=readUInt32LE(20)`、`uncompSize=readUInt32LE(24)`、`nameLen`/`extraLen`/`commentLen`、`localOffset=readUInt32LE(42)`、`name=slice`）→ 逐条判定（加密位 `flags & 1`、路径、上限）→ 按 `localOffset` 读 LFH 取 `nameLen`/`extraLen` 定位数据 → `method===0 ? data : inflateRawSync(data)`。所有 `readUInt*` 前先校验 `offset + n <= buf.length`，越界即 `ZipError`。 | `zip.ts`：`readZipEntries`/`deriveFolderName`/`ZipError`/`DEFAULT_ZIP_LIMITS` | **中高**。ZIP 容器是稳定的公开格式；只实现读路径、只支持两种压缩方法。风险集中在偏移算术，由 7 类对抗样本 + 每次读前的边界校验兜住。 |
| CP-8 边界判定 | 顺序：先 `buffer.length > maxArchiveBytes` → 拒；再逐条累计 `entries.length > maxEntries`、`uncompSize > maxEntryBytes`、`total > maxTotalBytes` → 拒；全部解压后 `total / buffer.length > maxRatio` → 拒。路径判定：`if (/(^|[\\/])\.\.([\\/]|$)/.test(name) \|\| /^([a-zA-Z]:)?[\\/]/.test(name) \|\| name.includes("\\")) throw new ZipError(...)`。**任一拒绝都在写盘之前**（解析器根本不碰 fs）。 | `zip.ts` 判定分支 | **高**（纯判定，逐条可越界测试） |
| CP-3 archive 分支 | `api/skills/route.ts`：`const archive = form.get("archive"); if (archive instanceof File) { const buf = Buffer.from(await archive.arrayBuffer()); const { entries, skipped } = readZipEntries(buf); const d = deriveFolderName(entries, archive.name); files = d.entries.map(e => ({ path: e.path, content: e.content.toString("utf8") })); folderName = d.folderName; excluded.push(...skipped); }` —— 之后与文件夹分支汇合，二进制/超大过滤沿用既有逻辑。`catch (e) { if (e instanceof ZipError) return 400 }`。 | `api/skills/route.ts` | **高**（一个前置分支，出口形状与既有一致） |
| CP-5 excluded 回执 | 既有过滤点从 `continue` 改为 `excluded.push({path, reason})` 后 `continue`；注册成功后再对**已落盘**文件按 `isSkillTextPath` 判一次，非白名单的记 `not-injected`。回执 `{...result, excluded}`。 | `api/skills/route.ts`、`skills.registerSkill` 返回值扩展 | **高** |
| CP-6 白名单 | `SKILL_TEXT_EXTENSIONS` 加 5 项。 | `skills.ts` 常量 | **高** |
| CP-1 preventDefault | `handleDrop` 首行：`if (!event.dataTransfer \|\| !Array.from(event.dataTransfer.types).includes("Files")) return; event.preventDefault(); setDragActive(false);` —— **先阻止默认再分类**。分类失败走 `appendSystemMessage`。 | `FloatingChat.handleDrop` | **高**（顺序修正） |
| CP-2/CP-11 入口收敛 | `submitSkillUpload(input)` 统一构造 `FormData`：folder → `folderName` + 多个 `file`；archive → `archive`。三处调用：`handleDrop`（两种分支）、`folderInputRef.onChange`、`archiveInputRef.onChange`。`<button onClick={() => 弹一个两项选择}>` —— P3 定选：直接放两个按钮「上传文件夹」「上传 zip」，比自造菜单简单且可访问性更好。 | `FloatingChat`：`submitSkillUpload`、两个 `useRef<HTMLInputElement>` | **高** |
| CP-4/CP-9/CP-12 可见性 | `SkillList`：`useState<SkillSummary[]>` + `useEffect` 首次 fetch + 监听 `SKILLS_CHANGED_EVENT` 重取；`page.tsx` 在 `<CornerMenu>` 内加 `<SkillList />`。路由层命中技能时尾事件加 `{type:"skill", name: route.skill}`；`FloatingChat` 收到后 `appendSystemMessage(\`本轮使用技能：${name}\`)`。 | `SkillList.tsx`、`page.tsx`、`chat/stream/route.ts`、`FloatingChat` | **高**（完全复用 `jarvis:providers-changed` 与 `insight` 尾事件两个既有模式） |
| CP-13/14/15 | 见测试说明书。 | — | —（测试角色） |

**模块总判**：技术可行性 **CP-7 中高（手写二进制解析，风险由对抗样本兜住）**，其余全高。**零新增运行依赖、零 schema 变更**。最大重构点是 `FloatingChat` 的三入口收敛（CP-11），但净效果是把现有内联逻辑提出为一处，代码量下降。提交顺序：TASK-040（纯函数，可先单测）→ TASK-042（服务端接入）→ TASK-041（前端入口）→ TASK-043（可见性）。

## 变更响应 · CR-20260910-test-subprocess-encoding

| CP | 模块响应 | 判定 |
|---|---|---|
| CP-1 | 不落本层：修复全部在测试侧，`tools/` 下无改动。 | 不变 |
| CP-2 | **本层约束**：`tools/migrate_specs.py`（TASK-051 交付物）逐字节不变。它在 GBK 控制台被直接调用时中文显示正常，强制其输出 UTF-8 会破坏交互使用；编码约定归调用方。P3 以 `git diff --stat` 不含该文件为凭。 | 不变（约束） |

TASK-051 保持 DONE，交付物与验收不受影响。

## 变更响应 · CR-20260910-ui-foundation

### 变化点影响矩阵

模块开发角色逐项承接架构说明书的 CP-1..CP-12。一次性重写表示最终发布态不允许新旧视觉并存，不取消每项任务独立提交、回滚和验证的要求。

| CP | 既有模块影响 | 分类 | 派生任务 |
|---|---|---|---|
| CP-1 | `globals.css` 与全部已批准 UI 组件 | 大改 | TASK-044..048 |
| CP-2 | `CornerMenu`、`SettingsDialog`、`ModelSettings`、`FloatingChat` | 大改但业务语义不变 | TASK-045..047 |
| CP-3 | 已批准组件纳入；P3 `SkillList` 和上传候选代码排除 | 范围隔离 | TASK-044, TASK-048 |
| CP-4 | API、store、auth、Provider、SSE 保持 | 不变 | TASK-045..047 的接口回归约束 |
| CP-5 | `ThemeToggle`、`layout.tsx`、全局主题 token | 大改 | TASK-044, TASK-045 |
| CP-6 | `package.json`、PostCSS、components registry、本地 primitive | 新增外部依赖链 | TASK-044 |
| CP-7 | `globals.css`、ThemeToggle bootstrap | 大改 | TASK-044 |
| CP-8 | 新 `components/ui` 与业务组件 import 图 | 新增边界 | TASK-044, TASK-048 |
| CP-9 | 当前脏工作区与 P3 CR | 实施前置 | TASK-044 |
| CP-10 | 旧 `.display-screen*`、`.floating-chat*`、`.corner-menu*`、`.dialog*`、`.settings-*` 样式 | 删除替换实现 | TASK-048 |
| CP-11 | Provider、聊天、菜单、展示屏、设置真实入口 | 邻近回归 | TASK-045..048 |
| CP-12 | Testing Library、ui-contract、Playwright 与浏览器矩阵 | 测试派生 | TEST-047..050 |

### 技术设计

| 任务 | 输入与输出 | 关键约束 | 完成边界 |
|---|---|---|---|
| TASK-044 | 输入：受控已批准 UI 基线。输出：可被业务组件使用的 `ui/*` primitive、HSL token 与构建配置。 | P3 文件不进入工作树；shadcn 生成代码进入仓库；无隐式 Theme Provider；token 为唯一色值来源。 | typecheck/build 可加载，尚不宣称视觉迁移完成。 |
| TASK-045 | 输入：TASK-044 primitive。输出：一次性新主页、展示屏、左下角菜单和主题壳。 | 仅替换渲染与样式；保持 Display 事件、菜单焦点、z-index 以及「模型」入口。 | 页面所有已批准壳层都使用新 primitive。 |
| TASK-046 | 输入：TASK-044 primitive 和现有 Provider props/API。输出：新的模型与账号 dialog、设置页。 | 不改变 FormData、Provider CRUD、排序、连接测试或认证状态。 | 设置/模型路径没有旧 dialog/form class。 |
| TASK-047 | 输入：TASK-044 primitive 与既有 chat state/SSE。输出：新的聊天交互视觉。 | 不新增 Provider selector；不消费 P3 上传或技能能力；聊天状态机和 SSE type 不变。 | 聊天和 Markdown 无旧样式类。 |
| TASK-048 | 输入：TASK-045..047。输出：最终单一 UI 系统、无替代旧样式。 | 只以构建基线回滚；不能保留第二套可达样式。 | 全套 R4 测试与真实入口通过后才允许交付。 |

## 批准状态

- CR-20260910-process-hardening（R3）：新增 **TASK-049**（`--cr` 作用域 + `check <stage>`）、**TASK-050**（`new-cr` / `matrix` 脚手架）、**TASK-051**（`migrate_specs.py` 迁移 + `check-specs` 结构自检）、**TASK-052**（`tsconfig.json` 基线归一化 + `verify:all`）。状态 **DONE**。（起草时曾与并行的 CR-20260910-ui-foundation 同占 TASK-044..047，按先到先得改号至 TASK-049..052；**TASK-044..048 现归 CR-20260910-ui-foundation**。编号与状态由 CR-20260910-record-accuracy 校正为与本文件任务表一致。）零 `src/`、零 schema、零依赖变更；`governance.py` 缺省路径行为不变（CP-5/CP-6 硬约束，TEST-051/054 守卫）。R3 评审见该 CR 的 `## R3 评审意见` 与 `## R3 评审矩阵`。
- 当前状态：APPROVED（含 CR-20260908-floating-chat-functional-fixes、CR-20260908-ui-light-theme-minimal-home 迭代）
- CR-20260909-minimal-floating-chat：**TASK-021..029 已实现并标 DONE（P3 完成）**。CONDITIONAL 条款均满足——TASK-025 按 3 子项提交（`store` 优先级 + `FloatingChat` 收敛 + 会话生命周期分开）；TASK-024 首屏渲染灯为「检测中」，探测在客户端 `useEffect` 完成，不阻塞 SSR。DEC-011「新对话刷新空白」选定 `sessionStorage` 标记（`jarvis:chat-session-ended`），FloatingChat 在 `useState` 初始值处读取，SSR 首帧即按 restored 判定，无旧会话闪现（详见 EV-2026-09-09-minimal-floating-chat-impl）。`node scripts/ui-contract.mjs` 45/0/0；`npm test` 114 通过；smoke + 6 e2e 通过。
- CR-20260909-collapsible-panel：**TASK-030 已实现并标 DONE（P3 完成）**。`FloatingChat` 的 `expanded` 拆为 `hasTranscript`（派生）+ `userCollapsed`（`useState(() => chatCollapsed())`），派生 `showTranscript`，删除全部 `setExpanded` 与 `rollbackOptimistic` 内联；`chatCollapsed()`/`writeChatCollapsed()`/`applyCollapsed()` 三个 helper 镜像 `sessionEnded`/`markSessionEnded`；状态栏 `.floating-chat__toggle` `<button aria-expanded>`；`justFinished` + `finishTimerRef` 驱动 `.floating-chat__light--done` 一次性脉冲；发送首行 `applyCollapsed(false)`。ui-contract：LB-03 改判派生模型 + 新增 LB-07（46 规则）。`npm test` 120、`ui-contract` 46/0/0、smoke、7 e2e（新增折叠 + reload）、`build:verify` 全绿；TEST-030 与「发送后展开」e2e 回归通过（详见 EV-2026-09-09-collapsible-panel-impl）。
- CR-20260910-skill-intake（R3）：新增 **TASK-040**（`src/lib/zip.ts` 有界零依赖解析器，纯函数不碰 fs）、**TASK-041**（三入口收敛为单一 `submitSkillUpload` + `handleDrop` 首行 `preventDefault` 修根因 + 两个 `<input>` 与两个真实按钮）、**TASK-042**（服务端 `archive` 分支 + 白名单 11→16 项 + `excluded` 四类回执）、**TASK-043**（`SkillList` 挂进 `CornerMenu` + `skill` 尾事件 + `display-events.ts` 更名 `ui-events.ts`）。**TASK-040..043 已实现并标 DONE（P3/P4 完成）**。影响矩阵逐 CP-1..CP-15 响应；CP-1/CP-2/CP-5 明确记为**缺陷修复**（CP-2 为 TASK-033 Ⅰ 欠账），无既有任务废止。R3 评审：**四角色全部 APPROVED**（R1 模块 CONDITIONAL 两条已闭环）。零新增运行依赖、零 schema 变更。
- CR-20260909-display-screen（R3）：新增 **TASK-036**（`display_state` 全局单行表 + `getDisplayState`/`setDisplayState` get-or-create）、**TASK-037**（`DisplayScreen` 组件 + `GET /api/display` + `jarvis:display-changed` 事件流 + 不可关闭提示条 + 未沙箱化 `<iframe srcDoc>`）、**TASK-038**（`page.tsx` 首页重写为全屏 DisplayScreen，按 2 子项：Ⅰ 组件/数据层不碰现有首页；Ⅱ 布局 + z-index + TEST-021/032 回归）、**TASK-039**（`routeTurn` `display` 字段消费 + 路由层写 `display_state`）。**TASK-036..039 已实现并标 DONE（P3/P4 完成，与 CR-20260909-skills 合并实现）**。影响矩阵 + 技术设计逐 CP-1..CP-15 响应，无既有任务废止。R3 评审：**四角色全部 APPROVED**（R1 模块 CONDITIONAL 两条已闭环——2 子项写死 TASK-038、`routeTurn` 从第一行即目标签名、F1/F2 合并 P3）。零新增运行依赖。
- CR-20260909-skills（R3）：新增 **TASK-033**（技能上传→SKILL.md 生成→注册，3 子项）、**TASK-034**（HTML 捕获 `onInsight` 回调 + `insights` 写库在路由层 + `GET /api/insights` + 系统消息）、**TASK-035**（`src/lib/skills.ts` 路由 + 注入，`runChatTurn` 加 `skill?`；`routeTurn` 从第一行即 `{skill, display}` 签名）。**TASK-033/034/035 已实现并标 DONE（P3/P4 完成）**；P3 定选：`onInsight?`/`onInsightMissing?` 合成为单个 `onFinal(finalText, status, conversationId) => ChatDelta[]`，捕获判定提为 MOD-SKILLS 纯函数 `captureSkillHtml`（详见 CR 的「P3/P4 实施记录」）。影响矩阵与技术设计表逐 CP-1..CP-14 响应，无既有任务废止；`runChatTurn` 加 3 个可选参数缺省行为不变（CP-14 守卫：TEST-013 + TEST-038 + grep「`chat.ts` 无 `insertInsight`」）。R3 评审：模块自审 / 产品 APPROVED；架构 CONDITIONAL（路径穿越防护已并入 TASK-033 Ⅰ，P3 已落地并被 `tests/skills.test.ts` 断言）；测试 CONDITIONAL（`routeTurn` 单测覆盖 4 条 fail-open 路径，已并入 TEST-034 并通过）。零新增运行依赖（frontmatter 极简自解析）。新增 env `JARVIS_SKILLS_PATH` 用于隔离技能目录。
- CR-20260909-corner-menu：**TASK-031 已实现并标 DONE（P3 完成）**。新增 `src/components/CornerMenu.tsx`（受控非模态 disclosure：☰ `<button aria-haspopup="menu" aria-expanded>` + `document` mousedown/Escape 关闭 + 焦点回触发器，`z-index: 30`）；`page.tsx` header 只剩 `<h1>`，三个组件（`ThemeToggle`/`SettingsDialog`/`AccountDialog`）作 `children` 传入；`SettingsDialog` 删「外观」分区 + `ThemeToggle` 导入、触发文案「配置」→「模型」、弹窗标题「模型 Provider」；`chat.ts` 的 409 提示「请在「配置」中」→「请在「模型」中」；`next.config.mjs` `devIndicators: { position: "bottom-right" }`；`globals.css` `.corner-menu*` + 删死代码 `.home__actions` 及其移动端规则；`ui-contract.mjs` 新增 LB-08（header 无功能按钮 + CornerMenu 存在 + z-index > 20），文件表加 `CornerMenu.tsx`。**2 子项提交**：Ⅰ 组件 + 接线 + config（提交 a…）；Ⅱ SettingsDialog 瘦身 + 测试返工。TEST-032 PASS；TEST-019/021 + appearance/account e2e 回归重写并通过；smoke 首页断言改判 ☰ 触发器。`npm test` 124、`ui-contract` 47/0/0、smoke、**8 e2e**（新增「☰ menu reachable while chat expanded」）、`build:verify`、17 治理单测全绿（详见 EV-2026-09-09-corner-menu-impl）。
