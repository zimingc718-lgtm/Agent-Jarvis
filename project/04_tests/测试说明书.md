# 测试说明书

责任人：测试 owner

## 制定依据

本测试说明书仅根据已批准的 `产品需求说明书.md`、`架构设计说明书.md` 和 `模块任务开发说明书.md` 制定。测试覆盖关系以需求 ID、模块 ID 和任务 ID 为准。

## 测试矩阵

| 测试 ID | 类型 | 覆盖需求 | 覆盖模块/任务 | 断言目标 | 命令 | 必选 |
|---|---|---|---|---|---|---|
| TEST-001 | API/Auth（负向） | REQ-F-001, REQ-NF-002 | MOD-AUTH / TASK-001 | 未登录访问受保护 API 返回 401；生产环境拒绝 `JARVIS_TEST_USER_ID` 旁路。**不覆盖成功登录**，正向路径见 TEST-022。 | `npm run test:auth && npm run test:auth-config` | 是 |
| TEST-002 | UI/Auth（配置态 + 旁路态） | REQ-F-001 | MOD-AUTH / TASK-001, TASK-019 | 缺 OAuth 配置时账号弹窗显示所缺变量名而非登录按钮；配置齐全时显示登录入口；使用测试旁路登录态后可访问主页与对话入口。**不覆盖真实 Google 授权跳转**，见 TEST-022。 | `npm run test:auth-ui && npm run test:e2e` | 是 |
| TEST-003 | Unit/Security | REQ-F-009 | MOD-DB / TASK-002 | Provider secret 加密后数据库不出现明文 | `npm test -- crypto` | 是 |
| TEST-004 | Unit/Data | REQ-F-013, REQ-NF-002 | MOD-DB / TASK-002 | 会话和消息按用户隔离保存 | `npm test -- store` | 是 |
| TEST-005 | API/Provider | REQ-F-006, REQ-F-007, REQ-F-008, REQ-NF-003 | MOD-PROVIDER / TASK-003 | Provider CRUD 返回正确认证模式和脱敏状态，且不提供非官方网页登录自动化 | `npm test -- providers` | 是 |
| TEST-006 | API/Provider（真实入口） | REQ-F-007, REQ-F-010, REQ-F-011, REQ-F-012 | MOD-PROVIDER / TASK-003, TASK-011 | `POST /api/providers/test` 真实探测 `{baseUrl}/models`，返回成功或区分 401/404/超时/网络的可读错误 | `npm run test:provider-test` | 是 |
| TEST-007 | Unit+API/Adapter | REQ-F-010, REQ-F-011, REQ-F-012 | MOD-ADAPTER / TASK-004, TASK-013 | 三类 Provider 统一走 `/chat/completions`，流式归一为 ChatDelta，HTTP 错误体归一、超时归一、abort→stopped | `npm run test:adapters` | 是 |
| TEST-008 | API/Chat | REQ-F-004, REQ-F-013 | MOD-CHAT / TASK-005 | `/api/chat/stream` 返回 SSE token 流并保存消息 | `npm run test:chat-stream` | 是 |
| TEST-009 | API/Chat + E2E | REQ-F-005 | MOD-CHAT / TASK-009 | 停止后前端与服务端都停止追加，落库 `status=stopped`（e2e 通过真实浏览器 Stop 按钮验证服务端 abort） | `npm run test:chat-stop && npm run test:e2e` | 是 |
| TEST-010 | UI/Settings（真实入口） | REQ-F-007, REQ-F-008, REQ-F-009 | MOD-SETTINGS-UI / TASK-011, TASK-012 | 设置页可新增、编辑（不产生重复行）、启用、停用、删除、测试 Provider；OAuth 保存被拒 | `npm run test:settings-ui && npm run test:provider-crud && npm run test:e2e` | 是 |
| TEST-011 | UI/Chat | REQ-F-002, REQ-F-003, REQ-F-006 | MOD-CHAT-UI / TASK-007, TASK-010, TASK-023 | 底部输入条默认可见；发送后展开；多轮气泡独立；**浮窗不渲染 Provider/模型切换控件，对话请求不携带 `providerId`/`model`**（CR-20260909 改写，原「>1 Provider 渲染切换器」断言作废） | `npm run test:floating-chat` | 是 |
| TEST-012 | UI/Visual+A11y | REQ-F-014, REQ-F-003 | MOD-CHAT-UI / TASK-007, TASK-014, TASK-018, TASK-024, TASK-026 | `scripts/ui-contract.mjs` 静态层 0 FAIL：Design Tokens、**浅色与深色两套调色板**对比度（正文 4.5:1 / 非文本 3:1，含**状态灯四态**）、焦点、reflow、语义、DEC-005 布局、展开面板限高（**≤50vh**，CR-20260909 由 65vh 收敛） | `npm run test:visual` | 是 |
| TEST-013 | E2E/Smoke（真实入口） | REQ-NF-001, REQ-NF-004 | MOD-CHAT-UI / TASK-008 | 真实 Next.js：登录态 → 设置 → 本地 Provider → 编辑不重复 → 连接测试 → 双轮对话上下文（ctx=2→4）→ 经 `/api/conversations/[id]/messages` 刷新恢复 → 禁用/删除 | `npm run test:smoke` | 是 |
| TEST-014 | API/Chat | REQ-F-013 | MOD-CHAT / TASK-010 | `runChatTurn` 复用 `conversationId`、回放历史 + system prompt、拒绝非属主会话 | `npm run test:chat-stream` | 是 |
| TEST-015 | API/Conversations（真实入口） | REQ-F-013, REQ-NF-002 | MOD-CHAT / TASK-010 | `GET /api/conversations/[id]/messages` 只向属主返回消息，他人 404；recent 按用户隔离 | `npm run test:conversations` | 是 |
| TEST-016 | API/Provider（真实入口） | REQ-F-007, REQ-NF-002 | MOD-PROVIDER / TASK-011, TASK-012 | `POST/PATCH/DELETE /api/providers[/:id]`：upsert 不重复、启停/删除仅属主生效、未认证 401 | `npm run test:provider-crud` | 是 |
| TEST-017 | Unit/Security | REQ-F-009 | MOD-DB / TASK-013 | 缺 `JARVIS_SECRET_KEY` 时 `createStore` 抛错；密钥变更后 `listProviders` 标 `connected:false`+note，`getProviderForUser` 抛 `ProviderSecretError` | `npm run test:store` | 是 |
| TEST-018 | UI/A11y（真实浏览器） | REQ-F-014 | MOD-CHAT-UI / TASK-014, TASK-018 | `scripts/ui-contract.mjs --live`：320/390/1280px 无横向滚动、**浅色与深色**计算对比度达标、焦点可见、控件尺寸、reduced-motion、地标 | `npm run test:ui-contract:live`（需真实服务） | 是 |
| TEST-019 | UI/Theme（组件 + 真实浏览器） | REQ-F-014 | MOD-CHAT-UI, MOD-SETTINGS-UI / TASK-015, TASK-031 | 默认浅色；「深色」写入 `data-theme` 与 localStorage 且刷新保持；无效存储值被忽略；bootstrap 脚本在首帧前生效。**CR-20260909-corner-menu：`ThemeToggle` 宿主由「模型」弹窗改为左下角 ☰ 浮层——e2e 经 ☰ 触发主题切换、且切换时不打开任何弹窗；组件断言 `ThemeToggle` 独立可用不变。** | `npm run test:theme && npm run test:e2e` | 是 |
| TEST-020 | UI/Markdown | REQ-F-016 | MOD-CHAT-UI / TASK-017 | 代码块（含未闭合围栏）、行内代码、粗体、斜体、有序/无序列表、h3–h5 标题正确渲染；仅 http/https/mailto 成链；模型输出的原始 HTML 被转义 | `npm run test:markdown && npm run test:e2e` | 是 |
| TEST-021 | UI/Home（组件 + 真实浏览器） | REQ-F-015, REQ-F-007, REQ-F-001 | MOD-CHAT-UI, MOD-SETTINGS-UI, MOD-DISPLAY / TASK-016, TASK-031, TASK-038 | **CR-20260909-display-screen 改写（回归门）**：首页为**全屏 `DisplayScreen`**（默认标题视图含产品名 `<h1>`），**页面无独立 `<header>`**；底部对话 + 左下角 ☰ 触发器（`aria-haspopup` + `aria-expanded`）浮在展示屏之上；点 ☰ 开浮层，含 浅色/深色（内联）+「模型」+「账号登录」；Esc 关浮层且焦点回 ☰、点击外部关闭；「模型」弹窗只含 Provider 设置（无外观分区）且不产生第二个 h1/嵌套 main；账号弹窗展示登录状态与「账号 ≠ 模型授权」边界。**旧断言「首页顶部仅产品标题 header」反转为「标题在展示屏 home 视图、无 header」。** | `npm run test:auth-ui && npm run test:e2e` | 是 |
| TEST-022 | **人工验证**/Auth（真实 OAuth）**— DEFERRED（随 REQ-F-001，CR-20260909）** | REQ-F-001 | MOD-AUTH / TASK-019 | 在配置真实 Google OAuth client 且**不设** `JARVIS_TEST_USER_ID` 的情况下：点击账号弹窗内「Sign in with Google」→ 跳转 Google 授权 → 回调 `/api/auth/callback/google` → 返回首页显示已登录 → 可打开配置弹窗保存 Provider 并完成一轮对话 → 登出后受保护 API 恢复 401。证据须记录执行人、时间与截图/日志。 | 人工执行；结果写入 `test-results.json`（`verification: manual` + `verified_by` + `verified_at`） | 是 |
| TEST-023 | Config/Preflight + API | REQ-NF-001, REQ-F-009 | MOD-AUTH, MOD-DB / TASK-019 | `scripts/check-config.mjs` 正确区分「未设置」「被同名环境变量遮蔽」「`missing-` 占位」三种状态，且不受 `NODE_ENV=test` 与 `__NEXT_PROCESSED_ENV` 缓存影响；缺 `JARVIS_SECRET_KEY` 时 store 相关路由返回可诊断的 503 而非抛异常，首页与设置弹窗显示具名阻断态而非 500 | `npm run test:config` | 是 |
| TEST-024 | API/Auth（真实入口） | REQ-F-001, REQ-NF-004 | MOD-AUTH / TASK-020 | `/api/auth/csrf` 返回 200 + `csrfToken`、`/api/auth/providers` 暴露 google provider、`/api/auth/signin` 返回 200（非 500）—— 覆盖 NextAuth 路由处理器可加载性，捕获 `.next` 构建污染导致的 `vendor-chunks/jose.js` MODULE_NOT_FOUND | `npm run test:smoke && npm run test:e2e` | 是 |
| TEST-025 | API/Provider+Chat | REQ-F-006 | MOD-PROVIDER, MOD-CHAT / TASK-021, TASK-022 | `resolveActiveProvider` 在多个 `enabled` Provider 中按 `priority` 升序取首个 `connected` 者；最高优先级 `connected:false` 时 fallthrough 到下一个；`PATCH /api/providers/[id]` 上移/下移交换 `priority` 并改变解析结果；`/api/chat/stream` 不带 `providerId` 时用解析结果 | `npm run test:providers && npm run test:chat-stream` | 是 |
| TEST-026 | API/Chat（负向） | REQ-F-006 | MOD-CHAT / TASK-022 | 无 `enabled && connected` 的 Provider 时 `/api/chat/stream` 返回可读错误、**不创建会话记录**；前端据此渲染红字且状态灯为「灭」 | `npm run test:chat-stream` | 是 |
| TEST-027 | UI/Chat（组件 + 真实浏览器） | REQ-F-018 | MOD-CHAT-UI, MOD-ADAPTER / TASK-024 | 组件层用 mock 探测结果断言四态映射：探测未回→`检测中`；无已启用或全失败→`灭`；≥1 成功→`常亮`；流式中→`闪烁`；四态各有独立 class/token。e2e 层断言首屏渲染时灯为`检测中`、探测落定后才转`常亮`（验证 DEC-012 探测不阻塞首屏）。**不在 vitest/CI 打真实网络** | `npm run test:floating-chat && npm run test:visual && npm run test:e2e` | 是 |
| TEST-028 | UI/Chat（组件 + 真实入口） | REQ-F-016 | MOD-CHAT-UI / TASK-026 | `/api/chat/stream` 在首段前失败（401/超时/网络）时：输入框上方出现一行红字、内容不含 API Key、`messages` 不新增条目、会话未结束、输入框仍可输入；回复已开始后中断走气泡「生成失败」（区分两类错误） | `npm run test:floating-chat && npm run test:e2e` | 是 |
| TEST-029 | API/Chat + E2E | REQ-F-005, REQ-F-017 | MOD-CHAT, MOD-CHAT-UI / TASK-025 | 点击「停止」：服务端 abort、落库 `status=stopped`、前端丢弃 `conversationId`；此后发送的下一条消息带**新的 conversationId**（会话已结束）；被中断回复留在记录区带「已停止」标记 | `npm run test:chat-stop && npm run test:e2e` | 是 |
| TEST-030 | UI/Chat（组件 + 真实浏览器） | REQ-F-017, REQ-F-003 | MOD-CHAT-UI / TASK-023, TASK-025, TASK-026 | 六个独立断言（不合并判定，原则 15）：①空闲且输入框空时按钮文案为「新对话」（有/无历史均是）；②点击后记录区 DOM 清空；③`conversationId` 丢弃；④发送前无新会话记录（DB/recent 不新增）；⑤点「新对话」后刷新页面记录区为空（断言层级为 DOM 可观察结果，不绑定 `sessionStorage` 键名/服务端字段）；⑥未点「新对话」/「停止」则刷新恢复上一会话。另断言展开面板计算高度 ≤ 视口 50%（TASK-026）。收敛后 `__chip`/`__state`/`__model`/settings 链接不在 DOM（TASK-023）。**CR-20260909-collapsible-panel：`expanded`→`showTranscript` 重构后本用例须重跑，断言按新派生逻辑校准。** | `npm run test:floating-chat && npm run test:e2e` | 是 |
| TEST-031 | UI/Chat（组件 + 真实浏览器） | REQ-F-019, REQ-F-003 | MOD-CHAT-UI / TASK-030 | 七组独立断言（原则 15）：①有对话记录时状态灯行显示收起/展开控件，无记录时不显示；②点收起 → `.floating-chat__messages` 不在 DOM，`conversationId` 不变，`jarvis:chat-session-ended` 未置位，下一次发送复用同一 `conversationId`；③点展开 → 记录区回来，消息与折叠前一致；④折叠时收到流式回复 → 保持折叠、状态灯 `--busy`，回复完成 → 状态灯一次瞬时提示后落定，全程未自动展开；⑤折叠时发送 → 自动展开；⑥（**真实浏览器**，原则 12）折叠 → `page.reload()` → 仍折叠；展开 → reload → 仍展开；⑦`localStorage['jarvis:chat-collapsed']='true'` 但无对话记录时不显示控件、呈现输入条形态，无「折叠了却空白」中间态。 | `npm run test:floating-chat && npm run test:e2e` | 是 |
| TEST-032 | UI/Home 菜单（组件 + 真实浏览器） | REQ-F-015, REQ-F-014 | MOD-SETTINGS-UI, MOD-DISPLAY / TASK-031, TASK-038 | 独立断言（原则 15）：①首页无独立 header 功能按钮，左下角有 ☰ 触发器，**全屏 DisplayScreen 在其下作基底**；②点 ☰ 开浮层（`aria-expanded` true）、再点或 Esc 关（focus 回 ☰）、点浮层外关闭；③浮层含 浅色/深色（内联）+「模型」+「账号登录」；④浮层里点「深色」→ `data-theme=dark` 且**未打开任何 `<dialog>`**；⑤（**真实浏览器**，原则 12）经 ☰ 切深色 → `page.reload()` 仍深色；⑥点「模型」→ 打开 ModelSettings 弹窗（`heading "Model Providers"`）；点「账号登录」→ 打开账号弹窗；⑦（**真实浏览器**）对话面板展开时 ☰ 仍可点开浮层（z-index：`.corner-menu` 30 > `.floating-chat` 20 > `.display-screen` 基底）。**CR-20260909-display-screen 回归门：☰/浮层在全屏 DisplayScreen 上仍可交互。** | `npm run test:auth-ui && npm run test:e2e` | 是 |

| TEST-033 | 治理/Review（真实入口） | —（流程控制） | 治理工具 / TASK-032 | `governance.py review`：构造"含 `## 变化点登记` 表的 CR + 对应层响应"→ `REVIEW_R2_PASS`；某 CP 未在架构说明书出现 → `REVIEW_R2_COVERAGE_GAP`；矩阵含 `REJECTED` → `REVIEW_R2_BLOCKED`；r1 无拍板痕迹 → `REVIEW_R1_BLOCKED`；无 CR 用 CP 格式 → 空过；覆盖检查按 CR 自有小节作用域（兄弟 CR 小节的 CP 提及不算数）。 | `python -m unittest tests.test_governance` | 是 |
| TEST-034 | Unit/Skills 路由（mock，**禁真实网络**） | REQ-F-021 | MOD-SKILLS / TASK-035 | `routeTurn(msg, skills)` 返回 `{skill, display}`（F1 测 `skill`；`display` 见 TEST-042）：mock `/chat/completions` → ①`{"skill":"<已注册名>"}` → `skill` 为该名；②`{"skill":null}` → `skill` 为 `null`；③**fail-open 四条**：无 `resolveActiveProvider` 结果 / `AbortSignal.timeout` 触发 / 非 JSON 文本 / 未注册的技能名 —— 全部返回 `{skill:null, display:null}` 且**不抛**。断言非流式请求体 `stream:false`、`max_tokens` 小、`messages` 不含历史。 | `npm run test:skills` | 是 |
| TEST-035 | Unit/Skills 注入 | REQ-F-022, REQ-F-020 | MOD-SKILLS / TASK-035, TASK-033 | `resolveSkillForTurn(name)`：①含 `SKILL.md` 正文 + 白名单扩展文件（带路径分隔），排除 `.png`/`.exe` 等非白名单；②拼接 > 32KB → 截断且尾部有截断注记，返回 ≤ 32KB；③返回值仅供当轮，不写任何持久存储（断言无 `store` 调用）。`generateSkillDoc` frontmatter 解析失败 → `{name:文件夹名, description:"（未生成描述）"}`。 | `npm run test:skills` | 是 |
| TEST-036 | Unit/Chat HTML 捕获 | REQ-F-023 | MOD-CHAT, MOD-SKILLS / TASK-034 | `runChatTurn` + 路由层 `onFinal`（`skill` 非空）：①回复含两个 ```html``` 块 → 捕获**最后一个**块内容并发 `insight` 尾事件；②无 ```html``` 块 → `insight-missing` reason `"none"`；③有 ` ```html ` 开围栏但流结束未闭合 → `insight-missing` reason `"incomplete"`；④`skill` 为空（普通轮）→ 既无 `insight` 也无 `insight-missing`，SSE 仍以 `event: done` 收尾（CP-14 缺省行为不变）。 | `npm run test:chat-stream` | 是 |
| TEST-037 | API/Insights（真实入口） | REQ-F-024 | MOD-CHAT, MOD-DB / TASK-034 | 技能轮产出 HTML → `insights` 落一行（`kind="skill"`）→ `GET /api/insights?conversationId=<id>` 返回该行（`created_at` 倒序、含 `html`）；非属主会话 → 404；未登录 → 401；同会话两次产出 → 两行不覆盖。 | `npm run test:insights` | 是 |
| TEST-038 | E2E/Skills（真实入口） | REQ-F-020, REQ-F-021, REQ-F-023, REQ-F-024 | MOD-SKILLS, MOD-CHAT-UI / TASK-033, TASK-034, TASK-035 | 真实浏览器：①拖一个技能文件夹进底部对话框 → 记录区出现「已注册技能：&lt;name&gt; — …」；②发送一条与该技能相关的消息 → 该轮注入技能、回复产出 ```html``` → 记录区出现「已生成洞察…」且 `GET /api/insights` 有该行；③**再发一条无关消息 → 不重复触发技能**（`routeTurn` 每轮独立判定，per-message 非 per-session，CP-14）；④发送一条技能相关但模型未产出 HTML 的消息 → 记录区「本轮未产出 HTML」。 | `npm run test:e2e` | 是 |
| TEST-039 | Unit/Display 状态 | REQ-F-026 | MOD-DISPLAY, MOD-DB / TASK-036 | `getDisplayState` / `setDisplayState`：①空库 → `getDisplayState()` 返回 `{kind:"home", refId:null}`（不建行也可）；②`setDisplayState({kind:"insight", refId:"x"})` 后 `getDisplayState()` 返回该值；③再 `setDisplayState({kind:"home"})` → **同一行更新**（表内始终 ≤1 行，断言 `SELECT count(*)` = 1）；④`updated_at` 每次写入刷新。 | `npm run test:display` | 是 |
| TEST-040 | UI/Display（组件 + 真实浏览器） | REQ-F-015, REQ-F-025, REQ-F-026 | MOD-DISPLAY / TASK-037, TASK-038 | 独立断言（原则 15）：①`initial={{kind:"home"}}` → 渲染标题视图（产品名 `<h1>` + 简介），**页面无独立 `<header>`**；②`initial={{kind:"insight", html:"<p>hi</p>"}}` → 渲染 `<iframe>` 且其 `srcdoc` 含该 html、**`<iframe>` 无 `sandbox` 属性**；③insight 视图有 `.display-screen__notice` 且**无关闭按钮**（`queryByRole("button")` 在 notice 内为 null），按 Esc 不移除它；④`initial={{kind:"unknown-future-kind"}}` → 回退渲染标题视图（不崩）；⑤派发 `jarvis:display-changed` 事件 → 组件重新 `GET /api/display`（mock）并按新 state 渲染。 | `npm run test:display && npm run test:e2e` | 是 |
| TEST-041 | E2E/Display（真实入口） | REQ-F-015, REQ-F-026, REQ-F-027 | MOD-DISPLAY, MOD-CHAT-UI / TASK-037, TASK-038, TASK-039 | 真实浏览器：①首页加载 → 全屏展示屏为标题视图，底部对话 + ☰ 菜单浮其上且可交互；②技能轮产出洞察 → 展示屏切换为该 HTML 报告（含不可关闭提示条）；③发送「显示首页」类消息 → 展示屏回标题视图；④**`page.reload()` → 展示屏内容保持**（②之后刷新仍是洞察，③之后刷新仍是标题）；⑤展开对话面板时展示屏仍在其下、☰ 仍可点（z-index）。 | `npm run test:e2e` | 是 |
| TEST-042 | Unit/Chat 路由 display 字段 | REQ-F-027 | MOD-SKILLS, MOD-CHAT / TASK-039 | `routeTurn` 的 `display` 字段：①mock 返回 `{"display":"home"}` → `display` 为 `"home"`；②mock 返回无 `display` 键 / `display:"garbage"` → `display` 为 `null`（fail-open，展示屏不变）；③路由层收到 `display:"home"` → 调 `setDisplayState({kind:"home"})` + SSE 尾发 `{type:"display",kind:"home"}`；④`skill` 与 `display` 互不阻塞（`{"skill":"s","display":"home"}` → 两者都生效）。 | `npm run test:skills && npm run test:chat-stream` | 是 |
| TEST-043 | Unit/Zip 解析（对抗性） | REQ-F-020, REQ-NF-005 | MOD-ZIP / TASK-040 | `readZipEntries` 逐条独立断言（原则 15）。**正向**：①store(method 0) 条目原样取出；②deflate(method 8) 条目经 `inflateRawSync` 还原；③嵌套路径 `a/b/c.md` 保留；④目录条目（名以 `/` 结尾）跳过且不计入 `maxEntries`；⑤**合法的高压缩比**样本（大量重复文本，比值高但在 `maxRatio` 内）**不得误杀**；⑥`deriveFolderName`：单顶层目录 → 用它并剥前缀；多顶层 → 用 zip 基名。**整体拒绝（抛 `ZipError`）**：⑦条目名含 `../`；⑧绝对路径 `/etc/passwd`；⑨盘符 `C:\\x`；⑩反斜杠分隔 `a\\b`；⑪加密位(GPBF bit 0)置位；⑫条目数 > `maxEntries`；⑬单条解压后 > `maxEntryBytes`；⑭总解压后 > `maxTotalBytes`；⑮压缩比 > `maxRatio`；⑯归档本身 > `maxArchiveBytes`；⑰无 EOCD / 截断 / 偏移越界。**逐条排除（进 `skipped`，不拒整包）**：⑱压缩方法非 `0`/`8`。另断言 `zip.ts` **不 import `node:fs`**（源码 grep 守卫，CP-10）。 | `npm run test:zip` | 是 |
| TEST-044 | UI/Skills 投递（组件 + 真实入口） | REQ-F-020 | MOD-CHAT-UI / TASK-041 | 独立断言：①**拖入非文件夹非 zip**（如单个 `.pdf`）→ `event.preventDefault()` **被调用**（合成事件断言）且记录区出现「只能接收技能文件夹或 zip 压缩包…」说明——**本次 P6 根因的回归守卫**；②拖入文件夹 → 走 `submitSkillUpload({kind:"folder"})`，POST `/api/skills` 带 `folderName` + 多个 `file`；③拖入 `.zip` → 走 `submitSkillUpload({kind:"archive"})`，POST 带 `archive` 字段；④悬浮条有两个真实 `<button>`「上传文件夹」「上传 zip」，点击各自触发对应隐藏 `<input>`；⑤三条路径的成功回执渲染**共用同一段代码**（断言三者产生同形状的系统消息）。 | `npm run test:floating-chat && npm run test:e2e` | 是 |
| TEST-045 | API/Skills 回执与白名单 | REQ-F-020, REQ-NF-005 | MOD-SKILLS, MOD-ZIP / TASK-042 | 独立断言：①白名单放宽——`.mdx`/`.toml`/`.sh`/`.jsonl`/`.xml` 文件**进入**注入内容（`resolveSkillForTurn` 返回值含之）；②`excluded` 四类各一：`binary`（含 NUL 字节，未落盘）、`too-large`（超单文件上限，未落盘）、`not-injected`（如 `.rs`，**已原样落盘**但不进注入）、`unsupported-zip-method`（zip 内 method 12 条目）；③违反 REQ-NF-005 的归档 → **400 且 `.data/skills/` 下不产生任何目录或文件**（部分写入检查）；④zip 与文件夹两条路径注册出的 slug 与 `SKILL.md` 一致（`deriveFolderName` 生效）。 | `npm run test:skills` | 是 |
| TEST-046 | UI/技能可见性（组件 + 真实浏览器） | REQ-F-028 | MOD-SETTINGS-UI, MOD-CHAT-UI, MOD-CHAT / TASK-043 | 独立断言：①☰ 菜单内 `SkillList` 渲染「名称 — 描述」列表；②无技能时显示「尚未注册技能」；③（**真实浏览器**，原则 12）注册成功后**不刷新页面**列表即出现新技能（`SKILLS_CHANGED_EVENT` 生效）；④命中技能的轮次记录区出现「本轮使用技能：&lt;name&gt;」；⑤未命中技能的轮次**不出现**该提示；⑥`SkillList` 只读——列表项内无删除/编辑按钮（非目标守卫）。 | `npm run test:auth-ui && npm run test:e2e` | 是 |

| TEST-047 | UI/Foundation 静态契约 | REQ-NF-006, REQ-F-014 | MOD-UI-FOUNDATION / TASK-044, TASK-048 | ①`components.json` 指向 `src/app/globals.css`、RSC 与 CSS variables；②`globals.css` 含 DEC-019 所列语义 HSL 三元组、浅色默认和 `[data-theme="dark"]` 覆盖，且无按 OS 偏好自动深色的 media rule；③本地 `components/ui` primitive 存在；④被替换的旧视觉 class selector、手绘 UI 图标和第二套可达 stylesheet 不残留；⑤Token、focus、reduced-motion、两主题对比度静态规则 0 FAIL。 | `npm run test:visual && npm run test:typecheck` | 是 |
| TEST-048 | UI/Home、主题与菜单（组件 + 真实浏览器） | REQ-F-002, REQ-F-006, REQ-F-014, REQ-F-015, REQ-NF-006 | MOD-DISPLAY, MOD-SETTINGS-UI / TASK-045 | ①默认浅色紫色，显式深色写 `data-theme` 与 `jarvis-theme` 并重载保持；②DisplayScreen、底部聊天、左下角菜单在同一视图正常层叠；③菜单通过键盘打开/关闭并回焦点；④菜单项名称为「模型」，打开模型设置；⑤对话不存在 Provider/模型选择器，发送请求不含 `providerId`/`model`；⑥Display API 与 `jarvis:display-changed` 原有行为不回归。 | `npm run test:theme && npm run test:auth-ui && npm run test:e2e` | 是 |
| TEST-049 | UI/模型设置与弹窗（组件 + 真实浏览器） | REQ-F-006, REQ-F-007, REQ-F-008, REQ-F-014, REQ-NF-006 | MOD-SETTINGS-UI / TASK-046 | ①模型弹窗与深链页面使用同一表单语义；②新增、编辑、测试、启停、优先级上移/下移仍经既有 Provider API；③Dialog 打开时焦点进入、Esc/关闭后焦点返回，长表单可滚动；④标签、错误反馈、图标按钮 tooltip 和触控尺寸符合 UI-GOV-001；⑤无旧 dialog/form 样式残留。 | `npm run test:settings-ui && npm run test:providers && npm run test:e2e` | 是 |
| TEST-050 | UI/聊天与全界面真实入口 | REQ-F-002, REQ-F-003, REQ-F-004, REQ-F-005, REQ-F-016, REQ-F-017, REQ-F-018, REQ-F-019, REQ-NF-004, REQ-NF-006 | MOD-CHAT-UI, MOD-DISPLAY / TASK-047, TASK-048 | ①流式聊天、停止、新对话、折叠/重载、状态灯、请求级错误与 Markdown 行为不变；②聊天控件采用可访问的 Button/Input/Textarea/Tooltip，状态灯语义与焦点可见；③在 320/390/1280px 的浅色紫色和深色主题下无横向溢出、遮挡、焦点丢失或不可点菜单；④真实浏览器走 Provider 优先级配置→聊天→展示屏→主题→重载路径；⑤P3 上传/技能 UI 不在断言或通过证据中。 | `npm run test:floating-chat && npm run test:markdown && npm run test:e2e && npm run test:ui-contract:live` | 是 |
| TEST-051 | 治理/作用域与阶段（真实入口） | —（流程控制） | 治理工具 / TASK-044, TASK-047 | ①`gate g3 --cr <name>` 只判该 CR 关联的 TEST——构造「本 CR 测试全 PASS、他 CR 测试缺证据」的场景，`--cr` 给出 PASS 而**缺省仍 FAIL**（证明缺省语义未被削弱）；②`review r2 --cr <name>` 同理只判该 CR；③`gate g4 --cr x` 与 `check release --cr x` **报错拒绝**；④`check p1\|p2\|p3` 各自跑对应门禁集合，任一子门 FAIL 则整体 FAIL；⑤`tsconfig.json` 的 `include` 增删 `.next-*/types/**/*.ts` 项后 `verify` **不报** `BASELINE_CHANGED`，但改动其它字段**仍报**。 | `python -m unittest tests.test_governance` | 是 |
| TEST-052 | 治理/脚手架（真实入口） | —（流程控制） | 治理工具 / TASK-045 | ①`new-cr <name>` 产物直接通过 `check-changes`（13 标签齐全、全用半角冒号）；②同名已存在时拒绝覆盖且不改动原文件；③`matrix <cr>` 依 CP 登记表生成三张矩阵，行数 = CP 数、列含四角色、格为 `TODO`；④刚生成、未填裁决的矩阵跑 `review r2` **必报 `MATRIX_INVALID`**（`TODO` 不是合法裁决）；⑤已有矩阵时默认跳过，`--force` 才重写。 | `python -m unittest tests.test_governance` | 是 |
| TEST-053 | 治理/结构契约（真实入口） | —（流程控制） | 治理工具 / TASK-046 | ①`check-specs` 对合规说明书 PASS；②节名不在白名单 → FAIL 并指名该节；③同一 CR 在同一层出现两个变更响应节 → FAIL；④说明书内残留评审/复盘节（五种历史节名各一）→ FAIL；⑤`migrate_specs.py` **幂等**：对已迁移的文档二次运行产生零 diff。 | `python -m unittest tests.test_governance` | 是 |
| TEST-054 | 治理/向后兼容（真实入口，**硬门**） | —（流程控制） | 治理工具 / TASK-046 | 迁移**前后**分别对全部 CP-model CR（CR-20260909-skills、CR-20260909-display-screen、CR-20260910-skill-intake、CR-20260910-ui-foundation、CR-20260910-process-hardening）运行 `review r1\|r2\|r3\|r4`，断言**结果完全一致且全 PASS**——迁移不得打断任何在途 CR；并断言每份被移走的评审文字在对应 CR 内可检索到（信息不丢）。 | `python -m unittest tests.test_governance && python tools/governance.py review r1` | 是 |
| TEST-055 | 治理/分档门（真实入口） | —（流程控制） | MOD-GOVERNANCE / TASK-053 | ①`check-doors` 对全部 CP 模型 CR 通过；②**阳性**：构造 `发现方式=发现不了` + `门=双向` 的 CP → 断言 `CHECK_DOORS_UNDETECTABLE_TWO_WAY`；③**阳性**：构造含单向 CP 但「回滚方式」为占位的 CR → 断言 `CHECK_DOORS_MISSING_ROLLBACK`；④**阳性**：门取值非 {单向,双向} → 阻断；⑤缺列即阻断。 | `python tools/governance.py check-doors && python -m unittest tests.test_governance` | 是 |
| TEST-056 | 治理/ID 占用（真实入口，**含事故回归**） | —（流程控制） | MOD-GOVERNANCE / TASK-054, TASK-055 | ①`check-ids` 对 17 个回填后的 CR 通过；②**事故回归**：构造两个 CR 各声明 `TASK-044..047` → 断言 `CHECK_IDS_OVERLAP`（复现 2026-09-10 实际撞号）；③**阳性**：声明 `TASK-999` 而表中不存在 → `CHECK_IDS_DANGLING`；④**阳性**：同一任务表中 `TASK-053` 出现两行定义 → `CHECK_IDS_DUPLICATE`；⑤**不变量**：TASK-055 回填前后对全部 CP 模型 CR 跑 `review r1..r4`，结果逐字一致。 | `python tools/governance.py check-ids && python -m unittest tests.test_governance` | 是 |
| TEST-057 | 治理/证据可核验（真实入口，**含事故回归**） | —（流程控制） | MOD-GOVERNANCE / TASK-056 | ①`check-warnings` 对迁移后的 `known_warnings` 通过；②**事故回归**：构造 `{check:"gate_red:g3"}` 而 `g3` 实测 PASS → 断言 `CHECK_WARNINGS_STALE`（复现 LV-AXE / g3 两条过期断言）；③**阳性**：`{check:"dep_absent:axe-core"}` 而 `axe-core` 在 `package.json` 中 → 阻断；④`manual` 缺 `reviewed_at` 或超 90 天 → 报陈旧；⑤自由文本条目报 `UNVERIFIABLE` 且**不**阻断。 | `python tools/governance.py check-warnings && python -m unittest tests.test_governance` | 是 |
| TEST-058 | 运行时/dev server 一致性（**真实浏览器入口**，含事故回归） | REQ-NF-006 | MOD-GOVERNANCE / TASK-057 | ①对真实运行的 dev server 取首页引用的样式表，断言含 Tailwind 工具类与 token；②**事故回归**：喂入一份不含工具类的 CSS → 断言识别为「dev server 早于构建配置启动」（复现 2026-09-10「元素挤在左上角」）；③服务器未运行时**显式 SKIP 并说明原因**，退出码不伪装成通过。 | `node scripts/check-dev-server.mjs` | 是 |
| TEST-059 | 治理/结构规则合流（真实入口，含事故回归） | —（流程控制） | MOD-GOVERNANCE / TASK-058 ① | ①`check-specs` 与 `migrate_specs.py --dry-run` 判定一致；②**事故回归**：把一个 `## 变更响应` 节放到不合规位置 → 断言 `check-specs` 报 `SPECS_NOT_CANONICAL`（此前该场景 `check-specs` 放行而迁移器判定需重写）；③迁移幂等性回归（TEST-053 ⑤ 承接）。 | `python tools/governance.py check-specs && python tools/migrate_specs.py --dry-run` | 是 |
| TEST-060 | 治理/如实报告（真实入口，含事故回归） | —（流程控制） | MOD-GOVERNANCE / TASK-058 ② | ①`review r1..r4` 对无 CP 表的 CR 输出 `SKIPPED_BY_LEVEL` 并列出 CR 名（**不再静默**）；②**阳性**：无 CP 表但级别非 L1 的 CR → 阻断；③既有 8 个 CP 模型 CR 的判定结果不变（向后兼容）。 | `python tools/governance.py review r1 && python -m unittest tests.test_governance` | 是 |
| TEST-061 | Unit/迁移框架 | REQ-F-013, REQ-F-040, REQ-NF-007 | MOD-DB / TASK-059 | ①`up` 到最新后 `PRAGMA user_version` 正确；②**`up→down→up` 零 diff**（schema 与数据）；③缺 `down` 的迁移注册即抛；④`down` 前导出被删列数据到 `.data/rollback-*.jsonl` 且内容可读回；⑤version 1（`migrateProviderPriority`）行为与迁移前逐字一致。 | `npm test -- migrations` | 是 |
| TEST-062 | Unit/Data（含向后兼容） | REQ-F-013, REQ-F-037, REQ-F-038, REQ-F-040 | MOD-DB, MOD-CHAT / TASK-060, TASK-071 | ①六处 schema 变更后老行读取路径不变（新列 NULL/0）；②**混合新旧行的会话**重建不抛且顺序正确；③**同毫秒 5 行工具记录**按 `seq` 重建顺序稳定；④`role:"tool"` 行可写可读；⑤`GET /api/conversations/[id]/messages` 返回含工具记录且旧客户端字段仍在（向后兼容）；⑥`app_settings` 读写。 | `npm test -- store && npm test -- conversation-routes` | 是 |
| TEST-063 | Unit/Adapter（两组分块 fixture） | REQ-F-029, REQ-NF-010 | MOD-ADAPTER / TASK-061 | ①请求体含 `tools` 时序列化为 OpenAI 形状；②**分片累积**——`id`/`name`/`arguments` 跨多 chunk 到达仍拼出完整 `tool_calls`，用**两组粒度不同的 fixture**（模拟 DeepSeek 与本地服务）；③**不依赖 `finish_reason`**（构造缺该字段的流仍正确收束）；④同轮多个 `tool_calls` 按 `index` 各自累积不串；⑤`role:"tool"` 消息带 `tool_call_id` 正确序列化。 | `npm run test:adapters` | 是 |
| TEST-064 | Unit/Adapter（usage 与兼容回退） | REQ-F-037, REQ-F-012 | MOD-ADAPTER / TASK-062 | ①带 `stream_options` 时末 chunk `choices:[]` **不被视为异常**且发出 `usage` delta；②`kind==="local"` 不发该参数；③发送后 4xx 指向该参数 → 自动重试一次不带参并记住，**REQ-F-012 路径仍通**；④拿不到 usage 时本地估算且 `estimated:true`；⑤估算值对中英文混排在合理区间。 | `npm run test:adapters` | 是 |
| TEST-065 | Unit/Provider 能力探测 | REQ-F-040, REQ-F-007 | MOD-ADAPTER, MOD-PROVIDER / TASK-063 | ①探测走 **POST `/chat/completions`** 且请求体含最小 `tools`（断言方法与体，`GET /models` 即失败）；②三态 `yes`/`no`/**`未探测`** 各一断言；③**200 但静默忽略 `tools`** 的服务判为 `no`；④`default_model` 变更后该模型记录失效；⑤运行时 `no` 与 `未探测` 均降级纯对话 + **每会话仅一次**提醒；⑥REQ-F-006 优先级**不因能力跳过**。 | `npm run test:provider-routes` | 是 |
| TEST-066 | Unit/工具注册表 | REQ-NF-008, REQ-NF-010, REQ-NF-011 | MOD-TOOLS / TASK-064 | ①**注册一个假工具即可被循环调用**（不改对话核心）；②四种不注册场景各一断言——无技能 / 联网关 / URL 未配 / Provider 不支持，且**未注册的定义不进 prompt**；③工具集合在一次发送内不变（短路发生在 `execute` 内，描述符仍在）；④description ≤200 字符守卫；⑤**grep 守卫**：对话核心不 import 具体工具模块；⑥**grep 守卫**：无 subagent / sandbox / shell 类工具注册。 | `npm test -- tools-registry` | 是 |
| TEST-067 | Unit/循环内核 | REQ-F-029, REQ-F-005 | MOD-TOOLS, MOD-CHAT / TASK-065, TASK-071 | ①**每次 `tool_call` 计 1 步**；②第 11 步被拒、落 `status=truncated`、发提醒、**成果保留**、`truncated` 可重放；③**触顶不结束会话**（可继续追问）；④工具失败作结果回喂、循环继续；⑤同工具 + 同规范化 JSON 参数（键序无关）连续失败 2 次后第 3 次被拒；⑥同轮多 `tool_calls` 并发执行；⑦**工具执行期 abort** 立即生效（可控 promise 假工具），已完成步骤落库；⑧**悬空 `tool_calls`** 回放时合成「已中止」tool 行，**外发序列合法**。 | `npm run test:chat-stream` | 是 |
| TEST-068 | Unit/预算与前缀（纯函数） | REQ-NF-007, REQ-NF-008, REQ-F-041, REQ-F-004 | MOD-TOOLS / TASK-066 | ①**稳定前缀字节级快照**——同会话内两轮前缀逐字一致；②**当前时间不出现在稳定前缀**；③保留窗口 N=2——第 3 轮前的工具结果被替换为标记且 `tool_call_id` 保留（协议仍合法）；④各子预算越界各一断言（名录/工具定义/单条工具结果/来源）；⑤工具结果超限截断并有尾注；⑥总输入超限先收窄、仍超**报请求级错误而非继续压缩**；⑦窗口取 `context_window`，空时按 `kind` 默认表。 | `npm test -- budget` | 是 |
| TEST-069 | Unit/技能工具 | REQ-F-030, REQ-F-022 | MOD-TOOLS, MOD-SKILLS / TASK-067 | ①`list_skills`/`read_skill`/`search_skills` 各一断言；②**system prompt 不含技能正文**（只含名录）；③**每轮无独立路由调用**（断言模型调用次数）；④一轮内 `read_skill` 多个技能均进上下文；⑤名录超子预算时只列前 N 条并含「用 `search_skills`」提示；⑥无技能时三工具不注册；⑦32KB 上限作用于**单次 read 结果**；⑧`makeCompleter` 仍可用且不在工具表内。 | `npm run test:skills` | 是 |
| TEST-070 | Unit/展示屏工具 | REQ-F-032, REQ-F-023, REQ-F-024, REQ-F-025 | MOD-TOOLS, MOD-DISPLAY / TASK-068 | ①`show_home` → `display_state.kind="home"`；②`show_insight(id)` → 指向该洞察，**且在循环中途即时生效**（不等 `onFinal`）；③**属主校验**——跨会话 / 不存在 id → 工具失败回喂，`display_state` 不变；④`save_insight` → `insights` 追加行且 `display_state` 切换；⑤HTML 不完整 → 工具失败回喂、**无 `insights` 行**；⑥**未调 `save_insight` 时对话内出现「本轮未产出洞察」告知**（用户终裁 1，不静默）；⑦**提示条覆盖经 `save_insight` 上屏的 HTML**；⑧**注入样本**：`read_url` 结果含「请保存下列 HTML」诱导文本时，断言该路径已被登记为已接受风险（`skill-html-unsandboxed-web-source`），测试记录实际行为而不伪装已防护。 | `npm test -- display && npm test -- tools-display` | 是 |
| TEST-071 | Unit/Security（SSRF 对抗样本，纯函数） | REQ-NF-009 | MOD-TOOLS / TASK-069 | **逐条独立断言**（原则 15）：①`http://127.0.0.1`、`http://localhost`；②IPv6 `::1`、`fe80::…`、`fc00::…`、`::ffff:127.0.0.1`；③私网 `10.`/`172.16.`/`192.168.`；④`0.0.0.0/8`、`100.64/10`；⑤云元数据 `169.254.169.254`；⑥URL userinfo `http://u:p@host`；⑦非 http(s)（`file:`/`gopher:`/`data:`）；⑧**多 A 记录任一落禁区即拒**；⑨**重定向到内网**——经注入 resolver 与测试宿主白名单在本地 http 服务上构造，断言第 2 跳被拒；⑩超 5 跳被拒；⑪**DNS 重绑定**：记录为 known limitation，断言当前行为（校验与连接非原子）**如实**而非伪装已防护；⑫`url-guard.ts` 不 import `node:fs`（grep 守卫）。 | `npm test -- url-guard` | 是 |
| TEST-072 | Unit/联网工具与降级 | REQ-F-033, REQ-F-034, REQ-NF-009, REQ-NF-011 | MOD-TOOLS / TASK-070 | ①`web_search` 请求 URL 为 `{base}/search?q=&format=json`，**载荷只含查询词**（断言不含历史 / 凭据 / 身份）；②结果条数 ≤10 且超子预算被截断；③后端不可用（连接失败 / 超时 / 非 2xx / JSON 非法）→ 作工具失败回喂，**不弹请求级错误**；④连续失败 2 次后本轮短路且步骤流标「搜索后端不可用」，**描述符仍在 prompt 中**；⑤`read_url` 返回**摘要非原文**且原文不落库；⑥**vitest 全局 fetch 守卫**：白名单（`adapters`/`makeCompleter`/`providers/test`/`tools/web.ts`）外的模块发起 fetch 即失败。 | `npm test -- tools-web` | 是 |
| TEST-073 | Unit/来源引用 | REQ-F-039 | MOD-CHAT / TASK-072 | ①用了联网工具的轮次回答附来源列表（URL + 标题）；②**伪造 URL 被剔除**——不在本轮工具结果集合内的 URL 不出现在来源中且步骤流有标记；③来源为**独立结构**（尾事件 + 落库字段），断言**正文未被改写**；④未用联网工具的轮次无来源；⑤来源链接按 REQ-F-016 仅 http(s) 可点击。 | `npm test -- sources` | 是 |
| TEST-074 | API+UI/技能管理 | REQ-F-031, REQ-NF-005 | MOD-SKILLS, MOD-SETTINGS-UI / TASK-073 | ①`DELETE /api/skills/[name]` 删除后 DB 行与目录均消失、列表免刷新更新；②删除需**二次确认**（未确认不发请求）；③`PATCH` 重命名后目录 slug、DB 行、`SKILL.md` frontmatter `name` **三处同步**；④重名 → 冲突错误；⑤`../x` 类非法名被拒（`path.resolve` 越界校验）；⑥目录删失败时留孤儿目录**并在回执告知**（不产生 DB 有行目录没了的态）；⑦历史会话中已 read 的技能内容不受影响。 | `npm run test:skills` | 是 |
| TEST-075 | UI/搜索配置与 token 用量 | REQ-F-038, REQ-F-037 | MOD-SETTINGS-UI / TASK-074 | ①☰ 出现「搜索」项且**与「模型」并列**（不在「模型」弹窗内）；②「联网」总开关**默认开**；③总开关关 → 两工具不注册；仅 URL 未配 → 只 `web_search` 不注册；④URL 含 userinfo **拒绝保存**；⑤「测试连接」判据 2xx **且** JSON 含 `results`；**SearXNG 未开 json 输出**（返回 HTML）时给针对性报错；⑥☰ 显示会话累计 token 且随对话增长；⑦估算值**标注「估算」**；⑧新对话清零；⑨`CornerMenu` 本体未被修改（代码审查 + 既有测试回归）。 | `npm run test:settings-ui` | 是 |
| TEST-076 | UI/步骤流 | REQ-F-035, REQ-F-028, REQ-F-016 | MOD-CHAT-UI / TASK-075 | ①`tool_call` 事件 → 记录区出步骤行（工具名 + 参数摘要 **≤200 字符单行**）；②`tool_result` → 该行更新完成/失败；③**默认折叠**，展开控件为真实 `<button>` + `aria-expanded="false"`；④刷新后按落库记录**重建**且与当时一致；⑤**事件类型单一来源**——`types.ts` 新增 variant 后 `FloatingChat` 无需改白名单即可消费（typecheck + 断言不静默丢弃）；⑥「本轮使用技能」提示**已移除**；⑦工具执行错误显示在步骤流内（**不是**输入框上方红字、**不是**气泡「生成失败」）；⑧结果摘要 Markdown 渲染且链接仅 http(s)/mailto。 | `npm run test:floating-chat` | 是 |
| TEST-077 | UI/灯态与布局（含 a11y） | REQ-F-036, REQ-F-018, REQ-F-019, REQ-F-003, REQ-F-014, REQ-NF-006 | MOD-CHAT-UI / TASK-076 | ①**五态**各有独立 `aria-label`；②态间非文本对比 ≥3:1 **或**形状差异，两套主题各测；③**`prefers-reduced-motion` 下「生成文本中」与「执行工具中」仍可辨**（不得只靠动画）；④折叠态收到 `tool_call` 保持折叠、灯转「执行工具中」、**不自动展开**；⑤完成/触顶/失败各做一次瞬时提示后落定；⑥面板高度——纯对话 ≤50vh、含步骤流 ≤75vh 各一断言；⑦**10 步场景下最终回复首行可见**（boundingBox 落在面板可视区）；⑧`ui-contract` LB-06 五态、RF-09 双阈值、步骤流 a11y 规则全过。 | `npm run test:visual && npm run test:ui-contract && npm run governance:ui` | 是 |
| TEST-078 | **真实入口冒烟/E2E（两条 agent 主路径）** | REQ-NF-004, REQ-F-029..041 | MOD-TOOLS, MOD-CHAT, MOD-CHAT-UI / TASK-059..076 | ①**mock 模型发 OpenAI `tool_calls` 流式增量脚本**（`smoke.mjs` / `run-e2e.mjs` 现仅回文本 delta，须扩展）；②**内置 mock SearXNG**，使 `test:e2e`/`test:smoke` 在**无真实 SearXNG 环境下全绿**；③路径一：注册技能 → 提问 → 步骤流出现 `read_skill` 行 → 回答；④路径二：配置搜索 → 提问 → 步骤流出现 `web_search`/`read_url` → 回答**附来源**；⑤路径三：**未配置搜索**时对话正常、联网工具不注册；⑥刷新后步骤流重建；⑦真实 SearXNG 与弱模型（PM-2）为**人工冒烟**，`test-results.json` 标 `verification: manual`。 | `npm run test:smoke && npm run test:e2e` | 是 |
| TEST-079 | Unit/压缩判定（纯函数，**含真实会话形状基准**） | REQ-F-042 ①③, REQ-NF-012 ①, REQ-NF-007 ④ | MOD-TOOLS, MOD-CHAT / TASK-077, TASK-081 | ①**基准用 EV-2026-09-11 §1 的真实会话形状**（109 / 78 / 28 / 18 / 10 条），非构造的理想数据；②**长会话**（109、78 条）压缩后总输入下降 **≥70%**；③**短会话不得变贵**——18 条与 10 条两种形状断言 `shouldCompact === false`（净省校验拦截），且总输入**不高于**未压缩时；④未达 `COMPACT_TRIGGER_RATIO` 时不压缩，**不论轮数多少**（守住「绝不按轮数触发」）；⑤三级顺序：先收窄工具结果、再压缩、仍超才抛 `ContextOverflowError`；⑥一轮内最多压缩一次（不循环）。 | `npm test -- budget` | 是 |
| TEST-080 | Unit/摘要生成（含失败注入） | REQ-F-042 ②, REQ-NF-012 ②③④ | MOD-CHAT / TASK-078 | ①摘要调用走**当前对话解析出的同一 Provider / 模型**，非流式，`max_tokens` 有界（断言请求形状）；②**增量**——第二次压缩的输入**含上一份摘要**而非全部原文；③**结构守卫**（机器能守的部分）：摘要非空、token 不超 `SUMMARY_MAX_TOKENS`、包含被压轮次中的关键实体；④**fail-open** 三种注入各一断言——completer 抛错 / 超时 / 返回空字符串，均断言**对话照常完成**且**未落库摘要行**；⑤摘要生成的 token 经 `addUsage` 计入会话累计（断言累计增加）。**语义准确性不在本用例覆盖范围内**，属真实入口人工发现项（见 CR CP-2）。 | `npm test -- compaction` | 是 |
| TEST-081 | Unit/回放与回滚安全 | REQ-F-004, REQ-F-042 ⑥ | MOD-CHAT, MOD-DB / TASK-079 | ①回放序列 = **最后一条摘要 + 其后的原文**，摘要之前的消息不再发送；①′ 摘要行**落在被覆盖区间末尾之后、保留轮次之前**（而非库尾），被保留的 K 轮与触发轮在下一轮仍逐字在场；同一毫秒写入的相邻行顺序由 `seq` 保证；②**原始消息行仍在库中**（断言 `listMessages` 仍返回它们）；③多次压缩时只有**最后一条**摘要生效；④**回滚安全**：用旧的 `REPLAYABLE_STATUSES`（不含 `summary`）过滤时，摘要行被排除、退回全量回放，**摘要绝不被当作真实对话内容外发**；⑤摘要行的 `role='system'`，不与 user/assistant 混淆。 | `npm test -- compaction` | 是 |
| TEST-082 | UI/压缩边界标记（含 a11y） | REQ-F-043 | MOD-CHAT-UI / TASK-080 | ①压缩边界渲染一条标记，**不是消息气泡**（断言不带 `floating-chat__message` 类）；②默认折叠；③展开控件是真实 `<button>` + `aria-expanded="false"`，有可访问名；④点开后显示摘要全文；⑤刷新后按落库记录**重建**该标记；⑥`ui-contract` 新规则通过；⑦压缩当轮的 `compacted` 事件即时在边界处（锚点行之后）插入标记；锚点行不在当前列表时按保留轮数回退定位。 | `npm run test:floating-chat && npm run test:ui-contract` | 是 |
| TEST-083 | 真实入口/长对话压缩 | REQ-F-042, REQ-F-043, REQ-NF-012 | MOD-CHAT, MOD-CHAT-UI / TASK-077..081 | ①真实入口驱动一个**超过触发阈值**的会话，断言压缩发生、对话继续、未报错；②压缩边界标记在真实浏览器中可见且可展开；③刷新后标记与摘要仍在；④`GET /api/conversations/[id]/messages` 返回摘要行且**向后兼容**（旧字段仍在）。 | `npm run test:e2e` | 是 |
| TEST-084 | Unit/知识存储（文件、frontmatter、名称、边界、待采纳区） | REQ-F-044 ①②, REQ-F-046 ③, REQ-NF-013 ①② | MOD-KNOWLEDGE / TASK-082 | ①保存即带 frontmatter 的 `.md`，读回标题 / 来源 / 正文一致；②手写无 frontmatter 文件可读，标题取标题行、来源 file；③标题缺省取首行，同名加序号不覆盖；④空内容 400、超 64KB 413；⑤路径穿越在 slug 层消失，非法名读 / 删均「不存在」；⑥删除后不列出；⑦待采纳区：提议不进列表、不进检索，采纳后进入，忽略即删；⑧采纳时与已有条目同名加序号。 | `npm test -- knowledge.test` | 是 |
| TEST-085 | Unit/知识检索（纯函数 + 目录） | REQ-F-045 ①②④ | MOD-KNOWLEDGE / TASK-082 | ①`tokenize`：CJK 二元、拉丁词、全角归一、标点不产 token；②`rankBm25`：含查询词的文档排前、不含的不出现、空入空出；③`snippetFor` 围绕首个命中词；④目录检索：一句话查询命中正确条目且片段含命中词、`limit` 生效、空查询空返回；⑤索引随目录变化失效：新增文件即可检到、删除即消失。**语义 / 同义检索不在覆盖范围**（真实入口，CR CP-2）。 | `npm test -- knowledge.test` | 是 |
| TEST-086 | Unit/知识工具 | REQ-F-045 ③, REQ-F-046 ③, REQ-NF-013 ③ | MOD-TOOLS / TASK-083 | ①三条 description ≤ 200 字符、可注册；②知识库为空时只注册 `save_knowledge`；③`search_knowledge` 返回「名称｜标题：片段」、`read_knowledge` 返回全文、未知名称失败并提示先检索、缺参失败；④`read_knowledge` 超子预算截断并留标记；⑤`save_knowledge` 只写待采纳区、附 `knowledge_pending` 事件、**采纳前检索不到**；⑥空 / 超大内容作失败结果回喂而非抛错。 | `npm test -- knowledge-tools` | 是 |
| TEST-087 | API/知识路由 | REQ-F-044 ③④, REQ-F-046 ①②③, REQ-NF-013 | MOD-KNOWLEDGE / TASK-084 | ①multipart `.md` → 201 且标题取标题行、`.pdf` → 400、超大 → 413；②JSON（source=conversation）→ 201 并直接进 entries；空内容 400；③待采纳：GET 分列、`POST pending/[name]` 采纳进 entries、`DELETE` 忽略、不存在 404；④`GET/DELETE /[name]`，删除后 404，穿越形名称 404；⑤未登录全部 401。 | `npm test -- knowledge-route` | 是 |
| TEST-088 | UI/知识库列表与对话侧入口（含 a11y） | REQ-F-044 ③④, REQ-F-046 ①②③ | MOD-SETTINGS-UI, MOD-CHAT-UI / TASK-085 | ①列表渲染标题 / 来源标签 / 名称，空态说明如何添加；②待采纳队列为 `role=region` + 可访问名，顶部显示计数，采纳走 `POST /api/knowledge/pending/<encoded>` 后刷新；忽略走 DELETE，失败显示服务端文案；③`jarvis:knowledge-changed` 触发重取；删除先确认；④拖入 `.md` → `POST /api/knowledge` multipart，收据「已存入知识库：<标题>」；⑤助手气泡「存入知识库」为真实 `<button>` + 可访问名，只出现在助手回复上，POST JSON `{content, source:"conversation"}`；⑥`knowledge_pending` 事件 → 转录区提示 + 派发事件；⑦`ui-contract` LB-12 通过；TEST-044 ① 拒绝文案更新。 | `npm test -- knowledge-list && npm run test:floating-chat && npm run test:ui-contract` | 是 |
| TEST-089 | 真实入口/知识库全链路 | REQ-F-044 ⑤, REQ-F-045, REQ-F-046 | MOD-KNOWLEDGE, MOD-TOOLS, MOD-CHAT-UI / TASK-082..086 | ①经 API 存一条知识后，**新对话**里模型调 `search_knowledge` 检到它（步骤行可见、回复带条目内容）；②模型调 `save_knowledge` → 转录区提示、`GET /api/knowledge` 里在 pending 不在 entries、☰ 待采纳队列可见并显示计数、点「采纳」后进入 entries；③真实浏览器拖入 `.md` → 收据 + 条目出现；④刷新后 ☰ 列表首开即含全部条目。 | `npm run test:e2e` | 是 |
| TEST-100 | Unit/唤醒设置与日用量 | REQ-F-060 ①②③, REQ-NF-020 ② | MOD-WAKE / TASK-100 | ①默认关、30 分钟、20,000；②写入校验：间隔 0 / 1.5、上限 -1、enabled 非布尔均拒绝；合法值落库读回；③日用量按日期归零、坏 JSON 视为空。 | `npm test -- wake.test` | 是 |
| TEST-101 | Unit/一次唤醒（含失败注入与不回放） | REQ-F-060 ④⑤, REQ-F-061, REQ-NF-020 ①③ | MOD-WAKE / TASK-100 | ①未开且非手动 → 跳过不调模型；②手动忽略开关不忽略上限，达上限跳过不调模型；③无对话 / 无 Provider → 跳过并说明；④NOOP → 不落库、不显示，token 计日用量与会话累计（估算）；`max_tokens` 200；⑤有内容 → `system/wake` 行带「主动提醒：」前缀，转录区还原为 system 行；⑥**提醒绝不回放**：下一轮真实发送内容不含提醒，行仍在库；⑦调用失败 → 跳过、输入 token 计入、不落库；⑧日用量跨次累加，达上限后下一次跳过；`buildWakeMessages` 只取文本行、排除工具/摘要/早前提醒、超长从头截断留标记；`isNoop` 边界。 | `npm test -- wake.test` | 是 |
| TEST-102 | API/唤醒路由 | REQ-F-060 ①②③⑤, REQ-F-061 | MOD-WAKE / TASK-101 | ①GET 默认值 + 当日零用量；②PUT 校验 400 带原因、合法值持久；③无 Provider 的 POST 是 200 `skipped`；④有 Provider：关闭状态 `manual:false` → `skipped disabled` 且**无模型调用**；`manual:true` → **恰一次** `stream:false`、`max_tokens:200` 调用，落 `system/wake` 行；⑤未登录 401。 | `npm test -- wake-route` | 是 |
| TEST-103 | UI/唤醒控件与调度器（含 a11y、假时钟） | REQ-F-060 ②④⑤, REQ-F-061 ①② | MOD-SETTINGS-UI, MOD-CHAT-UI / TASK-102 | ①开关 `role=switch` + 可访问名、初值关，间隔 / 上限 / 今日用量同屏；②开关即保存并派发 `jarvis:wake-changed`，非法间隔显示服务端原因；③「现在唤醒」真实按钮：提醒经 `jarvis:wake-notice` 通告、上限跳过显示文案、用量刷新；④假时钟（只假 `setInterval` 与 `Date`）：开且安静满一个间隔 → 恰一次唤醒，提醒成为 system 行；④′默认关：无定时器无调用；⑤菜单来的提醒进转录区且去重；⑥`ui-contract` LB-13 通过。 | `npm run test:floating-chat && npm test -- wake-settings && npm run test:ui-contract` | 是 |
| TEST-104 | 真实入口/主动唤醒 | REQ-F-060, REQ-F-061, REQ-NF-020 | MOD-WAKE, MOD-CHAT-UI / TASK-100..103 | ①☰ 开关默认关、用量可见；②「现在唤醒」→ 模型**恰一次**非流式调用（mock 断言 `max_tokens` 200 且提示含唤醒指令）→ 转录区出现「主动提醒：…」system 行；③刷新后行仍在，`GET messages` 返回 `role=system status=wake` 且字段与既有行一致；④用量计数 ≥ 1；上限设 1 后再唤醒 → 「已达上限」且**零**模型调用；⑤开关开 → 服务端 `enabled:true`。 | `npm run test:e2e` | 是 |
| TEST-120 | Unit/实体存储与两个指示 | REQ-F-070, REQ-NF-030 ①②④ | MOD-ENTITY / TASK-120 | ①保存即带 frontmatter 的 `.md`，读回一致；②手写最小文件可读、未知 kind 有兜底；③非法 kind / 空名拒绝，路径穿越在 slug 层消失；④同名加序号；⑤**顺序固定**：改过的对象不因此前移；⑥**健康度四例**：无源→未接入；有源但从未采集→**陈旧而非正常**；超期→陈旧；近期采集但解析失败→**解析失败而非正常**；⑦移除最后一个源退回未接入；源必须 http/https、拒凭据、拒重复；⑧写 `change` 令卡片未读，`markSeen` 后清除且变更文字仍在；⑨更新只接受白名单字段，证据按字段去重；⑩待采纳区：提议不进看板、采纳后进、忽略即删、采纳同名加序号。 | `npm test -- entities` | 是 |
| TEST-121 | Unit/实体工具与直写判据 | REQ-F-072, REQ-NF-030 ③ | MOD-TOOLS / TASK-122 | ①四条 description ≤ 200 可注册；②`list_entities` **每行都打印采集状态**，可按 kind 过滤；③`read_entity` 含容量、下一步、源，未知名称提示先列表；④`propose_entity` 只写待采纳区，看板看不到；⑤**缺 `source_url` 一律拒绝**，实体不变；⑥来源不在已登记源内→入队且实体不变；⑦来源与已登记源同域→直接生效并留证据；⑧**未登记任何源的对象，任何来源都不能直写**（防空白名单绕过）；⑨字段非白名单 / 对象不存在 / 链接非法均作失败回喂。 | `npm test -- entity-tools` | 是 |
| TEST-122 | API/看板路由与知识总览 | REQ-F-070 ④, REQ-F-071 ⑤, REQ-F-072, REQ-F-044 ①, REQ-F-045 ④ | MOD-ENTITY, MOD-KNOWLEDGE / TASK-123 | ①未登录每个端点 401；②GET 一次返回三队列；③POST 直接建实体，kind 非法 400；④PATCH 四种 action：`seen` 清未读、`field` 改值、增删源；未知 action / 非白名单字段 / 非法 URL 均 400，缺对象 404；⑤单个 GET/DELETE，穿越形名称 404；⑥待采纳实体采纳进看板、忽略即删；⑦待采纳修改采纳后写入并留证据、忽略即丢弃；已登记源的写入直接生效不入队；⑧总览：按实体、按类型、无归属计数与**缺口按次数排序**。 | `npm test -- entity-route` | 是 |
| TEST-123 | UI/知识看板（含 a11y） | REQ-F-071 | MOD-SETTINGS-UI / TASK-124 | ①三条泳道各就各位，友商网格、其余列表，并标明「顺序固定」；②**每张卡同时给出内容变化与采集健康度**，未配置源与解析失败各自可见；③汇总条数出未读 / 采集异常 / 待采纳；④**点开卡片即 PATCH seen**，`aria-expanded` 随之变化；⑤展开是源管理，无源时明说「安静不代表任何事实」，可增删源；⑥「问 Jarvis 关于这个对象」把问题交给对话框而非在卡内执行；⑦待采纳区同时列新对象与字段修改，采纳各走各的路由；⑧总览：缺口按次数排、无归属显式计数、卡片显示库内篇数；⑨**空看板不假装有数据**并说明如何添加。 | `npm test -- knowledge-dashboard` | 是 |
| TEST-124 | 回归/知识工具与路由 | REQ-F-044, REQ-F-045, REQ-F-046 | MOD-KNOWLEDGE / TASK-123 ④ | 知识条目新增三个可选 frontmatter 键后，TEST-086（知识工具）与 TEST-087（知识路由）全部仍通过：旧条目缺键可读、待采纳区语义不变、检索与截断不变。 | `npm test -- knowledge` | 是 |
| TEST-125 | Unit/采集执行与健康度映射 | REQ-F-070 ⑥, REQ-NF-009 | MOD-ENTITY / TASK-125 | ①`describeChange`：只数行、只引首条新增、顺序调换不算变化、过长截断；②首次采集建立基线且**不计为变化**（否则每配一个源卡片就亮）；③内容不变 → 正常无变更；内容变化 → 写入变更并附证据，卡片随之未读；④抓不到 → `failed_fetch` 且**不推进** `checkedAt`；⑤HTTP 错误同样是 `failed_fetch`；⑥抓到但取不出正文 → `parse_failed`（最危险的一种）；⑦解析失败压过时间：刚采集过也不显示为正常；⑧只采集已登记的源，对象不存在报错且**不发起请求**；⑨快照按「对象 × 源」分开存，互不覆盖。 | `npm test -- sources` | 是 |
| TEST-126 | Unit/链接入库与待采纳判据 | REQ-F-044, REQ-F-070 ⑥ | MOD-KNOWLEDGE / TASK-126 | ①存正文与原链接、**不存原件**，标题取自页面 `title`；②来源与该对象已登记源同域 → 直接入库且立刻可检索；③来源不在登记源内 → 进待采纳区，采纳前检索不到；④未指定归属对象同样进待采纳区；⑤`trustCaller` 让用户自己贴的链接不受白名单限制；⑥`alwaysPending` 可强制入队（模型路径）；⑦非法链接、未知对象、抓取失败、解析失败都作失败返回且不入库，未知对象**不发起请求**；⑧正文超上限拒绝并提示改为摘录；⑨无标题页面退回用主机名，条目仍可读回。 | `npm test -- ingest` | 是 |
| TEST-127 | Unit/字段抽取的证据核对 | REQ-F-072, REQ-F-070 | MOD-ENTITY / TASK-127 | ①原文在条目里且值在原文里 → 通过，来源同域故直接生效并留下「链接 + 条目内定位」的证据；②原文不在条目里 → 拒绝，不写入也不入队（伪造引用）；③原文属实但值不在其中 → 同样拒绝（**最容易漏的一种**：句子是真的，数字飘了）；④换行与多空格不算不一致；⑤条目来源不在已登记源内 → 核对通过也只进待采纳区；⑥一次调用里逐条判定，通过的写入、不合格的拒绝、互不牵连；⑦对象、条目、原始链接三者缺一即整次失败；⑧批量上限与引文长度上限；⑨工具层：全部不通过时 `ok:false` 且逐条理由可见，通过时 `sources` 带回原链接。 | `npm test -- extract` | 是 |
| TEST-128 | Unit/技能库的形状与惰性 | 无（内容类） | 仓库内容 / TASK-128 | ①六本齐备且纪律类在列；②每本 `SKILL.md` 的前置块可被登记解析，`name` / `description` 非空且不是占位符；③文件夹名 slug 化后非空（拖进去不会变成空名）；④每本注入后在单轮 32KB 预算内且不触发截断；⑤`src/` 内无任何代码引用仓库 `skills/`——运行时技能只在 `.data/skills/`。**不断言模型是否遵守**：那是真实入口，见 CR 的 CP-3。 | `npm test -- skills-library` | 是 |

## 真实入口冒烟

真实入口冒烟必须启动实际 Next.js 应用，并使用浏览器或等效 HTTP/UI 驱动执行：

```text
打开首页（仅标题 + 配置/账号两个弹窗按钮，管理员身份免登录直接进入）
  -> 观察状态灯（无 Provider 时为「灭」）
  -> 打开「配置」弹窗（或深链 /settings/models）
  -> 配置两个 Provider（本地 OpenAI-compatible + 另一个），各自连接测试
  -> 调整优先级（上移/下移），关闭弹窗
  -> 刷新首页，观察加载探测后状态灯转「常亮」
  -> 使用底部悬浮输入条发送消息（不选择 Provider），验证用的是优先级最高者
  -> 观察流式输出与 Markdown 渲染
  -> 再发一轮，验证上下文回放
  -> 点击停止，验证服务端也停止、落库 stopped、会话结束
  -> 再发一条，验证是新会话（新 conversationId）
  -> 点击「新对话」，验证记录区清空
  -> 切换深色主题，刷新页面
  -> 验证呈现空会话（因上一步「新对话」）且主题保持
  -> 制造请求级错误（停掉本地模型服务后发送），验证输入框上方红字、不入上下文
```

## 变更响应 · CR-20260909-collapsible-panel

### 任务→测试派生矩阵

| 模块任务 | 分类 | 测试任务 | 类型 | 真实入口 |
|---|---|---|---|---|
| TASK-030 `expanded`→`showTranscript` 重构 | 大改（行为相邻重构） | **TEST-030 与「发送后展开」e2e 在重构后重跑并可能重写断言（回归门）** | 组件 + 真实浏览器 | `test:floating-chat` + e2e |
| TASK-030 折叠控件 + 折叠行为 | 新增 | TEST-031（7 条独立断言） | 组件 + 真实浏览器 | `test:floating-chat` + e2e |
| REQ-F-003 验收放宽 | 小改 | TEST-011「发送后展开」断言改为「发送触发展开」；e2e 同步 | 组件 + 真实浏览器 | `test:floating-chat` + e2e |

### 测试设计

测试角色对每个变化点给出「验证手段 → 断言 → 可交付判断」。**每个 CP 必须有可验证证据方能交付。**

| 变化点 | 验证手段 | 关键断言 | 可交付 |
|---|---|---|---|
| CP-1 REQ-F-019（整体） | 由 CP-3..CP-7 的分项断言合成；TEST-031 承载 | 见下各行 | ✅ |
| CP-2 REQ-F-003 放宽 | 组件（TEST-011）+ e2e | 发送第一条 → `.floating-chat--expanded` 出现（触发展开）；随后点收起 → 消失；再发送 → 再出现。**不再断言「发送后必定展开且不可收起」** | ✅ |
| CP-3 状态模型 | 组件（TEST-031）+ **回归**（TEST-030 + 「发送后展开」e2e 全绿） | `showTranscript` 派生正确：无消息 → 无 `--expanded`；有消息且未折叠 → 有；有消息且折叠 → 无。删 `setExpanded` 后既有用例零回归 | ✅（回归门） |
| CP-4 持久化 | 组件（jsdom localStorage）+ **e2e（原则 12，必须真实浏览器）** | 组件：`applyCollapsed(true)` 后 `localStorage['jarvis:chat-collapsed']==='1'`；`false` 后被移除。e2e：折叠 → `page.reload()` → 仍折叠；展开 → reload → 仍展开 | ✅ |
| CP-5 折叠控件 | 组件（TEST-031）+ ui-contract（FK-03 真实 button；若加规则）+ e2e | 无消息时 `queryByRole("button",{name:/收起对话|展开对话/})` 为 null；发送后出现；`aria-expanded` 随 `showTranscript` 变；点击 toggle 记录区挂/卸 | ✅ |
| CP-6 折叠期间 | 组件（TEST-031，mock stream + fake timers） | ①折叠 + 流式 → 灯 class `--busy`、`.floating-chat__messages` 不在 DOM；②流结束 → 灯 class 出现 `--done`（瞬时），`vi.advanceTimersByTime(1200)` 后回 `--ready`；③折叠时 `fireEvent.submit` → `--expanded` 出现（自动展开）+ `localStorage` 变 expanded | ✅ |
| CP-7 无动画 | ui-contract 静态（MO-01 已有：任何 `transition/animation` 须被 `prefers-reduced-motion` 中和）+ 代码审查 | `.floating-chat--expanded` 无 `transition`；新增的 `--done` 脉冲被既有 reduced-motion 块覆盖 | ✅ |
| CP-8 回归重验 | `npm run test:floating-chat`（重写后）+ `npm run test:e2e`（「发送后展开」「新对话清空 + 刷新空白」「停止」三用例） | 全部 PASS；断言按 `showTranscript` 派生校准，无「假绿」 | ✅ |

**测试总判**：8 个变化点全部可验证、可交付。TEST-031 用 fake timers 覆盖 CP-6 的瞬时提示，持久化跨刷新走真实浏览器 e2e（CP-4），CP-8 作为回归门。无变化点缺可观察证据。

## 变更响应 · CR-20260909-corner-menu

### 测试设计

| CP | 验证手段 | 关键断言 | 可交付 |
|---|---|---|---|
| CP-1 REQ-F-015 重写 | TEST-021 改写（组件）+ TEST-032（组件）+ e2e | header 无「模型」/「账号登录」按钮；左下角 ☰ 存在（`aria-haspopup`/`aria-expanded`）——**旧断言「首页仅标题 + 两按钮」反转，作回归门** | ✅ |
| CP-2 「配置」→「模型」 | TEST-021 / home-dialogs 组件 + e2e（`saveProviderThroughSettingsDialog`） | 触发文案为「模型」；`getByRole("button",{name:"配置"})` 不再命中该入口 | ✅ |
| CP-3 主题内联 | TEST-019 改写 + TEST-032 断言④⑤ + home-dialogs（SettingsDialog 无「外观」分区） | ☰ 浮层里点「深色」→ `data-theme=dark` 且无 `<dialog open>`；e2e reload 保持；SettingsDialog 组件不再有 `role="group" name="外观主题"` | ✅ |
| CP-4 `CornerMenu` | TEST-032 断言①②③⑥⑦（组件）+ e2e 断言⑤⑦ | 开/关/Esc/焦点回归/light-dismiss/启动弹窗/对话展开时可点 | ✅ |
| CP-5 `next.config.mjs` | 静态：`next.config.mjs` 含 `devIndicators` 键（治理或代码审查）；`npm run build:verify` 不报配置错误 | Next 指示器位置是 dev-only shadow DOM，Playwright 不断言 —— 接受为无断言 dev 便利 | ✅（低优先级） |
| CP-6 回归 | `npm run test:auth-ui` + `npm run test:e2e`（appearance / account / 首页三用例经 ☰ 重跑） | 全 PASS，断言按新结构校准 | ✅（回归门） |

**测试总判**：6 个变化点全部可验证、可交付。TEST-032 承载菜单开合/焦点/主题内联/启动器/z-index；主题持久化 + 对话展开时可点走真实浏览器 e2e。CP-5（dev 指示器位置）是唯一无 Playwright 断言项，接受为 dev 便利。TEST-019/021 + appearance/account e2e 为回归门。

## 变更响应 · CR-20260909-display-screen

### 任务→测试派生矩阵

| 模块任务 | 分类 | 测试任务 | 类型 | 真实入口 |
|---|---|---|---|---|
| TASK-036 `display_state` 表 + 读写原语 | 新增 | TEST-039（get-or-create、单行、默认值、updated_at） | Unit | `test:display` |
| TASK-037 `DisplayScreen` + `GET /api/display` + 事件流 + 提示条 | 新增 | TEST-040（home/insight/提示条不可关闭/kind 回退/事件重取）、TEST-041 e2e | 组件 + 真实浏览器 | `test:display` + e2e |
| TASK-038 Ⅰ 组件/数据层 | 新增 | TEST-040（组件先行，`page.tsx` 未改时可测） | 组件 | `test:display` |
| TASK-038 Ⅱ `page.tsx` 首页重写 | 大改 | **TEST-021 + TEST-032 回归重写**（首页结构反转）、TEST-041 ⑤ | 组件 + 真实浏览器 | `test:auth-ui` + e2e |
| TASK-039 `routeTurn` display 消费 + 路由层写 `display_state` | 新增 | TEST-042（`display` 字段 + fail-open + 路由层写入 + skill/display 互不阻塞）、TEST-041 ③④ | Unit + E2E | `test:skills` + `test:chat-stream` + e2e |
| 既有 TEST-021/032（首页结构） | 大改（断言反转） | 作 CR 回归门，按「全屏 DisplayScreen + 浮层」新结构重写 | 组件 + 真实浏览器 | `test:auth-ui` + e2e |
| 既有 appearance/account e2e | 小改 | 首页结构变后重跑（☰/主题/账号弹窗流程不变，但选择器可能需调） | E2E | e2e |

### 测试设计

| 变化点 | 验证手段 | 关键断言 | 可交付 |
|---|---|---|---|
| CP-1 全屏 DisplayScreen | TEST-040 ① + TEST-041 ① | 首页渲染 `.display-screen` 基底，底部对话 + ☰ 浮其上且可交互 | ✅ |
| CP-2 标题视图 + header 迁入 | TEST-040 ① + TEST-021 回归 | `kind:home` → 标题视图含 `<h1>Agent-Jarvis</h1>`；**页面无独立 `<header>` 功能区** | ✅（回归门） |
| CP-3 技能洞察自动上屏 | TEST-041 ② | 技能轮产出洞察 → 展示屏切为该 HTML 报告 | ✅ |
| CP-4 跨刷新持久 | TEST-041 ④（**真实浏览器**，原则 12） | insight 视图 → `reload()` → 仍 insight；home → reload → 仍 home | ✅ |
| CP-5 「显示首页」指令 | TEST-042 ③ + TEST-041 ③ | `display:"home"` → 路由层 `setDisplayState({kind:"home"})` → 展示屏回标题 | ✅ |
| CP-6 `kind` 扩展点 | TEST-040 ④ | 未知 `kind` → 回退标题视图，不崩 | ✅ |
| CP-7 不可关闭提示条 | TEST-040 ③ | insight 视图有 `.display-screen__notice`；notice 内无 `button`；Esc 不移除 | ✅ |
| CP-8 `display_state` 全局单行 | TEST-039 | 表内始终 ≤1 行；无行 → 默认 `home`；重复写 → 更新同一行 | ✅ |
| CP-9 事件流无轮询/SSE | TEST-040 ⑤ + 代码审查 | 派发 `jarvis:display-changed` → 组件重取一次；`DisplayScreen` 无 `setInterval`/`EventSource` | ✅ |
| CP-10 `routeTurn` 扩 `display` | TEST-042 ①②④ | `display` 从响应解析；无/非法 → `null`；`skill` 与 `display` 互不阻塞 | ✅ |
| CP-11 未沙箱化 iframe | TEST-040 ② + 代码审查 | `<iframe>` 渲染 `srcdoc`、**无 `sandbox` 属性**；DEC-015 出口义务在文档可追溯 | ✅ |
| CP-12 `page.tsx` 2 子项 | 派生矩阵 | Ⅰ 组件可先单测（TEST-040），Ⅱ 触发 TEST-021/032 回归 | ✅ |
| CP-13 SSR 首帧 + 事件解耦 | TEST-041 ④ + 代码审查 | 刷新即正确内容（SSR）；`FloatingChat` 不 import `DisplayScreen`（grep） | ✅ |
| CP-14 每个可观察行为有真实入口测试 | 本表 | CP-1..CP-13 全映射 TEST-039..042 或审查项 | ✅ |
| CP-15 REQ-F-015 重写回归门 | TEST-021 + TEST-032 重写 | 首页结构断言按新形态；旧「header 两按钮/仅标题」断言反转 | ✅（回归门） |

**测试总判**：15 个变化点全部可验证、可交付。"刷新保持"（CP-4）与"☰ 在全屏展示屏上可点"（CP-1/CP-15）走真实浏览器 e2e（原则 12）。提示条不可关闭（CP-7）在组件层断言「无 close 途径」。`display_state` 单行语义（CP-8）在单测断言 `count(*)≤1`。CP-9/CP-11/CP-13 含代码审查项（`DisplayScreen` 无轮询、`<iframe>` 无 sandbox、`FloatingChat` 不 import DisplayScreen），可交付判定明确。

## 变更响应 · CR-20260909-minimal-floating-chat

### 任务→测试派生矩阵

测试角色对模块任务开发说明书的每个新/改任务派生测试任务，标注类型与真实入口对应（AI_STANDARD 原则 12）。**每个任务在下表必有对应行。**

| 模块任务 | 分类 | 测试任务 | 类型 | 真实入口 |
|---|---|---|---|---|
| TASK-021 优先级字段 + API + 排序 UI | 新增 | TEST-025（解析）、TEST-016 扩展（`PATCH` reorder 仅属主） | API + 组件 | `test:providers` + e2e 设置弹窗排序 |
| TASK-022 `resolveActiveProvider` + fallthrough | 新增 | TEST-025（fallthrough）、TEST-026（无可用拦截） | API | `test:chat-stream` |
| TASK-023 FloatingChat 收敛 | 大改 | TEST-011 改写（无切换控件、请求不带 provider）、TEST-030（元素不存在） | 组件 + 真实浏览器 | `test:floating-chat` + e2e |
| TASK-024 状态灯四态 + 不阻塞首屏 | 大改 | TEST-027（四态映射 mock）、e2e 断言首屏灯为「检测中」再落定 | 组件 + 真实浏览器 | `test:floating-chat` + e2e |
| TASK-025 会话生命周期（3 子项） | 新增/大改 | 子项①→TEST-029、子项②③→TEST-030 | API + E2E | `test:chat-stop` + e2e |
| TASK-026 请求级错误红字 + 50vh | 大改 | TEST-028（两类错误分流、脱敏、不入上下文）、TEST-030（50vh） | 组件 + 真实浏览器 | `test:floating-chat` + e2e |
| TASK-027 视觉简约化 token | 小改 | TEST-012（静态双主题对比度）、TEST-018（`--live` 计算对比度回归） | 视觉 + A11y | `test:visual` + `test:ui-contract:live` |
| TASK-028 ui-contract 规则适配 | 小改 | TEST-012 / TEST-018 自身规则集变更（删芯片规则、加四态规则、RF-09 阈值 50vh） | 视觉 + A11y | `test:visual` + `test:ui-contract:live` |
| TASK-029 config:check 单管理员适配 | 小改 | TEST-023 扩展（`JARVIS_TEST_USER_ID` 已设时 OAuth 项为「暂缓」不为「缺失」，退出码 0） | Config/Preflight | `test:config` |
| 既有 TASK-009（停止） | 小改（+结束会话） | TEST-009 保留 + TEST-029 承接新语义 | API + E2E | `test:chat-stop` + e2e |
| 既有 TASK-010（注水） | 小改（+活动判断） | TEST-014 保留 + TEST-030（未点新对话则恢复） | API + 真实浏览器 | `test:chat-stream` + e2e |
| 既有 TASK-001/002（登录负向） | 不变 | TEST-001/002 保留，措辞标「REQ-F-001 暂缓期仍覆盖旁路与未授权阻断」 | API + UI | `test:auth` + e2e |

## 变更响应 · CR-20260909-skills

### 任务→测试派生矩阵

测试角色对 TASK-033/034/035 + 受影响既有任务逐项派生测试，标注类型与真实入口对应（AI_STANDARD 原则 12）。**每个任务在下表必有对应行。**

| 模块任务 | 分类 | 测试任务 | 类型 | 真实入口 |
|---|---|---|---|---|
| TASK-033 Ⅰ 上传+落盘+路径穿越防护 | 新增 | TEST-038 ①（e2e 注册流）；单测：slug 冲突 409、路径穿越拒绝 | E2E + API | e2e 拖放 + `test:skills` |
| TASK-033 Ⅱ SKILL.md 生成 + 回退 | 新增 | TEST-035（frontmatter 解析失败回退） | Unit | `test:skills` |
| TASK-033 Ⅲ 注册 + list | 新增 | TEST-038 ①、`GET /api/skills` 单测 | API + E2E | `test:skills` + e2e |
| TASK-034 ① `runChatTurn` `onFinal` 回调 | 小改 | TEST-036（最后一块/无块/未闭合/普通轮四态） | Unit | `test:chat-stream` |
| TASK-034 ② 路由层写 `insights` + SSE 尾事件 | 新增 | TEST-037（写入 + 读回 + 属主）、TEST-038 ② | API + E2E | `test:insights` + e2e |
| TASK-034 ③ `GET /api/insights` | 新增 | TEST-037（倒序、404、401、不覆盖） | API | `test:insights` |
| TASK-034 ④ 系统消息渲染 | 新增 | TEST-038 ②④（「已生成洞察」/「本轮未产出 HTML」） | E2E | e2e |
| TASK-035 ① `routeTurn`（DEC-016） | 新增 | TEST-034（命中/未命中/**4 条 fail-open**/请求体形态） | Unit（mock） | `test:skills` |
| TASK-035 ② `resolveSkillForTurn` | 新增 | TEST-035（白名单 + 32KB 截断 + 仅当轮） | Unit | `test:skills` |
| TASK-035 ③ `runChatTurn` `skill?` 参数 | 小改 | TEST-036 ④（`skill` 空 → 行为不变）、TEST-013 冒烟回归 | Unit + Smoke | `test:chat-stream` + `test:smoke` |
| TASK-035 ④ `/api/chat/stream` 路由前置 | 小改 | TEST-038 ③（per-message 不延续）、TEST-013 | E2E + Smoke | e2e + `test:smoke` |
| 既有 TASK-005/010（`runChatTurn`） | 小改（+可选参数） | TEST-013/014 保留 + TEST-036 承接新分支 | API + Smoke | `test:chat-stream` + `test:smoke` |
| 既有 TASK-023（`FloatingChat`） | 小改（+拖放 + 系统消息） | TEST-011/030 保留（无技能路径零回归）+ TEST-038 | 组件 + E2E | `test:floating-chat` + e2e |

### 测试设计

测试角色对每个变化点给出「验证手段 → 关键断言 → 可交付判断」。**每个 CP 必须有可验证证据方能交付。**

| 变化点 | 验证手段 | 关键断言 | 可交付 |
|---|---|---|---|
| CP-1 技能上传与注册 | e2e（TEST-038 ①）+ API 单测 | 拖文件夹 → `.data/skills/<slug>/` 落盘、`skills` 表一行、记录区「已注册技能：&lt;name&gt;」；同名 slug → 409；`../` 路径 → 拒绝 | ✅ |
| CP-2 按发送路由技能 | 单测（TEST-034）+ e2e（TEST-038 ②③） | `listSkills` 空 → 不调 `routeTurn`；非空 → 每次发送调一次；命中仅影响当轮 | ✅ |
| CP-3 技能内容注入 | 单测（TEST-035） | `SKILL.md` + 白名单文本进当轮 system 段；非白名单排除；>32KB 截断且有注记；不持久化 | ✅ |
| CP-4 HTML 产出捕获 | 单测（TEST-036） | 最后一个闭合 ```html``` 块 → `insight` 尾事件；无块 → `insight-missing` `"none"`；未闭合 → `insight-missing` `"incomplete"`；普通轮不触发 | ✅ |
| CP-5 洞察存储与读回 | API 真实入口（TEST-037） | `insertInsight` 落行、`GET /api/insights` 倒序返回、属主 404/401、多次不覆盖——**读回入口存在即满足原则 13** | ✅ |
| CP-6 安全非目标 | 交付证据审查 | `test-results.json` `known_warnings` 含 `skill-html-unsandboxed`（引用 DEC-015）；F1 无沙箱代码是**有意**（非遗漏） | ✅（记录项） |
| CP-7 DEC-015 未沙箱化 | 文档可追溯性审查 | DEC-015 列出信任面 + 风险等级"高" + 出口义务；无 F1 代码断言（渲染是 F2） | ✅（DEC 可追溯） |
| CP-8 DEC-016 独立路由 | 单测（TEST-034） | 请求体 `stream:false`、`max_tokens` 小、`messages` 无历史；4 条 fail-open 全部 `return null` 不抛 | ✅ |
| CP-9 CP-1 拆 3 子任务 | 派生矩阵 | TASK-033 Ⅰ/Ⅱ/Ⅲ 各有独立测试行 | ✅ |
| CP-10 写库在路由层 | 代码审查 + grep 守卫 | `src/lib/chat.ts` 不出现 `insertInsight`/`insights`；写库在 `chat/stream/route.ts` | ✅ |
| CP-11 `src/lib/skills.ts` 集中 | 代码审查 | 路由 + 技能解析全在 `src/lib/skills.ts`；`store` 只给 `skills` 表 CRUD 原语 | ✅ |
| CP-12 拆 F1/F2 | 流程 | F1 CR 不含展示屏；F2 单独 CR | ✅ |
| CP-13 每个 CP 有真实入口测试 | 本表 | CP-1..CP-14 全部映射到 TEST-034..038 或审查项，无纯桩充数 | ✅ |
| CP-14 per-message + 无技能回归 | e2e（TEST-038 ③）+ 冒烟（TEST-013） | 第二次发送不重复触发；无技能时 `test:smoke` 双轮对话与 CR 前一致 | ✅（回归门） |

**测试总判**：14 个变化点全部可验证、可交付。`routeTurn` 严格 mock（TEST-034，**禁 CI 真实网络**），真实 LLM 仅 e2e 对本地 Provider。`GET /api/insights` 使 `insights` 写入有真实消费者（原则 13）。CP-6/CP-7/CP-10/CP-11 为审查/记录项（无可执行断言但可交付判定明确）。TEST-013 + TEST-038 ③ 为 CP-14 回归门。

## 变更响应 · CR-20260910-agent-tooling

### 任务→测试派生矩阵

测试角色对「模块任务开发说明书 · 变更响应 · CR-20260910-agent-tooling」的每个变化点与任务逐点派生。**每个变化点在下表必有对应行**；按 R1 测试角色意见，任务按**子项**列行绑断言。

| 变化点 | 任务 | 测试落点 | 类型 |
|---|---|---|---|
| CP-1 循环 ≤10 步 | TASK-065, TASK-071 ① | TEST-067 ①②③④⑤⑥ | 新增 |
| CP-2 技能工具化删路由 | TASK-067 | TEST-069 ①②③④⑥⑦⑧；**TEST-034 作废** | 新增 + 废止 |
| CP-3 技能管理 | TASK-073 | TEST-074 ①②③④⑥⑦ | 新增 |
| CP-4 展示屏工具化 | TASK-068 ①②③ | TEST-070 ①②③；**TEST-042 作废** | 新增 + 废止 |
| CP-5 `web_search` | TASK-070 ① | TEST-072 ①②③ | 新增 |
| CP-6 搜索配置入口 | TASK-074 ①②③④ | TEST-075 ①②③④⑤⑨ | 新增 |
| CP-7 `read_url` | TASK-069, TASK-070 ② | TEST-071, TEST-072 ⑤ | 新增 |
| CP-8 出网边界 MUST | TASK-069 | TEST-071, TEST-072 ①⑥ | 新增 |
| CP-9 步骤流 | TASK-075 | TEST-076 ①②③⑥⑦⑧ | 新增 |
| CP-10 工具落库与重建 | TASK-060 ①⑤, TASK-071 ②③⑥ | TEST-062 ①②③④⑤, TEST-076 ④ | 新增 |
| CP-11 状态灯五态 | TASK-076 ①④ | TEST-077 ①②③⑧；**TEST-027 反转** | 大改 |
| CP-12 折叠态长任务 | TASK-076 ② | TEST-077 ④⑤；**TEST-031 ④ 反转** | 小改 |
| CP-13 面板分情形高度 | TASK-076 ③④ | TEST-077 ⑥⑦⑧；**TEST-012/018/047/030 反转** | 大改 |
| CP-14 token 用量可见 | TASK-062, TASK-060 ③, TASK-074 ⑤ | TEST-064, TEST-075 ⑥⑦⑧ | 新增 |
| CP-15 来源引用 | TASK-072 | TEST-073 ①③④⑤ | 新增 |
| CP-16 能力探测与降级 | TASK-063, TASK-060 ② | TEST-065 ①②③④⑤⑥；**TEST-006/010/049 校准**（mock 需接受 `tools`） | 新增 |
| CP-17 保留窗口 | TASK-066 ② | TEST-068 ③ | 新增 |
| CP-18 REQ-F-004 重写 | TASK-066 ①, TASK-071 ③ | TEST-068 ①②；**TEST-008/013/014 校准** | 大改 |
| CP-19 循环中途停止 | TASK-065 ⑥ | TEST-067 ⑦；**TEST-009/029 扩展** | 小改 |
| CP-20 `save_insight` 工具化 | TASK-068 ④⑤⑥ | TEST-070 ④⑤⑥⑦⑧；**TEST-036 作废** | 大改 + 废止 |
| CP-21 上下文预算 | TASK-066 ③④⑤, TASK-060 ② | TEST-068 ④⑤⑥⑦ | 新增 |
| CP-22 前缀稳定 + 动态注册 | TASK-064 ①②③, TASK-066 ① | TEST-066 ②③, TEST-068 ①② | 新增 |
| CP-23 内核可扩展性 | TASK-064 ④ | TEST-066 ①⑤ | 新增 |
| CP-24 既有条款澄清 | TASK-067 ③, TASK-075 ⑥ | TEST-069 ⑧, TEST-076 ⑦⑧；**TEST-020 扩展** | 澄清 |
| CP-25 非目标处置 | 约束（TASK-064） | TEST-066 ⑥（grep 守卫） | 约束 |
| CP-26 迁移框架 | TASK-059 | TEST-061 ①②③④⑤ | 新增 |
| CP-27 线协议 + 分片累积 | TASK-061 | TEST-063 ①②③④⑤ | 大改 |
| CP-28 悬空 `tool_calls` | TASK-071 ④ | TEST-067 ⑧ | 新增 |
| CP-29 Provider 上下文窗口 | TASK-060 ②, TASK-066 ④ | TEST-068 ⑦ | 新增 |
| CP-30 `usage` 事件与存储 | TASK-062, TASK-060 ③ | TEST-064 ①②③④⑤, TEST-062 ① | 新增 |
| CP-31 来源列表独立结构 | TASK-072 ① | TEST-073 ②③ | 澄清 |
| CP-32 能力粒度三态 | TASK-063 ②③ | TEST-065 ②③④ | 新增 |
| CP-33 校验器可注入 resolver | TASK-069 ② | TEST-071 ⑨⑩⑪ | 新增 |
| CP-34 SSRF 样本集补全 | TASK-069 ① | TEST-071 ①..⑧ | 新增 |
| CP-35 mock 模型 + mock SearXNG | —（测试基建） | TEST-078 ①②⑦ | 大改 |
| CP-36 出网守卫 | —（测试基建） | TEST-072 ⑥, TEST-066 ⑤ | 新增 |
| CP-37 既有 TEST 作废与反转 | —（测试层） | 见「### 既有测试处置」 | 大改 |
| CP-38 `ui-contract` 三处改写 | TASK-076 ④ | TEST-077 ⑧ | 小改 |
| CP-39 行内序号 | TASK-060 ①⑤ | TEST-062 ③ | 小改 |
| CP-40 事件类型单一来源 | TASK-075 ④ | TEST-076 ⑤ | 小改 |
| CP-41 执行体短路 | TASK-064 ③, TASK-070 ④ | TEST-066 ③, TEST-072 ④ | 澄清 |
| CP-42 技能 name 与 slug | TASK-073 ④⑤ | TEST-074 ③④⑤ | 澄清 |
| CP-43 `show_insight` 属主校验 | TASK-068 ③ | TEST-070 ③ | 小改 |

### 既有测试处置（CP-37）

R1 测试角色指出 CR 原「影响测试: 无」不成立。逐条登记：

| 既有 TEST | 处置 | 原因与方向 |
|---|---|---|
| TEST-034 | **作废** | 测 `routeTurn`（REQ-F-021 SUPERSEDED，函数删除）。`tests/skills.test.ts` 路由断言整段移除，`test-results.json` 标 `superseded` 使 `gate g3` 不再索要其证据。 |
| TEST-036 | **作废** | 测「技能轮最后一个 html 围栏捕获」与 `insight-missing` 事件，二者随 CP-20 消失。由 TEST-070 取代。 |
| TEST-042 | **作废** | 测 REQ-F-027 路由 `display` 字段（SUPERSEDED）。由 TEST-070 ① 取代。 |
| TEST-035 | **反转** | ③「不写持久存储」与 CP-10 相反（`read_skill` 结果现在落库）。改为 `read_skill` 结果断言，保留白名单与 32KB（语义改为单次 read）。 |
| TEST-038 ②③④ | **重写** | mock 以 `stream===false` 识别路由调用（`skills-display.spec.ts`），CP-2 后整套失效；③ per-message、④「未产出 HTML」语义变更。改为「步骤流 `read_skill` 行」+ TEST-070 ⑥。 |
| TEST-041 ③ | **反转** | 「显示首页」经路由 → 改经 `show_home` 工具。 |
| TEST-046 ④⑤⑥ | **反转** | ④⑤「本轮使用技能」提示删除（作废）；⑥ 只读守卫（`skill-list.test.tsx` 断言 button 数为 0）反转为「有删除/重命名按钮」。 |
| TEST-027 / TEST-031 ④ | **反转** | 四态 → 五态；`floating-chat.test.tsx` 的 `--busy` 断言需加「执行工具中」映射。 |
| TEST-012 / 018 / 047 / 030 | **反转** | 面板 ≤50vh 硬上限 → 分情形（50vh / 75vh）。 |
| TEST-008 / 013 / 014 | **校准** | REQ-F-004 上下文组装重写；`smoke.mjs` 按 messages 长度计数的 `ctx=` 断言会因前缀拆分与工具记录改变数值。 |
| TEST-009 / 029 | **扩展** | REQ-F-005 扩到工具执行阶段。 |
| TEST-025 / 026 | **扩展** | 加 REQ-F-006 ⑦「不因工具能力跳过」断言。 |
| TEST-006 / 010 / 049 | **校准** | 「测试」动作顺带探测，provider-routes mock 需接受 `tools` 字段。 |
| TEST-032 ③ / 048 ④ | **校准** | ☰ 浮层新增「搜索」与 token 用量两项，菜单项清单更新。 |
| TEST-020 | **扩展** | REQ-F-016 澄清：工具结果摘要的 Markdown 渲染与链接协议限制。 |
| 其余 TEST-001..060 | **回归门** | 不变，全部重跑。 |

### 测试设计

TEST-061..078（18 条）。**可测性基础**：`chat.ts` 的 `providerStream` 是**工厂**（按回喂的 messages 决定下一段 yield），因此工具循环、预算、保留窗口、前缀稳定均可在 vitest 下**无外网闭环**；`url-guard` 与 `budget` 为纯函数，对抗样本与边界可穷举。

**P2 已定数值**（R1 测试角色要求的 10 项，现已由架构定，用例可写）：单工具超时 15s、`read_url` 体积上限 2 MiB / 跳数 ≤5、保留窗口 N=2、总输入 ≤60% 窗口、名录 ≤2% / 工具定义 ≤8% / 单条工具结果 ≤15% / 来源 ≤1%、description ≤200 字符、搜索结果 ≤10 条、步骤行参数摘要 ≤200 字符、循环上限 10 步。

**已登记的 known limitation（测试如实记录当前行为，不伪装已防护）**：①DNS 重绑定（TEST-071 ⑪）——校验与连接非原子，零依赖 `fetch` 下无法消除；②`skill-html-unsandboxed-web-source`（TEST-070 ⑧）——用户明示接受的信任面扩大；③弱模型不主动调工具（PM-2）——人工冒烟，非缺陷。

## 变更响应 · CR-20260910-process-hardening

### 任务→测试派生矩阵

按**子项**列行（CR-20260910-skill-intake 复盘固化的做法：整任务只绑一个测试会让子项漏做不被发现）。

| 模块任务（子项） | 分类 | 测试 | 类型 | 真实入口 |
|---|---|---|---|---|
| TASK-049 ① `--cr` 作用域 | 新增 | TEST-051 ①②③ | 治理单测 | `governance.py gate/review --cr` |
| TASK-049 ② `check <stage>` | 新增 | TEST-051 ④ | 治理单测 | `governance.py check p1..release` |
| TASK-050 ① `new-cr` | 新增 | TEST-052 ①② | 治理单测 | `governance.py new-cr` |
| TASK-050 ② `matrix` | 新增 | TEST-052 ③④⑤ | 治理单测 | `governance.py matrix` |
| TASK-051 ① 结构迁移 | 结构变更 | TEST-053 ⑤（幂等）、TEST-054（兼容） | 治理单测 | `migrate_specs.py` |
| TASK-051 ② 评审迁入 CR | 去重 | TEST-054（信息不丢） | 治理单测 | `review` + 文本检索 |
| TASK-051 ③ `check-specs` | 新增 | TEST-053 ①②③④ | 治理单测 | `governance.py check-specs` |
| TASK-051 ④ 向后兼容硬门 | 约束 | **TEST-054** | 治理单测 | `review r1..r4` 全 CR |
| TASK-052 ① `tsconfig` 归一化 | 小改 | TEST-051 ⑤ | 治理单测 | `governance.py verify` |
| TASK-052 ② `verify:all` | 小改 | 手工执行一次并记入证据 | 聚合脚本 | `npm run verify:all` |
| 既有全部 TEST-001..050 | 不变 | **回归门**：迁移后全部重跑 | 全量 | `npm test` + e2e + 治理单测 |

### 测试设计

| CP | 验证手段 | 关键断言 | 可交付 |
|---|---|---|---|
| CP-1 两段式结构 | `check-specs`（TEST-053 ①②） | 四本说明书 `##` 标题 ∈ 白名单 ∪ `变更响应 · CR-*` | ✅ |
| CP-2 评审归位 | `check-specs`（TEST-053 ④）+ 文本检索（TEST-054） | 说明书内五种历史评审节名**均不存在**；每份被移走的文字在对应 CR 内可检索到 | ✅ |
| CP-3 阶段聚合 | TEST-051 ④ | 每个 stage 跑对应门禁集合；任一子门 FAIL → 整体 FAIL | ✅ |
| CP-4 `new-cr` | TEST-052 ①② | 产物直接过 `check-changes`；拒绝覆盖 | ✅ |
| CP-5 `--cr` 作用域 | TEST-051 ①②③ | `--cr` 给 PASS 时**缺省仍 FAIL**（证明未削弱）；`g4` / `check release` 拒绝 `--cr` | ✅ |
| CP-6 向后兼容 | **TEST-054（硬门）** | 迁移前后 `review r1..r4` 对全部 CP-model CR 结果**完全一致且全 PASS** | ✅（硬门） |
| CP-7 `tsconfig` 抖动 | TEST-051 ⑤ | `include` 增删 `.next-*` 项不报 `BASELINE_CHANGED`；改其它字段**仍报** | ✅ |
| CP-8 脚本迁移 | TEST-053 ⑤ | `migrate_specs.py` 二次运行零 diff（幂等） | ✅ |
| CP-9 单一入口 | 代码审查 + `package.json` 检查 | 无第二个 CLI 入口；`package.json` 仅加 `verify:all` 一条 | ✅ |
| CP-10 `matrix` 生成 | TEST-052 ③④⑤ | 行数 = CP 数、四角色列、格 `TODO`；未填完跑 `review` 必 `MATRIX_INVALID` | ✅ |
| CP-11 新命令单测 | 本表 | 四个新子命令各有断言 | ✅ |
| CP-12 `check-specs` | TEST-053 | 四类违规各一断言 | ✅ |

**测试总判**：12 个变化点全部可验证、可交付。本 CR 的核心风险是**迁移打断在途 CR**，由 TEST-054 作硬门守住——它不是「跑一遍看看」，而是**迁移前后结果比对 + 信息不丢检索**双断言。`--cr` 的放松风险由 TEST-051 ①（`--cr` PASS 时缺省仍 FAIL）与 ③（`g4` 拒绝 `--cr`）封死。全量 TEST-001..050 作回归门。

## 变更响应 · CR-20260910-record-accuracy

| CP | 测试响应 | 判定 |
|---|---|---|
| CP-1 | **本层修正对象**：删除 `test-results.json` → `known_warnings` 中「LV-AXE 因 axe-core 未安装而 SKIP」的说明。凭据：`node scripts/ui-contract.mjs --live` 实测 `PASS LV-AXE`，该层 0 skipped。 | 小改（证据记录） |
| CP-2 | **本层修正对象**：删除同一数组中「g3 故意为红」的说明。**出口义务**：移除后必须实跑 `python tools/governance.py gate g3` 证明其仍 PASS，否则等于用删记录掩盖真实红门。TEST-022 保持 `DEFERRED`；恢复 Google 登录的变更仍须把它改回 `PENDING` 并实际执行——该义务由 TEST-022 条目自身承载，不依赖被删除的 warning。 | 小改（证据记录） |
| CP-3 | 不落本层：不改变任何 TEST 的定义、命令、真实入口或 result；TEST-051..054 与 CR-20260910-process-hardening 的绑定关系不变。 | 不变 |

本 CR 不新增测试：三处修正的验证手段是既有门禁与既有契约命令的实跑结果。**可考虑的加固**——用一条治理自检断言「`known_warnings` 不得声称某道门为红，而该门实测为绿」——是可实现的，但属独立变更，不在本 CR 范围。

## 变更响应 · CR-20260910-risk-scaled-gates

### 任务→测试派生矩阵

按**子项**列行（CR-20260910-skill-intake 的 P6 复盘固化：整任务只绑一个测试会让子项漏做不被发现）。

| 模块任务 | 分类 | 测试任务 | 类型 | 真实入口 |
|---|---|---|---|---|
| TASK-053 ① 三档判据入文档 | 新增 | TEST-055 ① | 静态/治理 | `check-doors` |
| TASK-053 ②③④ `check-doors` 三类阻断 | 新增 | TEST-055 ②③④⑤ | 治理单测（**含阳性**） | `check-doors` |
| TASK-054 ②③④ `check-ids` 三个错误码 | 新增 | TEST-056 ②③④ | 治理单测（**含阳性**） | `check-ids` |
| TASK-055 回填 17 个 CR | 新增 | TEST-056 ①⑤ | 治理 + 不变量硬门 | `review r1..r4` 前后对照 |
| TASK-056 ①②③ 谓词执行 | 新增 | TEST-057 ②③④ | 治理单测（**含阳性**） | `check-warnings` |
| TASK-056 ④ 迁移 4 条 | 小改 | TEST-057 ①⑤ | 治理 | `check-warnings` |
| TASK-057 dev server 一致性 | 新增 | TEST-058 ①②③ | **真实浏览器入口** | `node scripts/check-dev-server.mjs` |
| TASK-058 ① 结构规则合流 | 新增 | TEST-059 ①②③ | 静态/治理 | `check-specs` + `migrate_specs --dry-run` |
| TASK-058 ② 如实报告跳过 | 大改 | TEST-060 ①②③ | 治理单测 | `review r1..r4` |

### 测试设计

| 变化点 | 验证手段 | 关键断言 | 可交付 |
|---|---|---|---|
| CP-1 | TEST-055 ① | 三档判据可机器判定，不留主观口子。 | `check-doors` 输出 |
| CP-2 | TEST-055 ②③④⑤ | 两列存在、取值合法；`发现不了 + 双向` 必被阻断。 | 阳性用例 |
| CP-3 | TEST-055..060 全体 | 每条新规则都有绑定的 TEST-*，无文档-only 规则。 | R4 派生矩阵 |
| CP-4 | TEST-056..060 | 五个盲区各有一条**能复现原始事故**的回归守卫。 | 事故回归用例 |
| CP-5 | TEST-056 ②③④ | 区间重叠、悬空引用、重复定义三类各自可触发。 | 阳性用例 |
| CP-6 | TEST-057 ②③④ | 谓词真的执行门 / 查依赖，不是字符串匹配。 | 阳性用例 |
| CP-7 | TEST-055..060 | 无第二个 CLI 入口；`package.json` 仅增脚本别名。 | 静态审查 |
| CP-8 | 既有 `verify` | ledger 三类错误码仍由 `check_ledger` 覆盖，本 CR 未新增重复检查。 | 既有回归 |
| CP-9 | TEST-056 ⑤ | 回填前后 `review r1..r4` 结果**逐字一致**（向后兼容硬门）。 | 前后对照记录 |
| CP-10 | TEST-057 ⑤ | 自由文本报 `UNVERIFIABLE` 且不阻断，历史数据不被一刀切。 | `check-warnings` 输出 |
| CP-11 | TEST-055..060 | **每条检查均有阳性用例**；只断言退出码非零不算数，必须断言具体错误码。 | 治理单测净增数 |
| CP-12 | TEST-058 ③ | 服务器未运行显式 SKIP，不静默通过（静默通过 = 假覆盖）。 | 真实入口输出 |

**回归门**：TEST-001..054 全部作回归；重点 TEST-051..054（`--cr` 作用域、脚手架、`check-specs`、向后兼容硬门）。

## 变更响应 · CR-20260910-skill-intake

### 任务→测试派生矩阵

| 模块任务 | 分类 | 测试任务 | 类型 | 真实入口 |
|---|---|---|---|---|
| TASK-040 `readZipEntries` / `deriveFolderName` | 新增 | TEST-043（6 正向 + 11 整体拒绝 + 1 逐条排除 + grep 守卫） | Unit（对抗性） | `test:zip` |
| TASK-041 ② `handleDrop` 首行 `preventDefault` | **缺陷修复** | TEST-044 ①（**根因回归守卫**） | 组件 | `test:floating-chat` |
| TASK-041 ①③ 三入口收敛 + 两个上传按钮 | 新增 + 欠账补齐 | TEST-044 ②③④⑤ | 组件 + 真实浏览器 | `test:floating-chat` + e2e |
| TASK-042 ① `archive` 分支 | 新增 | TEST-045 ③④、TEST-043（解析层） | API | `test:skills` |
| TASK-042 ② 白名单 11→16 | 小改 | TEST-045 ① | API | `test:skills` |
| TASK-042 ③ `excluded` 回执 | **缺陷修复** | TEST-045 ② | API | `test:skills` |
| TASK-043 ①②⑤ `SkillList` + 更名 | 新增 | TEST-046 ①②③⑥ | 组件 + 真实浏览器 | `test:auth-ui` + e2e |
| TASK-043 ③④ `skill` 尾事件 | 小改 | TEST-046 ④⑤ | 组件 + 真实浏览器 | `test:floating-chat` + e2e |
| 既有 TASK-033/034/035（F1） | 不变 | **TEST-034..038 作回归门** | Unit + API + E2E | `test:skills` / `test:chat-stream` / e2e |
| 既有 TASK-036..039（F2） | 不变 | **TEST-039..042 作回归门** | Unit + API + E2E | `test:display` / e2e |

### 测试设计

| 变化点 | 验证手段 | 关键断言 | 可交付 |
|---|---|---|---|
| CP-1 非法拖放可见 | 组件（TEST-044 ①，合成 `drop` 事件） | `preventDefault` 被调用；记录区出现说明文案。**这是本次 P6 根因，必须是硬断言而非「顺带」** | ✅（根因回归门） |
| CP-2 显式上传入口 | 组件（TEST-044 ④）+ e2e | 两个真实 `<button>`；点击触发对应 `<input>`；e2e 用 `setInputFiles` 走通 | ✅ |
| CP-3 zip 成为合法来源 | API（TEST-045 ③④）+ e2e | zip 注册出的 slug/`SKILL.md` 与文件夹路径一致；`archive` 字段被服务端识别 | ✅ |
| CP-4 技能可见性 | 组件 + **真实浏览器**（TEST-046 ①②③） | 列表渲染、空态、注册后不刷新即更新 | ✅ |
| CP-5 被排除文件回执 | API（TEST-045 ②） | 四类 `reason` 各一；`not-injected` 的文件**确实已落盘**（区别于 `binary`/`too-large`） | ✅ |
| CP-6 白名单放宽 | API（TEST-045 ①） | 5 个新扩展名进入 `resolveSkillForTurn` 返回值 | ✅ |
| CP-7 零依赖有界解析 | Unit（TEST-043 ①②③④⑤⑥⑱） | store/deflate 还原正确；目录条目跳过；合法高压缩比不误杀；不支持方法逐条排除 | ✅ |
| CP-8 归档边界 | Unit（TEST-043 ⑦–⑰）+ API（TEST-045 ③） | 11 类整体拒绝逐条断言；被拒时 `.data/skills/` **零产物**（不得部分写入） | ✅ |
| CP-9 模块归属 | 代码审查 | 上传入口在 `FloatingChat`；`SkillList` 经 `children` 挂载，`CornerMenu.tsx` **未改动** | ✅ |
| CP-10 解析器纯函数 | grep 守卫（TEST-043 末） | `src/lib/zip.ts` 不出现 `node:fs` / `store` | ✅ |
| CP-11 三入口收敛 | 组件（TEST-044 ⑤） | 三条路径产生**同形状**的系统消息 —— 证明共用一段回执代码 | ✅ |
| CP-12 `skill` 尾事件 | 组件 + e2e（TEST-046 ④⑤） | 命中出现提示、未命中不出现 | ✅ |
| CP-13 对抗性单测 | 本表 | TEST-043 共 18 条独立断言 + 1 条 grep | ✅ |
| CP-14 拖放与三入口 | 本表 | TEST-044 五条 | ✅ |
| CP-15 可见性、回执与回归门 | 本表 | TEST-045/046 + **TEST-034..042 全部重跑** | ✅（回归门） |

**测试总判**：15 个变化点全部可验证、可交付。zip 解析器是本项目第一个需要**对抗性**测试的部件——纯函数 + 手工构造字节使 18 类样本可精确构造，无需真实 zip 工具。本次 P6 的根因（静默失败）由 TEST-044 ① 与 TEST-046 ①②③ **两处**守卫：既断言失败会被说出来，也断言成功会被看见。TEST-034..042 为回归门。

## 变更响应 · CR-20260910-test-subprocess-encoding

| CP | 测试响应 | 判定 |
|---|---|---|
| CP-1 | **本层修复对象**：TEST-053 子项「迁移幂等」所对应的 `tests/test_governance.py::test_053_5`。子进程环境固定 `PYTHONIOENCODING=utf-8` 使两端编码一致（根因修复），另加 `errors="replace"` 兜底与 `assertIsNotNone(result.stdout, ...)` 前置断言，使将来任何编码不一致表现为一条读得懂的失败，而不是 `stdout=None` 触发的 `TypeError`。**断言对象、命令与真实入口不变，`result` 保持 PASS。** | 小改（执行方式） |
| CP-2 | 不落本层的实现，但构成本层验收条件：修复不得改动被测工具，否则等于让测试去迁就实现。 | 不变 |

验收凭据不能只跑已迁移的仓库——那条路径输出纯 ASCII，根本不触发缺陷。必须在「子进程会打印含中文路径」的复现根上做前后对照，实测记录见 `EV-2026-09-10-test-subprocess-encoding.md`。

## 变更响应 · CR-20260910-ui-foundation

### 任务→测试派生矩阵

| 模块任务 | 分类 | 测试任务 | 类型 | 真实入口 |
|---|---|---|---|---|
| TASK-044 ① P3 隔离基线 | 实施前置 | TEST-047 ④ + 变更清单审查 | 静态/治理 | 工作树与 CR 记录 |
| TASK-044 ②③④ foundation、token、默认浅色 | 新增 | TEST-047 ①②③⑤ | 静态 + typecheck | `test:visual` |
| TASK-045 DisplayScreen、CornerMenu、ThemeToggle | 大改 | TEST-048 ①②③④⑥ | 组件 + E2E | `test:e2e` |
| TASK-045 Provider 自动优先级不变 | 回归 | TEST-048 ⑤ + TEST-025/026 | API + 组件 | `test:providers` / `test:chat-stream` |
| TASK-046 Dialog、模型设置、深链 | 大改 | TEST-049 ①②③④⑤ | 组件 + E2E | `test:settings-ui` / e2e |
| TASK-047 FloatingChat、Markdown、状态灯 | 大改 | TEST-050 ①② | 组件 | `test:floating-chat` / `test:markdown` |
| TASK-048 一次性删除旧视觉与整合 | 交付收敛 | TEST-047 ④⑤ + TEST-050 ③④⑤ | 静态 + E2E/live | `test:ui-contract:live` |

### 测试设计

| 变化点 | 验证手段 | 关键断言 | 可交付 |
|---|---|---|---|
| CP-1 | TEST-047、TEST-048、TEST-049、TEST-050 | 全部已批准界面进入统一 token/primitive 体系，浅色紫色为默认。 | 测试与真实入口证据 |
| CP-2 | TEST-048、TEST-049、TEST-025/026 | 「模型」菜单与 Provider 自动优先级保持；聊天无选择器和 providerId。 | 组件/API/E2E |
| CP-3 | TEST-047、TEST-050 | P3 文件和 TEST-043..046 不在 L3 的通过清单；已批准页面清单完整。 | 变更审查 |
| CP-4 | TEST-025/026、TEST-050 | Provider、聊天、展示屏、认证和上传 API 不因视觉迁移改变。 | API/真实入口 |
| CP-5 | TEST-047、TEST-048、TEST-050 | 默认浅色紫色；深色显式切换、持久化、首帧和重载保持。 | 组件/真实浏览器 |
| CP-6 | TEST-047 | 依赖、PostCSS、components.json 与本地 primitive 受控且可构建。 | 静态/typecheck |
| CP-7 | TEST-047、TEST-048 | HSL token、`data-theme`、焦点和计算样式符合 DEC-019。 | 静态/live |
| CP-8 | TEST-047 | `components/ui` 不依赖业务协议，业务组件通过受控 primitive 组合。 | import 静态审查 |
| CP-9 | TEST-047 | P3/L3 工作树和证据隔离；没有跨 CR 任务完成声明。 | 治理审查 |
| CP-10 | TEST-047、TEST-050 | 旧 class、手绘图标和双 CSS 不残留；回滚点是基线。 | 残留扫描/构建 |
| CP-11 | TEST-048、TEST-049、TEST-050 | 菜单、模型设置、展示屏、聊天全部从真实入口回归。 | 组件/E2E |
| CP-12 | TEST-047..050 | Testing Library、ui-contract、Playwright 覆盖 a11y、reflow、两主题和真实入口。 | 当前证据 |

## 变更响应 · CR-20260911-context-compaction

### 任务→测试派生矩阵

| 变化点 | 任务 | 测试落点 | 类型 |
|---|---|---|---|
| CP-1 按预算触发 | TASK-077 ①②④⑤ | TEST-079 ①②④⑤⑥ | 新增 |
| CP-2 同模型生成摘要 | TASK-078 ①②⑥ | TEST-080 ①③⑤（语义准确性**不覆盖**，见下） | 新增 |
| CP-3 净省校验 | TASK-077 ③ | TEST-079 ③ | 新增 |
| CP-4 边界可见可展开 | TASK-080 | TEST-082 | 新增 |
| CP-5 摘要即消息行 | TASK-079 ①②④ | TEST-081 ①②⑤ | 新增 |
| CP-6 增量压缩 | TASK-078 ③ | TEST-080 ② | 新增 |
| CP-7 fail-open | TASK-078 ⑤ | TEST-080 ④ | 新增 |
| CP-8 三级溢出顺序 | TASK-081 | TEST-079 ⑤⑥ | 小改 |
| CP-9 回滚安全 | TASK-079 ③ | TEST-081 ④ | 新增 |
| （真实入口） | TASK-077..081 | TEST-083 | 新增 |

### 基准数据来自真实会话，不用构造数据

TEST-079 的基准形状取自 `EV-2026-09-11-context-compaction.md` §1 实测的 **109 / 78 / 28 / 18 / 10 条**五种真实会话。理由是实测结论本身就推翻了构造数据会给出的答案：**短会话按轮数压缩倒亏 11–19%**。用理想数据永远测不出这一点，所以「短会话不得变贵」必须用真实形状断言。

### 一处如实登记的覆盖缺口

**CP-2 的摘要语义准确性无法用断言覆盖。** 机器能守的只有结构——非空、有界、含被压轮次的关键实体（TEST-080 ③）。摘要写坏了会让模型在后续每一轮都基于错误信息作答，而这**只有用户在对话中追问早前细节时才发现得了**。

故按 DEC-021 ② 如实填「真实入口」而非机器检查，本 CR 因此不走快车道。**不用一条宽松的机器断言把它伪装成已覆盖。**

## 变更响应 · CR-20260911-home-dashboard

### 任务→测试派生矩阵

| 变化点 | 任务 | 测试落点 | 类型 |
|---|---|---|---|
| CP-1 实体即文件 | TASK-120 ①②⑤ | TEST-120 ①②③④⑤⑩ | 新增 |
| CP-2 采集健康度五态 | TASK-120 ③ | TEST-120 ⑥⑦、TEST-123 ②（用户可感知性**不覆盖**，见下） | 新增 |
| CP-3 未读派生、点开即已读 | TASK-120 ③④、TASK-124 ② | TEST-120 ⑧、TEST-122 ④、TEST-123 ④ | 新增 |
| CP-4 模型只能提议 | TASK-121、TASK-122 ②③ | TEST-121 ④⑥⑧、TEST-122 ⑥⑦ | 新增 |
| CP-5 直写判据在工具里 | TASK-122 ③ | TEST-121 ⑤⑥⑦⑧ | 新增 |
| CP-6 知识条目实体主索引 | TASK-123 ④ | TEST-122 ⑧、TEST-124 | 小改 |
| CP-7 零结果缺口日志 | TASK-123 ④ | TEST-122 ⑧、TEST-123 ⑧ | 小改 |
| CP-8 看板两区 | TASK-124 | TEST-123 ①⑤⑥⑨（密度**不覆盖**，见下） | 新增 |
| CP-9 四组 API | TASK-123 ①②③⑤ | TEST-122 全部 | 新增 |
| CP-10 边界 | TASK-120 / 122 | TEST-120 ③⑦、TEST-121 ①⑨、TEST-122 ④⑤ | 新增 |
| 出口义务 5 采集、入库与回填 | TASK-125、TASK-126、TASK-127 | TEST-125、TEST-126、TEST-127 | 新增 |

### 两处如实登记的覆盖缺口

**CP-2 的可感知性**：机器能守的是状态推算本身——加了源但从未采集是陈旧而非正常，近期采集但解析失败是失败而非正常。「源坏掉时用户是否真的会注意到」只有开着用才知道。

**CP-8 的密度与分区比例**：泳道结构、顺序固定、交接入口都可断言；卡片密度是否好用、三条泳道的比例是否合适，属真实入口。设计草图（`design/`）是为此准备的对照物。

### 一条防绕过的断言

TEST-121 ⑧ 断言「未登记任何源的对象，任何来源都不能直写」。少了这一条，白名单可被空列表绕过：没有已登记源时若判定为「没有限制」，模型就能对新建对象任意直写。

## 变更响应 · CR-20260911-knowledge-base

### 任务→测试派生矩阵

| 变化点 | 任务 | 测试落点 | 类型 |
|---|---|---|---|
| CP-1 知识即文件 | TASK-082 ①② | TEST-084 ①②③⑥ | 新增 |
| CP-2 零依赖检索 | TASK-082 ③④ | TEST-085（语义质量**不覆盖**，见下） | 新增 |
| CP-3 只读工具按条目数注册 | TASK-083 ①③、TASK-086 ① | TEST-086 ①②③ | 新增 |
| CP-4 写工具只能提议 | TASK-082 ②、TASK-083 ③、TASK-084 ③ | TEST-084 ⑦⑧、TEST-086 ⑤、TEST-087 ③、TEST-089 ② | 新增 |
| CP-5 拖入文本笔记 | TASK-085 ② | TEST-088 ④、TEST-044 ①、TEST-089 ③ | 新增 + 小改 |
| CP-6 回复「存入知识库」 | TASK-085 ② | TEST-088 ⑤⑦ | 新增 |
| CP-7 ☰ 知识库列表 | TASK-085 ① | TEST-088 ①②③、TEST-089 ④ | 新增 |
| CP-8 `knowledge_pending` 事件 | TASK-083 ①② | TEST-067 事件用例、TEST-088 ⑥ | 新增 |
| CP-9 边界 | TASK-082 / 083 / 084 | TEST-084 ④⑤、TEST-086 ④⑥、TEST-087 ①④ | 新增 |
| CP-10 跨会话、不自动灌入 | TASK-086 ③ | TEST-089 ① | 新增 |

### 两处如实登记的覆盖缺口

**CP-2 检索的语义质量**：BM25 是词面匹配，机器能守的是「含关键词的条目排第一、片段含命中词、增删即时生效」（TEST-085）；「用同义改写查不到」只有真实使用才发现，故 CP-2 发现方式如实填「真实入口」，本 CR 因此走标准档。

**CP-4 模型提议是否值得采纳**：机器守的是审批机制本身——「采纳前检索不到」（TEST-086 ⑤）与采纳 / 忽略路由（TEST-087 ③）；提议的价值由用户在采纳时判断，这正是待采纳区存在的理由，不用一条宽松断言伪装成已覆盖。

### 基准数据来自真实消息

TEST-085 的判据阈值（整句应 top-1）来自 `EV-2026-09-11-knowledge-base.md` §1 在本机 57 条真实助手消息上的实测（55/56），不是构造语料上的理想值。

## 变更响应 · CR-20260911-knowledge-skills

### 任务→测试派生矩阵

| 变化点 | 任务 | 测试落点 | 类型 |
|---|---|---|---|
| CP-1 六本技能入库 | TASK-128 ①② | TEST-128 ①②③④ | 新增 |
| CP-2 仓库目录不是运行时目录 | TASK-128 ③④ | TEST-128 ⑤ | 新增 |
| CP-3 遵守与否机器看不出 | — | **不覆盖**，见下 | 新增 |

### 一处如实登记的覆盖缺口

**CP-3 的遵守性**：能断言的只有形状——文件在不在、前置块能不能解析、注入超不超预算、仓库目录有没有被代码读。「模型是否真按证据规范办事」只有真实使用能发现：在对话里让它写一个字段，看它是先抓条目再带引文，还是绕开 `extract_fields` 去编一个值。把这一条写成断言就是假覆盖。

## 变更响应 · CR-20260911-proactive-wake

### 任务→测试派生矩阵

| 变化点 | 任务 | 测试落点 | 类型 |
|---|---|---|---|
| CP-1 默认关 + 开关 + 间隔 + 日上限 | TASK-100 ①、TASK-101 ①、TASK-102 ① | TEST-100 ①②、TEST-102 ①②、TEST-103 ①②⑥、TEST-104 ①⑤ | 新增 |
| CP-2 一次有界调用 | TASK-100 ②③ | TEST-101（buildWakeMessages / max_tokens）、TEST-102 ④、TEST-104 ② | 新增 |
| CP-3 NOOP 静默、有事一行 | TASK-100 ③、TASK-102 ② | TEST-101 ④⑤、TEST-104 ②（质量**不覆盖**，见下） | 新增 |
| CP-4 不回放 | TASK-100 ②③ | TEST-101 ⑥ + buildWakeMessages 排除用例 | 新增 |
| CP-5 日上限 | TASK-100 ①③ | TEST-100 ③、TEST-101 ②⑧、TEST-104 ④ | 新增 |
| CP-6 客户端触发、服务端授权 | TASK-101 ②、TASK-102 ② | TEST-103 ④④′、TEST-102 ④ | 新增 |
| CP-7 「现在唤醒」 | TASK-102 ①、TASK-101 ② | TEST-101 ②、TEST-103 ③⑤、TEST-104 ② | 新增 |
| CP-8 失败静默但计费 | TASK-100 ③ | TEST-101 ④⑦ | 新增 |
| CP-9 `system` 行的转录还原 | TASK-102 ③ | TEST-076 ④ 新用例、TEST-104 ③ | 小改 |

### 一处如实登记的覆盖缺口

**CP-3 提醒是否值得**：机器守形状——NOOP 静默、有内容才落行、有界、不回放；「这条提醒有没有用」只有用户开着用一段时间才知道，故如实填「真实入口」，本 CR 因此走标准档。默认关与日上限把这一缺口的代价封顶。

### 假时钟的用法

TEST-103 ④ 只伪造 `setInterval` / `clearInterval` / `Date`，让 React 的调度器与测试库的 `waitFor` 保持真实计时——否则「安静满一个间隔」这条判定无法在单测里可靠断言，只能靠真等 60 秒。

### 基准数据来自真实会话

REQ-NF-020 ① 的「单次 215–1,611 token」与日上限默认值 20,000 的推算来自 `EV-2026-09-11-proactive-wake.md` §1 对本机最近 9 个真实会话的测算。

## 批准状态

- CR-20260910-process-hardening（R4）：新增 **TEST-051**（`--cr` 作用域 + `check <stage>` + `tsconfig` 归一化）、**TEST-052**（`new-cr` / `matrix` 脚手架产物合规）、**TEST-053**（`check-specs` 四类违规 + 迁移幂等）、**TEST-054**（**向后兼容硬门**：迁移前后 `review r1..r4` 对全部 CP-model CR 结果一致且全 PASS + 信息不丢）。TEST-001..050 全部作回归门。派生矩阵按**子项**列行。R4 评审见该 CR 的 `## R4 评审意见` 与 `## R4 评审矩阵`。**P3/P4 完成：TEST-051..054 全 PASS**（治理单测 44 项）；全量回归 194 vitest + 3 visual + 49 ui-contract + smoke + 10 e2e 全绿。**迁移暴露 REQ-F-025 假覆盖**：它此前在本文件里的唯一出现位置是一张评审表的表格行，评审归位后 `gate g2` 立刻报缺覆盖——`TEST-040 ②③` 本就在断言其验收标准 ①②，补进 TEST-040 覆盖需求列后 G2 恢复 PASS。详见该 CR 的 `## P3/P4 执行记录`。
- 当前状态：APPROVED（含 CR-20260908-floating-chat-functional-fixes、CR-20260908-ui-light-theme-minimal-home、CR-20260909-config-preflight-and-manual-auth、CR-20260909-next-build-dir-isolation 迭代）
- 当前状态：APPROVED（CR-20260909-minimal-floating-chat **P3/P4 完成**：TEST-025..030 全部 PASS，TEST-011/012 改写，TEST-016/023 扩展；`npm test` 114 通过、`ui-contract` 45/0/0、smoke + 6 Playwright e2e 通过、`build:verify` 通过）
- TEST-022（真实 Google 登录）随 REQ-F-001 **DEFERRED**：`test-results.json` 中 `result: "DEFERRED"`；`governance.py gate g3` 新增 DEFERRED 识别——不计入阻断，按「随 CR 恢复」报告。恢复 Google 登录的 CR 必须同时把 TEST-022 改回 `PENDING` 并执行。
- **g1/g2/g3/g3.5/g4 全部 PASS**（首次；此前 g3+ 因 TEST-022 长期为红）。
- 当前状态：APPROVED（CR-20260909-collapsible-panel **P3/P4 完成**：TEST-031 PASS（组件 7 断言 + e2e 折叠/reload/续接）；TEST-030 与「发送后展开」e2e 在 `expanded`→`showTranscript` 重构后**回归重验通过**；新增 e2e「collapsing the panel keeps the conversation and survives a reload」。`npm test` 120、`ui-contract` 46/0/0（LB-03 改判 + 新增 LB-07）、smoke、7 Playwright e2e、`build:verify`、17 治理单测全绿；g1-g4 全 PASS）。
- 当前状态：APPROVED（CR-20260909-corner-menu **P3/P4 完成**：TEST-032 PASS（组件 5 用例 + e2e）；TEST-019/021 + appearance/account e2e 在 ☰ 菜单重构后**回归重验通过**；新增 e2e「☰ menu reachable while chat expanded」；smoke 首页断言改判 ☰ 触发器。`npm test` 124、`ui-contract` 47/0/0（新增 LB-08）、smoke、8 Playwright e2e、`build:verify`、17 治理单测全绿；g1-g4 全 PASS）。
- CR-20260910-skill-intake（R4）：新增 **TEST-043**（zip 解析对抗性单测：6 正向 + 11 整体拒绝 + 1 逐条排除 + `zip.ts` 不 import `node:fs` 的 grep 守卫）、**TEST-044**（投递三入口 + **非法拖放 `preventDefault` 根因回归守卫**）、**TEST-045**（`excluded` 四类回执 + 白名单放宽 + 被拒归档零产物）、**TEST-046**（技能列表可见 + 注册后免刷新更新 + 「本轮使用技能」提示 + 只读守卫）。**TEST-034..042 全部作回归门。** 「任务→测试派生矩阵」按**子项**列行（本次 P6 复盘固化：整任务只绑一个测试会让子项漏做不被发现）。R4 评审：四角色 **全部 APPROVED**。**P3/P4 完成：TEST-043..046 PASS**；TEST-034..042 回归全绿；e2e 10 例（新增真机走「上传 zip」按钮 → 文件选择器 → 注册 → ☰ 列表免刷新更新）。
- CR-20260909-display-screen（R4）：新增 **TEST-039**（`display_state` get-or-create + 单行语义）、**TEST-040**（`DisplayScreen`：home/insight/提示条不可关闭/kind 回退/事件重取）、**TEST-041**（e2e：全屏展示屏 → 洞察上屏 → 「显示首页」回标题 → **刷新保持** → ☰ 在展示屏上可点）、**TEST-042**（`routeTurn` `display` 字段 + fail-open + 路由层写 `display_state` + skill/display 互不阻塞）。**TEST-021 / TEST-032 作回归门重写**（首页结构反转为「全屏 DisplayScreen + 浮层」）。「任务→测试派生矩阵」+「测试设计」表逐 CP-1..CP-15 响应。R4 评审：**四角色全部 APPROVED**。**P3/P4 完成：TEST-039..042 PASS**；TEST-021/032 在 e2e 按「全屏 DisplayScreen + 浮层」新结构回归重跑通过；`playwright.config.ts` 加 `workers: 1`（各 spec 共用 dev server 与 SQLite，`resolveActiveProvider` 走全局优先级序，并行会互抢 Provider）。
- CR-20260909-skills（R4）：新增 **TEST-034**（`routeTurn` mock，含 4 条 fail-open + 请求体形态）、**TEST-035**（`resolveSkillForTurn` 白名单 + 32KB + 仅当轮 + frontmatter 回退）、**TEST-036**（HTML 捕获四态）、**TEST-037**（`insights` 写入 + `GET /api/insights` 读回，真实入口，满足原则 13）、**TEST-038**（e2e：拖放注册 → 技能轮 → 洞察落库 → per-message 不延续 → 未产出提示）。TEST-013 冒烟承接 `runChatTurn` 可选参数回归。「任务→测试派生矩阵」+「测试设计」表逐 CP-1..CP-14 响应。R4 评审：四角色 APPROVED（TEST-034 mock 禁真实网络、补请求体形态断言；`GET /api/insights` 消费者补齐）。**P3/P4 完成：TEST-034..038 PASS**（`npm run test:skills` / `test:chat-stream` / `test:display` / `test:e2e`）。
