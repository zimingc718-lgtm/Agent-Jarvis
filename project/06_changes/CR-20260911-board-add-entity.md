# CR-20260911-board-add-entity

- 级别: L1（看板补一个**已有 API 早就支持**的入口：直接新增跟踪对象。不改需求、架构、接口与数据结构；只动 `KnowledgeDashboard` 一个组件）
- 提出人: 模块开发（`CR-20260911-scheduled-sweep` 的真实入口走查中发现：要往空看板里放一个对象，只能手工 POST 或先配好模型再在对话里提议）
- 状态: APPROVED（R1 人工终裁见下：本 CR 不新立决策，只把既有决策在界面上补齐）
- 占用 ID: TASK-130
- 评审模型: 快车道（DEC-021 ①：两个 CP 均为双向门且均有机器检查 → 不产出 R2–R4 矩阵）
- 影响需求: 无（REQ-F-070 ④ 已写明用户可直接建对象，API 也一直如此）
- 影响模块: MOD-SETTINGS-UI（`src/components/KnowledgeDashboard.tsx`）
- 影响任务: 新增 TASK-130；**小改** TASK-124（看板组件）
- 影响测试: **小改** TEST-123（看板用例增 ⑫⑬），不新立测试编号
- 当前证据: `project/05_evidence/EV-2026-09-11-scheduled-sweep.md` §5b（走查中必须手工 POST 才能建对象，这一步暴露了界面缺口）
- 方案选项:
  - A. 维持现状，只能在对话里让模型提议——否决。模型提议这条路要求先配好 Provider；**空看板因此依赖一个与看板无关的前置条件**，而 API 早就允许用户直接建。
  - B. **每条泳道各带一个新增输入框**（选中）。kind 由泳道决定，用户不需要选，也就选不错。
  - C. 顶部放一个「新增对象」按钮 + 弹窗（含 kind 下拉）——否决。多一次选择、多一层弹窗，换来的只是少两个输入框。
- 选择理由: 选 B。①**不新立决策**：`POST /api/entities` 从 CP-9 起就是「用户自己的动作，直接生效」，模型那条路才走待采纳区；本 CR 只是把这条既有分工放到界面上；②**门的形态**：一个组件内的表单，回滚即删；③**顺手修掉一句误导文案**：空态原来写「在对话里说一句，让 Jarvis 提议一个」，读起来像是唯一的办法。
- 回滚方式: `git revert` 本 CR 的提交；API 与数据不受影响（本 CR 未改二者）。回滚后重跑 `npm test`、`npm run test:ui-contract`、`verify | check-changes | check-doors | check-ids` 并重新 `snapshot`。
- 验收条件:
  - R1: 本文件有 `## 变化点登记` 表（每行有来源角色、门、发现方式）；`check-doors` PASS；`review r1` PASS。
  - 快车道不产出 R2/R3/R4 矩阵。
  - P3/P4: TASK-130 DONE、TEST-123 仍 PASS（含新增 ⑫⑬）；`gate g3|g3.5` PASS；零新增依赖。
- 评审记录: 快车道。两个 CP 都能被组件测试直接断言，无需人工发现。

## 变化点登记

「门」按撤回代价逐 CP 判定（`docs/CONTROLS.md`「分档判据」）。两个 CP 均双向且均有机器检查，故走快车道。

| CP | 来源角色 | 一句话 | 关联 ID | 类型 | 门 | 发现方式 |
|---|---|---|---|---|---|---|
| CP-1 | 模块开发 | **每条泳道可直接新增对象**，kind 由泳道决定而不是让用户选；走 `POST /api/entities`，即既有的「用户自己的动作直接生效」那条路，模型那条仍进待采纳区 | TASK-130 | 新增 | 双向 | 机器：TEST-123 ⑫（三条泳道各自带对的 kind）、⑬（空名不发请求） |
| CP-2 | 产品 | **空态文案改口**：原文「在对话里说一句，让 Jarvis 提议一个」把唯一出路指向了模型，而模型要先配 Provider。改为「可以在下面直接添加，也可以在对话里让 Jarvis 提议」 | TASK-130 | 小改 | 双向 | 机器：TEST-123 ⑨（空态文案断言） |

## R1 人工终裁

- 不新立决策：用户建对象直接生效、模型建对象进待采纳区，这条分工在 `CR-20260911-home-dashboard` CP-4 / CP-9 已定，API 也一直这么实现。本 CR 只补界面。
- 用户 2026-09-11 的第 5 条操作逻辑（模型在对话里建，建好进待采纳区）**不受影响**：模型那条路一个字没动。

## 实施记录（2026-09-11）

- `KnowledgeDashboard` 的泳道渲染多收一个 `kind` 参数，每条泳道末尾带一个「新增<泳道名>」输入框与「添加」按钮；空名直接返回，不发请求。
- 空态文案改为「还没有<泳道名>。可以在下面直接添加，也可以在对话里让 Jarvis 提议。」
- `tests/knowledge-dashboard.test.tsx` 增 ⑫⑬ 两例并更新 ⑨ 的文案断言（TEST-123 小改）。
- 全量 528 单测通过、ui-contract 53/0、typecheck 0、零新增依赖。
