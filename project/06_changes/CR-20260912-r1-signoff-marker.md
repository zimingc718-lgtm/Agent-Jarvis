# CR-20260912-r1-signoff-marker

- 级别: L2（改的是治理工具自身的判据。无产品行为变化、无 schema 变更、无数据迁移；但会改变既有变更记录的门禁结果，见「问题陈述」第 3 点）
- 提出人: 助手在为 `CR-20260912-knowledge-attribution` 等四条 CR 走 R1 时发现；用户 2026-09-12 裁定「另立一条 CR 修」
- 状态: CLOSED（闭环完成：2 个任务 DONE、1 条测试 PASS；2026-09-14 按 DEC-270 ③ 收口。原记：APPROVED（R1 人工终裁：用户 2026-09-13 在三个方案中选 A，并裁定既有记录统一脚本改写））
- 占用 ID: DEC-190, TASK-280, TEST-280（定案后声明；说明书对应行已落，`check-ids` PASS）
- 评审模型: 快车道（DEC-021 ①：三个 CP 均双向门且均有机器检查——判据本身可被反例断言，迁移结果可被全量 `review r1` 断言）
- 影响需求: 无（治理工具变更，不动产品需求）
- 影响模块: MOD-GOVERNANCE（`tools/governance.py`）、`docs/CONTROLS.md`（状态语义可能需补一行）
- 影响任务: 新增 TASK-280；**小改** TASK-049（R1 判据）
- 影响测试: 新增 TEST-280（落在 `tests/test_governance.py`，四例）
- 当前证据: 本文件「问题陈述」——现场取自 2026-09-12 为四条 CR 走 R1 时的实际输出，可复现
- 方案选项:
  - A. **要求一个带日期与裁定人的显式标记行**，例如 `- R1 人工终裁: 通过（<裁定人>，<YYYY-MM-DD>）`，正则改为匹配该行的结构而非任意含「R1」与「终裁」的句子。
  - B. 维持正则，但排除将来式措辞（如「时一并」「后再」「将」）——黑名单式，容易被下一种说法绕过。
  - C. 把终裁痕迹移出 CR 正文，改为一个独立的签置文件（如 `<CR>.signoff.json`），机器只查文件存在与字段完整。
  - 尚未选定。
- 选择理由: 选 A（用户 2026-09-13 裁定）。①**黑名单挡不住下一种说法**：B 只封「R1 时一并终裁」，「R1 通过后终裁」照样绕过，而绕过的方式是无穷的；②**签置是事实陈述，该有固定形状**：A 把它从散文变成一行可解析的断言，代价只是一次机械改写；③**不新增文件类别**：C 最干净，但与「CR 是单一事实来源」的现有习惯冲突，为一道门引入第二处事实来源不划算。④Q3（四角色意见真伪）**不并入**：那是 R2–R4 的判据，混进来会让回滚边界变脏。
- 回滚方式: `git revert` 本 CR 的提交。改的是检查器判据与（可能的）既有 CR 措辞，无运行时代码、无数据。回滚后门禁回到旧判据，既有 CR 的措辞若已改成新格式，旧判据仍能匹配（新格式同样含「R1」与「终裁」），故回滚不会造成门禁倒挂。
- 验收条件:
  - R1: 本文件有 `## 变化点登记` 表 + 结构化签置行；`check-doors` PASS；`review r1` PASS。
  - 快车道不产出 R2/R3/R4 矩阵；三层说明书各含 `变更响应 · CR-20260912-r1-signoff-marker` 节（因本 CR 声明了 DEC/TASK/TEST 编号）。
  - 机器检查：治理单测须包含一条**反例**——一个只写了将来式承诺（如「R1 时一并终裁」）的 CR 必须被判 FAIL。没有这条反例，本 CR 等于没修。
  - 须跑一次全量 `review r1`，逐条确认既有 38 条 CR 在新判据下的结果，并把由绿转红的记录列入本 CR 的处置清单。
- 评审记录: 快车道。三个 CP 都能被治理单测直接断言；反例（TEST-280 ①）是本 CR 的鉴别力所在。
- R1 终裁: 已完成 | 用户 | 2026-09-13

## 拟用 ID 区间（讨论定案后再正式声明到「占用 ID」）

DEC-190、TASK-280、TEST-280。当前刻意不声明占用——`check-ids` 会把「占用 ID」行里的编号当作真实预留，而说明书里尚无对应行，声明即报 DANGLING。

## 问题陈述（证据，非主张）

`tools/governance.py:858`：

```python
if not re.search(r"R1[^\n]*(拍板|终裁|人工确认)", cr_text):
    findings.append(f"FAIL REVIEW_R1_BLOCKED {name}: no R1 human sign-off marker ...")
```

三点：

1. **判据是「同一行里同时出现 R1 与终裁类词」，不区分时态。** 2026-09-12 为四条新 CR 走 R1 时，实际满足该正则的句子是「**R1 时一并终裁**」——一句关于将来的承诺。四条 CR 因此全部报 `REVIEW_R1_PASS`，而用户当时一次板都还没拍。
2. **它防的是「漏写签置」，防不了「签置尚未发生」。** 这两件事的区别在本项目里是实质性的：R1 是**人工终止门**（`docs/WORKFLOW.md`：「人工终止 … 四角色 Agent 出意见 → 人拍板」），机器绿灯被误读成「人已经拍过了」，正是这道门存在的意义被架空的方式。
3. **修它会让既有记录集体变红。** 现有 38 条通过 R1 的记录，其签置痕迹是按旧判据的自由文本写的（例如 `CR-20260910-ui-foundation` 写的是「用户已完成 R1 人工终裁」，格式与新要求不同但语义正确）。所以本 CR 不是改一行正则就完事，须同时给出既有记录的迁移处置——这也是它级别定 L2 而非 L1 的原因。

## 待用户裁定（2026-09-13 已全部裁定）

- ~~**Q1｜选 A / B / C。**~~ **已裁定：A。** A 最直接，但要求既有 38 条记录统一改写成新格式（机械改写，可脚本化）。B 改动最小但是黑名单思路，下一种将来式说法（「R1 通过后终裁」）照样能绕过。C 最干净——签置是结构化数据而不是散文——但新增一类文件，且与「CR 是单一事实来源」的现有习惯不一致。
- ~~**Q2｜既有 38 条怎么处置。**~~ **已裁定：统一脚本改写**（实际改写 39 条，DRAFT 那条按纪律跳过）。 统一脚本改写为新格式（会产生一次大范围 diff，且触及许多已 CLOSED 的历史记录）；还是只对新记录生效、历史记录按旧判据豁免（判据里带一个日期分界，实现简单但从此有两套规则）？
- ~~**Q3｜要不要顺带查「四角色意见」的真伪。**~~ **已裁定：不并入**（用户采纳助手倾向）。 同一道门还有一个类似的松弛处：标准档的资格只要求 `## 角色意见` 节里**存在表格行**（`governance.py:844`），不检查是否四个角色都出了意见、也不检查判定列的取值。要不要一起收紧？助手倾向**不并入**——那是 R2–R4 的判据，与本 CR 的 R1 签置不是同一件事，混在一起会让回滚边界变脏。

## 同一函数上的第二处缺陷（2026-09-12 追加）

`review r*` 从**CP 登记表与「级别」**判定一条记录的去向（`governance.py:781-789`：无 CP 表且级别为 L1 或含 `pre-R1234` 才算合法跳过），**从不读「状态」字段**。后果：一条 `状态: REJECTED` 却仍带 CP 表的记录会被当作在途变更，在 R2–R4 永久报红——它按定义永远不会做 P2。而 `CLAUDE.md` 规定收口与提交以缺省全量结果为准，于是一条已作废的记录足以卡住整条分支的收口。

实际发生过：`CR-20260912-knowledge-retrieval` 作废后保留文件留痕，随即在全量 `check p2` 里产生 3 条无法消除的 FAIL；最终只能把内容并进 `CR-20260912-knowledge-attribution` 并删除文件（见该 CR「被本 CR 吸收的…」一节）。**这意味着当前模型下「作废但保留留痕」这件事做不到**，而这正是治理最需要留痕的一类记录。

是否与本 CR 的 R1 签置判据一并修，见 Q4。

- **Q4｜REJECTED 记录要不要被评审门跳过。** 与 Q3 不同：Q3 是「判据太松」，本条是「判据缺一个维度」。两者都在 `review r*` 这一个函数里，回滚边界相同，故助手倾向**并入本 CR**（与 Q3 的建议相反）。若并入，须一并定义：跳过是只看 `状态: REJECTED`，还是要求同时给出接收方 CR 的名字（避免用 REJECTED 逃避评审）。

## 变化点登记

**拟定，待讨论后定稿。**「门」按撤回代价逐 CP 判定（`docs/CONTROLS.md`「分档判据」）。

| CP | 来源角色 | 一句话 | 关联 ID | 类型 | 门 | 发现方式 |
|---|---|---|---|---|---|---|
| CP-1 | 测试 | R1 签置判据改为可区分时态的显式标记（A/B/C 定案后填形态） | 待派生 | 缺陷修复 | 双向 | 机器：治理单测的反例——只写将来式承诺的 CR 必须 FAIL |
| CP-2 | 产品 | 既有 38 条记录的签置痕迹处置（Q2 定案后填） | 待派生 | 小改 | 双向 | 机器：全量 `review r1` 结果与处置清单逐条对齐 |

## R2 / R3 / R4 评审矩阵

未产出。方案定案、CP 表定稿后再跑 `governance.py matrix CR-20260912-r1-signoff-marker`。

## 实施记录（2026-09-13）

- `tools/governance.py`：新增 `R1_SIGNOFF_LINE` 与 `_is_draft`；`review r1` 改判结构化行；DRAFT 记录进 `drafts` 集合并在结果里按名字列出（`SKIPPED_DRAFT`），与既有两条 SKIPPED 行同形。
- `tools/migrate_r1_signoff.py`（新）：一次性改写既有记录。三条纪律写在模块头部——**不造日期**（只在签置措辞与日期同处一行时才写）、**不动原有散文**（结构化行是新增的一行，用户原话留在原处）、**跳过 DRAFT**（替一条没签过字的记录写「已完成」就是伪造）。幂等。
- 实跑：改写 39 条，其中 15 条查不到与签置同行的日期，按纪律只写到签置人为止；1 条 DRAFT 跳过（即本文件，改写时它还是 DRAFT）。
- `tests/test_governance.py`：新增四例（TEST-280）。治理单测 80 通过。
- 迁移后全量 `review r1` **零 FAIL**，输出含 `SKIPPED_DRAFT`。
- 说明书三层各加一节；`check-specs`、`check-ids` PASS。
- **一处如实记下的局限**：文本检查挡不住明知故写。本 CR 解决的是「随口一句被当成签置」，不是「有人故意造假」——后者只能靠人看。这条写进了 DEC-190 的风险列，不留在代码注释里。
