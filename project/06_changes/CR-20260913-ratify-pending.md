# CR-20260913-ratify-pending

- 级别: L1（只记录同意与否，不改需求、架构、接口、测试基线，不新增任何 ID——按 `docs/CONTROLS.md` 的 L1 轻量档，产出为一条 CR 加一条证据登记）
- 提出人: 助手 2026-09-13 巡检列出 A 组「14 项按助手建议采纳、待追认」；用户同日回「ABCD 参考业界最佳方案，作为我的决策。将所有的，一并开始起草，进行，闭环掉」
- 状态: CLOSED（闭环完成：无独立任务/测试编号（L1 轻量档）；2026-09-14 合入 main 后收口。原记：APPROVED（R1 人工终裁：用户 2026-09-13 明确授权按业界最佳实践代为决策，INPUT-2026-09-13-027））
- 占用 ID: 无（未创建 ID）
- 评审模型: 快车道（DEC-021 ①：两个 CP 均双向门；追认本身不改变任何运行行为，撤回即改回文字）
- 影响需求: 无
- 影响模块: 无（只动 `project/06_changes/` 七份记录的文字）
- 影响任务: 无
- 影响测试: 无
- 当前证据: 本文件「逐条裁定」；各 CR 的 `## 追认（2026-09-13）` 节
- 方案选项:
  - A. **逐条裁定并把依据写在各自的 CR 上**（选中）
  - B. 一句「全部追认」了事——否决。**不写依据的追认等于没追认**：下次翻出来一样说不清当初为什么这么定，而这批决策里有两项（沙箱档位、看护阈值）将来一定会被重新问起。
  - C. 全部退回等用户逐条拍板——否决。用户已明确授权代为决策，退回是把已经给出的授权再问一遍。
- 选择理由: 选 A。①每条裁定都写明依据与**将来该重新考虑它的触发条件**（如沙箱档位在洞察开始承载私有数据时要收紧）；②两项布局遗留不硬追认为终局，而是标为过渡并指向根治的 CR——**把「先这样」和「就这样」分开**。
- 回滚方式: `git revert` 本 CR 的提交，七份记录的 `## 追认` 节消失，决策回到「待追认」状态。无代码、无数据。
- 验收条件:
  - R1: 本文件有 `## 变化点登记` 表 + 结构化签置行；`check-doors` PASS；`review r1` PASS。
  - 七份记录各含 `## 追认（2026-09-13）` 节，逐条写明维持 / 过渡与依据。
  - L1 且不占用新 ID，按 `docs/CONTROLS.md` 不写三层说明书的变更响应节。
- 评审记录: 快车道。追认不改变运行行为，两个 CP 均可由文本检查证实。
- R1 终裁: 已完成 | 用户 | 2026-09-13

## 逐条裁定

12 项维持原样，2 项只作为**过渡**追认。

| # | 出处 | 决策 | 裁定 | 依据 |
|---|---|---|---|---|
| 1 | sandbox-and-budget | 沙箱取 `allow-scripts` | 维持 | 不带 `allow-same-origin` 时文档处于不透明源，够不着宿主 DOM / 存储 / Cookie——渲染不可信富文本的常规档位。**触发收紧的条件已写明**：洞察开始承载私有数据时改空沙箱 |
| 2 | local-documents | 只认绝对路径，不做文件选择器 | 维持 | 浏览器目录选择器给的是沙箱句柄，服务端读不到真实路径，**做出来是假的** |
| 3 | local-documents | 格式白名单 | 维持 | 不支持的格式给明确原因而非静默失败；pptx/xlsx 记为待办 |
| 4 | local-documents | 索引只在内存 | 维持 | 先测后缓存；出现实测卡顿再立 CR |
| 5 | context-degrade | 不改 `BUDGET_SHARES` 声明值 | 维持 | 影响每条工具输出长度，需单独实测；TEST-190 ① 会在有人改动时说话 |
| 6 | context-degrade | 削短按条等分 | 维持 | 相关性分配需额外判断，成本与不确定性都更高；确定性策略先兜底 |
| 7 | skill-paging | 清单按字母序 | 维持 | 清单带 KB 数，让模型按需取比替它猜更可靠，且少一次调用 |
| 8 | skill-paging | 横向冲突只做纵向缓解 | **过渡** | 根治并入 `CR-20260913-settings-on-display` |
| 9 | skill-report-bridge | 不改用户技能文件，约定附在 `read_skill` 结果后 | 维持 | 技能目录是用户资产，重装不该丢；宿主一侧一次生效 |
| 10 | skill-report-bridge | 面板与报告共用中轴 | **过渡** | 同 8，根治并入同一条 CR |
| 11 | runtime-visibility | `start:local` 保留 + `serve:local` | 维持 | 留一条「只起一个、不自动重启」给排查用 |
| 12 | runtime-visibility | 看护阈值 20 秒 / 3 次 | 维持 | 确无实测支撑，但可观察（`.data/server-events.log`）、改动代价近零；无数据时换一组数仍是无依据 |
| 13 | stage-reach | 开场不加最短驻留 | 维持 | 可打断优先于强制观看；3 秒不够改常量即可 |
| 14 | stage-reach | `show_board` 清持久态回 `home` | 维持 | 否则「打开看板」后屏幕上什么都没变；代价可由 `show_insight` 切回 |

第 8、10 两条其实是同一件事的两半：**报告与控制台争同一条中轴**。用户 2026-09-13 又提出「设置类界面放到动态屏」，三者是同一个布局问题，分开修会各修一次。

## 变化点登记

| CP | 来源角色 | 一句话 | 关联 ID | 类型 | 门 | 发现方式 |
|---|---|---|---|---|---|---|
| CP-1 | 产品 | **12 项维持原样**，依据与「将来该重新考虑它的触发条件」写进各自 CR 的 `## 追认` 节 | 无新增 | 小改 | 双向 | 机器：`check-changes` + 七份记录各含 `## 追认（2026-09-13）` 节 |
| CP-2 | 架构 | **2 项只作过渡追认**（布局遗留），根治指向 `CR-20260913-settings-on-display`，不硬认为终局 | 无新增 | 小改 | 双向 | 机器：`check-changes`；两份记录的追认节写明「仅追认为过渡」 |
