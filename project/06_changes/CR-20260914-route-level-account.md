# CR-20260914-route-level-account

- 级别: L2（真实入口对账再收紧一层：未执行可以逐路线声明。会改变既有记录的对账输出。无产品代码改动、无 schema 变更、零新增依赖）
- 提出人: 助手 2026-09-14 在一天之内第三次撞到同一个盲区；用户同日「继续」授权
- 状态: APPROVED（R1 人工终裁：用户 2026-09-14「继续」，承「所有的都进行」的总授权）
- 占用 ID: DEC-300, TASK-380, TEST-380
- 评审模型: 快车道（DEC-021 ①：两个 CP 均双向门且均有机器检查）
- 影响需求: 无（治理工具变更）
- 影响模块: MOD-GOVERNANCE（`tools/governance.py`）
- 影响任务: 新增 TASK-380；**小改** TASK-340（`check_real_entry`）
- 影响测试: TEST-380
- 当前证据: `project/05_evidence/EV-2026-09-14-route-level-account.md`
- 方案选项:
  - A. **CP 行可以自陈「未执行：原因」，该路线单独记账**（选中）
  - B. 记录级再加一种「部分执行」状态——否决。**「部分」是个没有粒度的词**：哪一部分？下次翻出来还是要人去数。而粒度本来就该落在 CP 行上，那里才写着每条路线要看什么。
  - C. 维持现状，靠 CR 正文的散文说明——否决。今天三次都证明了它不管用：`if ran` 一成立，正文里写的「仅剩 CP-4」在机器输出里一个字都不会出现。
- 选择理由: 选 A。①粒度对齐到声明的那一行；②与 DEC-250 ② 的「（证据：TEST-xxx）」同形，同一格里说同一类事；③**不再逼人做那个选择**——此前想让剩下的那条可见，只能不给已有的证据打勾，用瞒报一件事去换另一件事可见。
- 回滚方式: `git revert` 本 CR 的提交。对账回到记录级；CP 行里已写的「（未执行：…）」旧代码不读，留着无害。无数据、无迁移。
- 验收条件:
  - R1: 本文件有 `## 变化点登记` 表 + 结构化签置行；`check-doors` PASS；`review r1` PASS。
  - 快车道不产出 R2/R3/R4 矩阵；三层说明书各含 `变更响应 · CR-20260914-route-level-account` 节。
  - 机器检查：TEST-380 三例全绿；本仓库全量 `check-real-entry` exit 0，且**剩余项由「1 条记录」变成「3 条路线」**。
- 评审记录: 快车道。两个 CP 都能被治理单测直接断言。
- R1 终裁: 已完成 | 用户 | 2026-09-14

## 问题陈述

**一天之内撞到三次**：

| 记录 | 发生了什么 |
|---|---|
| `CR-20260912-technical-spine` | 与看板共用一条测试，那条测试一被标成真实入口 PASS，它就从名单里消失了（DEC-250 已修「靠别人的证据」这一半） |
| `CR-20260912-turn-budget-continue` | 四条路线跑掉三条，剩下的 CP-4 在账本里消失 |
| `CR-20260911-scheduled-sweep` | 跑通服务端那一半，客户端 tick 与「会不会被当爬虫」两条也消失 |

根子是对账只认**整条记录**跑没跑：`if ran:` 一进这个分支，记录里写的「未执行（仅剩 CP-4）」就再也没人看。

于是为了让剩下的那条可见，只剩一个办法——**不给已有的证据打勾**。用瞒报一件事去换另一件事可见，这不该是账本逼人做的选择。

## 变化点登记

| CP | 来源角色 | 一句话 | 关联 ID | 类型 | 门 | 发现方式 |
|---|---|---|---|---|---|---|
| CP-1 | 测试 | **路线级未执行声明**：CP 行的「发现方式」里写 `（未执行：原因）`，该路线单独记账，与记录整体是否有证据无关 | DEC-300, TASK-380 | 缺陷修复 | 双向 | 机器：TEST-380 ①（记录有别的证据时仍点名该路线） |
| CP-2 | 架构 | **全部路线自陈时记录不必再写**：每条声明的路线都自陈未执行，则记录级那一行是多余的 | DEC-300, TASK-380 | 新增 | 双向 | 机器：TEST-380 ②③（全自陈则放行；没写声明的仍按老规矩要求记录级交代） |

## 迁移结果

三条「跑了一半」的记录就地迁到路线级：

```
OK ADVISORY REAL_ENTRY_ROUTE_UNRUN 3 route(s) declare themselves not run, regardless of
whether their record has other evidence:
  CR-20260911-scheduled-sweep CP-4、CR-20260911-scheduled-sweep CP-5、
  CR-20260912-turn-budget-continue CP-4
```

账面剩余项由**「1 条记录」变成「3 条路线」**——数字变大了，看见的东西才是真的。
